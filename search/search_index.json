{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Neural Dimensionality Tracker (NDT)","text":"<p>Welcome to the documentation for Neural Dimensionality Tracker, a production-ready Python library for high-frequency monitoring of neural network representational dimensionality during training.</p>"},{"location":"#what-is-ndt","title":"What is NDT?","text":"<p>NDT enables you to:</p> <ul> <li>Track how your neural network's internal representations evolve during training</li> <li>Detect discrete phase transitions (jumps) in learning dynamics</li> <li>Understand the mechanistic behavior of deep learning models</li> <li>Visualize representational capacity expansion in real-time</li> <li>Export comprehensive dimensionality measurements for analysis</li> </ul>"},{"location":"#key-features","title":"Key Features","text":"<p>\u2728 Minimal Integration - Add tracking with just 3 lines of code</p> <p>\ud83c\udfd7\ufe0f Architecture-Agnostic - Works with MLPs, CNNs, Transformers, ViTs, and more</p> <p>\ud83d\udcca Multiple Metrics - Track 4 complementary dimensionality measures:    - Stable Rank (effective dimensionality)    - Participation Ratio (variance distribution)    - Cumulative Energy 90% (components for 90% variance)    - Nuclear Norm Ratio (normalized rank measure)</p> <p>\ud83d\udd0d Automatic Jump Detection - Identify phase transitions during training</p> <p>\ud83d\udcc8 Rich Visualization - Built-in plotting with Matplotlib and interactive Plotly dashboards</p> <p>\ud83d\udcbe Flexible Export - Save results as CSV, JSON, or HDF5</p> <p>\u26a1 Production-Ready - Fully typed, tested (&gt;90% coverage), &lt;10% overhead</p>"},{"location":"#quick-start","title":"Quick Start","text":""},{"location":"#installation","title":"Installation","text":"<pre><code>pip install ndtracker\n</code></pre>"},{"location":"#basic-usage","title":"Basic Usage","text":"<pre><code>import torch.nn as nn\nfrom ndt import HighFrequencyTracker\n\n# Your model\nmodel = nn.Sequential(\n    nn.Linear(784, 512), nn.ReLU(),\n    nn.Linear(512, 256), nn.ReLU(),\n    nn.Linear(256, 10)\n)\n\n# Create tracker\ntracker = HighFrequencyTracker(model, sampling_frequency=10)\n\n# Training loop\nfor step, (x, y) in enumerate(dataloader):\n    output = model(x)\n    loss = criterion(output, y)\n    loss.backward()\n    optimizer.step()\n\n    tracker.log(step, loss.item())  # One line!\n\n# Analyze\nresults = tracker.get_results()\nfrom ndt import plot_phases\nplot_phases(results, metric=\"stable_rank\")\n</code></pre> <p>That's it! See Quickstart Guide for more details.</p>"},{"location":"#why-track-dimensionality","title":"Why Track Dimensionality?","text":"<p>Recent research (Ansuini et al. 2019, Yang et al. 2024) has shown that neural networks expand their effective representational capacity during training, despite having fixed parameters. This challenges our understanding of how neural networks learn.</p> <p>Key insights:</p> <ol> <li>Capacity expands during training - Networks don't just explore a fixed space, they build new representational structures</li> <li>Transitions are discrete - Learning happens in distinct phases with rapid jumps</li> <li>Critical periods exist - Early training (first 25%) is when most structure forms</li> <li>High-frequency sampling reveals hidden dynamics - Coarse checkpointing misses most transitions</li> </ol> <p>NDT makes these insights accessible for any PyTorch model.</p>"},{"location":"#research-reproduction","title":"Research Reproduction","text":"<p>NDT includes a complete reproduction of the experiment from the Towards Data Science article \"I Measured Neural Network Training Every 5 Steps for 10,000 Iterations\":</p> <pre><code>python examples/03_reproduce_tds_experiment.py\n</code></pre> <p>Expected results: - Phase 1 (Collapse): Steps 0-300 - Phase 2 (Expansion): Steps 300-5000 - Phase 3 (Stabilization): Steps 5000-8000</p> <p>See TDS Article Reproduction for details.</p>"},{"location":"#documentation-structure","title":"Documentation Structure","text":""},{"location":"#getting-started","title":"Getting Started","text":"<ul> <li>Quickstart Guide - Get started in 5 minutes</li> <li>Installation - Detailed installation instructions</li> <li>Examples Gallery - Complete collection of examples</li> </ul>"},{"location":"#reference","title":"Reference","text":"<ul> <li>API Reference - Complete API documentation</li> <li>Architecture Support - Supported architectures and compatibility</li> <li>Performance Benchmarks - Overhead, memory, and scalability</li> <li>Troubleshooting - Common issues and solutions</li> </ul>"},{"location":"#research","title":"Research","text":"<ul> <li>TDS Article Reproduction - Reproduce research results</li> </ul>"},{"location":"#contributing","title":"Contributing","text":"<ul> <li>Contributing Guide - How to contribute</li> </ul>"},{"location":"#use-cases","title":"Use Cases","text":""},{"location":"#research_1","title":"Research","text":"<ul> <li>Interpretability: Understand when and how features form during training</li> <li>Architecture Design: Compare representational dynamics across architectures</li> <li>Training Dynamics: Study critical periods and phase transitions</li> <li>Transfer Learning: Analyze representation transfer between tasks</li> </ul>"},{"location":"#production","title":"Production","text":"<ul> <li>Monitoring: Track representation health during large-scale training</li> <li>Debugging: Diagnose training instability and architectural bottlenecks</li> <li>Optimization: Identify when to stop training or adjust hyperparameters</li> <li>Validation: Ensure consistent learning dynamics across experiments</li> </ul>"},{"location":"#supported-architectures","title":"Supported Architectures","text":"Architecture Auto-Detection Status MLP \u2705 Yes Fully supported CNN \u2705 Yes Fully supported ResNet \u2705 Yes Fully supported Transformer \u26a0\ufe0f Partial Specify FFN layers BERT \u26a0\ufe0f Partial Specify layers GPT \u26a0\ufe0f Partial Specify layers ViT \u26a0\ufe0f Partial Specify layers <p>See Architecture Support for complete matrix.</p>"},{"location":"#performance","title":"Performance","text":"Metric Target Measured Training overhead &lt;10% 2-8% (typical) Memory usage Minimal &lt;1MB per 1000 measurements Max model size 1B params Tested up to 1.5B Initialization &lt;1 second 50-200ms <p>See Performance Benchmarks for details.</p>"},{"location":"#citation","title":"Citation","text":"<p>If you use Neural Dimensionality Tracker in your research, please cite:</p> <pre><code>@software{marin2024ndt,\n  author = {Mar\u00edn, Javier},\n  title = {Neural Dimensionality Tracker: High-Frequency Monitoring of Neural Network Training Dynamics},\n  year = {2024},\n  publisher = {GitHub},\n  url = {https://github.com/Javihaus/ndt},\n  version = {0.1.0}\n}\n</code></pre> <p>Associated article: <pre><code>@article{marin2025measuring,\n  author = {Mar\u00edn, Javier},\n  title = {I Measured Neural Network Training Every 5 Steps for 10,000 Iterations},\n  journal = {Towards Data Science},\n  year = {2025},\n  month = {November}\n}\n</code></pre></p>"},{"location":"#community","title":"Community","text":"<ul> <li>GitHub: github.com/Javihaus/ndt</li> <li>Issues: Report bugs or request features</li> <li>Discussions: Ask questions</li> <li>PyPI: pypi.org/project/ndtracker</li> </ul>"},{"location":"#license","title":"License","text":"<p>MIT License - see LICENSE for details.</p>"},{"location":"#acknowledgments","title":"Acknowledgments","text":"<p>This work builds on research by:</p> <ul> <li>Ansuini et al. (2019) - Intrinsic dimension of data representations in deep neural networks</li> <li>Yang et al. (2024) - \u03b5-rank and the staircase phenomenon</li> <li>Achille et al. (2019) - Critical learning periods in deep networks</li> </ul>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Install NDT and try the Quickstart</li> <li>Explore Examples</li> <li>Read the API Reference</li> <li>Reproduce the TDS experiment</li> <li>Contribute to the project</li> </ul> <p>Author: Javier Mar\u00edn | LinkedIn | Twitter</p>"},{"location":"CONTRIBUTING/","title":"Contributing to Neural Dimensionality Tracker","text":"<p>Thank you for your interest in contributing to NDT! This document provides guidelines and instructions for contributing.</p>"},{"location":"CONTRIBUTING/#development-setup","title":"Development Setup","text":"<ol> <li> <p>Fork and clone the repository: <pre><code>git clone https://github.com/yourusername/ndt.git\ncd ndt\n</code></pre></p> </li> <li> <p>Create a virtual environment: <pre><code>python -m venv venv\nsource venv/bin/activate  # On Windows: venv\\Scripts\\activate\n</code></pre></p> </li> <li> <p>Install in development mode: <pre><code>pip install -e \".[dev]\"\n</code></pre></p> </li> </ol>"},{"location":"CONTRIBUTING/#code-style","title":"Code Style","text":"<p>We follow strict code quality standards:</p> <ul> <li>Formatting: Black (line length 100)</li> <li>Import sorting: isort</li> <li>Linting: flake8 (max complexity 10)</li> <li>Type hints: All functions must have type annotations</li> <li>Docstrings: Google-style docstrings for all public functions</li> </ul> <p>Run formatters: <pre><code>black src/ndt tests examples\nisort src/ndt tests examples\n</code></pre></p> <p>Check linting: <pre><code>flake8 src/ndt tests --max-line-length=100\n</code></pre></p>"},{"location":"CONTRIBUTING/#testing","title":"Testing","text":"<p>All contributions must include tests. We aim for &gt;90% test coverage.</p> <p>Run tests: <pre><code>pytest tests/ -v --cov=ndt --cov-report=html\n</code></pre></p> <p>View coverage: <pre><code>open htmlcov/index.html  # On macOS\n# Or navigate to htmlcov/index.html in your browser\n</code></pre></p>"},{"location":"CONTRIBUTING/#writing-tests","title":"Writing Tests","text":"<ul> <li>Place tests in <code>tests/</code> matching the module structure</li> <li>Use pytest fixtures from <code>tests/conftest.py</code></li> <li>Test both normal cases and edge cases</li> <li>Include docstrings explaining what each test does</li> </ul> <p>Example: <pre><code>def test_stable_rank_identity_matrix(identity_matrix):\n    \"\"\"Identity matrix should have stable rank equal to its dimension.\"\"\"\n    sr = stable_rank(identity_matrix)\n    assert sr == pytest.approx(50.0, rel=0.01)\n</code></pre></p>"},{"location":"CONTRIBUTING/#pull-request-process","title":"Pull Request Process","text":"<ol> <li> <p>Create a feature branch: <pre><code>git checkout -b feature/your-feature-name\n</code></pre></p> </li> <li> <p>Make your changes with tests</p> </li> <li> <p>Run all checks: <pre><code>black src/ndt tests\nisort src/ndt tests\nflake8 src/ndt tests\npytest tests/ -v --cov=ndt\n</code></pre></p> </li> <li> <p>Commit with descriptive messages: <pre><code>git commit -m \"Add feature: description\"\n</code></pre></p> </li> <li> <p>Push and create a Pull Request: <pre><code>git push origin feature/your-feature-name\n</code></pre></p> </li> <li> <p>Fill out the PR template completely</p> </li> </ol>"},{"location":"CONTRIBUTING/#types-of-contributions","title":"Types of Contributions","text":""},{"location":"CONTRIBUTING/#bug-fixes","title":"Bug Fixes","text":"<ul> <li>Include a test that fails before the fix</li> <li>Reference the issue number in the PR</li> </ul>"},{"location":"CONTRIBUTING/#new-features","title":"New Features","text":"<ul> <li>Discuss in an issue first for major features</li> <li>Include comprehensive tests</li> <li>Update documentation and examples</li> <li>Add to README if user-facing</li> </ul>"},{"location":"CONTRIBUTING/#documentation","title":"Documentation","text":"<ul> <li>Fix typos, clarify explanations</li> <li>Add examples</li> <li>Improve docstrings</li> </ul>"},{"location":"CONTRIBUTING/#performance-improvements","title":"Performance Improvements","text":"<ul> <li>Include benchmarks showing improvement</li> <li>Ensure no functionality changes</li> <li>Add tests if needed</li> </ul>"},{"location":"CONTRIBUTING/#reporting-bugs","title":"Reporting Bugs","text":"<p>Use the bug report template and include: - Minimal code to reproduce - Expected vs actual behavior - Environment details (OS, Python version, etc.) - Full error traceback</p>"},{"location":"CONTRIBUTING/#suggesting-features","title":"Suggesting Features","text":"<p>Use the feature request template and include: - Use case description - Proposed API (if applicable) - Alternatives considered</p>"},{"location":"CONTRIBUTING/#code-review","title":"Code Review","text":"<p>All submissions require review. We review for: - Correctness - Test coverage - Code style adherence - Documentation quality - Performance implications</p>"},{"location":"CONTRIBUTING/#release-process","title":"Release Process","text":"<p>Maintainers handle releases: 1. Update version in <code>pyproject.toml</code> and <code>__version__.py</code> 2. Update CHANGELOG.md 3. Create git tag: <code>git tag v0.1.0</code> 4. Push tag: <code>git push --tags</code> 5. GitHub Actions automatically publishes to PyPI</p>"},{"location":"CONTRIBUTING/#questions","title":"Questions?","text":"<ul> <li>Open an issue for questions</li> <li>Check existing issues and PRs first</li> <li>Email maintainers for sensitive issues</li> </ul> <p>Thank you for contributing to NDT!</p>"},{"location":"api/","title":"API Reference","text":"<p>This page provides the API reference for ndt (Neural Dimensionality Tracker). All public classes and functions are documented with their signatures, parameters, return types, and examples.</p> <p>For auto-generated documentation from source docstrings, ensure <code>mkdocstrings</code> is configured in your MkDocs build.</p>"},{"location":"api/#core","title":"Core","text":""},{"location":"api/#highfrequencytracker","title":"HighFrequencyTracker","text":""},{"location":"api/#ndt.core.tracker.HighFrequencyTracker","title":"<code>ndt.core.tracker.HighFrequencyTracker(model: nn.Module, layers: Optional[List[nn.Module]] = None, layer_names: Optional[List[str]] = None, sampling_frequency: int = 1, enable_jump_detection: bool = True, jump_window_size: int = 50, jump_z_threshold: float = 3.0, device: Optional[torch.device] = None)</code>","text":"<p>High-frequency tracker for neural network representational dimensionality.</p> <p>This class provides minimal-intrusion tracking of dimensionality metrics during neural network training. It automatically captures activations and computes multiple dimensionality estimates at specified intervals.</p> <p>Attributes:</p> Name Type Description <code>model</code> <p>The neural network model to track</p> <code>layers</code> <p>List of layers to monitor</p> <code>sampling_frequency</code> <p>Record metrics every N steps</p> <code>jump_detector</code> <p>Optional jump detector for identifying phase transitions</p> Example <p>import torch.nn as nn model = nn.Sequential( ...     nn.Linear(784, 512), nn.ReLU(), ...     nn.Linear(512, 256), nn.ReLU(), ...     nn.Linear(256, 10) ... ) tracker = HighFrequencyTracker(model, layers=[model[0], model[2], model[4]])</p> <p>Initialize the high-frequency tracker.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Neural network model to track</p> required <code>layers</code> <code>Optional[List[Module]]</code> <p>List of layers to monitor. If None, auto-detects based on architecture</p> <code>None</code> <code>layer_names</code> <code>Optional[List[str]]</code> <p>Optional custom names for layers</p> <code>None</code> <code>sampling_frequency</code> <code>int</code> <p>Record metrics every N steps (default: 1)</p> <code>1</code> <code>enable_jump_detection</code> <code>bool</code> <p>Whether to detect dimensionality jumps (default: True)</p> <code>True</code> <code>jump_window_size</code> <code>int</code> <p>Window size for jump detection (default: 50)</p> <code>50</code> <code>jump_z_threshold</code> <code>float</code> <p>Z-score threshold for jump detection (default: 3.0)</p> <code>3.0</code> <code>device</code> <code>Optional[device]</code> <p>Device for computations (default: model's device)</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If model or parameters are invalid</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __init__(\n    self,\n    model: nn.Module,\n    layers: Optional[List[nn.Module]] = None,\n    layer_names: Optional[List[str]] = None,\n    sampling_frequency: int = 1,\n    enable_jump_detection: bool = True,\n    jump_window_size: int = 50,\n    jump_z_threshold: float = 3.0,\n    device: Optional[torch.device] = None,\n) -&gt; None:\n    \"\"\"Initialize the high-frequency tracker.\n\n    Args:\n        model: Neural network model to track\n        layers: List of layers to monitor. If None, auto-detects based on architecture\n        layer_names: Optional custom names for layers\n        sampling_frequency: Record metrics every N steps (default: 1)\n        enable_jump_detection: Whether to detect dimensionality jumps (default: True)\n        jump_window_size: Window size for jump detection (default: 50)\n        jump_z_threshold: Z-score threshold for jump detection (default: 3.0)\n        device: Device for computations (default: model's device)\n\n    Raises:\n        ValueError: If model or parameters are invalid\n    \"\"\"\n    if not isinstance(model, nn.Module):\n        raise ValueError(\"model must be a torch.nn.Module\")\n\n    self.model = model\n    self.sampling_frequency = sampling_frequency\n    self.device = device or next(model.parameters()).device\n\n    # Auto-detect layers if not provided\n    if layers is None:\n        layers = self._auto_detect_layers()\n\n    if not layers:\n        raise ValueError(\n            \"No layers to track. Provide layers explicitly or use supported architecture.\"\n        )\n\n    # Set up activation capture\n    self.activation_capture = ActivationCapture()\n    self.activation_capture.register_hooks(model, layers, layer_names)\n    self.layer_names = layer_names or [\n        f\"{layer.__class__.__name__}_{i}\" for i, layer in enumerate(layers)\n    ]\n\n    # Initialize storage\n    self.metrics_history: Dict[str, List[DimensionalityMetrics]] = {\n        name: [] for name in self.layer_names\n    }\n    self.step_counter = 0\n\n    # Jump detection\n    self.enable_jump_detection = enable_jump_detection\n    if enable_jump_detection:\n        self.jump_detector = JumpDetector(\n            window_size=jump_window_size, z_threshold=jump_z_threshold\n        )\n    else:\n        self.jump_detector = None\n\n    # Logging\n    self.logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/#ndt.core.tracker.HighFrequencyTracker--in-training-loop","title":"In training loop:","text":"<p>for step, (x, y) in enumerate(dataloader): ...     loss = train_step(model, x, y) ...     tracker.log(step, loss) results = tracker.get_results() tracker.close()</p>"},{"location":"api/#ndt.core.tracker.HighFrequencyTracker-functions","title":"Methods:","text":""},{"location":"api/#ndt.core.tracker.HighFrequencyTracker.log","title":"<code>log(step: int, loss: float, grad_norm: Optional[float] = None, force: bool = False) -&gt; None</code>","text":"<p>Log dimensionality metrics for the current step.</p> <p>This is the main method called during training. It captures activations and computes dimensionality metrics according to the sampling frequency.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>Current training step</p> required <code>loss</code> <code>float</code> <p>Training loss value</p> required <code>grad_norm</code> <code>Optional[float]</code> <p>Optional gradient norm</p> <code>None</code> <code>force</code> <code>bool</code> <p>Force logging even if not at sampling interval</p> <code>False</code> Example <p>for step, (x, y) in enumerate(dataloader): ...     optimizer.zero_grad() ...     output = model(x) ...     loss = criterion(output, y) ...     loss.backward() ...     optimizer.step() ...     tracker.log(step, loss.item())</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def log(\n    self, step: int, loss: float, grad_norm: Optional[float] = None, force: bool = False\n) -&gt; None:\n    \"\"\"Log dimensionality metrics for the current step.\n\n    This is the main method called during training. It captures activations\n    and computes dimensionality metrics according to the sampling frequency.\n\n    Args:\n        step: Current training step\n        loss: Training loss value\n        grad_norm: Optional gradient norm\n        force: Force logging even if not at sampling interval\n\n    Example:\n        &gt;&gt;&gt; for step, (x, y) in enumerate(dataloader):\n        ...     optimizer.zero_grad()\n        ...     output = model(x)\n        ...     loss = criterion(output, y)\n        ...     loss.backward()\n        ...     optimizer.step()\n        ...     tracker.log(step, loss.item())\n    \"\"\"\n    # Check if we should record this step\n    if not force and step % self.sampling_frequency != 0:\n        return\n\n    self.step_counter = step\n\n    # Activations are already captured by hooks during forward pass\n    # Now compute metrics for each layer\n    with torch.no_grad():\n        for layer_name in self.layer_names:\n            activation = self.activation_capture.get_activation(layer_name)\n\n            if activation is None:\n                self.logger.warning(\n                    f\"No activation captured for layer {layer_name} at step {step}\"\n                )\n                continue\n\n            try:\n                # Reshape activation to 2D matrix (batch_size, features)\n                matrix = self._prepare_activation_matrix(activation)\n\n                # Compute all dimensionality metrics\n                sr, pr, ce90, nnr = compute_all_metrics(matrix)\n\n                # Create metrics object\n                metrics = DimensionalityMetrics(\n                    step=step,\n                    stable_rank=sr,\n                    participation_ratio=pr,\n                    cumulative_90=ce90,\n                    nuclear_norm_ratio=nnr,\n                    loss=loss,\n                    grad_norm=grad_norm,\n                )\n\n                # Store metrics\n                self.metrics_history[layer_name].append(metrics)\n\n            except Exception as e:\n                self.logger.error(\n                    f\"Error computing metrics for {layer_name} at step {step}: {e}\"\n                )\n\n    # Clear activations for next step\n    self.activation_capture.clear_activations()\n</code></pre>"},{"location":"api/#ndt.core.tracker.HighFrequencyTracker.get_results","title":"<code>get_results(layer_name: Optional[str] = None) -&gt; Union[pd.DataFrame, Dict[str, pd.DataFrame]]</code>","text":"<p>Get tracked metrics as pandas DataFrame(s).</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>Optional[str]</code> <p>If specified, return results for that layer only.        If None, return dict of DataFrames for all layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Union[DataFrame, Dict[str, DataFrame]]</code> <p>DataFrame or dict of DataFrames containing metrics history</p> Example <p>results = tracker.get_results() for layer_name, df in results.items(): ...     print(f\"{layer_name}: {len(df)} measurements\")</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def get_results(\n    self, layer_name: Optional[str] = None\n) -&gt; Union[pd.DataFrame, Dict[str, pd.DataFrame]]:\n    \"\"\"Get tracked metrics as pandas DataFrame(s).\n\n    Args:\n        layer_name: If specified, return results for that layer only.\n                   If None, return dict of DataFrames for all layers.\n\n    Returns:\n        DataFrame or dict of DataFrames containing metrics history\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; for layer_name, df in results.items():\n        ...     print(f\"{layer_name}: {len(df)} measurements\")\n    \"\"\"\n    if layer_name is not None:\n        if layer_name not in self.metrics_history:\n            raise ValueError(f\"Unknown layer name: {layer_name}\")\n        return self._metrics_to_dataframe(self.metrics_history[layer_name])\n\n    return {\n        name: self._metrics_to_dataframe(metrics)\n        for name, metrics in self.metrics_history.items()\n    }\n</code></pre>"},{"location":"api/#ndt.core.tracker.HighFrequencyTracker.detect_jumps","title":"<code>detect_jumps(layer_name: Optional[str] = None, metric: str = 'stable_rank') -&gt; Dict[str, List]</code>","text":"<p>Detect dimensionality jumps in tracked metrics.</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>Optional[str]</code> <p>If specified, detect jumps for that layer only.        If None, detect for all layers.</p> <code>None</code> <code>metric</code> <code>str</code> <p>Which metric to analyze (\"stable_rank\", \"participation_ratio\",    \"cumulative_90\", or \"nuclear_norm_ratio\")</p> <code>'stable_rank'</code> <p>Returns:</p> Type Description <code>Dict[str, List]</code> <p>Dictionary mapping layer names to lists of Jump objects</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If jump detection is disabled or metric name is invalid</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def detect_jumps(\n    self, layer_name: Optional[str] = None, metric: str = \"stable_rank\"\n) -&gt; Dict[str, List]:\n    \"\"\"Detect dimensionality jumps in tracked metrics.\n\n    Args:\n        layer_name: If specified, detect jumps for that layer only.\n                   If None, detect for all layers.\n        metric: Which metric to analyze (\"stable_rank\", \"participation_ratio\",\n               \"cumulative_90\", or \"nuclear_norm_ratio\")\n\n    Returns:\n        Dictionary mapping layer names to lists of Jump objects\n\n    Raises:\n        ValueError: If jump detection is disabled or metric name is invalid\n    \"\"\"\n    if not self.enable_jump_detection:\n        raise ValueError(\"Jump detection is disabled. Enable it during initialization.\")\n\n    valid_metrics = [\n        \"stable_rank\",\n        \"participation_ratio\",\n        \"cumulative_90\",\n        \"nuclear_norm_ratio\",\n    ]\n    if metric not in valid_metrics:\n        raise ValueError(f\"metric must be one of {valid_metrics}, got {metric}\")\n\n    layers_to_analyze = [layer_name] if layer_name else self.layer_names\n\n    jumps_dict = {}\n    for name in layers_to_analyze:\n        if name not in self.metrics_history:\n            continue\n\n        # Extract metric values\n        values = [getattr(m, metric) for m in self.metrics_history[name]]\n\n        # Detect jumps\n        jumps = self.jump_detector.detect_jumps(values, metric_name=f\"{name}_{metric}\")\n        jumps_dict[name] = jumps\n\n    return jumps_dict\n</code></pre>"},{"location":"api/#ndt.core.tracker.HighFrequencyTracker.close","title":"<code>close() -&gt; None</code>","text":"<p>Clean up hooks and resources.</p> <p>Should be called when done tracking to free resources.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Clean up hooks and resources.\n\n    Should be called when done tracking to free resources.\n    \"\"\"\n    self.activation_capture.remove_hooks()\n    self.logger.info(f\"Tracker closed. Recorded {self.step_counter} steps.\")\n</code></pre>"},{"location":"api/#ndt.core.tracker.HighFrequencyTracker.__enter__","title":"<code>__enter__() -&gt; HighFrequencyTracker</code>","text":"<p>Context manager entry.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __enter__(self) -&gt; \"HighFrequencyTracker\":\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"api/#ndt.core.tracker.HighFrequencyTracker.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb) -&gt; None</code>","text":"<p>Context manager exit.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb) -&gt; None:\n    \"\"\"Context manager exit.\"\"\"\n    self.close()\n</code></pre>"},{"location":"api/#ndt.core.tracker.HighFrequencyTracker.__del__","title":"<code>__del__() -&gt; None</code>","text":"<p>Cleanup on deletion.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __del__(self) -&gt; None:\n    \"\"\"Cleanup on deletion.\"\"\"\n    if hasattr(self, \"activation_capture\"):\n        self.activation_capture.remove_hooks()\n</code></pre>"},{"location":"api/#dimensionalitymetrics","title":"DimensionalityMetrics","text":""},{"location":"api/#ndt.core.tracker.DimensionalityMetrics","title":"<code>ndt.core.tracker.DimensionalityMetrics(step: int, stable_rank: float, participation_ratio: float, cumulative_90: int, nuclear_norm_ratio: float, loss: float, grad_norm: Optional[float] = None)</code>  <code>dataclass</code>","text":"<p>Container for dimensionality measurements at a single training step.</p> <p>Attributes:</p> Name Type Description <code>step</code> <code>int</code> <p>Training step number</p> <code>stable_rank</code> <code>float</code> <p>Stable rank estimate</p> <code>participation_ratio</code> <code>float</code> <p>Participation ratio estimate</p> <code>cumulative_90</code> <code>int</code> <p>Number of components for 90% energy</p> <code>nuclear_norm_ratio</code> <code>float</code> <p>Nuclear norm ratio estimate</p> <code>loss</code> <code>float</code> <p>Training loss at this step</p> <code>grad_norm</code> <code>Optional[float]</code> <p>Gradient norm at this step (optional)</p>"},{"location":"api/#ndt.core.tracker.DimensionalityMetrics-functions","title":"Methods:","text":""},{"location":"api/#ndt.core.tracker.DimensionalityMetrics.to_dict","title":"<code>to_dict() -&gt; Dict</code>","text":"<p>Convert to dictionary.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def to_dict(self) -&gt; Dict:\n    \"\"\"Convert to dictionary.\"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"api/#activationcapture","title":"ActivationCapture","text":""},{"location":"api/#ndt.core.hooks.ActivationCapture","title":"<code>ndt.core.hooks.ActivationCapture()</code>","text":"<p>Captures activations from specified layers using forward hooks.</p> <p>This class manages PyTorch forward hooks to capture activations during the forward pass without modifying the model code.</p> <p>Attributes:</p> Name Type Description <code>activations</code> <code>Dict[str, Tensor]</code> <p>Dictionary mapping layer names to captured activations</p> <code>hooks</code> <code>List[RemovableHandle]</code> <p>List of registered hook handles</p> Example <p>import torch.nn as nn model = nn.Sequential(nn.Linear(10, 20), nn.ReLU(), nn.Linear(20, 10)) capture = ActivationCapture() capture.register_hooks(model, [model[0], model[2]]) output = model(torch.randn(32, 10)) print(capture.activations.keys()) capture.remove_hooks()</p> <p>Initialize the activation capture.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialize the activation capture.\"\"\"\n    self.activations: Dict[str, torch.Tensor] = {}\n    self.hooks: List[torch.utils.hooks.RemovableHandle] = []\n    self._layer_names: Dict[nn.Module, str] = {}\n</code></pre>"},{"location":"api/#ndt.core.hooks.ActivationCapture-functions","title":"Methods:","text":""},{"location":"api/#ndt.core.hooks.ActivationCapture.register_hooks","title":"<code>register_hooks(model: nn.Module, layers: List[nn.Module], layer_names: Optional[List[str]] = None) -&gt; None</code>","text":"<p>Register forward hooks on specified layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>layers</code> <code>List[Module]</code> <p>List of layer modules to monitor</p> required <code>layer_names</code> <code>Optional[List[str]]</code> <p>Optional custom names for layers. If None, uses module class names.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If number of layer names doesn't match number of layers</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def register_hooks(\n    self, model: nn.Module, layers: List[nn.Module], layer_names: Optional[List[str]] = None\n) -&gt; None:\n    \"\"\"Register forward hooks on specified layers.\n\n    Args:\n        model: The neural network model\n        layers: List of layer modules to monitor\n        layer_names: Optional custom names for layers. If None, uses module class names.\n\n    Raises:\n        ValueError: If number of layer names doesn't match number of layers\n    \"\"\"\n    if layer_names is not None and len(layer_names) != len(layers):\n        raise ValueError(\n            f\"Number of layer names ({len(layer_names)}) must match \"\n            f\"number of layers ({len(layers)})\"\n        )\n\n    # Generate default names if not provided\n    if layer_names is None:\n        layer_names = [f\"{layer.__class__.__name__}_{i}\" for i, layer in enumerate(layers)]\n\n    # Register hooks\n    for layer, name in zip(layers, layer_names):\n        hook_handle = layer.register_forward_hook(self._create_hook(name))\n        self.hooks.append(hook_handle)\n        self._layer_names[layer] = name\n</code></pre>"},{"location":"api/#ndt.core.hooks.ActivationCapture.remove_hooks","title":"<code>remove_hooks() -&gt; None</code>","text":"<p>Remove all registered hooks and clear stored activations.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def remove_hooks(self) -&gt; None:\n    \"\"\"Remove all registered hooks and clear stored activations.\"\"\"\n    for hook in self.hooks:\n        hook.remove()\n    self.hooks.clear()\n    self.activations.clear()\n    self._layer_names.clear()\n</code></pre>"},{"location":"api/#ndt.core.hooks.ActivationCapture.clear_activations","title":"<code>clear_activations() -&gt; None</code>","text":"<p>Clear stored activations without removing hooks.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def clear_activations(self) -&gt; None:\n    \"\"\"Clear stored activations without removing hooks.\"\"\"\n    self.activations.clear()\n</code></pre>"},{"location":"api/#ndt.core.hooks.ActivationCapture.get_activation","title":"<code>get_activation(layer_name: str) -&gt; Optional[torch.Tensor]</code>","text":"<p>Get activation for a specific layer.</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>str</code> <p>Name of the layer</p> required <p>Returns:</p> Type Description <code>Optional[Tensor]</code> <p>Activation tensor if available, None otherwise</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def get_activation(self, layer_name: str) -&gt; Optional[torch.Tensor]:\n    \"\"\"Get activation for a specific layer.\n\n    Args:\n        layer_name: Name of the layer\n\n    Returns:\n        Activation tensor if available, None otherwise\n    \"\"\"\n    return self.activations.get(layer_name)\n</code></pre>"},{"location":"api/#ndt.core.hooks.ActivationCapture.get_all_activations","title":"<code>get_all_activations() -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Get all captured activations.</p> <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>Dictionary mapping layer names to activation tensors</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def get_all_activations(self) -&gt; Dict[str, torch.Tensor]:\n    \"\"\"Get all captured activations.\n\n    Returns:\n        Dictionary mapping layer names to activation tensors\n    \"\"\"\n    return self.activations.copy()\n</code></pre>"},{"location":"api/#ndt.core.hooks.ActivationCapture.__enter__","title":"<code>__enter__() -&gt; ActivationCapture</code>","text":"<p>Context manager entry.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __enter__(self) -&gt; \"ActivationCapture\":\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"api/#ndt.core.hooks.ActivationCapture.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb) -&gt; None</code>","text":"<p>Context manager exit - removes hooks.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb) -&gt; None:\n    \"\"\"Context manager exit - removes hooks.\"\"\"\n    self.remove_hooks()\n</code></pre>"},{"location":"api/#ndt.core.hooks.ActivationCapture.__del__","title":"<code>__del__() -&gt; None</code>","text":"<p>Cleanup hooks on deletion.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __del__(self) -&gt; None:\n    \"\"\"Cleanup hooks on deletion.\"\"\"\n    self.remove_hooks()\n</code></pre>"},{"location":"api/#jumpdetector","title":"JumpDetector","text":""},{"location":"api/#ndt.core.jump_detector.JumpDetector","title":"<code>ndt.core.jump_detector.JumpDetector(window_size: int = 50, z_threshold: float = 3.0, min_samples: int = 20)</code>","text":"<p>Detects significant jumps in dimensionality metrics using Z-score analysis.</p> <p>This class uses a rolling window to compute mean and standard deviation, then identifies jumps that exceed a specified Z-score threshold.</p> <p>Attributes:</p> Name Type Description <code>window_size</code> <p>Size of the rolling window for statistics</p> <code>z_threshold</code> <p>Z-score threshold for jump detection</p> <code>min_samples</code> <p>Minimum number of samples before detecting jumps</p> Example <p>detector = JumpDetector(window_size=50, z_threshold=3.0) values = [10.0] * 100 + [20.0] * 100  # Simulated jump jumps = detector.detect_jumps(values, metric_name=\"stable_rank\") for jump in jumps: ...     print(jump)</p> <p>Initialize the jump detector.</p> <p>Parameters:</p> Name Type Description Default <code>window_size</code> <code>int</code> <p>Size of rolling window for computing statistics (default: 50)</p> <code>50</code> <code>z_threshold</code> <code>float</code> <p>Z-score threshold for detecting jumps (default: 3.0)</p> <code>3.0</code> <code>min_samples</code> <code>int</code> <p>Minimum samples before starting detection (default: 20)</p> <code>20</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If parameters are invalid</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def __init__(\n    self, window_size: int = 50, z_threshold: float = 3.0, min_samples: int = 20\n) -&gt; None:\n    \"\"\"Initialize the jump detector.\n\n    Args:\n        window_size: Size of rolling window for computing statistics (default: 50)\n        z_threshold: Z-score threshold for detecting jumps (default: 3.0)\n        min_samples: Minimum samples before starting detection (default: 20)\n\n    Raises:\n        ValueError: If parameters are invalid\n    \"\"\"\n    if window_size &lt; 2:\n        raise ValueError(f\"window_size must be &gt;= 2, got {window_size}\")\n    if z_threshold &lt;= 0:\n        raise ValueError(f\"z_threshold must be &gt; 0, got {z_threshold}\")\n    if min_samples &lt; 1:\n        raise ValueError(f\"min_samples must be &gt;= 1, got {min_samples}\")\n\n    self.window_size = window_size\n    self.z_threshold = z_threshold\n    self.min_samples = min_samples\n</code></pre>"},{"location":"api/#ndt.core.jump_detector.JumpDetector-functions","title":"Methods:","text":""},{"location":"api/#ndt.core.jump_detector.JumpDetector.detect_jumps","title":"<code>detect_jumps(values: List[float], metric_name: str = 'dimensionality', step_offset: int = 0) -&gt; List[Jump]</code>","text":"<p>Detect jumps in a sequence of dimensionality values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <code>metric_name</code> <code>str</code> <p>Name of the metric being analyzed</p> <code>'dimensionality'</code> <code>step_offset</code> <code>int</code> <p>Offset to add to step numbers (for aligning with training steps)</p> <code>0</code> <p>Returns:</p> Type Description <code>List[Jump]</code> <p>List of detected Jump objects</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If values list is too short</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def detect_jumps(\n    self, values: List[float], metric_name: str = \"dimensionality\", step_offset: int = 0\n) -&gt; List[Jump]:\n    \"\"\"Detect jumps in a sequence of dimensionality values.\n\n    Args:\n        values: List of dimensionality measurements\n        metric_name: Name of the metric being analyzed\n        step_offset: Offset to add to step numbers (for aligning with training steps)\n\n    Returns:\n        List of detected Jump objects\n\n    Raises:\n        ValueError: If values list is too short\n    \"\"\"\n    if len(values) &lt; self.min_samples:\n        return []\n\n    values_array = np.array(values)\n    jumps = []\n\n    # Start detection after minimum samples\n    for i in range(self.min_samples, len(values_array)):\n        # Define window for statistics\n        window_start = max(0, i - self.window_size)\n        window = values_array[window_start:i]\n\n        # Compute statistics\n        mean = np.mean(window)\n        std = np.std(window)\n\n        # Avoid division by zero\n        if std &lt; 1e-10:\n            continue\n\n        # Compute z-score for current value\n        current_value = values_array[i]\n        z_score = abs((current_value - mean) / std)\n\n        # Detect jump\n        if z_score &gt; self.z_threshold:\n            jump = Jump(\n                step=i + step_offset,\n                z_score=z_score,\n                value_before=values_array[i - 1],\n                value_after=current_value,\n                metric_name=metric_name,\n            )\n            jumps.append(jump)\n\n    return jumps\n</code></pre>"},{"location":"api/#ndt.core.jump_detector.JumpDetector.detect_jumps_with_direction","title":"<code>detect_jumps_with_direction(values: List[float], metric_name: str = 'dimensionality', step_offset: int = 0, direction: Optional[str] = None) -&gt; List[Jump]</code>","text":"<p>Detect jumps with optional direction filtering.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <code>metric_name</code> <code>str</code> <p>Name of the metric being analyzed</p> <code>'dimensionality'</code> <code>step_offset</code> <code>int</code> <p>Offset to add to step numbers</p> <code>0</code> <code>direction</code> <code>Optional[str]</code> <p>Filter by direction - \"up\", \"down\", or None for both</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Jump]</code> <p>List of detected Jump objects filtered by direction</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction is not valid</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def detect_jumps_with_direction(\n    self,\n    values: List[float],\n    metric_name: str = \"dimensionality\",\n    step_offset: int = 0,\n    direction: Optional[str] = None,\n) -&gt; List[Jump]:\n    \"\"\"Detect jumps with optional direction filtering.\n\n    Args:\n        values: List of dimensionality measurements\n        metric_name: Name of the metric being analyzed\n        step_offset: Offset to add to step numbers\n        direction: Filter by direction - \"up\", \"down\", or None for both\n\n    Returns:\n        List of detected Jump objects filtered by direction\n\n    Raises:\n        ValueError: If direction is not valid\n    \"\"\"\n    if direction not in [None, \"up\", \"down\"]:\n        raise ValueError(f\"direction must be None, 'up', or 'down', got {direction}\")\n\n    all_jumps = self.detect_jumps(values, metric_name, step_offset)\n\n    if direction is None:\n        return all_jumps\n\n    # Filter by direction\n    filtered_jumps = []\n    for jump in all_jumps:\n        change = jump.value_after - jump.value_before\n        if direction == \"up\" and change &gt; 0:\n            filtered_jumps.append(jump)\n        elif direction == \"down\" and change &lt; 0:\n            filtered_jumps.append(jump)\n\n    return filtered_jumps\n</code></pre>"},{"location":"api/#ndt.core.jump_detector.JumpDetector.compute_rolling_statistics","title":"<code>compute_rolling_statistics(values: List[float]) -&gt; Tuple[np.ndarray, np.ndarray, np.ndarray]</code>","text":"<p>Compute rolling mean, std, and z-scores for visualization.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray, ndarray]</code> <p>Tuple of (means, stds, z_scores) as numpy arrays</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If values list is empty</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def compute_rolling_statistics(\n    self, values: List[float]\n) -&gt; Tuple[np.ndarray, np.ndarray, np.ndarray]:\n    \"\"\"Compute rolling mean, std, and z-scores for visualization.\n\n    Args:\n        values: List of dimensionality measurements\n\n    Returns:\n        Tuple of (means, stds, z_scores) as numpy arrays\n\n    Raises:\n        ValueError: If values list is empty\n    \"\"\"\n    if len(values) == 0:\n        raise ValueError(\"values list cannot be empty\")\n\n    values_array = np.array(values)\n    n = len(values_array)\n\n    means = np.zeros(n)\n    stds = np.zeros(n)\n    z_scores = np.zeros(n)\n\n    for i in range(self.min_samples, n):\n        window_start = max(0, i - self.window_size)\n        window = values_array[window_start:i]\n\n        means[i] = np.mean(window)\n        stds[i] = np.std(window)\n\n        if stds[i] &gt; 1e-10:\n            z_scores[i] = abs((values_array[i] - means[i]) / stds[i])\n\n    return means, stds, z_scores\n</code></pre>"},{"location":"api/#jump","title":"Jump","text":""},{"location":"api/#ndt.core.jump_detector.Jump","title":"<code>ndt.core.jump_detector.Jump(step: int, z_score: float, value_before: float, value_after: float, metric_name: str)</code>  <code>dataclass</code>","text":"<p>Represents a detected jump in dimensionality.</p> <p>Attributes:</p> Name Type Description <code>step</code> <code>int</code> <p>Training step where jump occurred</p> <code>z_score</code> <code>float</code> <p>Z-score magnitude of the jump</p> <code>value_before</code> <code>float</code> <p>Dimensionality value before jump</p> <code>value_after</code> <code>float</code> <p>Dimensionality value after jump</p> <code>metric_name</code> <code>str</code> <p>Name of the metric that jumped</p>"},{"location":"api/#ndt.core.jump_detector.Jump-functions","title":"Methods:","text":""},{"location":"api/#ndt.core.jump_detector.Jump.__repr__","title":"<code>__repr__() -&gt; str</code>","text":"<p>String representation of the jump.</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"String representation of the jump.\"\"\"\n    return (\n        f\"Jump(step={self.step}, metric={self.metric_name}, \"\n        f\"z_score={self.z_score:.2f}, \"\n        f\"\u0394={self.value_after - self.value_before:.2f})\"\n    )\n</code></pre>"},{"location":"api/#dimensionality-estimators","title":"Dimensionality Estimators","text":""},{"location":"api/#stable_rank","title":"stable_rank","text":""},{"location":"api/#ndt.core.estimators.stable_rank","title":"<code>ndt.core.estimators.stable_rank(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute stable rank of a matrix.</p> <p>Stable rank is the ratio of squared Frobenius norm to squared spectral norm. It estimates the effective number of linearly independent dimensions.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Stable rank value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or contains NaN/Inf values</p> Example <p>import torch A = torch.randn(100, 50) sr = stable_rank(A) print(f\"Stable rank: {sr:.2f}\")</p> References <p>Vershynin, R. (2010). Introduction to the non-asymptotic analysis of random matrices. arXiv preprint arXiv:1011.3027.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def stable_rank(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute stable rank of a matrix.\n\n    Stable rank is the ratio of squared Frobenius norm to squared spectral norm.\n    It estimates the effective number of linearly independent dimensions.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Stable rank value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or contains NaN/Inf values\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; sr = stable_rank(A)\n        &gt;&gt;&gt; print(f\"Stable rank: {sr:.2f}\")\n\n    References:\n        Vershynin, R. (2010). Introduction to the non-asymptotic analysis\n        of random matrices. arXiv preprint arXiv:1011.3027.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    if torch.isnan(matrix).any() or torch.isinf(matrix).any():\n        raise ValueError(\"Matrix contains NaN or Inf values\")\n\n    with torch.no_grad():\n        frobenius_norm = torch.norm(matrix, p=\"fro\")\n        # Spectral norm is the largest singular value\n        _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        spectral_norm = singular_values[0]\n\n        result = (frobenius_norm**2) / (spectral_norm**2 + eps)\n        return result.item()\n</code></pre>"},{"location":"api/#participation_ratio","title":"participation_ratio","text":""},{"location":"api/#ndt.core.estimators.participation_ratio","title":"<code>ndt.core.estimators.participation_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute participation ratio from singular values.</p> <p>Participation ratio is the inverse of the sum of squared normalized singular values. It measures the effective dimensionality of the representation space.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Participation ratio value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or SVD computation fails</p> Example <p>import torch A = torch.randn(100, 50) pr = participation_ratio(A) print(f\"Participation ratio: {pr:.2f}\")</p> References <p>Grassberger, P., &amp; Procaccia, I. (1983). Measuring the strangeness of strange attractors. Physica D: Nonlinear Phenomena, 9(1-2), 189-208.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def participation_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute participation ratio from singular values.\n\n    Participation ratio is the inverse of the sum of squared normalized\n    singular values. It measures the effective dimensionality of the\n    representation space.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Participation ratio value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or SVD computation fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; pr = participation_ratio(A)\n        &gt;&gt;&gt; print(f\"Participation ratio: {pr:.2f}\")\n\n    References:\n        Grassberger, P., &amp; Procaccia, I. (1983). Measuring the strangeness\n        of strange attractors. Physica D: Nonlinear Phenomena, 9(1-2), 189-208.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Normalize singular values\n        sv_normalized = singular_values / (torch.sum(singular_values) + eps)\n\n        # Compute participation ratio\n        pr = 1.0 / (torch.sum(sv_normalized**2) + eps)\n        return pr.item()\n</code></pre>"},{"location":"api/#cumulative_energy_90","title":"cumulative_energy_90","text":""},{"location":"api/#ndt.core.estimators.cumulative_energy_90","title":"<code>ndt.core.estimators.cumulative_energy_90(matrix: torch.Tensor, threshold: float = 0.9) -&gt; int</code>","text":"<p>Compute number of components needed for 90% cumulative energy.</p> <p>This metric counts how many principal components are needed to capture a given percentage (default 90%) of the total variance.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>threshold</code> <code>float</code> <p>Energy threshold (default: 0.90 for 90%)</p> <code>0.9</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of components needed (int)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D, threshold invalid, or SVD fails</p> Example <p>import torch A = torch.randn(100, 50) n_components = cumulative_energy_90(A) print(f\"Components for 90% energy: {n_components}\")</p> References <p>Jolliffe, I. T. (2002). Principal component analysis (2nd ed.). Springer.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def cumulative_energy_90(matrix: torch.Tensor, threshold: float = 0.90) -&gt; int:\n    \"\"\"Compute number of components needed for 90% cumulative energy.\n\n    This metric counts how many principal components are needed to capture\n    a given percentage (default 90%) of the total variance.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        threshold: Energy threshold (default: 0.90 for 90%)\n\n    Returns:\n        Number of components needed (int)\n\n    Raises:\n        ValueError: If matrix is not 2D, threshold invalid, or SVD fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; n_components = cumulative_energy_90(A)\n        &gt;&gt;&gt; print(f\"Components for 90% energy: {n_components}\")\n\n    References:\n        Jolliffe, I. T. (2002). Principal component analysis (2nd ed.).\n        Springer.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    if not 0 &lt; threshold &lt;= 1:\n        raise ValueError(f\"Threshold must be in (0, 1], got {threshold}\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Compute explained variance\n        explained_variance = singular_values**2\n        total_variance = torch.sum(explained_variance)\n\n        # Compute cumulative explained variance ratio\n        cumsum = torch.cumsum(explained_variance, dim=0)\n        cumsum_ratio = cumsum / total_variance\n\n        # Find number of components for threshold\n        n_components = torch.searchsorted(cumsum_ratio, threshold).item() + 1\n        return min(n_components, len(singular_values))\n</code></pre>"},{"location":"api/#nuclear_norm_ratio","title":"nuclear_norm_ratio","text":""},{"location":"api/#ndt.core.estimators.nuclear_norm_ratio","title":"<code>ndt.core.estimators.nuclear_norm_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute nuclear norm ratio (nuclear norm / spectral norm).</p> <p>This metric normalizes the nuclear norm (sum of singular values) by the spectral norm (largest singular value), providing a normalized measure of rank.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Nuclear norm ratio value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or SVD computation fails</p> Example <p>import torch A = torch.randn(100, 50) nnr = nuclear_norm_ratio(A) print(f\"Nuclear norm ratio: {nnr:.2f}\")</p> References <p>Cand\u00e8s, E. J., &amp; Recht, B. (2009). Exact matrix completion via convex optimization. Foundations of Computational mathematics, 9(6), 717-772.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def nuclear_norm_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute nuclear norm ratio (nuclear norm / spectral norm).\n\n    This metric normalizes the nuclear norm (sum of singular values) by\n    the spectral norm (largest singular value), providing a normalized\n    measure of rank.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Nuclear norm ratio value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or SVD computation fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; nnr = nuclear_norm_ratio(A)\n        &gt;&gt;&gt; print(f\"Nuclear norm ratio: {nnr:.2f}\")\n\n    References:\n        Cand\u00e8s, E. J., &amp; Recht, B. (2009). Exact matrix completion via\n        convex optimization. Foundations of Computational mathematics, 9(6), 717-772.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        nuclear_norm = torch.sum(singular_values)\n        spectral_norm = singular_values[0]  # Largest singular value\n\n        ratio = nuclear_norm / (spectral_norm + eps)\n        return ratio.item()\n</code></pre>"},{"location":"api/#compute_all_metrics","title":"compute_all_metrics","text":""},{"location":"api/#ndt.core.estimators.compute_all_metrics","title":"<code>ndt.core.estimators.compute_all_metrics(matrix: torch.Tensor, eps: float = 1e-10) -&gt; Tuple[float, float, int, float]</code>","text":"<p>Compute all four dimensionality metrics efficiently.</p> <p>This function computes SVD once and reuses it for all metrics, improving efficiency when all metrics are needed.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>Tuple[float, float, int, float]</code> <p>Tuple of (stable_rank, participation_ratio, cumulative_90, nuclear_norm_ratio)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or computations fail</p> Example <p>import torch A = torch.randn(100, 50) sr, pr, ce90, nnr = compute_all_metrics(A) print(f\"Metrics: SR={sr:.2f}, PR={pr:.2f}, CE90={ce90}, NNR={nnr:.2f}\")</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def compute_all_metrics(\n    matrix: torch.Tensor, eps: float = 1e-10\n) -&gt; Tuple[float, float, int, float]:\n    \"\"\"Compute all four dimensionality metrics efficiently.\n\n    This function computes SVD once and reuses it for all metrics,\n    improving efficiency when all metrics are needed.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Tuple of (stable_rank, participation_ratio, cumulative_90, nuclear_norm_ratio)\n\n    Raises:\n        ValueError: If matrix is not 2D or computations fail\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; sr, pr, ce90, nnr = compute_all_metrics(A)\n        &gt;&gt;&gt; print(f\"Metrics: SR={sr:.2f}, PR={pr:.2f}, CE90={ce90}, NNR={nnr:.2f}\")\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        # Compute SVD once for all metrics\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Compute norms for stable rank\n        frobenius_norm = torch.norm(matrix, p=\"fro\")\n        spectral_norm = singular_values[0]  # Largest singular value\n        sr = ((frobenius_norm**2) / (spectral_norm**2 + eps)).item()\n\n        # Participation ratio\n        sv_normalized = singular_values / (torch.sum(singular_values) + eps)\n        pr = (1.0 / (torch.sum(sv_normalized**2) + eps)).item()\n\n        # Cumulative energy 90%\n        explained_variance = singular_values**2\n        total_variance = torch.sum(explained_variance)\n        cumsum = torch.cumsum(explained_variance, dim=0)\n        cumsum_ratio = cumsum / total_variance\n        ce90 = torch.searchsorted(cumsum_ratio, 0.90).item() + 1\n        ce90 = min(ce90, len(singular_values))\n\n        # Nuclear norm ratio\n        nuclear_norm = torch.sum(singular_values)\n        nnr = (nuclear_norm / (singular_values[0] + eps)).item()\n\n        return sr, pr, ce90, nnr\n</code></pre>"},{"location":"api/#architectures","title":"Architectures","text":""},{"location":"api/#detect_architecture","title":"detect_architecture","text":""},{"location":"api/#ndt.architectures.detect_architecture","title":"<code>ndt.architectures.detect_architecture(model: nn.Module) -&gt; Optional[ArchitectureHandler]</code>","text":"<p>Automatically detect the architecture type of a model.</p> <p>Tries each handler in priority order and returns the first match.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>Optional[ArchitectureHandler]</code> <p>ArchitectureHandler instance if detected, None otherwise</p> Example <p>import torch.nn as nn model = nn.Sequential(nn.Linear(784, 512), nn.ReLU(), nn.Linear(512, 10)) handler = detect_architecture(model) print(handler.get_name()) 'MLP'</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def detect_architecture(model: nn.Module) -&gt; Optional[ArchitectureHandler]:\n    \"\"\"Automatically detect the architecture type of a model.\n\n    Tries each handler in priority order and returns the first match.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        ArchitectureHandler instance if detected, None otherwise\n\n    Example:\n        &gt;&gt;&gt; import torch.nn as nn\n        &gt;&gt;&gt; model = nn.Sequential(nn.Linear(784, 512), nn.ReLU(), nn.Linear(512, 10))\n        &gt;&gt;&gt; handler = detect_architecture(model)\n        &gt;&gt;&gt; print(handler.get_name())\n        'MLP'\n    \"\"\"\n    for handler in DEFAULT_HANDLERS:\n        if handler.validate_model(model):\n            return handler\n    return None\n</code></pre>"},{"location":"api/#get_handler","title":"get_handler","text":""},{"location":"api/#ndt.architectures.get_handler","title":"<code>ndt.architectures.get_handler(model: nn.Module, architecture: Optional[str] = None) -&gt; ArchitectureHandler</code>","text":"<p>Get an architecture handler for a model.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>architecture</code> <code>Optional[str]</code> <p>Optional architecture name (\"mlp\", \"cnn\", \"transformer\", \"vit\").          If None, auto-detects.</p> <code>None</code> <p>Returns:</p> Type Description <code>ArchitectureHandler</code> <p>ArchitectureHandler instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If architecture cannot be detected or invalid name provided</p> Example <p>handler = get_handler(model, architecture=\"mlp\") layers = handler.get_activation_layers(model)</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def get_handler(model: nn.Module, architecture: Optional[str] = None) -&gt; ArchitectureHandler:\n    \"\"\"Get an architecture handler for a model.\n\n    Args:\n        model: The neural network model\n        architecture: Optional architecture name (\"mlp\", \"cnn\", \"transformer\", \"vit\").\n                     If None, auto-detects.\n\n    Returns:\n        ArchitectureHandler instance\n\n    Raises:\n        ValueError: If architecture cannot be detected or invalid name provided\n\n    Example:\n        &gt;&gt;&gt; handler = get_handler(model, architecture=\"mlp\")\n        &gt;&gt;&gt; layers = handler.get_activation_layers(model)\n    \"\"\"\n    if architecture is not None:\n        # Explicit architecture specified\n        architecture = architecture.lower()\n        handler_map = {\n            \"mlp\": MLPHandler(),\n            \"cnn\": CNNHandler(),\n            \"transformer\": TransformerHandler(),\n            \"vit\": ViTHandler(),\n        }\n\n        if architecture not in handler_map:\n            raise ValueError(\n                f\"Unknown architecture: {architecture}. Must be one of {list(handler_map.keys())}\"\n            )\n\n        handler = handler_map[architecture]\n\n        # Validate that the model actually matches\n        if not handler.validate_model(model):\n            raise ValueError(\n                f\"Model does not appear to be a {architecture.upper()}. \"\n                \"Try auto-detection by not specifying architecture.\"\n            )\n\n        return handler\n\n    # Auto-detect architecture\n    handler = detect_architecture(model)\n    if handler is None:\n        raise ValueError(\n            \"Could not auto-detect model architecture. \"\n            \"Please specify architecture explicitly: 'mlp', 'cnn', 'transformer', or 'vit'\"\n        )\n\n    return handler\n</code></pre>"},{"location":"api/#mlphandler","title":"MLPHandler","text":""},{"location":"api/#ndt.architectures.MLPHandler","title":"<code>ndt.architectures.MLPHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for fully-connected (MLP) networks.</p> <p>Monitors all Linear layers in the network.</p>"},{"location":"api/#ndt.architectures.MLPHandler-functions","title":"Methods:","text":""},{"location":"api/#ndt.architectures.MLPHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is an MLP (contains Linear layers, no Conv/Attention).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be an MLP</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is an MLP (contains Linear layers, no Conv/Attention).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be an MLP\n    \"\"\"\n    has_linear = False\n    has_conv = False\n    has_attention = False\n\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            has_linear = True\n        elif isinstance(module, (nn.Conv1d, nn.Conv2d, nn.Conv3d)):\n            has_conv = True\n        elif isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n\n    # MLP should have Linear layers but not Conv or Attention\n    return has_linear and not has_conv and not has_attention\n</code></pre>"},{"location":"api/#ndt.architectures.MLPHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get all Linear layers for monitoring.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of Linear layer modules</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get all Linear layers for monitoring.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of Linear layer modules\n    \"\"\"\n    layers = []\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            layers.append(module)\n    return layers\n</code></pre>"},{"location":"api/#ndt.architectures.MLPHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for Linear layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names like \"Linear_0\", \"Linear_1\", etc.</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for Linear layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names like \"Linear_0\", \"Linear_1\", etc.\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    for i, layer in enumerate(layers):\n        in_features = layer.in_features\n        out_features = layer.out_features\n        names.append(f\"Linear_{i}_{in_features}x{out_features}\")\n\n    return names\n</code></pre>"},{"location":"api/#cnnhandler","title":"CNNHandler","text":""},{"location":"api/#ndt.architectures.CNNHandler","title":"<code>ndt.architectures.CNNHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Convolutional Neural Networks (CNNs).</p> <p>Monitors Conv2d layers and the first Linear layer (pre-classifier). Supports both simple CNNs and ResNet-style architectures.</p>"},{"location":"api/#ndt.architectures.CNNHandler-functions","title":"Methods:","text":""},{"location":"api/#ndt.architectures.CNNHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a CNN (contains Conv layers).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a CNN</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a CNN (contains Conv layers).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a CNN\n    \"\"\"\n    has_conv = False\n    for module in model.modules():\n        if isinstance(module, (nn.Conv1d, nn.Conv2d, nn.Conv3d)):\n            has_conv = True\n            break\n    return has_conv\n</code></pre>"},{"location":"api/#ndt.architectures.CNNHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get Conv2d and key Linear layers for monitoring.</p> <p>For ResNets, monitors after each residual block. For simple CNNs, monitors each conv layer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get Conv2d and key Linear layers for monitoring.\n\n    For ResNets, monitors after each residual block.\n    For simple CNNs, monitors each conv layer.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # Check if it's a ResNet-style architecture\n    if self._is_resnet(model):\n        layers = self._get_resnet_layers(model)\n    else:\n        # Simple CNN: monitor all conv layers\n        for module in model.modules():\n            if isinstance(module, nn.Conv2d):\n                layers.append(module)\n\n        # Also monitor first Linear layer (before classifier)\n        linear_layers = [m for m in model.modules() if isinstance(m, nn.Linear)]\n        if linear_layers:\n            layers.append(linear_layers[0])\n\n    return layers\n</code></pre>"},{"location":"api/#ndt.architectures.CNNHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate descriptive names for CNN layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate descriptive names for CNN layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    conv_count = 0\n    linear_count = 0\n\n    for layer in layers:\n        if isinstance(layer, nn.Conv2d):\n            names.append(f\"Conv_{conv_count}_{layer.out_channels}ch\")\n            conv_count += 1\n        elif isinstance(layer, nn.Linear):\n            names.append(f\"FC_{linear_count}_{layer.out_features}\")\n            linear_count += 1\n\n    return names\n</code></pre>"},{"location":"api/#transformerhandler","title":"TransformerHandler","text":""},{"location":"api/#ndt.architectures.TransformerHandler","title":"<code>ndt.architectures.TransformerHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Transformer-based architectures.</p> <p>Monitors attention outputs and feed-forward network outputs in each layer. Compatible with BERT, GPT, and similar transformer models.</p>"},{"location":"api/#ndt.architectures.TransformerHandler-functions","title":"Methods:","text":""},{"location":"api/#ndt.architectures.TransformerHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a Transformer (contains MultiheadAttention).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a Transformer</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a Transformer (contains MultiheadAttention).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a Transformer\n    \"\"\"\n    has_attention = False\n    for module in model.modules():\n        if isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n            break\n        # Also check for common transformer layer names\n        if module.__class__.__name__ in [\"TransformerEncoderLayer\", \"TransformerDecoderLayer\"]:\n            has_attention = True\n            break\n    return has_attention\n</code></pre>"},{"location":"api/#ndt.architectures.TransformerHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get attention and FFN layers for monitoring.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get attention and FFN layers for monitoring.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # Look for MultiheadAttention modules\n    for module in model.modules():\n        if isinstance(module, nn.MultiheadAttention):\n            layers.append(module)\n\n    # Also look for Linear layers in feed-forward blocks\n    # Typically the first Linear in each transformer block's FFN\n    in_transformer_block = False\n    for name, module in model.named_modules():\n        if \"encoder\" in name.lower() or \"decoder\" in name.lower() or \"layer\" in name.lower():\n            in_transformer_block = True\n\n        if in_transformer_block and isinstance(module, nn.Linear):\n            # Add FFN layers (typically 2 per transformer block)\n            if \"fc1\" in name or \"fc2\" in name or \"ffn\" in name.lower():\n                layers.append(module)\n\n    # If no structured layers found, fall back to sampling Linear layers\n    if not layers:\n        linear_layers = [m for m in model.modules() if isinstance(m, nn.Linear)]\n        # Sample every 2nd Linear layer to avoid too many\n        layers = linear_layers[::2] if len(linear_layers) &gt; 10 else linear_layers\n\n    return layers\n</code></pre>"},{"location":"api/#ndt.architectures.TransformerHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for Transformer layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for Transformer layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    attn_count = 0\n    ffn_count = 0\n    fc_count = 0\n\n    for layer in layers:\n        if isinstance(layer, nn.MultiheadAttention):\n            names.append(f\"Attention_{attn_count}\")\n            attn_count += 1\n        elif isinstance(layer, nn.Linear):\n            # Try to determine if it's an FFN layer\n            layer_name = None\n            for name, module in model.named_modules():\n                if module is layer:\n                    layer_name = name\n                    break\n\n            if layer_name and (\"fc\" in layer_name.lower() or \"ffn\" in layer_name.lower()):\n                names.append(f\"FFN_{ffn_count}_{layer.out_features}\")\n                ffn_count += 1\n            else:\n                names.append(f\"FC_{fc_count}_{layer.out_features}\")\n                fc_count += 1\n\n    return names\n</code></pre>"},{"location":"api/#vithandler","title":"ViTHandler","text":""},{"location":"api/#ndt.architectures.ViTHandler","title":"<code>ndt.architectures.ViTHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Vision Transformer architectures.</p> <p>Monitors patch embeddings, attention layers, and MLP blocks.</p>"},{"location":"api/#ndt.architectures.ViTHandler-functions","title":"Methods:","text":""},{"location":"api/#ndt.architectures.ViTHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a Vision Transformer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a ViT</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a Vision Transformer.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a ViT\n    \"\"\"\n    # ViTs typically have both conv (for patch embedding) and attention\n    has_conv = False\n    has_attention = False\n    has_patch_embed = False\n\n    for name, module in model.named_modules():\n        if isinstance(module, (nn.Conv2d, nn.Conv1d)):\n            has_conv = True\n            if \"patch\" in name.lower() or \"embed\" in name.lower():\n                has_patch_embed = True\n        if isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n\n    # ViT has conv (patch embedding) + attention, or explicit patch embedding\n    return (has_conv and has_attention) or has_patch_embed\n</code></pre>"},{"location":"api/#ndt.architectures.ViTHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get key layers to monitor in ViT.</p> <p>Monitors: patch embedding, attention layers, and MLP blocks.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get key layers to monitor in ViT.\n\n    Monitors: patch embedding, attention layers, and MLP blocks.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # 1. Patch embedding layer\n    for name, module in model.named_modules():\n        if isinstance(module, (nn.Conv2d, nn.Linear)):\n            if \"patch\" in name.lower() or \"embed\" in name.lower():\n                layers.append(module)\n                break  # Usually just one\n\n    # 2. Attention layers\n    attn_layers = [m for m in model.modules() if isinstance(m, nn.MultiheadAttention)]\n    layers.extend(attn_layers)\n\n    # 3. MLP/FFN blocks (sample to avoid too many)\n    mlp_layers = []\n    for name, module in model.named_modules():\n        if isinstance(module, nn.Linear) and (\"mlp\" in name.lower() or \"ffn\" in name.lower()):\n            mlp_layers.append(module)\n\n    # Sample MLP layers if too many (keep first Linear of each MLP block)\n    if len(mlp_layers) &gt; 10:\n        mlp_layers = mlp_layers[::2]\n\n    layers.extend(mlp_layers)\n\n    return layers\n</code></pre>"},{"location":"api/#ndt.architectures.ViTHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for ViT layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for ViT layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    patch_count = 0\n    attn_count = 0\n    mlp_count = 0\n    other_count = 0\n\n    for layer in layers:\n        # Identify layer type from model structure\n        layer_name = None\n        for name, module in model.named_modules():\n            if module is layer:\n                layer_name = name\n                break\n\n        if layer_name:\n            if \"patch\" in layer_name.lower() or \"embed\" in layer_name.lower():\n                names.append(f\"PatchEmbed_{patch_count}\")\n                patch_count += 1\n            elif isinstance(layer, nn.MultiheadAttention):\n                names.append(f\"Attention_{attn_count}\")\n                attn_count += 1\n            elif \"mlp\" in layer_name.lower() or \"ffn\" in layer_name.lower():\n                names.append(f\"MLP_{mlp_count}\")\n                mlp_count += 1\n            else:\n                names.append(f\"Layer_{other_count}\")\n                other_count += 1\n        else:\n            # Fallback naming\n            if isinstance(layer, nn.MultiheadAttention):\n                names.append(f\"Attention_{attn_count}\")\n                attn_count += 1\n            else:\n                names.append(f\"Layer_{other_count}\")\n                other_count += 1\n\n    return names\n</code></pre>"},{"location":"api/#visualization","title":"Visualization","text":""},{"location":"api/#plot_phases","title":"plot_phases","text":""},{"location":"api/#ndt.visualization.plot_phases","title":"<code>ndt.visualization.plot_phases(results_dict: Dict[str, pd.DataFrame], metric: str = 'stable_rank', figsize: tuple = (15, 8)) -&gt; plt.Figure</code>","text":"<p>Plot metric across multiple layers to visualize phase transitions.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>metric</code> <code>str</code> <p>Metric to plot</p> <code>'stable_rank'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(15, 8)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>results = tracker.get_results() fig = plot_phases(results, metric=\"stable_rank\") plt.savefig(\"phases.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_phases(\n    results_dict: Dict[str, pd.DataFrame], metric: str = \"stable_rank\", figsize: tuple = (15, 8)\n) -&gt; plt.Figure:\n    \"\"\"Plot metric across multiple layers to visualize phase transitions.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        metric: Metric to plot\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = plot_phases(results, metric=\"stable_rank\")\n        &gt;&gt;&gt; plt.savefig(\"phases.png\")\n    \"\"\"\n    fig, ax = plt.subplots(figsize=figsize)\n\n    for layer_name, df in results_dict.items():\n        if metric in df.columns:\n            ax.plot(df[\"step\"], df[metric], linewidth=2, label=layer_name, alpha=0.8)\n\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12)\n    ax.set_title(\n        f\"{metric.replace('_', ' ').title()} Across Layers\", fontsize=14, fontweight=\"bold\"\n    )\n    ax.legend(loc=\"best\", fontsize=10)\n    ax.grid(True, alpha=0.3)\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/#plot_jumps","title":"plot_jumps","text":""},{"location":"api/#ndt.visualization.plot_jumps","title":"<code>ndt.visualization.plot_jumps(df: pd.DataFrame, jumps: List[Jump], metric: str = 'stable_rank', layer_name: str = 'Layer', figsize: tuple = (14, 6)) -&gt; plt.Figure</code>","text":"<p>Plot metric with detected jumps highlighted.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>jumps</code> <code>List[Jump]</code> <p>List of detected Jump objects</p> required <code>metric</code> <code>str</code> <p>Metric name that was analyzed for jumps</p> <code>'stable_rank'</code> <code>layer_name</code> <code>str</code> <p>Name of the layer</p> <code>'Layer'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(14, 6)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>jumps = tracker.detect_jumps(layer_name=\"Linear_0\", metric=\"stable_rank\") fig = plot_jumps(results_df, jumps[\"Linear_0\"], metric=\"stable_rank\") plt.savefig(\"jumps.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_jumps(\n    df: pd.DataFrame,\n    jumps: List[Jump],\n    metric: str = \"stable_rank\",\n    layer_name: str = \"Layer\",\n    figsize: tuple = (14, 6),\n) -&gt; plt.Figure:\n    \"\"\"Plot metric with detected jumps highlighted.\n\n    Args:\n        df: DataFrame with metrics\n        jumps: List of detected Jump objects\n        metric: Metric name that was analyzed for jumps\n        layer_name: Name of the layer\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; jumps = tracker.detect_jumps(layer_name=\"Linear_0\", metric=\"stable_rank\")\n        &gt;&gt;&gt; fig = plot_jumps(results_df, jumps[\"Linear_0\"], metric=\"stable_rank\")\n        &gt;&gt;&gt; plt.savefig(\"jumps.png\")\n    \"\"\"\n    fig, ax = plt.subplots(figsize=figsize)\n\n    # Plot metric\n    ax.plot(df[\"step\"], df[metric], linewidth=2, color=\"steelblue\", label=metric)\n\n    # Highlight jumps\n    if jumps:\n        jump_steps = [j.step for j in jumps]\n        jump_values = [\n            (\n                df[df[\"step\"] == j.step][metric].values[0]\n                if j.step in df[\"step\"].values\n                else j.value_after\n            )\n            for j in jumps\n        ]\n\n        ax.scatter(\n            jump_steps,\n            jump_values,\n            color=\"red\",\n            s=100,\n            zorder=5,\n            label=f\"Jumps (n={len(jumps)})\",\n            marker=\"o\",\n            edgecolors=\"darkred\",\n            linewidths=2,\n        )\n\n        # Add vertical lines at jumps\n        for step in jump_steps:\n            ax.axvline(x=step, color=\"red\", linestyle=\"--\", alpha=0.3, linewidth=1)\n\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12)\n    ax.set_title(f\"Dimensionality Jumps - {layer_name}\", fontsize=14, fontweight=\"bold\")\n    ax.legend(loc=\"best\", fontsize=10)\n    ax.grid(True, alpha=0.3)\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/#plot_metrics_comparison","title":"plot_metrics_comparison","text":""},{"location":"api/#ndt.visualization.plot_metrics_comparison","title":"<code>ndt.visualization.plot_metrics_comparison(df: pd.DataFrame, metrics: Optional[List[str]] = None, layer_name: str = 'Layer', figsize: tuple = (15, 10)) -&gt; plt.Figure</code>","text":"<p>Plot multiple dimensionality metrics for comparison.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metric names to plot (default: all 4 main metrics)</p> <code>None</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(15, 10)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_metrics_comparison(results_df, layer_name=\"Linear_0\") plt.savefig(\"all_metrics.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_metrics_comparison(\n    df: pd.DataFrame,\n    metrics: Optional[List[str]] = None,\n    layer_name: str = \"Layer\",\n    figsize: tuple = (15, 10),\n) -&gt; plt.Figure:\n    \"\"\"Plot multiple dimensionality metrics for comparison.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metric names to plot (default: all 4 main metrics)\n        layer_name: Name of the layer for title\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_metrics_comparison(results_df, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; plt.savefig(\"all_metrics.png\")\n    \"\"\"\n    if metrics is None:\n        metrics = [\"stable_rank\", \"participation_ratio\", \"cumulative_90\", \"nuclear_norm_ratio\"]\n\n    # Filter to available metrics\n    metrics = [m for m in metrics if m in df.columns]\n\n    n_metrics = len(metrics)\n    fig, axes = plt.subplots(n_metrics, 1, figsize=figsize, sharex=True)\n\n    if n_metrics == 1:\n        axes = [axes]\n\n    for ax, metric in zip(axes, metrics):\n        ax.plot(df[\"step\"], df[metric], linewidth=2, color=\"steelblue\")\n        ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=11)\n        ax.grid(True, alpha=0.3)\n\n        # Add loss as background on last plot\n        if ax == axes[-1] and \"loss\" in df.columns:\n            ax2 = ax.twinx()\n            ax2.plot(df[\"step\"], df[\"loss\"], linewidth=1, alpha=0.4, color=\"coral\", linestyle=\"--\")\n            ax2.set_ylabel(\"Loss\", fontsize=10, color=\"coral\")\n            ax2.tick_params(axis=\"y\", labelcolor=\"coral\")\n\n    axes[-1].set_xlabel(\"Training Step\", fontsize=12)\n    fig.suptitle(f\"Dimensionality Metrics - {layer_name}\", fontsize=14, fontweight=\"bold\")\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/#plot_single_metric","title":"plot_single_metric","text":""},{"location":"api/#ndt.visualization.plot_single_metric","title":"<code>ndt.visualization.plot_single_metric(df: pd.DataFrame, metric: str = 'stable_rank', layer_name: str = 'Layer', ax: Optional[plt.Axes] = None, show_loss: bool = True, figsize: tuple = (12, 6)) -&gt; plt.Figure</code>","text":"<p>Plot a single dimensionality metric over training.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics (must have 'step' and metric columns)</p> required <code>metric</code> <code>str</code> <p>Name of metric to plot</p> <code>'stable_rank'</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>ax</code> <code>Optional[Axes]</code> <p>Optional matplotlib axis to plot on</p> <code>None</code> <code>show_loss</code> <code>bool</code> <p>Whether to show loss on secondary y-axis</p> <code>True</code> <code>figsize</code> <code>tuple</code> <p>Figure size if creating new figure</p> <code>(12, 6)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_single_metric(results_df, metric=\"stable_rank\", layer_name=\"Linear_0\") plt.savefig(\"stable_rank.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_single_metric(\n    df: pd.DataFrame,\n    metric: str = \"stable_rank\",\n    layer_name: str = \"Layer\",\n    ax: Optional[plt.Axes] = None,\n    show_loss: bool = True,\n    figsize: tuple = (12, 6),\n) -&gt; plt.Figure:\n    \"\"\"Plot a single dimensionality metric over training.\n\n    Args:\n        df: DataFrame with metrics (must have 'step' and metric columns)\n        metric: Name of metric to plot\n        layer_name: Name of the layer for title\n        ax: Optional matplotlib axis to plot on\n        show_loss: Whether to show loss on secondary y-axis\n        figsize: Figure size if creating new figure\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_single_metric(results_df, metric=\"stable_rank\", layer_name=\"Linear_0\")\n        &gt;&gt;&gt; plt.savefig(\"stable_rank.png\")\n    \"\"\"\n    if ax is None:\n        fig, ax = plt.subplots(figsize=figsize)\n    else:\n        fig = ax.get_figure()\n\n    # Plot main metric\n    ax.plot(df[\"step\"], df[metric], linewidth=2, label=metric, color=\"steelblue\")\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12, color=\"steelblue\")\n    ax.tick_params(axis=\"y\", labelcolor=\"steelblue\")\n    ax.grid(True, alpha=0.3)\n\n    # Plot loss on secondary axis if requested\n    if show_loss and \"loss\" in df.columns:\n        ax2 = ax.twinx()\n        ax2.plot(\n            df[\"step\"],\n            df[\"loss\"],\n            linewidth=1,\n            alpha=0.6,\n            label=\"Loss\",\n            color=\"coral\",\n            linestyle=\"--\",\n        )\n        ax2.set_ylabel(\"Loss\", fontsize=12, color=\"coral\")\n        ax2.tick_params(axis=\"y\", labelcolor=\"coral\")\n\n    ax.set_title(\n        f\"{metric.replace('_', ' ').title()} - {layer_name}\", fontsize=14, fontweight=\"bold\"\n    )\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/#create_interactive_plot","title":"create_interactive_plot","text":""},{"location":"api/#ndt.visualization.create_interactive_plot","title":"<code>ndt.visualization.create_interactive_plot(df: pd.DataFrame, metrics: Optional[List[str]] = None, layer_name: str = 'Layer', show_loss: bool = True) -&gt; go.Figure</code>","text":"<p>Create interactive plot of dimensionality metrics.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metric names to plot (default: all 4 main metrics)</p> <code>None</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>show_loss</code> <code>bool</code> <p>Whether to include loss curve</p> <code>True</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>fig = create_interactive_plot(results_df, layer_name=\"Linear_0\") fig.show() fig.write_html(\"interactive_plot.html\")</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_interactive_plot(\n    df: pd.DataFrame,\n    metrics: Optional[List[str]] = None,\n    layer_name: str = \"Layer\",\n    show_loss: bool = True,\n) -&gt; go.Figure:\n    \"\"\"Create interactive plot of dimensionality metrics.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metric names to plot (default: all 4 main metrics)\n        layer_name: Name of the layer for title\n        show_loss: Whether to include loss curve\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; fig = create_interactive_plot(results_df, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; fig.show()\n        &gt;&gt;&gt; fig.write_html(\"interactive_plot.html\")\n    \"\"\"\n    if metrics is None:\n        metrics = [\"stable_rank\", \"participation_ratio\", \"cumulative_90\", \"nuclear_norm_ratio\"]\n\n    # Filter to available metrics\n    metrics = [m for m in metrics if m in df.columns]\n\n    # Create subplots\n    n_plots = len(metrics) + (1 if show_loss and \"loss\" in df.columns else 0)\n    fig = make_subplots(\n        rows=n_plots,\n        cols=1,\n        subplot_titles=[m.replace(\"_\", \" \").title() for m in metrics]\n        + ([\"Loss\"] if show_loss and \"loss\" in df.columns else []),\n        vertical_spacing=0.05,\n    )\n\n    # Add metric traces\n    for i, metric in enumerate(metrics, 1):\n        fig.add_trace(\n            go.Scatter(\n                x=df[\"step\"],\n                y=df[metric],\n                mode=\"lines\",\n                name=metric.replace(\"_\", \" \").title(),\n                line=dict(width=2),\n                hovertemplate=\"Step: %{x}&lt;br&gt;\" + metric + \": %{y:.2f}&lt;extra&gt;&lt;/extra&gt;\",\n            ),\n            row=i,\n            col=1,\n        )\n\n    # Add loss if requested\n    if show_loss and \"loss\" in df.columns:\n        fig.add_trace(\n            go.Scatter(\n                x=df[\"step\"],\n                y=df[\"loss\"],\n                mode=\"lines\",\n                name=\"Loss\",\n                line=dict(width=2, color=\"coral\"),\n                hovertemplate=\"Step: %{x}&lt;br&gt;Loss: %{y:.3f}&lt;extra&gt;&lt;/extra&gt;\",\n            ),\n            row=n_plots,\n            col=1,\n        )\n\n    # Update layout\n    fig.update_layout(\n        title_text=f\"Dimensionality Metrics - {layer_name}\",\n        title_font_size=18,\n        height=300 * n_plots,\n        showlegend=False,\n        hovermode=\"x unified\",\n    )\n\n    # Update x-axes\n    fig.update_xaxes(title_text=\"Training Step\", row=n_plots, col=1)\n\n    return fig\n</code></pre>"},{"location":"api/#create_multi_layer_plot","title":"create_multi_layer_plot","text":""},{"location":"api/#ndt.visualization.create_multi_layer_plot","title":"<code>ndt.visualization.create_multi_layer_plot(results_dict: Dict[str, pd.DataFrame], metric: str = 'stable_rank') -&gt; go.Figure</code>","text":"<p>Create interactive plot comparing a metric across multiple layers.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>metric</code> <code>str</code> <p>Metric to plot</p> <code>'stable_rank'</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>results = tracker.get_results() fig = create_multi_layer_plot(results, metric=\"stable_rank\") fig.show()</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_multi_layer_plot(\n    results_dict: Dict[str, pd.DataFrame], metric: str = \"stable_rank\"\n) -&gt; go.Figure:\n    \"\"\"Create interactive plot comparing a metric across multiple layers.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        metric: Metric to plot\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = create_multi_layer_plot(results, metric=\"stable_rank\")\n        &gt;&gt;&gt; fig.show()\n    \"\"\"\n    fig = go.Figure()\n\n    for layer_name, df in results_dict.items():\n        if metric in df.columns:\n            fig.add_trace(\n                go.Scatter(\n                    x=df[\"step\"],\n                    y=df[metric],\n                    mode=\"lines\",\n                    name=layer_name,\n                    line=dict(width=2),\n                    hovertemplate=(\n                        f\"{layer_name}&lt;br&gt;Step: %{{x}}&lt;br&gt;{metric}: %{{y:.2f}}&lt;extra&gt;&lt;/extra&gt;\"\n                    ),\n                )\n            )\n\n    fig.update_layout(\n        title=f\"{metric.replace('_', ' ').title()} Across Layers\",\n        title_font_size=18,\n        xaxis_title=\"Training Step\",\n        yaxis_title=metric.replace(\"_\", \" \").title(),\n        hovermode=\"x unified\",\n        height=600,\n        legend=dict(yanchor=\"top\", y=0.99, xanchor=\"left\", x=0.01),\n    )\n\n    return fig\n</code></pre>"},{"location":"api/#export","title":"Export","text":""},{"location":"api/#export_to_csv","title":"export_to_csv","text":""},{"location":"api/#ndt.export.export_to_csv","title":"<code>ndt.export.export_to_csv(results: Dict[str, pd.DataFrame], output_path: str, separate_files: bool = False) -&gt; None</code>","text":"<p>Export tracking results to CSV format.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>output_path</code> <code>str</code> <p>Output file path or directory path</p> required <code>separate_files</code> <code>bool</code> <p>If True, create separate CSV for each layer.            If False, combine all layers into one CSV with layer column.</p> <code>False</code> Example <p>results = tracker.get_results() export_to_csv(results, \"results.csv\")</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/csv.py</code> <pre><code>def export_to_csv(\n    results: Dict[str, pd.DataFrame], output_path: str, separate_files: bool = False\n) -&gt; None:\n    \"\"\"Export tracking results to CSV format.\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames\n        output_path: Output file path or directory path\n        separate_files: If True, create separate CSV for each layer.\n                       If False, combine all layers into one CSV with layer column.\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; export_to_csv(results, \"results.csv\")\n        &gt;&gt;&gt; # Or separate files:\n        &gt;&gt;&gt; export_to_csv(results, \"results_dir/\", separate_files=True)\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    if separate_files:\n        # Create directory if it doesn't exist\n        output_path.mkdir(parents=True, exist_ok=True)\n\n        # Export each layer to separate file\n        for layer_name, df in results.items():\n            filename = output_path / f\"{layer_name}.csv\"\n            df.to_csv(filename, index=False)\n            print(f\"Exported {layer_name} to {filename}\")\n\n    else:\n        # Combine all layers into one CSV\n        combined_data = []\n        for layer_name, df in results.items():\n            df_copy = df.copy()\n            df_copy[\"layer\"] = layer_name\n            combined_data.append(df_copy)\n\n        if combined_data:\n            combined_df = pd.concat(combined_data, ignore_index=True)\n\n            # Reorder columns to put layer first\n            cols = [\"layer\"] + [col for col in combined_df.columns if col != \"layer\"]\n            combined_df = combined_df[cols]\n\n            combined_df.to_csv(output_path, index=False)\n            print(f\"Exported all layers to {output_path}\")\n        else:\n            print(\"No data to export\")\n</code></pre>"},{"location":"api/#ndt.export.export_to_csv--or-separate-files","title":"Or separate files:","text":"<p>export_to_csv(results, \"results_dir/\", separate_files=True)</p>"},{"location":"api/#export_to_json","title":"export_to_json","text":""},{"location":"api/#ndt.export.export_to_json","title":"<code>ndt.export.export_to_json(results: Dict[str, pd.DataFrame], output_path: str, pretty: bool = True) -&gt; None</code>","text":"<p>Export tracking results to JSON format.</p> <p>Parameters:</p> Name Type Description Default <code>output_path</code> <code>str</code> <p>Output file path</p> required <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>pretty</code> <code>bool</code> <p>If True, format JSON with indentation</p> <code>True</code> Example <p>results = tracker.get_results() export_to_json(results, \"results.json\")</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/json.py</code> <pre><code>def export_to_json(results: Dict[str, pd.DataFrame], output_path: str, pretty: bool = True) -&gt; None:\n    \"\"\"Export tracking results to JSON format.\n\n    Args:\n        output_path: Output file path\n        results: Dictionary mapping layer names to DataFrames\n        pretty: If True, format JSON with indentation\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; export_to_json(results, \"results.json\")\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    # Convert DataFrames to dictionaries\n    json_data = {}\n    for layer_name, df in results.items():\n        json_data[layer_name] = df.to_dict(orient=\"records\")\n\n    # Write to file\n    with open(output_path, \"w\") as f:\n        if pretty:\n            json.dump(json_data, f, indent=2)\n        else:\n            json.dump(json_data, f)\n\n    print(f\"Exported results to {output_path}\")\n</code></pre>"},{"location":"api/#export_to_hdf5","title":"export_to_hdf5","text":""},{"location":"api/#ndt.export.export_to_hdf5","title":"<code>ndt.export.export_to_hdf5(results: Dict[str, pd.DataFrame], output_path: str, compression: str = 'gzip', compression_opts: int = 4, metadata: Optional[Dict[str, Any]] = None) -&gt; None</code>","text":"<p>Export tracking results to HDF5 format (efficient for large data).</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>output_path</code> <code>str</code> <p>Output HDF5 file path</p> required <code>compression</code> <code>str</code> <p>Compression algorithm (\"gzip\", \"lzf\", or None)</p> <code>'gzip'</code> <code>compression_opts</code> <code>int</code> <p>Compression level (0-9 for gzip)</p> <code>4</code> <code>metadata</code> <code>Optional[Dict[str, Any]]</code> <p>Optional metadata to store as attributes</p> <code>None</code> Example <p>results = tracker.get_results() metadata = {\"model\": \"ResNet18\", \"dataset\": \"CIFAR-10\"} export_to_hdf5(results, \"results.h5\", metadata=metadata)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/hdf5.py</code> <pre><code>def export_to_hdf5(\n    results: Dict[str, pd.DataFrame],\n    output_path: str,\n    compression: str = \"gzip\",\n    compression_opts: int = 4,\n    metadata: Optional[Dict[str, Any]] = None,\n) -&gt; None:\n    \"\"\"Export tracking results to HDF5 format (efficient for large data).\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames\n        output_path: Output HDF5 file path\n        compression: Compression algorithm (\"gzip\", \"lzf\", or None)\n        compression_opts: Compression level (0-9 for gzip)\n        metadata: Optional metadata to store as attributes\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; metadata = {\"model\": \"ResNet18\", \"dataset\": \"CIFAR-10\"}\n        &gt;&gt;&gt; export_to_hdf5(results, \"results.h5\", metadata=metadata)\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    with h5py.File(output_path, \"w\") as f:\n        # Store metadata as root attributes\n        if metadata:\n            for key, value in metadata.items():\n                f.attrs[key] = value\n\n        # Store each layer's data as a group\n        for layer_name, df in results.items():\n            # Create group for this layer\n            group = f.create_group(layer_name)\n\n            # Store each column as a dataset\n            for col in df.columns:\n                data = df[col].values\n\n                # Handle different data types\n                if data.dtype == object:\n                    # Convert to string for object columns\n                    data = data.astype(str)\n\n                group.create_dataset(\n                    col,\n                    data=data,\n                    compression=compression,\n                    compression_opts=compression_opts if compression == \"gzip\" else None,\n                )\n\n            # Store column names as attribute\n            group.attrs[\"columns\"] = list(df.columns)\n\n    print(f\"Exported results to {output_path}\")\n</code></pre>"},{"location":"api/#utilities","title":"Utilities","text":""},{"location":"api/#setup_logger","title":"setup_logger","text":""},{"location":"api/#ndt.utils.setup_logger","title":"<code>ndt.utils.setup_logger(name: str = 'ndt', level: int = logging.INFO, log_file: Optional[str] = None, format_string: Optional[str] = None) -&gt; logging.Logger</code>","text":"<p>Set up a logger with console and optional file handlers.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Logger name (default: \"ndt\")</p> <code>'ndt'</code> <code>level</code> <code>int</code> <p>Logging level (default: INFO)</p> <code>INFO</code> <code>log_file</code> <code>Optional[str]</code> <p>Optional file path for logging</p> <code>None</code> <code>format_string</code> <code>Optional[str]</code> <p>Optional custom format string</p> <code>None</code> <p>Returns:</p> Type Description <code>Logger</code> <p>Configured logger instance</p> Example <p>logger = setup_logger(\"ndt\", level=logging.DEBUG, log_file=\"tracking.log\") logger.info(\"Starting tracking...\")</p> Source code in <code>src/ndt/utils/logging.py</code> <pre><code>def setup_logger(\n    name: str = \"ndt\",\n    level: int = logging.INFO,\n    log_file: Optional[str] = None,\n    format_string: Optional[str] = None,\n) -&gt; logging.Logger:\n    \"\"\"Set up a logger with console and optional file handlers.\n\n    Args:\n        name: Logger name (default: \"ndt\")\n        level: Logging level (default: INFO)\n        log_file: Optional file path for logging\n        format_string: Optional custom format string\n\n    Returns:\n        Configured logger instance\n\n    Example:\n        &gt;&gt;&gt; logger = setup_logger(\"ndt\", level=logging.DEBUG, log_file=\"tracking.log\")\n        &gt;&gt;&gt; logger.info(\"Starting tracking...\")\n    \"\"\"\n    logger = logging.getLogger(name)\n    logger.setLevel(level)\n\n    # Remove existing handlers to avoid duplicates\n    logger.handlers = []\n\n    # Default format\n    if format_string is None:\n        format_string = \"%(asctime)s - %(name)s - %(levelname)s - %(message)s\"\n\n    formatter = logging.Formatter(format_string)\n\n    # Console handler\n    console_handler = logging.StreamHandler(sys.stdout)\n    console_handler.setLevel(level)\n    console_handler.setFormatter(formatter)\n    logger.addHandler(console_handler)\n\n    # File handler if specified\n    if log_file:\n        file_handler = logging.FileHandler(log_file)\n        file_handler.setLevel(level)\n        file_handler.setFormatter(formatter)\n        logger.addHandler(file_handler)\n\n    return logger\n</code></pre>"},{"location":"api/#load_config","title":"load_config","text":""},{"location":"api/#ndt.utils.load_config","title":"<code>ndt.utils.load_config(config_path: str) -&gt; Dict[str, Any]</code>","text":"<p>Load configuration from YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to YAML configuration file</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Configuration dictionary</p> Example <p>config = load_config(\"tracker_config.yaml\") tracker = HighFrequencyTracker(**config)</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If config file doesn't exist</p> <code>YAMLError</code> <p>If YAML is invalid</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def load_config(config_path: str) -&gt; Dict[str, Any]:\n    \"\"\"Load configuration from YAML file.\n\n    Args:\n        config_path: Path to YAML configuration file\n\n    Returns:\n        Configuration dictionary\n\n    Example:\n        &gt;&gt;&gt; config = load_config(\"tracker_config.yaml\")\n        &gt;&gt;&gt; tracker = HighFrequencyTracker(**config)\n\n    Raises:\n        FileNotFoundError: If config file doesn't exist\n        yaml.YAMLError: If YAML is invalid\n    \"\"\"\n    config_path = Path(config_path)\n\n    if not config_path.exists():\n        raise FileNotFoundError(f\"Config file not found: {config_path}\")\n\n    with open(config_path) as f:\n        config = yaml.safe_load(f)\n\n    return config\n</code></pre>"},{"location":"api/#save_config","title":"save_config","text":""},{"location":"api/#ndt.utils.save_config","title":"<code>ndt.utils.save_config(config: Dict[str, Any], output_path: str) -&gt; None</code>","text":"<p>Save configuration to YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Dict[str, Any]</code> <p>Configuration dictionary</p> required <code>output_path</code> <code>str</code> <p>Output file path</p> required Example <p>config = { ...     \"sampling_frequency\": 10, ...     \"enable_jump_detection\": True, ...     \"jump_z_threshold\": 3.0 ... } save_config(config, \"tracker_config.yaml\")</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def save_config(config: Dict[str, Any], output_path: str) -&gt; None:\n    \"\"\"Save configuration to YAML file.\n\n    Args:\n        config: Configuration dictionary\n        output_path: Output file path\n\n    Example:\n        &gt;&gt;&gt; config = {\n        ...     \"sampling_frequency\": 10,\n        ...     \"enable_jump_detection\": True,\n        ...     \"jump_z_threshold\": 3.0\n        ... }\n        &gt;&gt;&gt; save_config(config, \"tracker_config.yaml\")\n    \"\"\"\n    output_path = Path(output_path)\n\n    with open(output_path, \"w\") as f:\n        yaml.dump(config, f, default_flow_style=False, sort_keys=False)\n\n    print(f\"Saved configuration to {output_path}\")\n</code></pre>"},{"location":"api/SUMMARY/","title":"SUMMARY","text":"<ul> <li>ndt<ul> <li>analysis<ul> <li>activation_analysis</li> <li>feature_visualization</li> </ul> </li> <li>architectures<ul> <li>base</li> <li>cnn</li> <li>mlp</li> <li>registry</li> <li>transformer</li> <li>vit</li> </ul> </li> <li>core<ul> <li>estimators</li> <li>hooks</li> <li>jump_detector</li> <li>tracker</li> </ul> </li> <li>export<ul> <li>csv</li> <li>hdf5</li> <li>json</li> </ul> </li> <li>utils<ul> <li>config</li> <li>logging</li> </ul> </li> <li>validity<ul> <li>fixtures</li> <li>harness</li> </ul> </li> <li>visualization<ul> <li>interactive</li> <li>plots</li> </ul> </li> </ul> </li> </ul>"},{"location":"api/ndt/","title":"Index","text":""},{"location":"api/ndt/#ndt","title":"<code>ndt</code>","text":"<p>Neural Dimensionality Tracker (NDT)</p> <p>High-frequency monitoring of neural network representational dimensionality during training.</p>"},{"location":"api/ndt/#ndt-classes","title":"Classes","text":""},{"location":"api/ndt/#ndt.CNNHandler","title":"<code>CNNHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Convolutional Neural Networks (CNNs).</p> <p>Monitors Conv2d layers and the first Linear layer (pre-classifier). Supports both simple CNNs and ResNet-style architectures.</p>"},{"location":"api/ndt/#ndt.CNNHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.CNNHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a CNN (contains Conv layers).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a CNN</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a CNN (contains Conv layers).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a CNN\n    \"\"\"\n    has_conv = False\n    for module in model.modules():\n        if isinstance(module, (nn.Conv1d, nn.Conv2d, nn.Conv3d)):\n            has_conv = True\n            break\n    return has_conv\n</code></pre>"},{"location":"api/ndt/#ndt.CNNHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get Conv2d and key Linear layers for monitoring.</p> <p>For ResNets, monitors after each residual block. For simple CNNs, monitors each conv layer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get Conv2d and key Linear layers for monitoring.\n\n    For ResNets, monitors after each residual block.\n    For simple CNNs, monitors each conv layer.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # Check if it's a ResNet-style architecture\n    if self._is_resnet(model):\n        layers = self._get_resnet_layers(model)\n    else:\n        # Simple CNN: monitor all conv layers\n        for module in model.modules():\n            if isinstance(module, nn.Conv2d):\n                layers.append(module)\n\n        # Also monitor first Linear layer (before classifier)\n        linear_layers = [m for m in model.modules() if isinstance(m, nn.Linear)]\n        if linear_layers:\n            layers.append(linear_layers[0])\n\n    return layers\n</code></pre>"},{"location":"api/ndt/#ndt.CNNHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate descriptive names for CNN layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate descriptive names for CNN layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    conv_count = 0\n    linear_count = 0\n\n    for layer in layers:\n        if isinstance(layer, nn.Conv2d):\n            names.append(f\"Conv_{conv_count}_{layer.out_channels}ch\")\n            conv_count += 1\n        elif isinstance(layer, nn.Linear):\n            names.append(f\"FC_{linear_count}_{layer.out_features}\")\n            linear_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/#ndt.MLPHandler","title":"<code>MLPHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for fully-connected (MLP) networks.</p> <p>Monitors all Linear layers in the network.</p>"},{"location":"api/ndt/#ndt.MLPHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.MLPHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is an MLP (contains Linear layers, no Conv/Attention).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be an MLP</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is an MLP (contains Linear layers, no Conv/Attention).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be an MLP\n    \"\"\"\n    has_linear = False\n    has_conv = False\n    has_attention = False\n\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            has_linear = True\n        elif isinstance(module, (nn.Conv1d, nn.Conv2d, nn.Conv3d)):\n            has_conv = True\n        elif isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n\n    # MLP should have Linear layers but not Conv or Attention\n    return has_linear and not has_conv and not has_attention\n</code></pre>"},{"location":"api/ndt/#ndt.MLPHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get all Linear layers for monitoring.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of Linear layer modules</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get all Linear layers for monitoring.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of Linear layer modules\n    \"\"\"\n    layers = []\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            layers.append(module)\n    return layers\n</code></pre>"},{"location":"api/ndt/#ndt.MLPHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for Linear layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names like \"Linear_0\", \"Linear_1\", etc.</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for Linear layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names like \"Linear_0\", \"Linear_1\", etc.\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    for i, layer in enumerate(layers):\n        in_features = layer.in_features\n        out_features = layer.out_features\n        names.append(f\"Linear_{i}_{in_features}x{out_features}\")\n\n    return names\n</code></pre>"},{"location":"api/ndt/#ndt.TransformerHandler","title":"<code>TransformerHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Transformer-based architectures.</p> <p>Monitors attention outputs and feed-forward network outputs in each layer. Compatible with BERT, GPT, and similar transformer models.</p>"},{"location":"api/ndt/#ndt.TransformerHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.TransformerHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a Transformer (contains MultiheadAttention).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a Transformer</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a Transformer (contains MultiheadAttention).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a Transformer\n    \"\"\"\n    has_attention = False\n    for module in model.modules():\n        if isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n            break\n        # Also check for common transformer layer names\n        if module.__class__.__name__ in [\"TransformerEncoderLayer\", \"TransformerDecoderLayer\"]:\n            has_attention = True\n            break\n    return has_attention\n</code></pre>"},{"location":"api/ndt/#ndt.TransformerHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get attention and FFN layers for monitoring.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get attention and FFN layers for monitoring.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # Look for MultiheadAttention modules\n    for module in model.modules():\n        if isinstance(module, nn.MultiheadAttention):\n            layers.append(module)\n\n    # Also look for Linear layers in feed-forward blocks\n    # Typically the first Linear in each transformer block's FFN\n    in_transformer_block = False\n    for name, module in model.named_modules():\n        if \"encoder\" in name.lower() or \"decoder\" in name.lower() or \"layer\" in name.lower():\n            in_transformer_block = True\n\n        if in_transformer_block and isinstance(module, nn.Linear):\n            # Add FFN layers (typically 2 per transformer block)\n            if \"fc1\" in name or \"fc2\" in name or \"ffn\" in name.lower():\n                layers.append(module)\n\n    # If no structured layers found, fall back to sampling Linear layers\n    if not layers:\n        linear_layers = [m for m in model.modules() if isinstance(m, nn.Linear)]\n        # Sample every 2nd Linear layer to avoid too many\n        layers = linear_layers[::2] if len(linear_layers) &gt; 10 else linear_layers\n\n    return layers\n</code></pre>"},{"location":"api/ndt/#ndt.TransformerHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for Transformer layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for Transformer layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    attn_count = 0\n    ffn_count = 0\n    fc_count = 0\n\n    for layer in layers:\n        if isinstance(layer, nn.MultiheadAttention):\n            names.append(f\"Attention_{attn_count}\")\n            attn_count += 1\n        elif isinstance(layer, nn.Linear):\n            # Try to determine if it's an FFN layer\n            layer_name = None\n            for name, module in model.named_modules():\n                if module is layer:\n                    layer_name = name\n                    break\n\n            if layer_name and (\"fc\" in layer_name.lower() or \"ffn\" in layer_name.lower()):\n                names.append(f\"FFN_{ffn_count}_{layer.out_features}\")\n                ffn_count += 1\n            else:\n                names.append(f\"FC_{fc_count}_{layer.out_features}\")\n                fc_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/#ndt.ViTHandler","title":"<code>ViTHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Vision Transformer architectures.</p> <p>Monitors patch embeddings, attention layers, and MLP blocks.</p>"},{"location":"api/ndt/#ndt.ViTHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.ViTHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a Vision Transformer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a ViT</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a Vision Transformer.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a ViT\n    \"\"\"\n    # ViTs typically have both conv (for patch embedding) and attention\n    has_conv = False\n    has_attention = False\n    has_patch_embed = False\n\n    for name, module in model.named_modules():\n        if isinstance(module, (nn.Conv2d, nn.Conv1d)):\n            has_conv = True\n            if \"patch\" in name.lower() or \"embed\" in name.lower():\n                has_patch_embed = True\n        if isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n\n    # ViT has conv (patch embedding) + attention, or explicit patch embedding\n    return (has_conv and has_attention) or has_patch_embed\n</code></pre>"},{"location":"api/ndt/#ndt.ViTHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get key layers to monitor in ViT.</p> <p>Monitors: patch embedding, attention layers, and MLP blocks.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get key layers to monitor in ViT.\n\n    Monitors: patch embedding, attention layers, and MLP blocks.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # 1. Patch embedding layer\n    for name, module in model.named_modules():\n        if isinstance(module, (nn.Conv2d, nn.Linear)):\n            if \"patch\" in name.lower() or \"embed\" in name.lower():\n                layers.append(module)\n                break  # Usually just one\n\n    # 2. Attention layers\n    attn_layers = [m for m in model.modules() if isinstance(m, nn.MultiheadAttention)]\n    layers.extend(attn_layers)\n\n    # 3. MLP/FFN blocks (sample to avoid too many)\n    mlp_layers = []\n    for name, module in model.named_modules():\n        if isinstance(module, nn.Linear) and (\"mlp\" in name.lower() or \"ffn\" in name.lower()):\n            mlp_layers.append(module)\n\n    # Sample MLP layers if too many (keep first Linear of each MLP block)\n    if len(mlp_layers) &gt; 10:\n        mlp_layers = mlp_layers[::2]\n\n    layers.extend(mlp_layers)\n\n    return layers\n</code></pre>"},{"location":"api/ndt/#ndt.ViTHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for ViT layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for ViT layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    patch_count = 0\n    attn_count = 0\n    mlp_count = 0\n    other_count = 0\n\n    for layer in layers:\n        # Identify layer type from model structure\n        layer_name = None\n        for name, module in model.named_modules():\n            if module is layer:\n                layer_name = name\n                break\n\n        if layer_name:\n            if \"patch\" in layer_name.lower() or \"embed\" in layer_name.lower():\n                names.append(f\"PatchEmbed_{patch_count}\")\n                patch_count += 1\n            elif isinstance(layer, nn.MultiheadAttention):\n                names.append(f\"Attention_{attn_count}\")\n                attn_count += 1\n            elif \"mlp\" in layer_name.lower() or \"ffn\" in layer_name.lower():\n                names.append(f\"MLP_{mlp_count}\")\n                mlp_count += 1\n            else:\n                names.append(f\"Layer_{other_count}\")\n                other_count += 1\n        else:\n            # Fallback naming\n            if isinstance(layer, nn.MultiheadAttention):\n                names.append(f\"Attention_{attn_count}\")\n                attn_count += 1\n            else:\n                names.append(f\"Layer_{other_count}\")\n                other_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture","title":"<code>ActivationCapture()</code>","text":"<p>Captures activations from specified layers using forward hooks.</p> <p>This class manages PyTorch forward hooks to capture activations during the forward pass without modifying the model code.</p> <p>Attributes:</p> Name Type Description <code>activations</code> <code>Dict[str, Tensor]</code> <p>Dictionary mapping layer names to captured activations</p> <code>hooks</code> <code>List[RemovableHandle]</code> <p>List of registered hook handles</p> Example <p>import torch.nn as nn model = nn.Sequential(nn.Linear(10, 20), nn.ReLU(), nn.Linear(20, 10)) capture = ActivationCapture() capture.register_hooks(model, [model[0], model[2]]) output = model(torch.randn(32, 10)) print(capture.activations.keys()) capture.remove_hooks()</p> <p>Initialize the activation capture.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialize the activation capture.\"\"\"\n    self.activations: Dict[str, torch.Tensor] = {}\n    self.hooks: List[torch.utils.hooks.RemovableHandle] = []\n    self._layer_names: Dict[nn.Module, str] = {}\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.ActivationCapture.register_hooks","title":"<code>register_hooks(model: nn.Module, layers: List[nn.Module], layer_names: Optional[List[str]] = None) -&gt; None</code>","text":"<p>Register forward hooks on specified layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>layers</code> <code>List[Module]</code> <p>List of layer modules to monitor</p> required <code>layer_names</code> <code>Optional[List[str]]</code> <p>Optional custom names for layers. If None, uses module class names.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If number of layer names doesn't match number of layers</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def register_hooks(\n    self, model: nn.Module, layers: List[nn.Module], layer_names: Optional[List[str]] = None\n) -&gt; None:\n    \"\"\"Register forward hooks on specified layers.\n\n    Args:\n        model: The neural network model\n        layers: List of layer modules to monitor\n        layer_names: Optional custom names for layers. If None, uses module class names.\n\n    Raises:\n        ValueError: If number of layer names doesn't match number of layers\n    \"\"\"\n    if layer_names is not None and len(layer_names) != len(layers):\n        raise ValueError(\n            f\"Number of layer names ({len(layer_names)}) must match \"\n            f\"number of layers ({len(layers)})\"\n        )\n\n    # Generate default names if not provided\n    if layer_names is None:\n        layer_names = [f\"{layer.__class__.__name__}_{i}\" for i, layer in enumerate(layers)]\n\n    # Register hooks\n    for layer, name in zip(layers, layer_names):\n        hook_handle = layer.register_forward_hook(self._create_hook(name))\n        self.hooks.append(hook_handle)\n        self._layer_names[layer] = name\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture.remove_hooks","title":"<code>remove_hooks() -&gt; None</code>","text":"<p>Remove all registered hooks and clear stored activations.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def remove_hooks(self) -&gt; None:\n    \"\"\"Remove all registered hooks and clear stored activations.\"\"\"\n    for hook in self.hooks:\n        hook.remove()\n    self.hooks.clear()\n    self.activations.clear()\n    self._layer_names.clear()\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture.clear_activations","title":"<code>clear_activations() -&gt; None</code>","text":"<p>Clear stored activations without removing hooks.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def clear_activations(self) -&gt; None:\n    \"\"\"Clear stored activations without removing hooks.\"\"\"\n    self.activations.clear()\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture.get_activation","title":"<code>get_activation(layer_name: str) -&gt; Optional[torch.Tensor]</code>","text":"<p>Get activation for a specific layer.</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>str</code> <p>Name of the layer</p> required <p>Returns:</p> Type Description <code>Optional[Tensor]</code> <p>Activation tensor if available, None otherwise</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def get_activation(self, layer_name: str) -&gt; Optional[torch.Tensor]:\n    \"\"\"Get activation for a specific layer.\n\n    Args:\n        layer_name: Name of the layer\n\n    Returns:\n        Activation tensor if available, None otherwise\n    \"\"\"\n    return self.activations.get(layer_name)\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture.get_all_activations","title":"<code>get_all_activations() -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Get all captured activations.</p> <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>Dictionary mapping layer names to activation tensors</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def get_all_activations(self) -&gt; Dict[str, torch.Tensor]:\n    \"\"\"Get all captured activations.\n\n    Returns:\n        Dictionary mapping layer names to activation tensors\n    \"\"\"\n    return self.activations.copy()\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture.__enter__","title":"<code>__enter__() -&gt; ActivationCapture</code>","text":"<p>Context manager entry.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __enter__(self) -&gt; \"ActivationCapture\":\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb) -&gt; None</code>","text":"<p>Context manager exit - removes hooks.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb) -&gt; None:\n    \"\"\"Context manager exit - removes hooks.\"\"\"\n    self.remove_hooks()\n</code></pre>"},{"location":"api/ndt/#ndt.ActivationCapture.__del__","title":"<code>__del__() -&gt; None</code>","text":"<p>Cleanup hooks on deletion.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __del__(self) -&gt; None:\n    \"\"\"Cleanup hooks on deletion.\"\"\"\n    self.remove_hooks()\n</code></pre>"},{"location":"api/ndt/#ndt.Jump","title":"<code>Jump(step: int, z_score: float, value_before: float, value_after: float, metric_name: str)</code>  <code>dataclass</code>","text":"<p>Represents a detected jump in dimensionality.</p> <p>Attributes:</p> Name Type Description <code>step</code> <code>int</code> <p>Training step where jump occurred</p> <code>z_score</code> <code>float</code> <p>Z-score magnitude of the jump</p> <code>value_before</code> <code>float</code> <p>Dimensionality value before jump</p> <code>value_after</code> <code>float</code> <p>Dimensionality value after jump</p> <code>metric_name</code> <code>str</code> <p>Name of the metric that jumped</p>"},{"location":"api/ndt/#ndt.Jump-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.Jump.__repr__","title":"<code>__repr__() -&gt; str</code>","text":"<p>String representation of the jump.</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"String representation of the jump.\"\"\"\n    return (\n        f\"Jump(step={self.step}, metric={self.metric_name}, \"\n        f\"z_score={self.z_score:.2f}, \"\n        f\"\u0394={self.value_after - self.value_before:.2f})\"\n    )\n</code></pre>"},{"location":"api/ndt/#ndt.JumpDetector","title":"<code>JumpDetector(window_size: int = 50, z_threshold: float = 3.0, min_samples: int = 20)</code>","text":"<p>Detects significant jumps in dimensionality metrics using Z-score analysis.</p> <p>This class uses a rolling window to compute mean and standard deviation, then identifies jumps that exceed a specified Z-score threshold.</p> <p>Attributes:</p> Name Type Description <code>window_size</code> <p>Size of the rolling window for statistics</p> <code>z_threshold</code> <p>Z-score threshold for jump detection</p> <code>min_samples</code> <p>Minimum number of samples before detecting jumps</p> Example <p>detector = JumpDetector(window_size=50, z_threshold=3.0) values = [10.0] * 100 + [20.0] * 100  # Simulated jump jumps = detector.detect_jumps(values, metric_name=\"stable_rank\") for jump in jumps: ...     print(jump)</p> <p>Initialize the jump detector.</p> <p>Parameters:</p> Name Type Description Default <code>window_size</code> <code>int</code> <p>Size of rolling window for computing statistics (default: 50)</p> <code>50</code> <code>z_threshold</code> <code>float</code> <p>Z-score threshold for detecting jumps (default: 3.0)</p> <code>3.0</code> <code>min_samples</code> <code>int</code> <p>Minimum samples before starting detection (default: 20)</p> <code>20</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If parameters are invalid</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def __init__(\n    self, window_size: int = 50, z_threshold: float = 3.0, min_samples: int = 20\n) -&gt; None:\n    \"\"\"Initialize the jump detector.\n\n    Args:\n        window_size: Size of rolling window for computing statistics (default: 50)\n        z_threshold: Z-score threshold for detecting jumps (default: 3.0)\n        min_samples: Minimum samples before starting detection (default: 20)\n\n    Raises:\n        ValueError: If parameters are invalid\n    \"\"\"\n    if window_size &lt; 2:\n        raise ValueError(f\"window_size must be &gt;= 2, got {window_size}\")\n    if z_threshold &lt;= 0:\n        raise ValueError(f\"z_threshold must be &gt; 0, got {z_threshold}\")\n    if min_samples &lt; 1:\n        raise ValueError(f\"min_samples must be &gt;= 1, got {min_samples}\")\n\n    self.window_size = window_size\n    self.z_threshold = z_threshold\n    self.min_samples = min_samples\n</code></pre>"},{"location":"api/ndt/#ndt.JumpDetector-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.JumpDetector.detect_jumps","title":"<code>detect_jumps(values: List[float], metric_name: str = 'dimensionality', step_offset: int = 0) -&gt; List[Jump]</code>","text":"<p>Detect jumps in a sequence of dimensionality values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <code>metric_name</code> <code>str</code> <p>Name of the metric being analyzed</p> <code>'dimensionality'</code> <code>step_offset</code> <code>int</code> <p>Offset to add to step numbers (for aligning with training steps)</p> <code>0</code> <p>Returns:</p> Type Description <code>List[Jump]</code> <p>List of detected Jump objects</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If values list is too short</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def detect_jumps(\n    self, values: List[float], metric_name: str = \"dimensionality\", step_offset: int = 0\n) -&gt; List[Jump]:\n    \"\"\"Detect jumps in a sequence of dimensionality values.\n\n    Args:\n        values: List of dimensionality measurements\n        metric_name: Name of the metric being analyzed\n        step_offset: Offset to add to step numbers (for aligning with training steps)\n\n    Returns:\n        List of detected Jump objects\n\n    Raises:\n        ValueError: If values list is too short\n    \"\"\"\n    if len(values) &lt; self.min_samples:\n        return []\n\n    values_array = np.array(values)\n    jumps = []\n\n    # Start detection after minimum samples\n    for i in range(self.min_samples, len(values_array)):\n        # Define window for statistics\n        window_start = max(0, i - self.window_size)\n        window = values_array[window_start:i]\n\n        # Compute statistics\n        mean = np.mean(window)\n        std = np.std(window)\n\n        # Avoid division by zero\n        if std &lt; 1e-10:\n            continue\n\n        # Compute z-score for current value\n        current_value = values_array[i]\n        z_score = abs((current_value - mean) / std)\n\n        # Detect jump\n        if z_score &gt; self.z_threshold:\n            jump = Jump(\n                step=i + step_offset,\n                z_score=z_score,\n                value_before=values_array[i - 1],\n                value_after=current_value,\n                metric_name=metric_name,\n            )\n            jumps.append(jump)\n\n    return jumps\n</code></pre>"},{"location":"api/ndt/#ndt.JumpDetector.detect_jumps_with_direction","title":"<code>detect_jumps_with_direction(values: List[float], metric_name: str = 'dimensionality', step_offset: int = 0, direction: Optional[str] = None) -&gt; List[Jump]</code>","text":"<p>Detect jumps with optional direction filtering.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <code>metric_name</code> <code>str</code> <p>Name of the metric being analyzed</p> <code>'dimensionality'</code> <code>step_offset</code> <code>int</code> <p>Offset to add to step numbers</p> <code>0</code> <code>direction</code> <code>Optional[str]</code> <p>Filter by direction - \"up\", \"down\", or None for both</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Jump]</code> <p>List of detected Jump objects filtered by direction</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction is not valid</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def detect_jumps_with_direction(\n    self,\n    values: List[float],\n    metric_name: str = \"dimensionality\",\n    step_offset: int = 0,\n    direction: Optional[str] = None,\n) -&gt; List[Jump]:\n    \"\"\"Detect jumps with optional direction filtering.\n\n    Args:\n        values: List of dimensionality measurements\n        metric_name: Name of the metric being analyzed\n        step_offset: Offset to add to step numbers\n        direction: Filter by direction - \"up\", \"down\", or None for both\n\n    Returns:\n        List of detected Jump objects filtered by direction\n\n    Raises:\n        ValueError: If direction is not valid\n    \"\"\"\n    if direction not in [None, \"up\", \"down\"]:\n        raise ValueError(f\"direction must be None, 'up', or 'down', got {direction}\")\n\n    all_jumps = self.detect_jumps(values, metric_name, step_offset)\n\n    if direction is None:\n        return all_jumps\n\n    # Filter by direction\n    filtered_jumps = []\n    for jump in all_jumps:\n        change = jump.value_after - jump.value_before\n        if direction == \"up\" and change &gt; 0:\n            filtered_jumps.append(jump)\n        elif direction == \"down\" and change &lt; 0:\n            filtered_jumps.append(jump)\n\n    return filtered_jumps\n</code></pre>"},{"location":"api/ndt/#ndt.JumpDetector.compute_rolling_statistics","title":"<code>compute_rolling_statistics(values: List[float]) -&gt; Tuple[np.ndarray, np.ndarray, np.ndarray]</code>","text":"<p>Compute rolling mean, std, and z-scores for visualization.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray, ndarray]</code> <p>Tuple of (means, stds, z_scores) as numpy arrays</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If values list is empty</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def compute_rolling_statistics(\n    self, values: List[float]\n) -&gt; Tuple[np.ndarray, np.ndarray, np.ndarray]:\n    \"\"\"Compute rolling mean, std, and z-scores for visualization.\n\n    Args:\n        values: List of dimensionality measurements\n\n    Returns:\n        Tuple of (means, stds, z_scores) as numpy arrays\n\n    Raises:\n        ValueError: If values list is empty\n    \"\"\"\n    if len(values) == 0:\n        raise ValueError(\"values list cannot be empty\")\n\n    values_array = np.array(values)\n    n = len(values_array)\n\n    means = np.zeros(n)\n    stds = np.zeros(n)\n    z_scores = np.zeros(n)\n\n    for i in range(self.min_samples, n):\n        window_start = max(0, i - self.window_size)\n        window = values_array[window_start:i]\n\n        means[i] = np.mean(window)\n        stds[i] = np.std(window)\n\n        if stds[i] &gt; 1e-10:\n            z_scores[i] = abs((values_array[i] - means[i]) / stds[i])\n\n    return means, stds, z_scores\n</code></pre>"},{"location":"api/ndt/#ndt.DimensionalityMetrics","title":"<code>DimensionalityMetrics(step: int, stable_rank: float, participation_ratio: float, cumulative_90: int, nuclear_norm_ratio: float, loss: float, grad_norm: Optional[float] = None)</code>  <code>dataclass</code>","text":"<p>Container for dimensionality measurements at a single training step.</p> <p>Attributes:</p> Name Type Description <code>step</code> <code>int</code> <p>Training step number</p> <code>stable_rank</code> <code>float</code> <p>Stable rank estimate</p> <code>participation_ratio</code> <code>float</code> <p>Participation ratio estimate</p> <code>cumulative_90</code> <code>int</code> <p>Number of components for 90% energy</p> <code>nuclear_norm_ratio</code> <code>float</code> <p>Nuclear norm ratio estimate</p> <code>loss</code> <code>float</code> <p>Training loss at this step</p> <code>grad_norm</code> <code>Optional[float]</code> <p>Gradient norm at this step (optional)</p>"},{"location":"api/ndt/#ndt.DimensionalityMetrics-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.DimensionalityMetrics.to_dict","title":"<code>to_dict() -&gt; Dict</code>","text":"<p>Convert to dictionary.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def to_dict(self) -&gt; Dict:\n    \"\"\"Convert to dictionary.\"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"api/ndt/#ndt.HighFrequencyTracker","title":"<code>HighFrequencyTracker(model: nn.Module, layers: Optional[List[nn.Module]] = None, layer_names: Optional[List[str]] = None, sampling_frequency: int = 1, enable_jump_detection: bool = True, jump_window_size: int = 50, jump_z_threshold: float = 3.0, device: Optional[torch.device] = None)</code>","text":"<p>High-frequency tracker for neural network representational dimensionality.</p> <p>This class provides minimal-intrusion tracking of dimensionality metrics during neural network training. It automatically captures activations and computes multiple dimensionality estimates at specified intervals.</p> <p>Attributes:</p> Name Type Description <code>model</code> <p>The neural network model to track</p> <code>layers</code> <p>List of layers to monitor</p> <code>sampling_frequency</code> <p>Record metrics every N steps</p> <code>jump_detector</code> <p>Optional jump detector for identifying phase transitions</p> Example <p>import torch.nn as nn model = nn.Sequential( ...     nn.Linear(784, 512), nn.ReLU(), ...     nn.Linear(512, 256), nn.ReLU(), ...     nn.Linear(256, 10) ... ) tracker = HighFrequencyTracker(model, layers=[model[0], model[2], model[4]])</p> <p>Initialize the high-frequency tracker.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Neural network model to track</p> required <code>layers</code> <code>Optional[List[Module]]</code> <p>List of layers to monitor. If None, auto-detects based on architecture</p> <code>None</code> <code>layer_names</code> <code>Optional[List[str]]</code> <p>Optional custom names for layers</p> <code>None</code> <code>sampling_frequency</code> <code>int</code> <p>Record metrics every N steps (default: 1)</p> <code>1</code> <code>enable_jump_detection</code> <code>bool</code> <p>Whether to detect dimensionality jumps (default: True)</p> <code>True</code> <code>jump_window_size</code> <code>int</code> <p>Window size for jump detection (default: 50)</p> <code>50</code> <code>jump_z_threshold</code> <code>float</code> <p>Z-score threshold for jump detection (default: 3.0)</p> <code>3.0</code> <code>device</code> <code>Optional[device]</code> <p>Device for computations (default: model's device)</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If model or parameters are invalid</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __init__(\n    self,\n    model: nn.Module,\n    layers: Optional[List[nn.Module]] = None,\n    layer_names: Optional[List[str]] = None,\n    sampling_frequency: int = 1,\n    enable_jump_detection: bool = True,\n    jump_window_size: int = 50,\n    jump_z_threshold: float = 3.0,\n    device: Optional[torch.device] = None,\n) -&gt; None:\n    \"\"\"Initialize the high-frequency tracker.\n\n    Args:\n        model: Neural network model to track\n        layers: List of layers to monitor. If None, auto-detects based on architecture\n        layer_names: Optional custom names for layers\n        sampling_frequency: Record metrics every N steps (default: 1)\n        enable_jump_detection: Whether to detect dimensionality jumps (default: True)\n        jump_window_size: Window size for jump detection (default: 50)\n        jump_z_threshold: Z-score threshold for jump detection (default: 3.0)\n        device: Device for computations (default: model's device)\n\n    Raises:\n        ValueError: If model or parameters are invalid\n    \"\"\"\n    if not isinstance(model, nn.Module):\n        raise ValueError(\"model must be a torch.nn.Module\")\n\n    self.model = model\n    self.sampling_frequency = sampling_frequency\n    self.device = device or next(model.parameters()).device\n\n    # Auto-detect layers if not provided\n    if layers is None:\n        layers = self._auto_detect_layers()\n\n    if not layers:\n        raise ValueError(\n            \"No layers to track. Provide layers explicitly or use supported architecture.\"\n        )\n\n    # Set up activation capture\n    self.activation_capture = ActivationCapture()\n    self.activation_capture.register_hooks(model, layers, layer_names)\n    self.layer_names = layer_names or [\n        f\"{layer.__class__.__name__}_{i}\" for i, layer in enumerate(layers)\n    ]\n\n    # Initialize storage\n    self.metrics_history: Dict[str, List[DimensionalityMetrics]] = {\n        name: [] for name in self.layer_names\n    }\n    self.step_counter = 0\n\n    # Jump detection\n    self.enable_jump_detection = enable_jump_detection\n    if enable_jump_detection:\n        self.jump_detector = JumpDetector(\n            window_size=jump_window_size, z_threshold=jump_z_threshold\n        )\n    else:\n        self.jump_detector = None\n\n    # Logging\n    self.logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/ndt/#ndt.HighFrequencyTracker--in-training-loop","title":"In training loop:","text":"<p>for step, (x, y) in enumerate(dataloader): ...     loss = train_step(model, x, y) ...     tracker.log(step, loss) results = tracker.get_results() tracker.close()</p>"},{"location":"api/ndt/#ndt.HighFrequencyTracker-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.HighFrequencyTracker.log","title":"<code>log(step: int, loss: float, grad_norm: Optional[float] = None, force: bool = False) -&gt; None</code>","text":"<p>Log dimensionality metrics for the current step.</p> <p>This is the main method called during training. It captures activations and computes dimensionality metrics according to the sampling frequency.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>Current training step</p> required <code>loss</code> <code>float</code> <p>Training loss value</p> required <code>grad_norm</code> <code>Optional[float]</code> <p>Optional gradient norm</p> <code>None</code> <code>force</code> <code>bool</code> <p>Force logging even if not at sampling interval</p> <code>False</code> Example <p>for step, (x, y) in enumerate(dataloader): ...     optimizer.zero_grad() ...     output = model(x) ...     loss = criterion(output, y) ...     loss.backward() ...     optimizer.step() ...     tracker.log(step, loss.item())</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def log(\n    self, step: int, loss: float, grad_norm: Optional[float] = None, force: bool = False\n) -&gt; None:\n    \"\"\"Log dimensionality metrics for the current step.\n\n    This is the main method called during training. It captures activations\n    and computes dimensionality metrics according to the sampling frequency.\n\n    Args:\n        step: Current training step\n        loss: Training loss value\n        grad_norm: Optional gradient norm\n        force: Force logging even if not at sampling interval\n\n    Example:\n        &gt;&gt;&gt; for step, (x, y) in enumerate(dataloader):\n        ...     optimizer.zero_grad()\n        ...     output = model(x)\n        ...     loss = criterion(output, y)\n        ...     loss.backward()\n        ...     optimizer.step()\n        ...     tracker.log(step, loss.item())\n    \"\"\"\n    # Check if we should record this step\n    if not force and step % self.sampling_frequency != 0:\n        return\n\n    self.step_counter = step\n\n    # Activations are already captured by hooks during forward pass\n    # Now compute metrics for each layer\n    with torch.no_grad():\n        for layer_name in self.layer_names:\n            activation = self.activation_capture.get_activation(layer_name)\n\n            if activation is None:\n                self.logger.warning(\n                    f\"No activation captured for layer {layer_name} at step {step}\"\n                )\n                continue\n\n            try:\n                # Reshape activation to 2D matrix (batch_size, features)\n                matrix = self._prepare_activation_matrix(activation)\n\n                # Compute all dimensionality metrics\n                sr, pr, ce90, nnr = compute_all_metrics(matrix)\n\n                # Create metrics object\n                metrics = DimensionalityMetrics(\n                    step=step,\n                    stable_rank=sr,\n                    participation_ratio=pr,\n                    cumulative_90=ce90,\n                    nuclear_norm_ratio=nnr,\n                    loss=loss,\n                    grad_norm=grad_norm,\n                )\n\n                # Store metrics\n                self.metrics_history[layer_name].append(metrics)\n\n            except Exception as e:\n                self.logger.error(\n                    f\"Error computing metrics for {layer_name} at step {step}: {e}\"\n                )\n\n    # Clear activations for next step\n    self.activation_capture.clear_activations()\n</code></pre>"},{"location":"api/ndt/#ndt.HighFrequencyTracker.get_results","title":"<code>get_results(layer_name: Optional[str] = None) -&gt; Union[pd.DataFrame, Dict[str, pd.DataFrame]]</code>","text":"<p>Get tracked metrics as pandas DataFrame(s).</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>Optional[str]</code> <p>If specified, return results for that layer only.        If None, return dict of DataFrames for all layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Union[DataFrame, Dict[str, DataFrame]]</code> <p>DataFrame or dict of DataFrames containing metrics history</p> Example <p>results = tracker.get_results() for layer_name, df in results.items(): ...     print(f\"{layer_name}: {len(df)} measurements\")</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def get_results(\n    self, layer_name: Optional[str] = None\n) -&gt; Union[pd.DataFrame, Dict[str, pd.DataFrame]]:\n    \"\"\"Get tracked metrics as pandas DataFrame(s).\n\n    Args:\n        layer_name: If specified, return results for that layer only.\n                   If None, return dict of DataFrames for all layers.\n\n    Returns:\n        DataFrame or dict of DataFrames containing metrics history\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; for layer_name, df in results.items():\n        ...     print(f\"{layer_name}: {len(df)} measurements\")\n    \"\"\"\n    if layer_name is not None:\n        if layer_name not in self.metrics_history:\n            raise ValueError(f\"Unknown layer name: {layer_name}\")\n        return self._metrics_to_dataframe(self.metrics_history[layer_name])\n\n    return {\n        name: self._metrics_to_dataframe(metrics)\n        for name, metrics in self.metrics_history.items()\n    }\n</code></pre>"},{"location":"api/ndt/#ndt.HighFrequencyTracker.detect_jumps","title":"<code>detect_jumps(layer_name: Optional[str] = None, metric: str = 'stable_rank') -&gt; Dict[str, List]</code>","text":"<p>Detect dimensionality jumps in tracked metrics.</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>Optional[str]</code> <p>If specified, detect jumps for that layer only.        If None, detect for all layers.</p> <code>None</code> <code>metric</code> <code>str</code> <p>Which metric to analyze (\"stable_rank\", \"participation_ratio\",    \"cumulative_90\", or \"nuclear_norm_ratio\")</p> <code>'stable_rank'</code> <p>Returns:</p> Type Description <code>Dict[str, List]</code> <p>Dictionary mapping layer names to lists of Jump objects</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If jump detection is disabled or metric name is invalid</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def detect_jumps(\n    self, layer_name: Optional[str] = None, metric: str = \"stable_rank\"\n) -&gt; Dict[str, List]:\n    \"\"\"Detect dimensionality jumps in tracked metrics.\n\n    Args:\n        layer_name: If specified, detect jumps for that layer only.\n                   If None, detect for all layers.\n        metric: Which metric to analyze (\"stable_rank\", \"participation_ratio\",\n               \"cumulative_90\", or \"nuclear_norm_ratio\")\n\n    Returns:\n        Dictionary mapping layer names to lists of Jump objects\n\n    Raises:\n        ValueError: If jump detection is disabled or metric name is invalid\n    \"\"\"\n    if not self.enable_jump_detection:\n        raise ValueError(\"Jump detection is disabled. Enable it during initialization.\")\n\n    valid_metrics = [\n        \"stable_rank\",\n        \"participation_ratio\",\n        \"cumulative_90\",\n        \"nuclear_norm_ratio\",\n    ]\n    if metric not in valid_metrics:\n        raise ValueError(f\"metric must be one of {valid_metrics}, got {metric}\")\n\n    layers_to_analyze = [layer_name] if layer_name else self.layer_names\n\n    jumps_dict = {}\n    for name in layers_to_analyze:\n        if name not in self.metrics_history:\n            continue\n\n        # Extract metric values\n        values = [getattr(m, metric) for m in self.metrics_history[name]]\n\n        # Detect jumps\n        jumps = self.jump_detector.detect_jumps(values, metric_name=f\"{name}_{metric}\")\n        jumps_dict[name] = jumps\n\n    return jumps_dict\n</code></pre>"},{"location":"api/ndt/#ndt.HighFrequencyTracker.close","title":"<code>close() -&gt; None</code>","text":"<p>Clean up hooks and resources.</p> <p>Should be called when done tracking to free resources.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Clean up hooks and resources.\n\n    Should be called when done tracking to free resources.\n    \"\"\"\n    self.activation_capture.remove_hooks()\n    self.logger.info(f\"Tracker closed. Recorded {self.step_counter} steps.\")\n</code></pre>"},{"location":"api/ndt/#ndt.HighFrequencyTracker.__enter__","title":"<code>__enter__() -&gt; HighFrequencyTracker</code>","text":"<p>Context manager entry.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __enter__(self) -&gt; \"HighFrequencyTracker\":\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ndt/#ndt.HighFrequencyTracker.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb) -&gt; None</code>","text":"<p>Context manager exit.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb) -&gt; None:\n    \"\"\"Context manager exit.\"\"\"\n    self.close()\n</code></pre>"},{"location":"api/ndt/#ndt.HighFrequencyTracker.__del__","title":"<code>__del__() -&gt; None</code>","text":"<p>Cleanup on deletion.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __del__(self) -&gt; None:\n    \"\"\"Cleanup on deletion.\"\"\"\n    if hasattr(self, \"activation_capture\"):\n        self.activation_capture.remove_hooks()\n</code></pre>"},{"location":"api/ndt/#ndt.ValidityReport","title":"<code>ValidityReport(detector_name: str, results: tuple[FixtureResult, ...], mean_recall: float, false_positives_on_null: int, valid: bool, message: str)</code>  <code>dataclass</code>","text":"<p>A verdict on whether a detector's transitions can be trusted.</p> <p><code>valid</code> is True only when the detector recovers most planted transitions and stays near-silent on the null controls. The thresholds are deliberately lenient; a detector that fails even these is not measuring transitions.</p>"},{"location":"api/ndt/#ndt.ValidityReport-functions","title":"Methods:","text":""},{"location":"api/ndt/#ndt.ValidityReport.render","title":"<code>render() -&gt; str</code>","text":"<p>Full multi-line report.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def render(self) -&gt; str:\n    \"\"\"Full multi-line report.\"\"\"\n    lines = [f\"Validity report for: {self.detector_name}\", \"\"]\n    for r in self.results:\n        if r.is_null:\n            lines.append(\n                f\"  {r.fixture:&lt;20} null control   \"\n                f\"false positives: {r.false_positives:&gt;3} \"\n                f\"({r.false_positive_rate_per_1000:.1f}/1000 steps)\"\n            )\n        else:\n            lines.append(\n                f\"  {r.fixture:&lt;20} recall: {r.recall:.2f}   \"\n                f\"false positives: {r.false_positives:&gt;3}\"\n            )\n    lines.append(\"\")\n    lines.append(f\"  mean recall on planted transitions : {self.mean_recall:.2f}\")\n    lines.append(f\"  false positives on null controls   : {self.false_positives_on_null}\")\n    lines.append(\"\")\n    lines.append(f\"  VERDICT: {'VALID' if self.valid else 'NOT VALID'} on these fixtures\")\n    lines.append(f\"  {self.message}\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"api/ndt/#ndt-functions","title":"Functions:","text":""},{"location":"api/ndt/#ndt.detect_architecture","title":"<code>detect_architecture(model: nn.Module) -&gt; Optional[ArchitectureHandler]</code>","text":"<p>Automatically detect the architecture type of a model.</p> <p>Tries each handler in priority order and returns the first match.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>Optional[ArchitectureHandler]</code> <p>ArchitectureHandler instance if detected, None otherwise</p> Example <p>import torch.nn as nn model = nn.Sequential(nn.Linear(784, 512), nn.ReLU(), nn.Linear(512, 10)) handler = detect_architecture(model) print(handler.get_name()) 'MLP'</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def detect_architecture(model: nn.Module) -&gt; Optional[ArchitectureHandler]:\n    \"\"\"Automatically detect the architecture type of a model.\n\n    Tries each handler in priority order and returns the first match.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        ArchitectureHandler instance if detected, None otherwise\n\n    Example:\n        &gt;&gt;&gt; import torch.nn as nn\n        &gt;&gt;&gt; model = nn.Sequential(nn.Linear(784, 512), nn.ReLU(), nn.Linear(512, 10))\n        &gt;&gt;&gt; handler = detect_architecture(model)\n        &gt;&gt;&gt; print(handler.get_name())\n        'MLP'\n    \"\"\"\n    for handler in DEFAULT_HANDLERS:\n        if handler.validate_model(model):\n            return handler\n    return None\n</code></pre>"},{"location":"api/ndt/#ndt.get_handler","title":"<code>get_handler(model: nn.Module, architecture: Optional[str] = None) -&gt; ArchitectureHandler</code>","text":"<p>Get an architecture handler for a model.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>architecture</code> <code>Optional[str]</code> <p>Optional architecture name (\"mlp\", \"cnn\", \"transformer\", \"vit\").          If None, auto-detects.</p> <code>None</code> <p>Returns:</p> Type Description <code>ArchitectureHandler</code> <p>ArchitectureHandler instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If architecture cannot be detected or invalid name provided</p> Example <p>handler = get_handler(model, architecture=\"mlp\") layers = handler.get_activation_layers(model)</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def get_handler(model: nn.Module, architecture: Optional[str] = None) -&gt; ArchitectureHandler:\n    \"\"\"Get an architecture handler for a model.\n\n    Args:\n        model: The neural network model\n        architecture: Optional architecture name (\"mlp\", \"cnn\", \"transformer\", \"vit\").\n                     If None, auto-detects.\n\n    Returns:\n        ArchitectureHandler instance\n\n    Raises:\n        ValueError: If architecture cannot be detected or invalid name provided\n\n    Example:\n        &gt;&gt;&gt; handler = get_handler(model, architecture=\"mlp\")\n        &gt;&gt;&gt; layers = handler.get_activation_layers(model)\n    \"\"\"\n    if architecture is not None:\n        # Explicit architecture specified\n        architecture = architecture.lower()\n        handler_map = {\n            \"mlp\": MLPHandler(),\n            \"cnn\": CNNHandler(),\n            \"transformer\": TransformerHandler(),\n            \"vit\": ViTHandler(),\n        }\n\n        if architecture not in handler_map:\n            raise ValueError(\n                f\"Unknown architecture: {architecture}. Must be one of {list(handler_map.keys())}\"\n            )\n\n        handler = handler_map[architecture]\n\n        # Validate that the model actually matches\n        if not handler.validate_model(model):\n            raise ValueError(\n                f\"Model does not appear to be a {architecture.upper()}. \"\n                \"Try auto-detection by not specifying architecture.\"\n            )\n\n        return handler\n\n    # Auto-detect architecture\n    handler = detect_architecture(model)\n    if handler is None:\n        raise ValueError(\n            \"Could not auto-detect model architecture. \"\n            \"Please specify architecture explicitly: 'mlp', 'cnn', 'transformer', or 'vit'\"\n        )\n\n    return handler\n</code></pre>"},{"location":"api/ndt/#ndt.compute_all_metrics","title":"<code>compute_all_metrics(matrix: torch.Tensor, eps: float = 1e-10) -&gt; Tuple[float, float, int, float]</code>","text":"<p>Compute all four dimensionality metrics efficiently.</p> <p>This function computes SVD once and reuses it for all metrics, improving efficiency when all metrics are needed.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>Tuple[float, float, int, float]</code> <p>Tuple of (stable_rank, participation_ratio, cumulative_90, nuclear_norm_ratio)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or computations fail</p> Example <p>import torch A = torch.randn(100, 50) sr, pr, ce90, nnr = compute_all_metrics(A) print(f\"Metrics: SR={sr:.2f}, PR={pr:.2f}, CE90={ce90}, NNR={nnr:.2f}\")</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def compute_all_metrics(\n    matrix: torch.Tensor, eps: float = 1e-10\n) -&gt; Tuple[float, float, int, float]:\n    \"\"\"Compute all four dimensionality metrics efficiently.\n\n    This function computes SVD once and reuses it for all metrics,\n    improving efficiency when all metrics are needed.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Tuple of (stable_rank, participation_ratio, cumulative_90, nuclear_norm_ratio)\n\n    Raises:\n        ValueError: If matrix is not 2D or computations fail\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; sr, pr, ce90, nnr = compute_all_metrics(A)\n        &gt;&gt;&gt; print(f\"Metrics: SR={sr:.2f}, PR={pr:.2f}, CE90={ce90}, NNR={nnr:.2f}\")\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        # Compute SVD once for all metrics\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Compute norms for stable rank\n        frobenius_norm = torch.norm(matrix, p=\"fro\")\n        spectral_norm = singular_values[0]  # Largest singular value\n        sr = ((frobenius_norm**2) / (spectral_norm**2 + eps)).item()\n\n        # Participation ratio\n        sv_normalized = singular_values / (torch.sum(singular_values) + eps)\n        pr = (1.0 / (torch.sum(sv_normalized**2) + eps)).item()\n\n        # Cumulative energy 90%\n        explained_variance = singular_values**2\n        total_variance = torch.sum(explained_variance)\n        cumsum = torch.cumsum(explained_variance, dim=0)\n        cumsum_ratio = cumsum / total_variance\n        ce90 = torch.searchsorted(cumsum_ratio, 0.90).item() + 1\n        ce90 = min(ce90, len(singular_values))\n\n        # Nuclear norm ratio\n        nuclear_norm = torch.sum(singular_values)\n        nnr = (nuclear_norm / (singular_values[0] + eps)).item()\n\n        return sr, pr, ce90, nnr\n</code></pre>"},{"location":"api/ndt/#ndt.cumulative_energy_90","title":"<code>cumulative_energy_90(matrix: torch.Tensor, threshold: float = 0.9) -&gt; int</code>","text":"<p>Compute number of components needed for 90% cumulative energy.</p> <p>This metric counts how many principal components are needed to capture a given percentage (default 90%) of the total variance.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>threshold</code> <code>float</code> <p>Energy threshold (default: 0.90 for 90%)</p> <code>0.9</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of components needed (int)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D, threshold invalid, or SVD fails</p> Example <p>import torch A = torch.randn(100, 50) n_components = cumulative_energy_90(A) print(f\"Components for 90% energy: {n_components}\")</p> References <p>Jolliffe, I. T. (2002). Principal component analysis (2nd ed.). Springer.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def cumulative_energy_90(matrix: torch.Tensor, threshold: float = 0.90) -&gt; int:\n    \"\"\"Compute number of components needed for 90% cumulative energy.\n\n    This metric counts how many principal components are needed to capture\n    a given percentage (default 90%) of the total variance.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        threshold: Energy threshold (default: 0.90 for 90%)\n\n    Returns:\n        Number of components needed (int)\n\n    Raises:\n        ValueError: If matrix is not 2D, threshold invalid, or SVD fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; n_components = cumulative_energy_90(A)\n        &gt;&gt;&gt; print(f\"Components for 90% energy: {n_components}\")\n\n    References:\n        Jolliffe, I. T. (2002). Principal component analysis (2nd ed.).\n        Springer.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    if not 0 &lt; threshold &lt;= 1:\n        raise ValueError(f\"Threshold must be in (0, 1], got {threshold}\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Compute explained variance\n        explained_variance = singular_values**2\n        total_variance = torch.sum(explained_variance)\n\n        # Compute cumulative explained variance ratio\n        cumsum = torch.cumsum(explained_variance, dim=0)\n        cumsum_ratio = cumsum / total_variance\n\n        # Find number of components for threshold\n        n_components = torch.searchsorted(cumsum_ratio, threshold).item() + 1\n        return min(n_components, len(singular_values))\n</code></pre>"},{"location":"api/ndt/#ndt.nuclear_norm_ratio","title":"<code>nuclear_norm_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute nuclear norm ratio (nuclear norm / spectral norm).</p> <p>This metric normalizes the nuclear norm (sum of singular values) by the spectral norm (largest singular value), providing a normalized measure of rank.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Nuclear norm ratio value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or SVD computation fails</p> Example <p>import torch A = torch.randn(100, 50) nnr = nuclear_norm_ratio(A) print(f\"Nuclear norm ratio: {nnr:.2f}\")</p> References <p>Cand\u00e8s, E. J., &amp; Recht, B. (2009). Exact matrix completion via convex optimization. Foundations of Computational mathematics, 9(6), 717-772.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def nuclear_norm_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute nuclear norm ratio (nuclear norm / spectral norm).\n\n    This metric normalizes the nuclear norm (sum of singular values) by\n    the spectral norm (largest singular value), providing a normalized\n    measure of rank.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Nuclear norm ratio value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or SVD computation fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; nnr = nuclear_norm_ratio(A)\n        &gt;&gt;&gt; print(f\"Nuclear norm ratio: {nnr:.2f}\")\n\n    References:\n        Cand\u00e8s, E. J., &amp; Recht, B. (2009). Exact matrix completion via\n        convex optimization. Foundations of Computational mathematics, 9(6), 717-772.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        nuclear_norm = torch.sum(singular_values)\n        spectral_norm = singular_values[0]  # Largest singular value\n\n        ratio = nuclear_norm / (spectral_norm + eps)\n        return ratio.item()\n</code></pre>"},{"location":"api/ndt/#ndt.participation_ratio","title":"<code>participation_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute participation ratio from singular values.</p> <p>Participation ratio is the inverse of the sum of squared normalized singular values. It measures the effective dimensionality of the representation space.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Participation ratio value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or SVD computation fails</p> Example <p>import torch A = torch.randn(100, 50) pr = participation_ratio(A) print(f\"Participation ratio: {pr:.2f}\")</p> References <p>Grassberger, P., &amp; Procaccia, I. (1983). Measuring the strangeness of strange attractors. Physica D: Nonlinear Phenomena, 9(1-2), 189-208.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def participation_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute participation ratio from singular values.\n\n    Participation ratio is the inverse of the sum of squared normalized\n    singular values. It measures the effective dimensionality of the\n    representation space.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Participation ratio value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or SVD computation fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; pr = participation_ratio(A)\n        &gt;&gt;&gt; print(f\"Participation ratio: {pr:.2f}\")\n\n    References:\n        Grassberger, P., &amp; Procaccia, I. (1983). Measuring the strangeness\n        of strange attractors. Physica D: Nonlinear Phenomena, 9(1-2), 189-208.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Normalize singular values\n        sv_normalized = singular_values / (torch.sum(singular_values) + eps)\n\n        # Compute participation ratio\n        pr = 1.0 / (torch.sum(sv_normalized**2) + eps)\n        return pr.item()\n</code></pre>"},{"location":"api/ndt/#ndt.stable_rank","title":"<code>stable_rank(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute stable rank of a matrix.</p> <p>Stable rank is the ratio of squared Frobenius norm to squared spectral norm. It estimates the effective number of linearly independent dimensions.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Stable rank value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or contains NaN/Inf values</p> Example <p>import torch A = torch.randn(100, 50) sr = stable_rank(A) print(f\"Stable rank: {sr:.2f}\")</p> References <p>Vershynin, R. (2010). Introduction to the non-asymptotic analysis of random matrices. arXiv preprint arXiv:1011.3027.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def stable_rank(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute stable rank of a matrix.\n\n    Stable rank is the ratio of squared Frobenius norm to squared spectral norm.\n    It estimates the effective number of linearly independent dimensions.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Stable rank value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or contains NaN/Inf values\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; sr = stable_rank(A)\n        &gt;&gt;&gt; print(f\"Stable rank: {sr:.2f}\")\n\n    References:\n        Vershynin, R. (2010). Introduction to the non-asymptotic analysis\n        of random matrices. arXiv preprint arXiv:1011.3027.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    if torch.isnan(matrix).any() or torch.isinf(matrix).any():\n        raise ValueError(\"Matrix contains NaN or Inf values\")\n\n    with torch.no_grad():\n        frobenius_norm = torch.norm(matrix, p=\"fro\")\n        # Spectral norm is the largest singular value\n        _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        spectral_norm = singular_values[0]\n\n        result = (frobenius_norm**2) / (spectral_norm**2 + eps)\n        return result.item()\n</code></pre>"},{"location":"api/ndt/#ndt.export_to_csv","title":"<code>export_to_csv(results: Dict[str, pd.DataFrame], output_path: str, separate_files: bool = False) -&gt; None</code>","text":"<p>Export tracking results to CSV format.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>output_path</code> <code>str</code> <p>Output file path or directory path</p> required <code>separate_files</code> <code>bool</code> <p>If True, create separate CSV for each layer.            If False, combine all layers into one CSV with layer column.</p> <code>False</code> Example <p>results = tracker.get_results() export_to_csv(results, \"results.csv\")</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/csv.py</code> <pre><code>def export_to_csv(\n    results: Dict[str, pd.DataFrame], output_path: str, separate_files: bool = False\n) -&gt; None:\n    \"\"\"Export tracking results to CSV format.\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames\n        output_path: Output file path or directory path\n        separate_files: If True, create separate CSV for each layer.\n                       If False, combine all layers into one CSV with layer column.\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; export_to_csv(results, \"results.csv\")\n        &gt;&gt;&gt; # Or separate files:\n        &gt;&gt;&gt; export_to_csv(results, \"results_dir/\", separate_files=True)\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    if separate_files:\n        # Create directory if it doesn't exist\n        output_path.mkdir(parents=True, exist_ok=True)\n\n        # Export each layer to separate file\n        for layer_name, df in results.items():\n            filename = output_path / f\"{layer_name}.csv\"\n            df.to_csv(filename, index=False)\n            print(f\"Exported {layer_name} to {filename}\")\n\n    else:\n        # Combine all layers into one CSV\n        combined_data = []\n        for layer_name, df in results.items():\n            df_copy = df.copy()\n            df_copy[\"layer\"] = layer_name\n            combined_data.append(df_copy)\n\n        if combined_data:\n            combined_df = pd.concat(combined_data, ignore_index=True)\n\n            # Reorder columns to put layer first\n            cols = [\"layer\"] + [col for col in combined_df.columns if col != \"layer\"]\n            combined_df = combined_df[cols]\n\n            combined_df.to_csv(output_path, index=False)\n            print(f\"Exported all layers to {output_path}\")\n        else:\n            print(\"No data to export\")\n</code></pre>"},{"location":"api/ndt/#ndt.export_to_csv--or-separate-files","title":"Or separate files:","text":"<p>export_to_csv(results, \"results_dir/\", separate_files=True)</p>"},{"location":"api/ndt/#ndt.export_to_hdf5","title":"<code>export_to_hdf5(results: Dict[str, pd.DataFrame], output_path: str, compression: str = 'gzip', compression_opts: int = 4, metadata: Optional[Dict[str, Any]] = None) -&gt; None</code>","text":"<p>Export tracking results to HDF5 format (efficient for large data).</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>output_path</code> <code>str</code> <p>Output HDF5 file path</p> required <code>compression</code> <code>str</code> <p>Compression algorithm (\"gzip\", \"lzf\", or None)</p> <code>'gzip'</code> <code>compression_opts</code> <code>int</code> <p>Compression level (0-9 for gzip)</p> <code>4</code> <code>metadata</code> <code>Optional[Dict[str, Any]]</code> <p>Optional metadata to store as attributes</p> <code>None</code> Example <p>results = tracker.get_results() metadata = {\"model\": \"ResNet18\", \"dataset\": \"CIFAR-10\"} export_to_hdf5(results, \"results.h5\", metadata=metadata)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/hdf5.py</code> <pre><code>def export_to_hdf5(\n    results: Dict[str, pd.DataFrame],\n    output_path: str,\n    compression: str = \"gzip\",\n    compression_opts: int = 4,\n    metadata: Optional[Dict[str, Any]] = None,\n) -&gt; None:\n    \"\"\"Export tracking results to HDF5 format (efficient for large data).\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames\n        output_path: Output HDF5 file path\n        compression: Compression algorithm (\"gzip\", \"lzf\", or None)\n        compression_opts: Compression level (0-9 for gzip)\n        metadata: Optional metadata to store as attributes\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; metadata = {\"model\": \"ResNet18\", \"dataset\": \"CIFAR-10\"}\n        &gt;&gt;&gt; export_to_hdf5(results, \"results.h5\", metadata=metadata)\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    with h5py.File(output_path, \"w\") as f:\n        # Store metadata as root attributes\n        if metadata:\n            for key, value in metadata.items():\n                f.attrs[key] = value\n\n        # Store each layer's data as a group\n        for layer_name, df in results.items():\n            # Create group for this layer\n            group = f.create_group(layer_name)\n\n            # Store each column as a dataset\n            for col in df.columns:\n                data = df[col].values\n\n                # Handle different data types\n                if data.dtype == object:\n                    # Convert to string for object columns\n                    data = data.astype(str)\n\n                group.create_dataset(\n                    col,\n                    data=data,\n                    compression=compression,\n                    compression_opts=compression_opts if compression == \"gzip\" else None,\n                )\n\n            # Store column names as attribute\n            group.attrs[\"columns\"] = list(df.columns)\n\n    print(f\"Exported results to {output_path}\")\n</code></pre>"},{"location":"api/ndt/#ndt.export_to_json","title":"<code>export_to_json(results: Dict[str, pd.DataFrame], output_path: str, pretty: bool = True) -&gt; None</code>","text":"<p>Export tracking results to JSON format.</p> <p>Parameters:</p> Name Type Description Default <code>output_path</code> <code>str</code> <p>Output file path</p> required <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>pretty</code> <code>bool</code> <p>If True, format JSON with indentation</p> <code>True</code> Example <p>results = tracker.get_results() export_to_json(results, \"results.json\")</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/json.py</code> <pre><code>def export_to_json(results: Dict[str, pd.DataFrame], output_path: str, pretty: bool = True) -&gt; None:\n    \"\"\"Export tracking results to JSON format.\n\n    Args:\n        output_path: Output file path\n        results: Dictionary mapping layer names to DataFrames\n        pretty: If True, format JSON with indentation\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; export_to_json(results, \"results.json\")\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    # Convert DataFrames to dictionaries\n    json_data = {}\n    for layer_name, df in results.items():\n        json_data[layer_name] = df.to_dict(orient=\"records\")\n\n    # Write to file\n    with open(output_path, \"w\") as f:\n        if pretty:\n            json.dump(json_data, f, indent=2)\n        else:\n            json.dump(json_data, f)\n\n    print(f\"Exported results to {output_path}\")\n</code></pre>"},{"location":"api/ndt/#ndt.load_config","title":"<code>load_config(config_path: str) -&gt; Dict[str, Any]</code>","text":"<p>Load configuration from YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to YAML configuration file</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Configuration dictionary</p> Example <p>config = load_config(\"tracker_config.yaml\") tracker = HighFrequencyTracker(**config)</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If config file doesn't exist</p> <code>YAMLError</code> <p>If YAML is invalid</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def load_config(config_path: str) -&gt; Dict[str, Any]:\n    \"\"\"Load configuration from YAML file.\n\n    Args:\n        config_path: Path to YAML configuration file\n\n    Returns:\n        Configuration dictionary\n\n    Example:\n        &gt;&gt;&gt; config = load_config(\"tracker_config.yaml\")\n        &gt;&gt;&gt; tracker = HighFrequencyTracker(**config)\n\n    Raises:\n        FileNotFoundError: If config file doesn't exist\n        yaml.YAMLError: If YAML is invalid\n    \"\"\"\n    config_path = Path(config_path)\n\n    if not config_path.exists():\n        raise FileNotFoundError(f\"Config file not found: {config_path}\")\n\n    with open(config_path) as f:\n        config = yaml.safe_load(f)\n\n    return config\n</code></pre>"},{"location":"api/ndt/#ndt.save_config","title":"<code>save_config(config: Dict[str, Any], output_path: str) -&gt; None</code>","text":"<p>Save configuration to YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Dict[str, Any]</code> <p>Configuration dictionary</p> required <code>output_path</code> <code>str</code> <p>Output file path</p> required Example <p>config = { ...     \"sampling_frequency\": 10, ...     \"enable_jump_detection\": True, ...     \"jump_z_threshold\": 3.0 ... } save_config(config, \"tracker_config.yaml\")</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def save_config(config: Dict[str, Any], output_path: str) -&gt; None:\n    \"\"\"Save configuration to YAML file.\n\n    Args:\n        config: Configuration dictionary\n        output_path: Output file path\n\n    Example:\n        &gt;&gt;&gt; config = {\n        ...     \"sampling_frequency\": 10,\n        ...     \"enable_jump_detection\": True,\n        ...     \"jump_z_threshold\": 3.0\n        ... }\n        &gt;&gt;&gt; save_config(config, \"tracker_config.yaml\")\n    \"\"\"\n    output_path = Path(output_path)\n\n    with open(output_path, \"w\") as f:\n        yaml.dump(config, f, default_flow_style=False, sort_keys=False)\n\n    print(f\"Saved configuration to {output_path}\")\n</code></pre>"},{"location":"api/ndt/#ndt.setup_logger","title":"<code>setup_logger(name: str = 'ndt', level: int = logging.INFO, log_file: Optional[str] = None, format_string: Optional[str] = None) -&gt; logging.Logger</code>","text":"<p>Set up a logger with console and optional file handlers.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Logger name (default: \"ndt\")</p> <code>'ndt'</code> <code>level</code> <code>int</code> <p>Logging level (default: INFO)</p> <code>INFO</code> <code>log_file</code> <code>Optional[str]</code> <p>Optional file path for logging</p> <code>None</code> <code>format_string</code> <code>Optional[str]</code> <p>Optional custom format string</p> <code>None</code> <p>Returns:</p> Type Description <code>Logger</code> <p>Configured logger instance</p> Example <p>logger = setup_logger(\"ndt\", level=logging.DEBUG, log_file=\"tracking.log\") logger.info(\"Starting tracking...\")</p> Source code in <code>src/ndt/utils/logging.py</code> <pre><code>def setup_logger(\n    name: str = \"ndt\",\n    level: int = logging.INFO,\n    log_file: Optional[str] = None,\n    format_string: Optional[str] = None,\n) -&gt; logging.Logger:\n    \"\"\"Set up a logger with console and optional file handlers.\n\n    Args:\n        name: Logger name (default: \"ndt\")\n        level: Logging level (default: INFO)\n        log_file: Optional file path for logging\n        format_string: Optional custom format string\n\n    Returns:\n        Configured logger instance\n\n    Example:\n        &gt;&gt;&gt; logger = setup_logger(\"ndt\", level=logging.DEBUG, log_file=\"tracking.log\")\n        &gt;&gt;&gt; logger.info(\"Starting tracking...\")\n    \"\"\"\n    logger = logging.getLogger(name)\n    logger.setLevel(level)\n\n    # Remove existing handlers to avoid duplicates\n    logger.handlers = []\n\n    # Default format\n    if format_string is None:\n        format_string = \"%(asctime)s - %(name)s - %(levelname)s - %(message)s\"\n\n    formatter = logging.Formatter(format_string)\n\n    # Console handler\n    console_handler = logging.StreamHandler(sys.stdout)\n    console_handler.setLevel(level)\n    console_handler.setFormatter(formatter)\n    logger.addHandler(console_handler)\n\n    # File handler if specified\n    if log_file:\n        file_handler = logging.FileHandler(log_file)\n        file_handler.setLevel(level)\n        file_handler.setFormatter(formatter)\n        logger.addHandler(file_handler)\n\n    return logger\n</code></pre>"},{"location":"api/ndt/#ndt.jump_detector_as_callable","title":"<code>jump_detector_as_callable(detector, metric_name: str = 'metric') -&gt; Detector</code>","text":"<p>Adapt an <code>ndt.JumpDetector</code> to the harness detector protocol.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def jump_detector_as_callable(detector, metric_name: str = \"metric\") -&gt; Detector:\n    \"\"\"Adapt an ``ndt.JumpDetector`` to the harness detector protocol.\"\"\"\n\n    def _call(values: Sequence[float]) -&gt; list[int]:\n        jumps = detector.detect_jumps(list(values), metric_name=metric_name)\n        return [j.step for j in jumps]\n\n    return _call\n</code></pre>"},{"location":"api/ndt/#ndt.standard_battery","title":"<code>standard_battery() -&gt; list[Fixture]</code>","text":"<p>The default validity battery: one positive control and three negatives.</p> <p>Returns a planted single transition, a planted multi transition, pure noise, and a continuous drift. A detector worth trusting recovers the planted transitions and stays silent on the noise and the drift.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def standard_battery() -&gt; list[Fixture]:\n    \"\"\"The default validity battery: one positive control and three negatives.\n\n    Returns a planted single transition, a planted multi transition, pure noise,\n    and a continuous drift. A detector worth trusting recovers the planted\n    transitions and stays silent on the noise and the drift.\n    \"\"\"\n    return [\n        planted_transition(),\n        planted_multi(),\n        pure_noise(),\n        drift_no_jump(),\n    ]\n</code></pre>"},{"location":"api/ndt/#ndt.validate_detector","title":"<code>validate_detector(detector: Detector, fixtures: Sequence[Fixture] | None = None, *, name: str = 'detector', min_recall: float = 0.75, max_false_positives_per_1000: float = 2.0) -&gt; ValidityReport</code>","text":"<p>Run <code>detector</code> against ground-truth fixtures and return a verdict.</p> <p>Parameters:</p> Name Type Description Default <code>detector</code> <code>Detector</code> <p>A callable mapping a value sequence to detected step indices. Wrap an <code>ndt.JumpDetector</code> with :func:<code>jump_detector_as_callable</code>.</p> required <code>fixtures</code> <code>Sequence[Fixture] | None</code> <p>The ground-truth battery. Defaults to :func:<code>ndt.validity.fixtures.standard_battery</code>.</p> <code>None</code> <code>name</code> <code>str</code> <p>Label for the report.</p> <code>'detector'</code> <code>min_recall</code> <code>float</code> <p>Minimum mean recall on planted transitions to pass.</p> <code>0.75</code> <code>max_false_positives_per_1000</code> <code>float</code> <p>Maximum false-positive rate on any null control to pass.</p> <code>2.0</code> <p>Returns:</p> Name Type Description <code>A</code> <code>ValidityReport</code> <p>class:<code>ValidityReport</code>. <code>valid</code> is True only when the detector</p> <code>ValidityReport</code> <p>clears both bars.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def validate_detector(\n    detector: Detector,\n    fixtures: Sequence[Fixture] | None = None,\n    *,\n    name: str = \"detector\",\n    min_recall: float = 0.75,\n    max_false_positives_per_1000: float = 2.0,\n) -&gt; ValidityReport:\n    \"\"\"Run ``detector`` against ground-truth fixtures and return a verdict.\n\n    Args:\n        detector: A callable mapping a value sequence to detected step indices.\n            Wrap an ``ndt.JumpDetector`` with :func:`jump_detector_as_callable`.\n        fixtures: The ground-truth battery. Defaults to\n            :func:`ndt.validity.fixtures.standard_battery`.\n        name: Label for the report.\n        min_recall: Minimum mean recall on planted transitions to pass.\n        max_false_positives_per_1000: Maximum false-positive rate on any null\n            control to pass.\n\n    Returns:\n        A :class:`ValidityReport`. ``valid`` is True only when the detector\n        clears both bars.\n    \"\"\"\n    fixtures = list(fixtures) if fixtures is not None else standard_battery()\n    results: list[FixtureResult] = []\n    recalls: list[float] = []\n    fp_on_null = 0\n    worst_null_rate = 0.0\n\n    for fx in fixtures:\n        detected = [int(d) for d in detector(np.asarray(fx.values))]\n        tp, fp, fn = _match(detected, fx.ground_truth, fx.tolerance)\n        res = FixtureResult(\n            fixture=fx.name,\n            n_steps=len(fx.values),\n            n_ground_truth=len(fx.ground_truth),\n            detected=tuple(sorted(detected)),\n            true_positives=tp,\n            false_positives=fp,\n            false_negatives=fn,\n        )\n        results.append(res)\n        if res.is_null:\n            fp_on_null += res.false_positives\n            worst_null_rate = max(worst_null_rate, res.false_positive_rate_per_1000)\n        else:\n            recalls.append(res.recall)\n\n    mean_recall = float(np.mean(recalls)) if recalls else 0.0\n    passes_recall = mean_recall &gt;= min_recall\n    passes_fp = worst_null_rate &lt;= max_false_positives_per_1000\n    valid = passes_recall and passes_fp\n\n    if valid:\n        message = (\n            \"Recovers planted transitions and stays quiet on noise and drift. \"\n            \"Its detections are trustworthy on data like these fixtures.\"\n        )\n    elif not passes_fp and not passes_recall:\n        message = (\n            \"Misses planted transitions AND fires on the null controls. \"\n            \"These detections reflect the method, not the data.\"\n        )\n    elif not passes_fp:\n        message = (\n            \"Fires on pure noise and continuous drift. It manufactures \"\n            \"transitions where there are none, so a detection on real data \"\n            \"cannot be trusted without this check.\"\n        )\n    else:\n        message = (\n            \"Stays quiet on the null controls but misses planted transitions. \"\n            \"It under-reports; real transitions can pass unnoticed.\"\n        )\n\n    return ValidityReport(\n        detector_name=name,\n        results=tuple(results),\n        mean_recall=mean_recall,\n        false_positives_on_null=fp_on_null,\n        valid=valid,\n        message=message,\n    )\n</code></pre>"},{"location":"api/ndt/#ndt.create_interactive_plot","title":"<code>create_interactive_plot(df: pd.DataFrame, metrics: Optional[List[str]] = None, layer_name: str = 'Layer', show_loss: bool = True) -&gt; go.Figure</code>","text":"<p>Create interactive plot of dimensionality metrics.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metric names to plot (default: all 4 main metrics)</p> <code>None</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>show_loss</code> <code>bool</code> <p>Whether to include loss curve</p> <code>True</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>fig = create_interactive_plot(results_df, layer_name=\"Linear_0\") fig.show() fig.write_html(\"interactive_plot.html\")</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_interactive_plot(\n    df: pd.DataFrame,\n    metrics: Optional[List[str]] = None,\n    layer_name: str = \"Layer\",\n    show_loss: bool = True,\n) -&gt; go.Figure:\n    \"\"\"Create interactive plot of dimensionality metrics.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metric names to plot (default: all 4 main metrics)\n        layer_name: Name of the layer for title\n        show_loss: Whether to include loss curve\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; fig = create_interactive_plot(results_df, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; fig.show()\n        &gt;&gt;&gt; fig.write_html(\"interactive_plot.html\")\n    \"\"\"\n    if metrics is None:\n        metrics = [\"stable_rank\", \"participation_ratio\", \"cumulative_90\", \"nuclear_norm_ratio\"]\n\n    # Filter to available metrics\n    metrics = [m for m in metrics if m in df.columns]\n\n    # Create subplots\n    n_plots = len(metrics) + (1 if show_loss and \"loss\" in df.columns else 0)\n    fig = make_subplots(\n        rows=n_plots,\n        cols=1,\n        subplot_titles=[m.replace(\"_\", \" \").title() for m in metrics]\n        + ([\"Loss\"] if show_loss and \"loss\" in df.columns else []),\n        vertical_spacing=0.05,\n    )\n\n    # Add metric traces\n    for i, metric in enumerate(metrics, 1):\n        fig.add_trace(\n            go.Scatter(\n                x=df[\"step\"],\n                y=df[metric],\n                mode=\"lines\",\n                name=metric.replace(\"_\", \" \").title(),\n                line=dict(width=2),\n                hovertemplate=\"Step: %{x}&lt;br&gt;\" + metric + \": %{y:.2f}&lt;extra&gt;&lt;/extra&gt;\",\n            ),\n            row=i,\n            col=1,\n        )\n\n    # Add loss if requested\n    if show_loss and \"loss\" in df.columns:\n        fig.add_trace(\n            go.Scatter(\n                x=df[\"step\"],\n                y=df[\"loss\"],\n                mode=\"lines\",\n                name=\"Loss\",\n                line=dict(width=2, color=\"coral\"),\n                hovertemplate=\"Step: %{x}&lt;br&gt;Loss: %{y:.3f}&lt;extra&gt;&lt;/extra&gt;\",\n            ),\n            row=n_plots,\n            col=1,\n        )\n\n    # Update layout\n    fig.update_layout(\n        title_text=f\"Dimensionality Metrics - {layer_name}\",\n        title_font_size=18,\n        height=300 * n_plots,\n        showlegend=False,\n        hovermode=\"x unified\",\n    )\n\n    # Update x-axes\n    fig.update_xaxes(title_text=\"Training Step\", row=n_plots, col=1)\n\n    return fig\n</code></pre>"},{"location":"api/ndt/#ndt.create_multi_layer_plot","title":"<code>create_multi_layer_plot(results_dict: Dict[str, pd.DataFrame], metric: str = 'stable_rank') -&gt; go.Figure</code>","text":"<p>Create interactive plot comparing a metric across multiple layers.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>metric</code> <code>str</code> <p>Metric to plot</p> <code>'stable_rank'</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>results = tracker.get_results() fig = create_multi_layer_plot(results, metric=\"stable_rank\") fig.show()</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_multi_layer_plot(\n    results_dict: Dict[str, pd.DataFrame], metric: str = \"stable_rank\"\n) -&gt; go.Figure:\n    \"\"\"Create interactive plot comparing a metric across multiple layers.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        metric: Metric to plot\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = create_multi_layer_plot(results, metric=\"stable_rank\")\n        &gt;&gt;&gt; fig.show()\n    \"\"\"\n    fig = go.Figure()\n\n    for layer_name, df in results_dict.items():\n        if metric in df.columns:\n            fig.add_trace(\n                go.Scatter(\n                    x=df[\"step\"],\n                    y=df[metric],\n                    mode=\"lines\",\n                    name=layer_name,\n                    line=dict(width=2),\n                    hovertemplate=(\n                        f\"{layer_name}&lt;br&gt;Step: %{{x}}&lt;br&gt;{metric}: %{{y:.2f}}&lt;extra&gt;&lt;/extra&gt;\"\n                    ),\n                )\n            )\n\n    fig.update_layout(\n        title=f\"{metric.replace('_', ' ').title()} Across Layers\",\n        title_font_size=18,\n        xaxis_title=\"Training Step\",\n        yaxis_title=metric.replace(\"_\", \" \").title(),\n        hovermode=\"x unified\",\n        height=600,\n        legend=dict(yanchor=\"top\", y=0.99, xanchor=\"left\", x=0.01),\n    )\n\n    return fig\n</code></pre>"},{"location":"api/ndt/#ndt.plot_jumps","title":"<code>plot_jumps(df: pd.DataFrame, jumps: List[Jump], metric: str = 'stable_rank', layer_name: str = 'Layer', figsize: tuple = (14, 6)) -&gt; plt.Figure</code>","text":"<p>Plot metric with detected jumps highlighted.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>jumps</code> <code>List[Jump]</code> <p>List of detected Jump objects</p> required <code>metric</code> <code>str</code> <p>Metric name that was analyzed for jumps</p> <code>'stable_rank'</code> <code>layer_name</code> <code>str</code> <p>Name of the layer</p> <code>'Layer'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(14, 6)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>jumps = tracker.detect_jumps(layer_name=\"Linear_0\", metric=\"stable_rank\") fig = plot_jumps(results_df, jumps[\"Linear_0\"], metric=\"stable_rank\") plt.savefig(\"jumps.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_jumps(\n    df: pd.DataFrame,\n    jumps: List[Jump],\n    metric: str = \"stable_rank\",\n    layer_name: str = \"Layer\",\n    figsize: tuple = (14, 6),\n) -&gt; plt.Figure:\n    \"\"\"Plot metric with detected jumps highlighted.\n\n    Args:\n        df: DataFrame with metrics\n        jumps: List of detected Jump objects\n        metric: Metric name that was analyzed for jumps\n        layer_name: Name of the layer\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; jumps = tracker.detect_jumps(layer_name=\"Linear_0\", metric=\"stable_rank\")\n        &gt;&gt;&gt; fig = plot_jumps(results_df, jumps[\"Linear_0\"], metric=\"stable_rank\")\n        &gt;&gt;&gt; plt.savefig(\"jumps.png\")\n    \"\"\"\n    fig, ax = plt.subplots(figsize=figsize)\n\n    # Plot metric\n    ax.plot(df[\"step\"], df[metric], linewidth=2, color=\"steelblue\", label=metric)\n\n    # Highlight jumps\n    if jumps:\n        jump_steps = [j.step for j in jumps]\n        jump_values = [\n            (\n                df[df[\"step\"] == j.step][metric].values[0]\n                if j.step in df[\"step\"].values\n                else j.value_after\n            )\n            for j in jumps\n        ]\n\n        ax.scatter(\n            jump_steps,\n            jump_values,\n            color=\"red\",\n            s=100,\n            zorder=5,\n            label=f\"Jumps (n={len(jumps)})\",\n            marker=\"o\",\n            edgecolors=\"darkred\",\n            linewidths=2,\n        )\n\n        # Add vertical lines at jumps\n        for step in jump_steps:\n            ax.axvline(x=step, color=\"red\", linestyle=\"--\", alpha=0.3, linewidth=1)\n\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12)\n    ax.set_title(f\"Dimensionality Jumps - {layer_name}\", fontsize=14, fontweight=\"bold\")\n    ax.legend(loc=\"best\", fontsize=10)\n    ax.grid(True, alpha=0.3)\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/#ndt.plot_metrics_comparison","title":"<code>plot_metrics_comparison(df: pd.DataFrame, metrics: Optional[List[str]] = None, layer_name: str = 'Layer', figsize: tuple = (15, 10)) -&gt; plt.Figure</code>","text":"<p>Plot multiple dimensionality metrics for comparison.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metric names to plot (default: all 4 main metrics)</p> <code>None</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(15, 10)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_metrics_comparison(results_df, layer_name=\"Linear_0\") plt.savefig(\"all_metrics.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_metrics_comparison(\n    df: pd.DataFrame,\n    metrics: Optional[List[str]] = None,\n    layer_name: str = \"Layer\",\n    figsize: tuple = (15, 10),\n) -&gt; plt.Figure:\n    \"\"\"Plot multiple dimensionality metrics for comparison.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metric names to plot (default: all 4 main metrics)\n        layer_name: Name of the layer for title\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_metrics_comparison(results_df, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; plt.savefig(\"all_metrics.png\")\n    \"\"\"\n    if metrics is None:\n        metrics = [\"stable_rank\", \"participation_ratio\", \"cumulative_90\", \"nuclear_norm_ratio\"]\n\n    # Filter to available metrics\n    metrics = [m for m in metrics if m in df.columns]\n\n    n_metrics = len(metrics)\n    fig, axes = plt.subplots(n_metrics, 1, figsize=figsize, sharex=True)\n\n    if n_metrics == 1:\n        axes = [axes]\n\n    for ax, metric in zip(axes, metrics):\n        ax.plot(df[\"step\"], df[metric], linewidth=2, color=\"steelblue\")\n        ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=11)\n        ax.grid(True, alpha=0.3)\n\n        # Add loss as background on last plot\n        if ax == axes[-1] and \"loss\" in df.columns:\n            ax2 = ax.twinx()\n            ax2.plot(df[\"step\"], df[\"loss\"], linewidth=1, alpha=0.4, color=\"coral\", linestyle=\"--\")\n            ax2.set_ylabel(\"Loss\", fontsize=10, color=\"coral\")\n            ax2.tick_params(axis=\"y\", labelcolor=\"coral\")\n\n    axes[-1].set_xlabel(\"Training Step\", fontsize=12)\n    fig.suptitle(f\"Dimensionality Metrics - {layer_name}\", fontsize=14, fontweight=\"bold\")\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/#ndt.plot_phases","title":"<code>plot_phases(results_dict: Dict[str, pd.DataFrame], metric: str = 'stable_rank', figsize: tuple = (15, 8)) -&gt; plt.Figure</code>","text":"<p>Plot metric across multiple layers to visualize phase transitions.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>metric</code> <code>str</code> <p>Metric to plot</p> <code>'stable_rank'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(15, 8)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>results = tracker.get_results() fig = plot_phases(results, metric=\"stable_rank\") plt.savefig(\"phases.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_phases(\n    results_dict: Dict[str, pd.DataFrame], metric: str = \"stable_rank\", figsize: tuple = (15, 8)\n) -&gt; plt.Figure:\n    \"\"\"Plot metric across multiple layers to visualize phase transitions.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        metric: Metric to plot\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = plot_phases(results, metric=\"stable_rank\")\n        &gt;&gt;&gt; plt.savefig(\"phases.png\")\n    \"\"\"\n    fig, ax = plt.subplots(figsize=figsize)\n\n    for layer_name, df in results_dict.items():\n        if metric in df.columns:\n            ax.plot(df[\"step\"], df[metric], linewidth=2, label=layer_name, alpha=0.8)\n\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12)\n    ax.set_title(\n        f\"{metric.replace('_', ' ').title()} Across Layers\", fontsize=14, fontweight=\"bold\"\n    )\n    ax.legend(loc=\"best\", fontsize=10)\n    ax.grid(True, alpha=0.3)\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/#ndt.plot_single_metric","title":"<code>plot_single_metric(df: pd.DataFrame, metric: str = 'stable_rank', layer_name: str = 'Layer', ax: Optional[plt.Axes] = None, show_loss: bool = True, figsize: tuple = (12, 6)) -&gt; plt.Figure</code>","text":"<p>Plot a single dimensionality metric over training.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics (must have 'step' and metric columns)</p> required <code>metric</code> <code>str</code> <p>Name of metric to plot</p> <code>'stable_rank'</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>ax</code> <code>Optional[Axes]</code> <p>Optional matplotlib axis to plot on</p> <code>None</code> <code>show_loss</code> <code>bool</code> <p>Whether to show loss on secondary y-axis</p> <code>True</code> <code>figsize</code> <code>tuple</code> <p>Figure size if creating new figure</p> <code>(12, 6)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_single_metric(results_df, metric=\"stable_rank\", layer_name=\"Linear_0\") plt.savefig(\"stable_rank.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_single_metric(\n    df: pd.DataFrame,\n    metric: str = \"stable_rank\",\n    layer_name: str = \"Layer\",\n    ax: Optional[plt.Axes] = None,\n    show_loss: bool = True,\n    figsize: tuple = (12, 6),\n) -&gt; plt.Figure:\n    \"\"\"Plot a single dimensionality metric over training.\n\n    Args:\n        df: DataFrame with metrics (must have 'step' and metric columns)\n        metric: Name of metric to plot\n        layer_name: Name of the layer for title\n        ax: Optional matplotlib axis to plot on\n        show_loss: Whether to show loss on secondary y-axis\n        figsize: Figure size if creating new figure\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_single_metric(results_df, metric=\"stable_rank\", layer_name=\"Linear_0\")\n        &gt;&gt;&gt; plt.savefig(\"stable_rank.png\")\n    \"\"\"\n    if ax is None:\n        fig, ax = plt.subplots(figsize=figsize)\n    else:\n        fig = ax.get_figure()\n\n    # Plot main metric\n    ax.plot(df[\"step\"], df[metric], linewidth=2, label=metric, color=\"steelblue\")\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12, color=\"steelblue\")\n    ax.tick_params(axis=\"y\", labelcolor=\"steelblue\")\n    ax.grid(True, alpha=0.3)\n\n    # Plot loss on secondary axis if requested\n    if show_loss and \"loss\" in df.columns:\n        ax2 = ax.twinx()\n        ax2.plot(\n            df[\"step\"],\n            df[\"loss\"],\n            linewidth=1,\n            alpha=0.6,\n            label=\"Loss\",\n            color=\"coral\",\n            linestyle=\"--\",\n        )\n        ax2.set_ylabel(\"Loss\", fontsize=12, color=\"coral\")\n        ax2.tick_params(axis=\"y\", labelcolor=\"coral\")\n\n    ax.set_title(\n        f\"{metric.replace('_', ' ').title()} - {layer_name}\", fontsize=14, fontweight=\"bold\"\n    )\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/analysis/","title":"Index","text":""},{"location":"api/ndt/analysis/#ndt.analysis","title":"<code>ndt.analysis</code>","text":"<p>Analysis modules for Phase 2 mechanistic interpretability.</p> <p>This package provides tools for: - Activation analysis (PCA, clustering, manifold visualization) - Feature visualization (CAM, saliency, attention maps) - Neuron importance scoring</p>"},{"location":"api/ndt/analysis/#ndt.analysis-classes","title":"Classes","text":""},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer","title":"<code>ActivationAnalyzer()</code>","text":"<p>Analyzes neural network activations for mechanistic interpretability.</p> <p>This class provides tools to understand what representations are learned at different layers and training stages, particularly focusing on critical moments identified by dimensionality jumps.</p> <p>Attributes:</p> Name Type Description <code>results</code> <code>Dict[str, Any]</code> <p>Dictionary storing analysis results</p> Example <p>analyzer = ActivationAnalyzer()</p> <p>Initialize the activation analyzer.</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialize the activation analyzer.\"\"\"\n    self.results: Dict[str, Any] = {}\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer--capture-activations-from-model","title":"Capture activations from model","text":"<p>activations = capture.get_all_activations()</p>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer--flatten-for-analysis","title":"Flatten for analysis","text":"<p>flat_acts = activations['layer_0'].view(batch_size, -1).cpu().numpy()</p>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer--run-pca-analysis","title":"Run PCA analysis","text":"<p>pca_results = analyzer.pca_analysis(flat_acts, n_components=10)</p>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer--visualize","title":"Visualize","text":"<p>analyzer.plot_pca_variance(pca_results)</p>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer-functions","title":"Methods:","text":""},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer.flatten_activation","title":"<code>flatten_activation(activation: torch.Tensor) -&gt; np.ndarray</code>","text":"<p>Flatten activation tensor to 2D array for analysis.</p> <p>Parameters:</p> Name Type Description Default <code>activation</code> <code>Tensor</code> <p>Activation tensor of shape (batch, ...)</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Flattened array of shape (batch, features)</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def flatten_activation(self, activation: torch.Tensor) -&gt; np.ndarray:\n    \"\"\"Flatten activation tensor to 2D array for analysis.\n\n    Args:\n        activation: Activation tensor of shape (batch, ...)\n\n    Returns:\n        Flattened array of shape (batch, features)\n    \"\"\"\n    if isinstance(activation, torch.Tensor):\n        activation = activation.detach().cpu().numpy()\n\n    batch_size = activation.shape[0]\n    return activation.reshape(batch_size, -1)\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer.pca_analysis","title":"<code>pca_analysis(activations: np.ndarray, n_components: Optional[int] = None, return_transformed: bool = True) -&gt; Dict[str, Any]</code>","text":"<p>Perform PCA analysis on activations.</p> <p>Analyzes the principal components of the activation space to understand the main directions of variation in representations.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <code>n_components</code> <code>Optional[int]</code> <p>Number of components to compute (default: min(n_samples, n_features))</p> <code>None</code> <code>return_transformed</code> <code>bool</code> <p>Whether to include transformed data</p> <code>True</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'explained_variance_ratio': Variance explained by each component - 'cumulative_variance': Cumulative variance explained - 'components': Principal component vectors - 'singular_values': Singular values - 'n_components_90': Components needed for 90% variance - 'n_components_95': Components needed for 95% variance - 'transformed': Transformed data (if return_transformed=True)</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def pca_analysis(\n    self,\n    activations: np.ndarray,\n    n_components: Optional[int] = None,\n    return_transformed: bool = True,\n) -&gt; Dict[str, Any]:\n    \"\"\"Perform PCA analysis on activations.\n\n    Analyzes the principal components of the activation space to understand\n    the main directions of variation in representations.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n        n_components: Number of components to compute (default: min(n_samples, n_features))\n        return_transformed: Whether to include transformed data\n\n    Returns:\n        Dictionary containing:\n            - 'explained_variance_ratio': Variance explained by each component\n            - 'cumulative_variance': Cumulative variance explained\n            - 'components': Principal component vectors\n            - 'singular_values': Singular values\n            - 'n_components_90': Components needed for 90% variance\n            - 'n_components_95': Components needed for 95% variance\n            - 'transformed': Transformed data (if return_transformed=True)\n    \"\"\"\n    if n_components is None:\n        n_components = min(activations.shape)\n\n    pca = PCA(n_components=n_components)\n    transformed = pca.fit_transform(activations)\n\n    cumulative_var = np.cumsum(pca.explained_variance_ratio_)\n    n_90 = np.searchsorted(cumulative_var, 0.90) + 1\n    n_95 = np.searchsorted(cumulative_var, 0.95) + 1\n\n    results = {\n        \"explained_variance_ratio\": pca.explained_variance_ratio_,\n        \"cumulative_variance\": cumulative_var,\n        \"components\": pca.components_,\n        \"singular_values\": pca.singular_values_,\n        \"n_components_90\": min(n_90, n_components),\n        \"n_components_95\": min(n_95, n_components),\n        \"mean\": pca.mean_,\n        \"pca_object\": pca,\n    }\n\n    if return_transformed:\n        results[\"transformed\"] = transformed\n\n    return results\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer.cluster_analysis","title":"<code>cluster_analysis(activations: np.ndarray, method: str = 'kmeans', n_clusters: int = 5, **kwargs) -&gt; Dict[str, Any]</code>","text":"<p>Perform clustering analysis on activations.</p> <p>Identifies groups of similar activation patterns, useful for understanding how different inputs are represented.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <code>method</code> <code>str</code> <p>Clustering method ('kmeans' or 'dbscan')</p> <code>'kmeans'</code> <code>n_clusters</code> <code>int</code> <p>Number of clusters (for kmeans)</p> <code>5</code> <code>**kwargs</code> <p>Additional arguments for clustering algorithm</p> <code>{}</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'labels': Cluster assignments - 'n_clusters': Number of clusters found - 'silhouette_score': Silhouette score if applicable - 'cluster_centers': Cluster centroids (kmeans only) - 'inertia': Within-cluster sum of squares (kmeans only)</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def cluster_analysis(\n    self, activations: np.ndarray, method: str = \"kmeans\", n_clusters: int = 5, **kwargs\n) -&gt; Dict[str, Any]:\n    \"\"\"Perform clustering analysis on activations.\n\n    Identifies groups of similar activation patterns, useful for\n    understanding how different inputs are represented.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n        method: Clustering method ('kmeans' or 'dbscan')\n        n_clusters: Number of clusters (for kmeans)\n        **kwargs: Additional arguments for clustering algorithm\n\n    Returns:\n        Dictionary containing:\n            - 'labels': Cluster assignments\n            - 'n_clusters': Number of clusters found\n            - 'silhouette_score': Silhouette score if applicable\n            - 'cluster_centers': Cluster centroids (kmeans only)\n            - 'inertia': Within-cluster sum of squares (kmeans only)\n    \"\"\"\n    if method == \"kmeans\":\n        clusterer = KMeans(n_clusters=n_clusters, random_state=42, **kwargs)\n        labels = clusterer.fit_predict(activations)\n\n        results = {\n            \"labels\": labels,\n            \"n_clusters\": n_clusters,\n            \"cluster_centers\": clusterer.cluster_centers_,\n            \"inertia\": clusterer.inertia_,\n        }\n\n    elif method == \"dbscan\":\n        eps = kwargs.get(\"eps\", 0.5)\n        min_samples = kwargs.get(\"min_samples\", 5)\n        clusterer = DBSCAN(eps=eps, min_samples=min_samples)\n        labels = clusterer.fit_predict(activations)\n\n        n_found = len(set(labels)) - (1 if -1 in labels else 0)\n        results = {\"labels\": labels, \"n_clusters\": n_found, \"n_noise\": np.sum(labels == -1)}\n    else:\n        raise ValueError(f\"Unknown clustering method: {method}\")\n\n    # Compute silhouette score if more than 1 cluster\n    unique_labels = set(labels)\n    if len(unique_labels) &gt; 1 and -1 not in unique_labels:\n        results[\"silhouette_score\"] = silhouette_score(activations, labels)\n    elif len(unique_labels) &gt; 2:  # Has noise but also clusters\n        mask = labels != -1\n        if mask.sum() &gt; 1:\n            results[\"silhouette_score\"] = silhouette_score(activations[mask], labels[mask])\n\n    return results\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer.manifold_embedding","title":"<code>manifold_embedding(activations: np.ndarray, method: str = 'tsne', n_components: int = 2, **kwargs) -&gt; Dict[str, Any]</code>","text":"<p>Compute low-dimensional manifold embedding.</p> <p>Reduces high-dimensional activations to 2D/3D for visualization, preserving local structure.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <code>method</code> <code>str</code> <p>Embedding method ('tsne' or 'umap')</p> <code>'tsne'</code> <code>n_components</code> <code>int</code> <p>Output dimensions (2 or 3)</p> <code>2</code> <code>**kwargs</code> <p>Additional arguments for embedding algorithm</p> <code>{}</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'embedding': Low-dimensional coordinates - 'method': Method used - 'params': Parameters used</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def manifold_embedding(\n    self, activations: np.ndarray, method: str = \"tsne\", n_components: int = 2, **kwargs\n) -&gt; Dict[str, Any]:\n    \"\"\"Compute low-dimensional manifold embedding.\n\n    Reduces high-dimensional activations to 2D/3D for visualization,\n    preserving local structure.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n        method: Embedding method ('tsne' or 'umap')\n        n_components: Output dimensions (2 or 3)\n        **kwargs: Additional arguments for embedding algorithm\n\n    Returns:\n        Dictionary containing:\n            - 'embedding': Low-dimensional coordinates\n            - 'method': Method used\n            - 'params': Parameters used\n    \"\"\"\n    if method == \"tsne\":\n        perplexity = kwargs.get(\"perplexity\", min(30, activations.shape[0] - 1))\n        tsne = TSNE(\n            n_components=n_components,\n            perplexity=perplexity,\n            random_state=42,\n            **{k: v for k, v in kwargs.items() if k != \"perplexity\"},\n        )\n        embedding = tsne.fit_transform(activations)\n\n        results = {\n            \"embedding\": embedding,\n            \"method\": \"tsne\",\n            \"params\": {\"perplexity\": perplexity, \"n_components\": n_components},\n        }\n\n    elif method == \"umap\":\n        if not HAS_UMAP:\n            raise ImportError(\"UMAP not installed. Install with: pip install umap-learn\")\n\n        n_neighbors = kwargs.get(\"n_neighbors\", min(15, activations.shape[0] - 1))\n        reducer = umap.UMAP(\n            n_components=n_components,\n            n_neighbors=n_neighbors,\n            random_state=42,\n            **{k: v for k, v in kwargs.items() if k != \"n_neighbors\"},\n        )\n        embedding = reducer.fit_transform(activations)\n\n        results = {\n            \"embedding\": embedding,\n            \"method\": \"umap\",\n            \"params\": {\"n_neighbors\": n_neighbors, \"n_components\": n_components},\n        }\n    else:\n        raise ValueError(f\"Unknown embedding method: {method}\")\n\n    return results\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer.singular_value_analysis","title":"<code>singular_value_analysis(activations: np.ndarray) -&gt; Dict[str, Any]</code>","text":"<p>Analyze singular value distribution of activations.</p> <p>Computes detailed statistics about the singular value spectrum, useful for understanding representation geometry.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'singular_values': All singular values - 'normalized_sv': Normalized singular values - 'stable_rank': Stable rank - 'participation_ratio': Participation ratio - 'entropy': Spectral entropy - 'condition_number': Condition number</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def singular_value_analysis(self, activations: np.ndarray) -&gt; Dict[str, Any]:\n    \"\"\"Analyze singular value distribution of activations.\n\n    Computes detailed statistics about the singular value spectrum,\n    useful for understanding representation geometry.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n\n    Returns:\n        Dictionary containing:\n            - 'singular_values': All singular values\n            - 'normalized_sv': Normalized singular values\n            - 'stable_rank': Stable rank\n            - 'participation_ratio': Participation ratio\n            - 'entropy': Spectral entropy\n            - 'condition_number': Condition number\n    \"\"\"\n    U, S, Vt = np.linalg.svd(activations, full_matrices=False)\n\n    # Normalized singular values\n    S_norm = S / S.sum()\n\n    # Stable rank\n    stable_rank = (S**2).sum() / (S[0] ** 2) if S[0] &gt; 0 else 0\n\n    # Participation ratio\n    participation_ratio = 1.0 / (S_norm**2).sum() if S_norm.sum() &gt; 0 else 0\n\n    # Spectral entropy\n    S_norm_nonzero = S_norm[S_norm &gt; 0]\n    entropy = -np.sum(S_norm_nonzero * np.log(S_norm_nonzero))\n\n    # Condition number\n    condition_number = S[0] / S[-1] if S[-1] &gt; 0 else np.inf\n\n    return {\n        \"singular_values\": S,\n        \"normalized_sv\": S_norm,\n        \"stable_rank\": stable_rank,\n        \"participation_ratio\": participation_ratio,\n        \"spectral_entropy\": entropy,\n        \"condition_number\": condition_number,\n    }\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer.compare_activations","title":"<code>compare_activations(activations_before: np.ndarray, activations_after: np.ndarray) -&gt; Dict[str, Any]</code>","text":"<p>Compare activations before and after a critical moment (e.g., jump).</p> <p>Analyzes how representations change during dimensionality transitions.</p> <p>Parameters:</p> Name Type Description Default <code>activations_before</code> <code>ndarray</code> <p>Activations before critical moment</p> required <code>activations_after</code> <code>ndarray</code> <p>Activations after critical moment</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing comparison metrics: - 'pca_before': PCA results before - 'pca_after': PCA results after - 'sv_before': Singular value analysis before - 'sv_after': Singular value analysis after - 'dim_change': Change in effective dimensionality - 'subspace_overlap': Overlap between principal subspaces</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def compare_activations(\n    self, activations_before: np.ndarray, activations_after: np.ndarray\n) -&gt; Dict[str, Any]:\n    \"\"\"Compare activations before and after a critical moment (e.g., jump).\n\n    Analyzes how representations change during dimensionality transitions.\n\n    Args:\n        activations_before: Activations before critical moment\n        activations_after: Activations after critical moment\n\n    Returns:\n        Dictionary containing comparison metrics:\n            - 'pca_before': PCA results before\n            - 'pca_after': PCA results after\n            - 'sv_before': Singular value analysis before\n            - 'sv_after': Singular value analysis after\n            - 'dim_change': Change in effective dimensionality\n            - 'subspace_overlap': Overlap between principal subspaces\n    \"\"\"\n    # PCA analysis\n    pca_before = self.pca_analysis(activations_before, return_transformed=False)\n    pca_after = self.pca_analysis(activations_after, return_transformed=False)\n\n    # Singular value analysis\n    sv_before = self.singular_value_analysis(activations_before)\n    sv_after = self.singular_value_analysis(activations_after)\n\n    # Dimensionality change\n    dim_change = {\n        \"stable_rank\": sv_after[\"stable_rank\"] - sv_before[\"stable_rank\"],\n        \"participation_ratio\": (\n            sv_after[\"participation_ratio\"] - sv_before[\"participation_ratio\"]\n        ),\n        \"n_components_90\": (pca_after[\"n_components_90\"] - pca_before[\"n_components_90\"]),\n    }\n\n    # Subspace overlap (using principal angles)\n    n_components = min(\n        10, min(pca_before[\"components\"].shape[0], pca_after[\"components\"].shape[0])\n    )\n    V1 = pca_before[\"components\"][:n_components].T\n    V2 = pca_after[\"components\"][:n_components].T\n\n    # Compute principal angles via SVD\n    M = V1.T @ V2\n    _, s, _ = np.linalg.svd(M)\n    principal_angles = np.arccos(np.clip(s, -1, 1))\n    subspace_overlap = np.cos(principal_angles).mean()\n\n    return {\n        \"pca_before\": pca_before,\n        \"pca_after\": pca_after,\n        \"sv_before\": sv_before,\n        \"sv_after\": sv_after,\n        \"dim_change\": dim_change,\n        \"subspace_overlap\": subspace_overlap,\n        \"principal_angles\": principal_angles,\n    }\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer.neuron_importance","title":"<code>neuron_importance(activations: np.ndarray, method: str = 'variance') -&gt; Dict[str, Any]</code>","text":"<p>Compute importance scores for individual neurons.</p> <p>Identifies which neurons contribute most to the representation.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_neurons)</p> required <code>method</code> <code>str</code> <p>Scoring method ('variance', 'mean_abs', 'sparsity')</p> <code>'variance'</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'scores': Importance score for each neuron - 'ranking': Indices sorted by importance - 'dead_neurons': Indices of neurons with zero activation - 'top_10_indices': Top 10 most important neurons</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def neuron_importance(\n    self, activations: np.ndarray, method: str = \"variance\"\n) -&gt; Dict[str, Any]:\n    \"\"\"Compute importance scores for individual neurons.\n\n    Identifies which neurons contribute most to the representation.\n\n    Args:\n        activations: Array of shape (n_samples, n_neurons)\n        method: Scoring method ('variance', 'mean_abs', 'sparsity')\n\n    Returns:\n        Dictionary containing:\n            - 'scores': Importance score for each neuron\n            - 'ranking': Indices sorted by importance\n            - 'dead_neurons': Indices of neurons with zero activation\n            - 'top_10_indices': Top 10 most important neurons\n    \"\"\"\n    if method == \"variance\":\n        scores = np.var(activations, axis=0)\n    elif method == \"mean_abs\":\n        scores = np.mean(np.abs(activations), axis=0)\n    elif method == \"sparsity\":\n        # Lower sparsity = higher importance\n        scores = 1.0 - np.mean(activations == 0, axis=0)\n    else:\n        raise ValueError(f\"Unknown importance method: {method}\")\n\n    ranking = np.argsort(scores)[::-1]\n    dead_neurons = np.where(np.all(activations == 0, axis=0))[0]\n\n    return {\n        \"scores\": scores,\n        \"ranking\": ranking,\n        \"dead_neurons\": dead_neurons,\n        \"n_dead\": len(dead_neurons),\n        \"top_10_indices\": ranking[:10],\n        \"method\": method,\n    }\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.ActivationAnalyzer.activation_statistics","title":"<code>activation_statistics(activations: np.ndarray) -&gt; Dict[str, float]</code>","text":"<p>Compute summary statistics for activations.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <p>Returns:</p> Type Description <code>Dict[str, float]</code> <p>Dictionary of statistics</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def activation_statistics(self, activations: np.ndarray) -&gt; Dict[str, float]:\n    \"\"\"Compute summary statistics for activations.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n\n    Returns:\n        Dictionary of statistics\n    \"\"\"\n    return {\n        \"mean\": float(np.mean(activations)),\n        \"std\": float(np.std(activations)),\n        \"min\": float(np.min(activations)),\n        \"max\": float(np.max(activations)),\n        \"sparsity\": float(np.mean(activations == 0)),\n        \"n_samples\": activations.shape[0],\n        \"n_features\": activations.shape[1],\n    }\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer","title":"<code>FeatureVisualizer(model: nn.Module)</code>","text":"<p>Visualizes learned features in neural networks.</p> <p>Provides methods for understanding what features the network has learned at different layers and training stages.</p> Example <p>visualizer = FeatureVisualizer(model)</p> <p>Initialize the feature visualizer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model to visualize</p> required Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def __init__(self, model: nn.Module) -&gt; None:\n    \"\"\"Initialize the feature visualizer.\n\n    Args:\n        model: The neural network model to visualize\n    \"\"\"\n    self.model = model\n    self.gradients: Dict[str, torch.Tensor] = {}\n    self.activations: Dict[str, torch.Tensor] = {}\n    self.hooks: List[torch.utils.hooks.RemovableHandle] = []\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer--generate-cam-for-an-image","title":"Generate CAM for an image","text":"<p>cam = visualizer.grad_cam(input_image, target_class, 'conv5')</p>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer--visualize-saliency","title":"Visualize saliency","text":"<p>saliency = visualizer.saliency_map(input_image, target_class)</p>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer-functions","title":"Methods:","text":""},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.register_hooks","title":"<code>register_hooks(layer_names: Dict[str, nn.Module]) -&gt; None</code>","text":"<p>Register hooks on specified layers.</p> <p>Parameters:</p> Name Type Description Default <code>layer_names</code> <code>Dict[str, Module]</code> <p>Dictionary mapping names to layer modules</p> required Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def register_hooks(self, layer_names: Dict[str, nn.Module]) -&gt; None:\n    \"\"\"Register hooks on specified layers.\n\n    Args:\n        layer_names: Dictionary mapping names to layer modules\n    \"\"\"\n    for name, layer in layer_names.items():\n        hook = layer.register_forward_hook(self._save_activation(name))\n        self.hooks.append(hook)\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.remove_hooks","title":"<code>remove_hooks() -&gt; None</code>","text":"<p>Remove all registered hooks.</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def remove_hooks(self) -&gt; None:\n    \"\"\"Remove all registered hooks.\"\"\"\n    for hook in self.hooks:\n        hook.remove()\n    self.hooks.clear()\n    self.gradients.clear()\n    self.activations.clear()\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.grad_cam","title":"<code>grad_cam(input_tensor: torch.Tensor, target_class: int, target_layer: nn.Module, layer_name: str = 'target') -&gt; np.ndarray</code>","text":"<p>Generate Grad-CAM visualization.</p> <p>Grad-CAM uses gradients flowing into the target layer to produce a coarse localization map highlighting important regions.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input image tensor (1, C, H, W)</p> required <code>target_class</code> <code>int</code> <p>Target class index</p> required <code>target_layer</code> <code>Module</code> <p>Layer to compute CAM for</p> required <code>layer_name</code> <code>str</code> <p>Name for the layer (for storage)</p> <code>'target'</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>CAM heatmap as numpy array</p> References <p>Selvaraju et al., \"Grad-CAM: Visual Explanations from Deep Networks via Gradient-based Localization\", ICCV 2017</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def grad_cam(\n    self,\n    input_tensor: torch.Tensor,\n    target_class: int,\n    target_layer: nn.Module,\n    layer_name: str = \"target\",\n) -&gt; np.ndarray:\n    \"\"\"Generate Grad-CAM visualization.\n\n    Grad-CAM uses gradients flowing into the target layer to produce\n    a coarse localization map highlighting important regions.\n\n    Args:\n        input_tensor: Input image tensor (1, C, H, W)\n        target_class: Target class index\n        target_layer: Layer to compute CAM for\n        layer_name: Name for the layer (for storage)\n\n    Returns:\n        CAM heatmap as numpy array\n\n    References:\n        Selvaraju et al., \"Grad-CAM: Visual Explanations from Deep Networks\n        via Gradient-based Localization\", ICCV 2017\n    \"\"\"\n    self.model.eval()\n\n    # Register hooks\n    activations = []\n    gradients = []\n\n    def forward_hook(module, input, output):\n        activations.append(output.detach())\n\n    def backward_hook(module, grad_input, grad_output):\n        gradients.append(grad_output[0].detach())\n\n    forward_handle = target_layer.register_forward_hook(forward_hook)\n    backward_handle = target_layer.register_full_backward_hook(backward_hook)\n\n    try:\n        # Forward pass\n        output = self.model(input_tensor)\n\n        # Get prediction score for target class\n        if output.dim() == 1:\n            score = output[target_class]\n        else:\n            score = output[0, target_class]\n\n        # Backward pass\n        self.model.zero_grad()\n        score.backward()\n\n        # Get the activations and gradients\n        activation = activations[0]  # (1, C, H, W)\n        gradient = gradients[0]  # (1, C, H, W)\n\n        # Global average pooling of gradients\n        weights = torch.mean(gradient, dim=(2, 3), keepdim=True)  # (1, C, 1, 1)\n\n        # Weighted combination of activation maps\n        cam = torch.sum(weights * activation, dim=1, keepdim=True)  # (1, 1, H, W)\n\n        # ReLU to keep only positive contributions\n        cam = F.relu(cam)\n\n        # Normalize\n        cam = cam - cam.min()\n        if cam.max() &gt; 0:\n            cam = cam / cam.max()\n\n        return cam.squeeze().cpu().numpy()\n\n    finally:\n        forward_handle.remove()\n        backward_handle.remove()\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.saliency_map","title":"<code>saliency_map(input_tensor: torch.Tensor, target_class: int, absolute: bool = True) -&gt; np.ndarray</code>","text":"<p>Generate vanilla gradient saliency map.</p> <p>Computes the gradient of the target class score with respect to the input pixels.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input tensor (1, C, H, W) or (1, features)</p> required <code>target_class</code> <code>int</code> <p>Target class index</p> required <code>absolute</code> <code>bool</code> <p>Whether to take absolute value of gradients</p> <code>True</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Saliency map as numpy array</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def saliency_map(\n    self, input_tensor: torch.Tensor, target_class: int, absolute: bool = True\n) -&gt; np.ndarray:\n    \"\"\"Generate vanilla gradient saliency map.\n\n    Computes the gradient of the target class score with respect to\n    the input pixels.\n\n    Args:\n        input_tensor: Input tensor (1, C, H, W) or (1, features)\n        target_class: Target class index\n        absolute: Whether to take absolute value of gradients\n\n    Returns:\n        Saliency map as numpy array\n    \"\"\"\n    self.model.eval()\n\n    input_tensor = input_tensor.clone().requires_grad_(True)\n\n    # Forward pass\n    output = self.model(input_tensor)\n\n    # Get score for target class\n    if output.dim() == 1:\n        score = output[target_class]\n    else:\n        score = output[0, target_class]\n\n    # Backward pass\n    self.model.zero_grad()\n    score.backward()\n\n    # Get gradients\n    saliency = input_tensor.grad.detach()\n\n    if absolute:\n        saliency = torch.abs(saliency)\n\n    # For multi-channel inputs, take max across channels\n    if saliency.dim() == 4 and saliency.shape[1] &gt; 1:\n        saliency = saliency.max(dim=1, keepdim=True)[0]\n\n    return saliency.squeeze().cpu().numpy()\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.integrated_gradients","title":"<code>integrated_gradients(input_tensor: torch.Tensor, target_class: int, baseline: Optional[torch.Tensor] = None, steps: int = 50) -&gt; np.ndarray</code>","text":"<p>Compute Integrated Gradients attribution.</p> <p>Accumulates gradients along the path from baseline to input, providing more stable attributions than vanilla gradients.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input tensor</p> required <code>target_class</code> <code>int</code> <p>Target class index</p> required <code>baseline</code> <code>Optional[Tensor]</code> <p>Baseline tensor (default: zeros)</p> <code>None</code> <code>steps</code> <code>int</code> <p>Number of interpolation steps</p> <code>50</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Attribution map as numpy array</p> References <p>Sundararajan et al., \"Axiomatic Attribution for Deep Networks\", ICML 2017</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def integrated_gradients(\n    self,\n    input_tensor: torch.Tensor,\n    target_class: int,\n    baseline: Optional[torch.Tensor] = None,\n    steps: int = 50,\n) -&gt; np.ndarray:\n    \"\"\"Compute Integrated Gradients attribution.\n\n    Accumulates gradients along the path from baseline to input,\n    providing more stable attributions than vanilla gradients.\n\n    Args:\n        input_tensor: Input tensor\n        target_class: Target class index\n        baseline: Baseline tensor (default: zeros)\n        steps: Number of interpolation steps\n\n    Returns:\n        Attribution map as numpy array\n\n    References:\n        Sundararajan et al., \"Axiomatic Attribution for Deep Networks\", ICML 2017\n    \"\"\"\n    self.model.eval()\n\n    if baseline is None:\n        baseline = torch.zeros_like(input_tensor)\n\n    # Generate interpolated inputs\n    alphas = torch.linspace(0, 1, steps).to(input_tensor.device)\n    scaled_inputs = torch.stack(\n        [baseline + alpha * (input_tensor - baseline) for alpha in alphas]\n    )\n\n    # Compute gradients for all interpolated inputs\n    scaled_inputs.requires_grad_(True)\n\n    # Forward pass (batch all steps together)\n    outputs = self.model(scaled_inputs.view(-1, *input_tensor.shape[1:]))\n\n    # Get scores\n    if outputs.dim() == 1:\n        scores = outputs.view(steps, -1)[:, target_class]\n    else:\n        scores = outputs[:, target_class]\n\n    # Backward pass\n    self.model.zero_grad()\n    scores.sum().backward()\n\n    # Average gradients\n    avg_gradients = scaled_inputs.grad.mean(dim=0)\n\n    # Integrated gradients\n    ig = (input_tensor - baseline) * avg_gradients\n\n    return ig.squeeze().detach().cpu().numpy()\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.attention_visualization","title":"<code>attention_visualization(attention_weights: torch.Tensor, head_idx: Optional[int] = None) -&gt; np.ndarray</code>","text":"<p>Visualize attention weights from transformer models.</p> <p>Parameters:</p> Name Type Description Default <code>attention_weights</code> <code>Tensor</code> <p>Attention tensor (batch, heads, seq, seq)</p> required <code>head_idx</code> <code>Optional[int]</code> <p>Specific head to visualize (None for average)</p> <code>None</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Attention matrix as numpy array</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def attention_visualization(\n    self, attention_weights: torch.Tensor, head_idx: Optional[int] = None\n) -&gt; np.ndarray:\n    \"\"\"Visualize attention weights from transformer models.\n\n    Args:\n        attention_weights: Attention tensor (batch, heads, seq, seq)\n        head_idx: Specific head to visualize (None for average)\n\n    Returns:\n        Attention matrix as numpy array\n    \"\"\"\n    if head_idx is not None:\n        attn = attention_weights[:, head_idx]\n    else:\n        # Average across heads\n        attn = attention_weights.mean(dim=1)\n\n    return attn.squeeze().detach().cpu().numpy()\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.feature_maps","title":"<code>feature_maps(activation: torch.Tensor, top_k: int = 16) -&gt; List[np.ndarray]</code>","text":"<p>Extract top-k feature maps from a convolutional layer.</p> <p>Parameters:</p> Name Type Description Default <code>activation</code> <code>Tensor</code> <p>Activation tensor (batch, channels, H, W)</p> required <code>top_k</code> <code>int</code> <p>Number of feature maps to return</p> <code>16</code> <p>Returns:</p> Type Description <code>List[ndarray]</code> <p>List of feature map arrays</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def feature_maps(self, activation: torch.Tensor, top_k: int = 16) -&gt; List[np.ndarray]:\n    \"\"\"Extract top-k feature maps from a convolutional layer.\n\n    Args:\n        activation: Activation tensor (batch, channels, H, W)\n        top_k: Number of feature maps to return\n\n    Returns:\n        List of feature map arrays\n    \"\"\"\n    # Compute activation magnitudes\n    magnitudes = activation.abs().mean(dim=(2, 3))  # (batch, channels)\n\n    # Get top-k channels\n    _, top_indices = magnitudes[0].topk(min(top_k, activation.shape[1]))\n\n    feature_maps = []\n    for idx in top_indices:\n        fmap = activation[0, idx].detach().cpu().numpy()\n        # Normalize\n        fmap = (fmap - fmap.min()) / (fmap.max() - fmap.min() + 1e-8)\n        feature_maps.append(fmap)\n\n    return feature_maps\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.neuron_activation_maximization","title":"<code>neuron_activation_maximization(target_layer: nn.Module, neuron_idx: int, input_shape: Tuple[int, ...], steps: int = 100, lr: float = 0.1) -&gt; torch.Tensor</code>","text":"<p>Generate input that maximizes a specific neuron's activation.</p> <p>Uses gradient ascent to find an input that maximally activates a target neuron.</p> <p>Parameters:</p> Name Type Description Default <code>target_layer</code> <code>Module</code> <p>Layer containing the neuron</p> required <code>neuron_idx</code> <code>int</code> <p>Index of the neuron to maximize</p> required <code>input_shape</code> <code>Tuple[int, ...]</code> <p>Shape of input tensor</p> required <code>steps</code> <code>int</code> <p>Number of optimization steps</p> <code>100</code> <code>lr</code> <code>float</code> <p>Learning rate</p> <code>0.1</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Optimized input tensor</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def neuron_activation_maximization(\n    self,\n    target_layer: nn.Module,\n    neuron_idx: int,\n    input_shape: Tuple[int, ...],\n    steps: int = 100,\n    lr: float = 0.1,\n) -&gt; torch.Tensor:\n    \"\"\"Generate input that maximizes a specific neuron's activation.\n\n    Uses gradient ascent to find an input that maximally activates\n    a target neuron.\n\n    Args:\n        target_layer: Layer containing the neuron\n        neuron_idx: Index of the neuron to maximize\n        input_shape: Shape of input tensor\n        steps: Number of optimization steps\n        lr: Learning rate\n\n    Returns:\n        Optimized input tensor\n    \"\"\"\n    # Initialize with random noise\n    input_tensor = torch.randn(1, *input_shape, requires_grad=True)\n\n    activation_value = []\n\n    def hook(module, input, output):\n        if output.dim() == 2:\n            activation_value.append(output[0, neuron_idx])\n        elif output.dim() == 4:\n            # For conv layers, take spatial mean\n            activation_value.append(output[0, neuron_idx].mean())\n\n    handle = target_layer.register_forward_hook(hook)\n\n    try:\n        for _ in range(steps):\n            activation_value.clear()\n\n            # Forward pass\n            _ = self.model(input_tensor)\n\n            # Get activation\n            act = activation_value[0]\n\n            # Backward pass\n            self.model.zero_grad()\n            act.backward()\n\n            # Gradient ascent\n            with torch.no_grad():\n                input_tensor += lr * input_tensor.grad\n                input_tensor.grad.zero_()\n\n        return input_tensor.detach()\n\n    finally:\n        handle.remove()\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.layer_conductance","title":"<code>layer_conductance(input_tensor: torch.Tensor, target_class: int, target_layer: nn.Module) -&gt; np.ndarray</code>","text":"<p>Compute layer conductance attribution.</p> <p>Measures how much each neuron in a layer contributes to the final prediction.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input tensor</p> required <code>target_class</code> <code>int</code> <p>Target class index</p> required <code>target_layer</code> <code>Module</code> <p>Layer to compute conductance for</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Conductance values for each neuron</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def layer_conductance(\n    self, input_tensor: torch.Tensor, target_class: int, target_layer: nn.Module\n) -&gt; np.ndarray:\n    \"\"\"Compute layer conductance attribution.\n\n    Measures how much each neuron in a layer contributes to the\n    final prediction.\n\n    Args:\n        input_tensor: Input tensor\n        target_class: Target class index\n        target_layer: Layer to compute conductance for\n\n    Returns:\n        Conductance values for each neuron\n    \"\"\"\n    # Use integrated gradients through the layer\n    activation_grads = []\n\n    def hook(module, input, output):\n        output.retain_grad()\n        activation_grads.append(output)\n\n    handle = target_layer.register_forward_hook(hook)\n\n    try:\n        self.model.eval()\n\n        # Forward pass\n        output = self.model(input_tensor)\n\n        # Get score\n        if output.dim() == 1:\n            score = output[target_class]\n        else:\n            score = output[0, target_class]\n\n        # Backward pass\n        self.model.zero_grad()\n        score.backward()\n\n        # Get activation and gradient\n        activation = activation_grads[0]\n        gradient = activation.grad\n\n        # Conductance = activation * gradient\n        conductance = (activation * gradient).sum(dim=0)\n\n        return conductance.detach().cpu().numpy()\n\n    finally:\n        handle.remove()\n</code></pre>"},{"location":"api/ndt/analysis/#ndt.analysis.FeatureVisualizer.compare_features","title":"<code>compare_features(input_tensor: torch.Tensor, target_class: int, target_layer: nn.Module, checkpoint_before: str, checkpoint_after: str) -&gt; Dict[str, Any]</code>","text":"<p>Compare feature visualizations before and after a critical moment.</p> <p>Loads model weights from checkpoints and compares Grad-CAM visualizations.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input to visualize</p> required <code>target_class</code> <code>int</code> <p>Target class</p> required <code>target_layer</code> <code>Module</code> <p>Layer for visualization</p> required <code>checkpoint_before</code> <code>str</code> <p>Path to checkpoint before critical moment</p> required <code>checkpoint_after</code> <code>str</code> <p>Path to checkpoint after critical moment</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary with CAMs before and after</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def compare_features(\n    self,\n    input_tensor: torch.Tensor,\n    target_class: int,\n    target_layer: nn.Module,\n    checkpoint_before: str,\n    checkpoint_after: str,\n) -&gt; Dict[str, Any]:\n    \"\"\"Compare feature visualizations before and after a critical moment.\n\n    Loads model weights from checkpoints and compares Grad-CAM visualizations.\n\n    Args:\n        input_tensor: Input to visualize\n        target_class: Target class\n        target_layer: Layer for visualization\n        checkpoint_before: Path to checkpoint before critical moment\n        checkpoint_after: Path to checkpoint after critical moment\n\n    Returns:\n        Dictionary with CAMs before and after\n    \"\"\"\n    # Load before checkpoint\n    state_before = torch.load(checkpoint_before, map_location=\"cpu\")\n    self.model.load_state_dict(state_before[\"model_state_dict\"])\n    cam_before = self.grad_cam(input_tensor, target_class, target_layer)\n\n    # Load after checkpoint\n    state_after = torch.load(checkpoint_after, map_location=\"cpu\")\n    self.model.load_state_dict(state_after[\"model_state_dict\"])\n    cam_after = self.grad_cam(input_tensor, target_class, target_layer)\n\n    # Compute difference\n    cam_diff = cam_after - cam_before\n\n    return {\n        \"cam_before\": cam_before,\n        \"cam_after\": cam_after,\n        \"cam_diff\": cam_diff,\n        \"mean_change\": np.abs(cam_diff).mean(),\n    }\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/","title":"Activation analysis","text":""},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis","title":"<code>ndt.analysis.activation_analysis</code>","text":"<p>Activation analysis tools for mechanistic interpretability.</p> <p>This module provides tools for analyzing neural network activations: - PCA for understanding principal directions - Clustering for identifying activation patterns - Manifold visualization (t-SNE, UMAP) - Geometry analysis (effective dimensionality, singular value distributions)</p>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis-classes","title":"Classes","text":""},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer","title":"<code>ActivationAnalyzer()</code>","text":"<p>Analyzes neural network activations for mechanistic interpretability.</p> <p>This class provides tools to understand what representations are learned at different layers and training stages, particularly focusing on critical moments identified by dimensionality jumps.</p> <p>Attributes:</p> Name Type Description <code>results</code> <code>Dict[str, Any]</code> <p>Dictionary storing analysis results</p> Example <p>analyzer = ActivationAnalyzer()</p> <p>Initialize the activation analyzer.</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialize the activation analyzer.\"\"\"\n    self.results: Dict[str, Any] = {}\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer--capture-activations-from-model","title":"Capture activations from model","text":"<p>activations = capture.get_all_activations()</p>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer--flatten-for-analysis","title":"Flatten for analysis","text":"<p>flat_acts = activations['layer_0'].view(batch_size, -1).cpu().numpy()</p>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer--run-pca-analysis","title":"Run PCA analysis","text":"<p>pca_results = analyzer.pca_analysis(flat_acts, n_components=10)</p>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer--visualize","title":"Visualize","text":"<p>analyzer.plot_pca_variance(pca_results)</p>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer-functions","title":"Methods:","text":""},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer.flatten_activation","title":"<code>flatten_activation(activation: torch.Tensor) -&gt; np.ndarray</code>","text":"<p>Flatten activation tensor to 2D array for analysis.</p> <p>Parameters:</p> Name Type Description Default <code>activation</code> <code>Tensor</code> <p>Activation tensor of shape (batch, ...)</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Flattened array of shape (batch, features)</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def flatten_activation(self, activation: torch.Tensor) -&gt; np.ndarray:\n    \"\"\"Flatten activation tensor to 2D array for analysis.\n\n    Args:\n        activation: Activation tensor of shape (batch, ...)\n\n    Returns:\n        Flattened array of shape (batch, features)\n    \"\"\"\n    if isinstance(activation, torch.Tensor):\n        activation = activation.detach().cpu().numpy()\n\n    batch_size = activation.shape[0]\n    return activation.reshape(batch_size, -1)\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer.pca_analysis","title":"<code>pca_analysis(activations: np.ndarray, n_components: Optional[int] = None, return_transformed: bool = True) -&gt; Dict[str, Any]</code>","text":"<p>Perform PCA analysis on activations.</p> <p>Analyzes the principal components of the activation space to understand the main directions of variation in representations.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <code>n_components</code> <code>Optional[int]</code> <p>Number of components to compute (default: min(n_samples, n_features))</p> <code>None</code> <code>return_transformed</code> <code>bool</code> <p>Whether to include transformed data</p> <code>True</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'explained_variance_ratio': Variance explained by each component - 'cumulative_variance': Cumulative variance explained - 'components': Principal component vectors - 'singular_values': Singular values - 'n_components_90': Components needed for 90% variance - 'n_components_95': Components needed for 95% variance - 'transformed': Transformed data (if return_transformed=True)</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def pca_analysis(\n    self,\n    activations: np.ndarray,\n    n_components: Optional[int] = None,\n    return_transformed: bool = True,\n) -&gt; Dict[str, Any]:\n    \"\"\"Perform PCA analysis on activations.\n\n    Analyzes the principal components of the activation space to understand\n    the main directions of variation in representations.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n        n_components: Number of components to compute (default: min(n_samples, n_features))\n        return_transformed: Whether to include transformed data\n\n    Returns:\n        Dictionary containing:\n            - 'explained_variance_ratio': Variance explained by each component\n            - 'cumulative_variance': Cumulative variance explained\n            - 'components': Principal component vectors\n            - 'singular_values': Singular values\n            - 'n_components_90': Components needed for 90% variance\n            - 'n_components_95': Components needed for 95% variance\n            - 'transformed': Transformed data (if return_transformed=True)\n    \"\"\"\n    if n_components is None:\n        n_components = min(activations.shape)\n\n    pca = PCA(n_components=n_components)\n    transformed = pca.fit_transform(activations)\n\n    cumulative_var = np.cumsum(pca.explained_variance_ratio_)\n    n_90 = np.searchsorted(cumulative_var, 0.90) + 1\n    n_95 = np.searchsorted(cumulative_var, 0.95) + 1\n\n    results = {\n        \"explained_variance_ratio\": pca.explained_variance_ratio_,\n        \"cumulative_variance\": cumulative_var,\n        \"components\": pca.components_,\n        \"singular_values\": pca.singular_values_,\n        \"n_components_90\": min(n_90, n_components),\n        \"n_components_95\": min(n_95, n_components),\n        \"mean\": pca.mean_,\n        \"pca_object\": pca,\n    }\n\n    if return_transformed:\n        results[\"transformed\"] = transformed\n\n    return results\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer.cluster_analysis","title":"<code>cluster_analysis(activations: np.ndarray, method: str = 'kmeans', n_clusters: int = 5, **kwargs) -&gt; Dict[str, Any]</code>","text":"<p>Perform clustering analysis on activations.</p> <p>Identifies groups of similar activation patterns, useful for understanding how different inputs are represented.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <code>method</code> <code>str</code> <p>Clustering method ('kmeans' or 'dbscan')</p> <code>'kmeans'</code> <code>n_clusters</code> <code>int</code> <p>Number of clusters (for kmeans)</p> <code>5</code> <code>**kwargs</code> <p>Additional arguments for clustering algorithm</p> <code>{}</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'labels': Cluster assignments - 'n_clusters': Number of clusters found - 'silhouette_score': Silhouette score if applicable - 'cluster_centers': Cluster centroids (kmeans only) - 'inertia': Within-cluster sum of squares (kmeans only)</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def cluster_analysis(\n    self, activations: np.ndarray, method: str = \"kmeans\", n_clusters: int = 5, **kwargs\n) -&gt; Dict[str, Any]:\n    \"\"\"Perform clustering analysis on activations.\n\n    Identifies groups of similar activation patterns, useful for\n    understanding how different inputs are represented.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n        method: Clustering method ('kmeans' or 'dbscan')\n        n_clusters: Number of clusters (for kmeans)\n        **kwargs: Additional arguments for clustering algorithm\n\n    Returns:\n        Dictionary containing:\n            - 'labels': Cluster assignments\n            - 'n_clusters': Number of clusters found\n            - 'silhouette_score': Silhouette score if applicable\n            - 'cluster_centers': Cluster centroids (kmeans only)\n            - 'inertia': Within-cluster sum of squares (kmeans only)\n    \"\"\"\n    if method == \"kmeans\":\n        clusterer = KMeans(n_clusters=n_clusters, random_state=42, **kwargs)\n        labels = clusterer.fit_predict(activations)\n\n        results = {\n            \"labels\": labels,\n            \"n_clusters\": n_clusters,\n            \"cluster_centers\": clusterer.cluster_centers_,\n            \"inertia\": clusterer.inertia_,\n        }\n\n    elif method == \"dbscan\":\n        eps = kwargs.get(\"eps\", 0.5)\n        min_samples = kwargs.get(\"min_samples\", 5)\n        clusterer = DBSCAN(eps=eps, min_samples=min_samples)\n        labels = clusterer.fit_predict(activations)\n\n        n_found = len(set(labels)) - (1 if -1 in labels else 0)\n        results = {\"labels\": labels, \"n_clusters\": n_found, \"n_noise\": np.sum(labels == -1)}\n    else:\n        raise ValueError(f\"Unknown clustering method: {method}\")\n\n    # Compute silhouette score if more than 1 cluster\n    unique_labels = set(labels)\n    if len(unique_labels) &gt; 1 and -1 not in unique_labels:\n        results[\"silhouette_score\"] = silhouette_score(activations, labels)\n    elif len(unique_labels) &gt; 2:  # Has noise but also clusters\n        mask = labels != -1\n        if mask.sum() &gt; 1:\n            results[\"silhouette_score\"] = silhouette_score(activations[mask], labels[mask])\n\n    return results\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer.manifold_embedding","title":"<code>manifold_embedding(activations: np.ndarray, method: str = 'tsne', n_components: int = 2, **kwargs) -&gt; Dict[str, Any]</code>","text":"<p>Compute low-dimensional manifold embedding.</p> <p>Reduces high-dimensional activations to 2D/3D for visualization, preserving local structure.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <code>method</code> <code>str</code> <p>Embedding method ('tsne' or 'umap')</p> <code>'tsne'</code> <code>n_components</code> <code>int</code> <p>Output dimensions (2 or 3)</p> <code>2</code> <code>**kwargs</code> <p>Additional arguments for embedding algorithm</p> <code>{}</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'embedding': Low-dimensional coordinates - 'method': Method used - 'params': Parameters used</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def manifold_embedding(\n    self, activations: np.ndarray, method: str = \"tsne\", n_components: int = 2, **kwargs\n) -&gt; Dict[str, Any]:\n    \"\"\"Compute low-dimensional manifold embedding.\n\n    Reduces high-dimensional activations to 2D/3D for visualization,\n    preserving local structure.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n        method: Embedding method ('tsne' or 'umap')\n        n_components: Output dimensions (2 or 3)\n        **kwargs: Additional arguments for embedding algorithm\n\n    Returns:\n        Dictionary containing:\n            - 'embedding': Low-dimensional coordinates\n            - 'method': Method used\n            - 'params': Parameters used\n    \"\"\"\n    if method == \"tsne\":\n        perplexity = kwargs.get(\"perplexity\", min(30, activations.shape[0] - 1))\n        tsne = TSNE(\n            n_components=n_components,\n            perplexity=perplexity,\n            random_state=42,\n            **{k: v for k, v in kwargs.items() if k != \"perplexity\"},\n        )\n        embedding = tsne.fit_transform(activations)\n\n        results = {\n            \"embedding\": embedding,\n            \"method\": \"tsne\",\n            \"params\": {\"perplexity\": perplexity, \"n_components\": n_components},\n        }\n\n    elif method == \"umap\":\n        if not HAS_UMAP:\n            raise ImportError(\"UMAP not installed. Install with: pip install umap-learn\")\n\n        n_neighbors = kwargs.get(\"n_neighbors\", min(15, activations.shape[0] - 1))\n        reducer = umap.UMAP(\n            n_components=n_components,\n            n_neighbors=n_neighbors,\n            random_state=42,\n            **{k: v for k, v in kwargs.items() if k != \"n_neighbors\"},\n        )\n        embedding = reducer.fit_transform(activations)\n\n        results = {\n            \"embedding\": embedding,\n            \"method\": \"umap\",\n            \"params\": {\"n_neighbors\": n_neighbors, \"n_components\": n_components},\n        }\n    else:\n        raise ValueError(f\"Unknown embedding method: {method}\")\n\n    return results\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer.singular_value_analysis","title":"<code>singular_value_analysis(activations: np.ndarray) -&gt; Dict[str, Any]</code>","text":"<p>Analyze singular value distribution of activations.</p> <p>Computes detailed statistics about the singular value spectrum, useful for understanding representation geometry.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'singular_values': All singular values - 'normalized_sv': Normalized singular values - 'stable_rank': Stable rank - 'participation_ratio': Participation ratio - 'entropy': Spectral entropy - 'condition_number': Condition number</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def singular_value_analysis(self, activations: np.ndarray) -&gt; Dict[str, Any]:\n    \"\"\"Analyze singular value distribution of activations.\n\n    Computes detailed statistics about the singular value spectrum,\n    useful for understanding representation geometry.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n\n    Returns:\n        Dictionary containing:\n            - 'singular_values': All singular values\n            - 'normalized_sv': Normalized singular values\n            - 'stable_rank': Stable rank\n            - 'participation_ratio': Participation ratio\n            - 'entropy': Spectral entropy\n            - 'condition_number': Condition number\n    \"\"\"\n    U, S, Vt = np.linalg.svd(activations, full_matrices=False)\n\n    # Normalized singular values\n    S_norm = S / S.sum()\n\n    # Stable rank\n    stable_rank = (S**2).sum() / (S[0] ** 2) if S[0] &gt; 0 else 0\n\n    # Participation ratio\n    participation_ratio = 1.0 / (S_norm**2).sum() if S_norm.sum() &gt; 0 else 0\n\n    # Spectral entropy\n    S_norm_nonzero = S_norm[S_norm &gt; 0]\n    entropy = -np.sum(S_norm_nonzero * np.log(S_norm_nonzero))\n\n    # Condition number\n    condition_number = S[0] / S[-1] if S[-1] &gt; 0 else np.inf\n\n    return {\n        \"singular_values\": S,\n        \"normalized_sv\": S_norm,\n        \"stable_rank\": stable_rank,\n        \"participation_ratio\": participation_ratio,\n        \"spectral_entropy\": entropy,\n        \"condition_number\": condition_number,\n    }\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer.compare_activations","title":"<code>compare_activations(activations_before: np.ndarray, activations_after: np.ndarray) -&gt; Dict[str, Any]</code>","text":"<p>Compare activations before and after a critical moment (e.g., jump).</p> <p>Analyzes how representations change during dimensionality transitions.</p> <p>Parameters:</p> Name Type Description Default <code>activations_before</code> <code>ndarray</code> <p>Activations before critical moment</p> required <code>activations_after</code> <code>ndarray</code> <p>Activations after critical moment</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing comparison metrics: - 'pca_before': PCA results before - 'pca_after': PCA results after - 'sv_before': Singular value analysis before - 'sv_after': Singular value analysis after - 'dim_change': Change in effective dimensionality - 'subspace_overlap': Overlap between principal subspaces</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def compare_activations(\n    self, activations_before: np.ndarray, activations_after: np.ndarray\n) -&gt; Dict[str, Any]:\n    \"\"\"Compare activations before and after a critical moment (e.g., jump).\n\n    Analyzes how representations change during dimensionality transitions.\n\n    Args:\n        activations_before: Activations before critical moment\n        activations_after: Activations after critical moment\n\n    Returns:\n        Dictionary containing comparison metrics:\n            - 'pca_before': PCA results before\n            - 'pca_after': PCA results after\n            - 'sv_before': Singular value analysis before\n            - 'sv_after': Singular value analysis after\n            - 'dim_change': Change in effective dimensionality\n            - 'subspace_overlap': Overlap between principal subspaces\n    \"\"\"\n    # PCA analysis\n    pca_before = self.pca_analysis(activations_before, return_transformed=False)\n    pca_after = self.pca_analysis(activations_after, return_transformed=False)\n\n    # Singular value analysis\n    sv_before = self.singular_value_analysis(activations_before)\n    sv_after = self.singular_value_analysis(activations_after)\n\n    # Dimensionality change\n    dim_change = {\n        \"stable_rank\": sv_after[\"stable_rank\"] - sv_before[\"stable_rank\"],\n        \"participation_ratio\": (\n            sv_after[\"participation_ratio\"] - sv_before[\"participation_ratio\"]\n        ),\n        \"n_components_90\": (pca_after[\"n_components_90\"] - pca_before[\"n_components_90\"]),\n    }\n\n    # Subspace overlap (using principal angles)\n    n_components = min(\n        10, min(pca_before[\"components\"].shape[0], pca_after[\"components\"].shape[0])\n    )\n    V1 = pca_before[\"components\"][:n_components].T\n    V2 = pca_after[\"components\"][:n_components].T\n\n    # Compute principal angles via SVD\n    M = V1.T @ V2\n    _, s, _ = np.linalg.svd(M)\n    principal_angles = np.arccos(np.clip(s, -1, 1))\n    subspace_overlap = np.cos(principal_angles).mean()\n\n    return {\n        \"pca_before\": pca_before,\n        \"pca_after\": pca_after,\n        \"sv_before\": sv_before,\n        \"sv_after\": sv_after,\n        \"dim_change\": dim_change,\n        \"subspace_overlap\": subspace_overlap,\n        \"principal_angles\": principal_angles,\n    }\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer.neuron_importance","title":"<code>neuron_importance(activations: np.ndarray, method: str = 'variance') -&gt; Dict[str, Any]</code>","text":"<p>Compute importance scores for individual neurons.</p> <p>Identifies which neurons contribute most to the representation.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_neurons)</p> required <code>method</code> <code>str</code> <p>Scoring method ('variance', 'mean_abs', 'sparsity')</p> <code>'variance'</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary containing: - 'scores': Importance score for each neuron - 'ranking': Indices sorted by importance - 'dead_neurons': Indices of neurons with zero activation - 'top_10_indices': Top 10 most important neurons</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def neuron_importance(\n    self, activations: np.ndarray, method: str = \"variance\"\n) -&gt; Dict[str, Any]:\n    \"\"\"Compute importance scores for individual neurons.\n\n    Identifies which neurons contribute most to the representation.\n\n    Args:\n        activations: Array of shape (n_samples, n_neurons)\n        method: Scoring method ('variance', 'mean_abs', 'sparsity')\n\n    Returns:\n        Dictionary containing:\n            - 'scores': Importance score for each neuron\n            - 'ranking': Indices sorted by importance\n            - 'dead_neurons': Indices of neurons with zero activation\n            - 'top_10_indices': Top 10 most important neurons\n    \"\"\"\n    if method == \"variance\":\n        scores = np.var(activations, axis=0)\n    elif method == \"mean_abs\":\n        scores = np.mean(np.abs(activations), axis=0)\n    elif method == \"sparsity\":\n        # Lower sparsity = higher importance\n        scores = 1.0 - np.mean(activations == 0, axis=0)\n    else:\n        raise ValueError(f\"Unknown importance method: {method}\")\n\n    ranking = np.argsort(scores)[::-1]\n    dead_neurons = np.where(np.all(activations == 0, axis=0))[0]\n\n    return {\n        \"scores\": scores,\n        \"ranking\": ranking,\n        \"dead_neurons\": dead_neurons,\n        \"n_dead\": len(dead_neurons),\n        \"top_10_indices\": ranking[:10],\n        \"method\": method,\n    }\n</code></pre>"},{"location":"api/ndt/analysis/activation_analysis/#ndt.analysis.activation_analysis.ActivationAnalyzer.activation_statistics","title":"<code>activation_statistics(activations: np.ndarray) -&gt; Dict[str, float]</code>","text":"<p>Compute summary statistics for activations.</p> <p>Parameters:</p> Name Type Description Default <code>activations</code> <code>ndarray</code> <p>Array of shape (n_samples, n_features)</p> required <p>Returns:</p> Type Description <code>Dict[str, float]</code> <p>Dictionary of statistics</p> Source code in <code>src/ndt/analysis/activation_analysis.py</code> <pre><code>def activation_statistics(self, activations: np.ndarray) -&gt; Dict[str, float]:\n    \"\"\"Compute summary statistics for activations.\n\n    Args:\n        activations: Array of shape (n_samples, n_features)\n\n    Returns:\n        Dictionary of statistics\n    \"\"\"\n    return {\n        \"mean\": float(np.mean(activations)),\n        \"std\": float(np.std(activations)),\n        \"min\": float(np.min(activations)),\n        \"max\": float(np.max(activations)),\n        \"sparsity\": float(np.mean(activations == 0)),\n        \"n_samples\": activations.shape[0],\n        \"n_features\": activations.shape[1],\n    }\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/","title":"Feature visualization","text":""},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization","title":"<code>ndt.analysis.feature_visualization</code>","text":"<p>Feature visualization tools for mechanistic interpretability.</p> <p>This module provides tools for visualizing what neural networks learn: - Class Activation Mapping (CAM) for CNNs - Gradient-based saliency maps - Attention visualization for Transformers</p>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization-classes","title":"Classes","text":""},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer","title":"<code>FeatureVisualizer(model: nn.Module)</code>","text":"<p>Visualizes learned features in neural networks.</p> <p>Provides methods for understanding what features the network has learned at different layers and training stages.</p> Example <p>visualizer = FeatureVisualizer(model)</p> <p>Initialize the feature visualizer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model to visualize</p> required Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def __init__(self, model: nn.Module) -&gt; None:\n    \"\"\"Initialize the feature visualizer.\n\n    Args:\n        model: The neural network model to visualize\n    \"\"\"\n    self.model = model\n    self.gradients: Dict[str, torch.Tensor] = {}\n    self.activations: Dict[str, torch.Tensor] = {}\n    self.hooks: List[torch.utils.hooks.RemovableHandle] = []\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer--generate-cam-for-an-image","title":"Generate CAM for an image","text":"<p>cam = visualizer.grad_cam(input_image, target_class, 'conv5')</p>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer--visualize-saliency","title":"Visualize saliency","text":"<p>saliency = visualizer.saliency_map(input_image, target_class)</p>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer-functions","title":"Methods:","text":""},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.register_hooks","title":"<code>register_hooks(layer_names: Dict[str, nn.Module]) -&gt; None</code>","text":"<p>Register hooks on specified layers.</p> <p>Parameters:</p> Name Type Description Default <code>layer_names</code> <code>Dict[str, Module]</code> <p>Dictionary mapping names to layer modules</p> required Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def register_hooks(self, layer_names: Dict[str, nn.Module]) -&gt; None:\n    \"\"\"Register hooks on specified layers.\n\n    Args:\n        layer_names: Dictionary mapping names to layer modules\n    \"\"\"\n    for name, layer in layer_names.items():\n        hook = layer.register_forward_hook(self._save_activation(name))\n        self.hooks.append(hook)\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.remove_hooks","title":"<code>remove_hooks() -&gt; None</code>","text":"<p>Remove all registered hooks.</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def remove_hooks(self) -&gt; None:\n    \"\"\"Remove all registered hooks.\"\"\"\n    for hook in self.hooks:\n        hook.remove()\n    self.hooks.clear()\n    self.gradients.clear()\n    self.activations.clear()\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.grad_cam","title":"<code>grad_cam(input_tensor: torch.Tensor, target_class: int, target_layer: nn.Module, layer_name: str = 'target') -&gt; np.ndarray</code>","text":"<p>Generate Grad-CAM visualization.</p> <p>Grad-CAM uses gradients flowing into the target layer to produce a coarse localization map highlighting important regions.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input image tensor (1, C, H, W)</p> required <code>target_class</code> <code>int</code> <p>Target class index</p> required <code>target_layer</code> <code>Module</code> <p>Layer to compute CAM for</p> required <code>layer_name</code> <code>str</code> <p>Name for the layer (for storage)</p> <code>'target'</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>CAM heatmap as numpy array</p> References <p>Selvaraju et al., \"Grad-CAM: Visual Explanations from Deep Networks via Gradient-based Localization\", ICCV 2017</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def grad_cam(\n    self,\n    input_tensor: torch.Tensor,\n    target_class: int,\n    target_layer: nn.Module,\n    layer_name: str = \"target\",\n) -&gt; np.ndarray:\n    \"\"\"Generate Grad-CAM visualization.\n\n    Grad-CAM uses gradients flowing into the target layer to produce\n    a coarse localization map highlighting important regions.\n\n    Args:\n        input_tensor: Input image tensor (1, C, H, W)\n        target_class: Target class index\n        target_layer: Layer to compute CAM for\n        layer_name: Name for the layer (for storage)\n\n    Returns:\n        CAM heatmap as numpy array\n\n    References:\n        Selvaraju et al., \"Grad-CAM: Visual Explanations from Deep Networks\n        via Gradient-based Localization\", ICCV 2017\n    \"\"\"\n    self.model.eval()\n\n    # Register hooks\n    activations = []\n    gradients = []\n\n    def forward_hook(module, input, output):\n        activations.append(output.detach())\n\n    def backward_hook(module, grad_input, grad_output):\n        gradients.append(grad_output[0].detach())\n\n    forward_handle = target_layer.register_forward_hook(forward_hook)\n    backward_handle = target_layer.register_full_backward_hook(backward_hook)\n\n    try:\n        # Forward pass\n        output = self.model(input_tensor)\n\n        # Get prediction score for target class\n        if output.dim() == 1:\n            score = output[target_class]\n        else:\n            score = output[0, target_class]\n\n        # Backward pass\n        self.model.zero_grad()\n        score.backward()\n\n        # Get the activations and gradients\n        activation = activations[0]  # (1, C, H, W)\n        gradient = gradients[0]  # (1, C, H, W)\n\n        # Global average pooling of gradients\n        weights = torch.mean(gradient, dim=(2, 3), keepdim=True)  # (1, C, 1, 1)\n\n        # Weighted combination of activation maps\n        cam = torch.sum(weights * activation, dim=1, keepdim=True)  # (1, 1, H, W)\n\n        # ReLU to keep only positive contributions\n        cam = F.relu(cam)\n\n        # Normalize\n        cam = cam - cam.min()\n        if cam.max() &gt; 0:\n            cam = cam / cam.max()\n\n        return cam.squeeze().cpu().numpy()\n\n    finally:\n        forward_handle.remove()\n        backward_handle.remove()\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.saliency_map","title":"<code>saliency_map(input_tensor: torch.Tensor, target_class: int, absolute: bool = True) -&gt; np.ndarray</code>","text":"<p>Generate vanilla gradient saliency map.</p> <p>Computes the gradient of the target class score with respect to the input pixels.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input tensor (1, C, H, W) or (1, features)</p> required <code>target_class</code> <code>int</code> <p>Target class index</p> required <code>absolute</code> <code>bool</code> <p>Whether to take absolute value of gradients</p> <code>True</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Saliency map as numpy array</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def saliency_map(\n    self, input_tensor: torch.Tensor, target_class: int, absolute: bool = True\n) -&gt; np.ndarray:\n    \"\"\"Generate vanilla gradient saliency map.\n\n    Computes the gradient of the target class score with respect to\n    the input pixels.\n\n    Args:\n        input_tensor: Input tensor (1, C, H, W) or (1, features)\n        target_class: Target class index\n        absolute: Whether to take absolute value of gradients\n\n    Returns:\n        Saliency map as numpy array\n    \"\"\"\n    self.model.eval()\n\n    input_tensor = input_tensor.clone().requires_grad_(True)\n\n    # Forward pass\n    output = self.model(input_tensor)\n\n    # Get score for target class\n    if output.dim() == 1:\n        score = output[target_class]\n    else:\n        score = output[0, target_class]\n\n    # Backward pass\n    self.model.zero_grad()\n    score.backward()\n\n    # Get gradients\n    saliency = input_tensor.grad.detach()\n\n    if absolute:\n        saliency = torch.abs(saliency)\n\n    # For multi-channel inputs, take max across channels\n    if saliency.dim() == 4 and saliency.shape[1] &gt; 1:\n        saliency = saliency.max(dim=1, keepdim=True)[0]\n\n    return saliency.squeeze().cpu().numpy()\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.integrated_gradients","title":"<code>integrated_gradients(input_tensor: torch.Tensor, target_class: int, baseline: Optional[torch.Tensor] = None, steps: int = 50) -&gt; np.ndarray</code>","text":"<p>Compute Integrated Gradients attribution.</p> <p>Accumulates gradients along the path from baseline to input, providing more stable attributions than vanilla gradients.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input tensor</p> required <code>target_class</code> <code>int</code> <p>Target class index</p> required <code>baseline</code> <code>Optional[Tensor]</code> <p>Baseline tensor (default: zeros)</p> <code>None</code> <code>steps</code> <code>int</code> <p>Number of interpolation steps</p> <code>50</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Attribution map as numpy array</p> References <p>Sundararajan et al., \"Axiomatic Attribution for Deep Networks\", ICML 2017</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def integrated_gradients(\n    self,\n    input_tensor: torch.Tensor,\n    target_class: int,\n    baseline: Optional[torch.Tensor] = None,\n    steps: int = 50,\n) -&gt; np.ndarray:\n    \"\"\"Compute Integrated Gradients attribution.\n\n    Accumulates gradients along the path from baseline to input,\n    providing more stable attributions than vanilla gradients.\n\n    Args:\n        input_tensor: Input tensor\n        target_class: Target class index\n        baseline: Baseline tensor (default: zeros)\n        steps: Number of interpolation steps\n\n    Returns:\n        Attribution map as numpy array\n\n    References:\n        Sundararajan et al., \"Axiomatic Attribution for Deep Networks\", ICML 2017\n    \"\"\"\n    self.model.eval()\n\n    if baseline is None:\n        baseline = torch.zeros_like(input_tensor)\n\n    # Generate interpolated inputs\n    alphas = torch.linspace(0, 1, steps).to(input_tensor.device)\n    scaled_inputs = torch.stack(\n        [baseline + alpha * (input_tensor - baseline) for alpha in alphas]\n    )\n\n    # Compute gradients for all interpolated inputs\n    scaled_inputs.requires_grad_(True)\n\n    # Forward pass (batch all steps together)\n    outputs = self.model(scaled_inputs.view(-1, *input_tensor.shape[1:]))\n\n    # Get scores\n    if outputs.dim() == 1:\n        scores = outputs.view(steps, -1)[:, target_class]\n    else:\n        scores = outputs[:, target_class]\n\n    # Backward pass\n    self.model.zero_grad()\n    scores.sum().backward()\n\n    # Average gradients\n    avg_gradients = scaled_inputs.grad.mean(dim=0)\n\n    # Integrated gradients\n    ig = (input_tensor - baseline) * avg_gradients\n\n    return ig.squeeze().detach().cpu().numpy()\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.attention_visualization","title":"<code>attention_visualization(attention_weights: torch.Tensor, head_idx: Optional[int] = None) -&gt; np.ndarray</code>","text":"<p>Visualize attention weights from transformer models.</p> <p>Parameters:</p> Name Type Description Default <code>attention_weights</code> <code>Tensor</code> <p>Attention tensor (batch, heads, seq, seq)</p> required <code>head_idx</code> <code>Optional[int]</code> <p>Specific head to visualize (None for average)</p> <code>None</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>Attention matrix as numpy array</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def attention_visualization(\n    self, attention_weights: torch.Tensor, head_idx: Optional[int] = None\n) -&gt; np.ndarray:\n    \"\"\"Visualize attention weights from transformer models.\n\n    Args:\n        attention_weights: Attention tensor (batch, heads, seq, seq)\n        head_idx: Specific head to visualize (None for average)\n\n    Returns:\n        Attention matrix as numpy array\n    \"\"\"\n    if head_idx is not None:\n        attn = attention_weights[:, head_idx]\n    else:\n        # Average across heads\n        attn = attention_weights.mean(dim=1)\n\n    return attn.squeeze().detach().cpu().numpy()\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.feature_maps","title":"<code>feature_maps(activation: torch.Tensor, top_k: int = 16) -&gt; List[np.ndarray]</code>","text":"<p>Extract top-k feature maps from a convolutional layer.</p> <p>Parameters:</p> Name Type Description Default <code>activation</code> <code>Tensor</code> <p>Activation tensor (batch, channels, H, W)</p> required <code>top_k</code> <code>int</code> <p>Number of feature maps to return</p> <code>16</code> <p>Returns:</p> Type Description <code>List[ndarray]</code> <p>List of feature map arrays</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def feature_maps(self, activation: torch.Tensor, top_k: int = 16) -&gt; List[np.ndarray]:\n    \"\"\"Extract top-k feature maps from a convolutional layer.\n\n    Args:\n        activation: Activation tensor (batch, channels, H, W)\n        top_k: Number of feature maps to return\n\n    Returns:\n        List of feature map arrays\n    \"\"\"\n    # Compute activation magnitudes\n    magnitudes = activation.abs().mean(dim=(2, 3))  # (batch, channels)\n\n    # Get top-k channels\n    _, top_indices = magnitudes[0].topk(min(top_k, activation.shape[1]))\n\n    feature_maps = []\n    for idx in top_indices:\n        fmap = activation[0, idx].detach().cpu().numpy()\n        # Normalize\n        fmap = (fmap - fmap.min()) / (fmap.max() - fmap.min() + 1e-8)\n        feature_maps.append(fmap)\n\n    return feature_maps\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.neuron_activation_maximization","title":"<code>neuron_activation_maximization(target_layer: nn.Module, neuron_idx: int, input_shape: Tuple[int, ...], steps: int = 100, lr: float = 0.1) -&gt; torch.Tensor</code>","text":"<p>Generate input that maximizes a specific neuron's activation.</p> <p>Uses gradient ascent to find an input that maximally activates a target neuron.</p> <p>Parameters:</p> Name Type Description Default <code>target_layer</code> <code>Module</code> <p>Layer containing the neuron</p> required <code>neuron_idx</code> <code>int</code> <p>Index of the neuron to maximize</p> required <code>input_shape</code> <code>Tuple[int, ...]</code> <p>Shape of input tensor</p> required <code>steps</code> <code>int</code> <p>Number of optimization steps</p> <code>100</code> <code>lr</code> <code>float</code> <p>Learning rate</p> <code>0.1</code> <p>Returns:</p> Type Description <code>Tensor</code> <p>Optimized input tensor</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def neuron_activation_maximization(\n    self,\n    target_layer: nn.Module,\n    neuron_idx: int,\n    input_shape: Tuple[int, ...],\n    steps: int = 100,\n    lr: float = 0.1,\n) -&gt; torch.Tensor:\n    \"\"\"Generate input that maximizes a specific neuron's activation.\n\n    Uses gradient ascent to find an input that maximally activates\n    a target neuron.\n\n    Args:\n        target_layer: Layer containing the neuron\n        neuron_idx: Index of the neuron to maximize\n        input_shape: Shape of input tensor\n        steps: Number of optimization steps\n        lr: Learning rate\n\n    Returns:\n        Optimized input tensor\n    \"\"\"\n    # Initialize with random noise\n    input_tensor = torch.randn(1, *input_shape, requires_grad=True)\n\n    activation_value = []\n\n    def hook(module, input, output):\n        if output.dim() == 2:\n            activation_value.append(output[0, neuron_idx])\n        elif output.dim() == 4:\n            # For conv layers, take spatial mean\n            activation_value.append(output[0, neuron_idx].mean())\n\n    handle = target_layer.register_forward_hook(hook)\n\n    try:\n        for _ in range(steps):\n            activation_value.clear()\n\n            # Forward pass\n            _ = self.model(input_tensor)\n\n            # Get activation\n            act = activation_value[0]\n\n            # Backward pass\n            self.model.zero_grad()\n            act.backward()\n\n            # Gradient ascent\n            with torch.no_grad():\n                input_tensor += lr * input_tensor.grad\n                input_tensor.grad.zero_()\n\n        return input_tensor.detach()\n\n    finally:\n        handle.remove()\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.layer_conductance","title":"<code>layer_conductance(input_tensor: torch.Tensor, target_class: int, target_layer: nn.Module) -&gt; np.ndarray</code>","text":"<p>Compute layer conductance attribution.</p> <p>Measures how much each neuron in a layer contributes to the final prediction.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input tensor</p> required <code>target_class</code> <code>int</code> <p>Target class index</p> required <code>target_layer</code> <code>Module</code> <p>Layer to compute conductance for</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Conductance values for each neuron</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def layer_conductance(\n    self, input_tensor: torch.Tensor, target_class: int, target_layer: nn.Module\n) -&gt; np.ndarray:\n    \"\"\"Compute layer conductance attribution.\n\n    Measures how much each neuron in a layer contributes to the\n    final prediction.\n\n    Args:\n        input_tensor: Input tensor\n        target_class: Target class index\n        target_layer: Layer to compute conductance for\n\n    Returns:\n        Conductance values for each neuron\n    \"\"\"\n    # Use integrated gradients through the layer\n    activation_grads = []\n\n    def hook(module, input, output):\n        output.retain_grad()\n        activation_grads.append(output)\n\n    handle = target_layer.register_forward_hook(hook)\n\n    try:\n        self.model.eval()\n\n        # Forward pass\n        output = self.model(input_tensor)\n\n        # Get score\n        if output.dim() == 1:\n            score = output[target_class]\n        else:\n            score = output[0, target_class]\n\n        # Backward pass\n        self.model.zero_grad()\n        score.backward()\n\n        # Get activation and gradient\n        activation = activation_grads[0]\n        gradient = activation.grad\n\n        # Conductance = activation * gradient\n        conductance = (activation * gradient).sum(dim=0)\n\n        return conductance.detach().cpu().numpy()\n\n    finally:\n        handle.remove()\n</code></pre>"},{"location":"api/ndt/analysis/feature_visualization/#ndt.analysis.feature_visualization.FeatureVisualizer.compare_features","title":"<code>compare_features(input_tensor: torch.Tensor, target_class: int, target_layer: nn.Module, checkpoint_before: str, checkpoint_after: str) -&gt; Dict[str, Any]</code>","text":"<p>Compare feature visualizations before and after a critical moment.</p> <p>Loads model weights from checkpoints and compares Grad-CAM visualizations.</p> <p>Parameters:</p> Name Type Description Default <code>input_tensor</code> <code>Tensor</code> <p>Input to visualize</p> required <code>target_class</code> <code>int</code> <p>Target class</p> required <code>target_layer</code> <code>Module</code> <p>Layer for visualization</p> required <code>checkpoint_before</code> <code>str</code> <p>Path to checkpoint before critical moment</p> required <code>checkpoint_after</code> <code>str</code> <p>Path to checkpoint after critical moment</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dictionary with CAMs before and after</p> Source code in <code>src/ndt/analysis/feature_visualization.py</code> <pre><code>def compare_features(\n    self,\n    input_tensor: torch.Tensor,\n    target_class: int,\n    target_layer: nn.Module,\n    checkpoint_before: str,\n    checkpoint_after: str,\n) -&gt; Dict[str, Any]:\n    \"\"\"Compare feature visualizations before and after a critical moment.\n\n    Loads model weights from checkpoints and compares Grad-CAM visualizations.\n\n    Args:\n        input_tensor: Input to visualize\n        target_class: Target class\n        target_layer: Layer for visualization\n        checkpoint_before: Path to checkpoint before critical moment\n        checkpoint_after: Path to checkpoint after critical moment\n\n    Returns:\n        Dictionary with CAMs before and after\n    \"\"\"\n    # Load before checkpoint\n    state_before = torch.load(checkpoint_before, map_location=\"cpu\")\n    self.model.load_state_dict(state_before[\"model_state_dict\"])\n    cam_before = self.grad_cam(input_tensor, target_class, target_layer)\n\n    # Load after checkpoint\n    state_after = torch.load(checkpoint_after, map_location=\"cpu\")\n    self.model.load_state_dict(state_after[\"model_state_dict\"])\n    cam_after = self.grad_cam(input_tensor, target_class, target_layer)\n\n    # Compute difference\n    cam_diff = cam_after - cam_before\n\n    return {\n        \"cam_before\": cam_before,\n        \"cam_after\": cam_after,\n        \"cam_diff\": cam_diff,\n        \"mean_change\": np.abs(cam_diff).mean(),\n    }\n</code></pre>"},{"location":"api/ndt/architectures/","title":"Index","text":""},{"location":"api/ndt/architectures/#ndt.architectures","title":"<code>ndt.architectures</code>","text":"<p>Architecture-specific handlers for dimensionality tracking.</p>"},{"location":"api/ndt/architectures/#ndt.architectures-classes","title":"Classes","text":""},{"location":"api/ndt/architectures/#ndt.architectures.ArchitectureHandler","title":"<code>ArchitectureHandler</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for architecture-specific dimensionality tracking.</p> <p>Each architecture handler implements methods to identify relevant layers and extract activations appropriately for that architecture type.</p>"},{"location":"api/ndt/architectures/#ndt.architectures.ArchitectureHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/#ndt.architectures.ArchitectureHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>  <code>abstractmethod</code>","text":"<p>Check if model matches this handler's architecture.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model is compatible with this handler</p> Source code in <code>src/ndt/architectures/base.py</code> <pre><code>@abstractmethod\ndef validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model matches this handler's architecture.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model is compatible with this handler\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.ArchitectureHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>  <code>abstractmethod</code>","text":"<p>Return list of layers to monitor for dimensionality.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/base.py</code> <pre><code>@abstractmethod\ndef get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Return list of layers to monitor for dimensionality.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.ArchitectureHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>  <code>abstractmethod</code>","text":"<p>Generate descriptive names for the layers to monitor.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names corresponding to activation layers</p> Source code in <code>src/ndt/architectures/base.py</code> <pre><code>@abstractmethod\ndef get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate descriptive names for the layers to monitor.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names corresponding to activation layers\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.ArchitectureHandler.get_name","title":"<code>get_name() -&gt; str</code>","text":"<p>Get the name of this architecture handler.</p> <p>Returns:</p> Type Description <code>str</code> <p>Architecture name (e.g., \"MLP\", \"CNN\", \"Transformer\")</p> Source code in <code>src/ndt/architectures/base.py</code> <pre><code>def get_name(self) -&gt; str:\n    \"\"\"Get the name of this architecture handler.\n\n    Returns:\n        Architecture name (e.g., \"MLP\", \"CNN\", \"Transformer\")\n    \"\"\"\n    return self.__class__.__name__.replace(\"Handler\", \"\")\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.CNNHandler","title":"<code>CNNHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Convolutional Neural Networks (CNNs).</p> <p>Monitors Conv2d layers and the first Linear layer (pre-classifier). Supports both simple CNNs and ResNet-style architectures.</p>"},{"location":"api/ndt/architectures/#ndt.architectures.CNNHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/#ndt.architectures.CNNHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a CNN (contains Conv layers).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a CNN</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a CNN (contains Conv layers).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a CNN\n    \"\"\"\n    has_conv = False\n    for module in model.modules():\n        if isinstance(module, (nn.Conv1d, nn.Conv2d, nn.Conv3d)):\n            has_conv = True\n            break\n    return has_conv\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.CNNHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get Conv2d and key Linear layers for monitoring.</p> <p>For ResNets, monitors after each residual block. For simple CNNs, monitors each conv layer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get Conv2d and key Linear layers for monitoring.\n\n    For ResNets, monitors after each residual block.\n    For simple CNNs, monitors each conv layer.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # Check if it's a ResNet-style architecture\n    if self._is_resnet(model):\n        layers = self._get_resnet_layers(model)\n    else:\n        # Simple CNN: monitor all conv layers\n        for module in model.modules():\n            if isinstance(module, nn.Conv2d):\n                layers.append(module)\n\n        # Also monitor first Linear layer (before classifier)\n        linear_layers = [m for m in model.modules() if isinstance(m, nn.Linear)]\n        if linear_layers:\n            layers.append(linear_layers[0])\n\n    return layers\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.CNNHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate descriptive names for CNN layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate descriptive names for CNN layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    conv_count = 0\n    linear_count = 0\n\n    for layer in layers:\n        if isinstance(layer, nn.Conv2d):\n            names.append(f\"Conv_{conv_count}_{layer.out_channels}ch\")\n            conv_count += 1\n        elif isinstance(layer, nn.Linear):\n            names.append(f\"FC_{linear_count}_{layer.out_features}\")\n            linear_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.MLPHandler","title":"<code>MLPHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for fully-connected (MLP) networks.</p> <p>Monitors all Linear layers in the network.</p>"},{"location":"api/ndt/architectures/#ndt.architectures.MLPHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/#ndt.architectures.MLPHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is an MLP (contains Linear layers, no Conv/Attention).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be an MLP</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is an MLP (contains Linear layers, no Conv/Attention).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be an MLP\n    \"\"\"\n    has_linear = False\n    has_conv = False\n    has_attention = False\n\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            has_linear = True\n        elif isinstance(module, (nn.Conv1d, nn.Conv2d, nn.Conv3d)):\n            has_conv = True\n        elif isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n\n    # MLP should have Linear layers but not Conv or Attention\n    return has_linear and not has_conv and not has_attention\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.MLPHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get all Linear layers for monitoring.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of Linear layer modules</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get all Linear layers for monitoring.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of Linear layer modules\n    \"\"\"\n    layers = []\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            layers.append(module)\n    return layers\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.MLPHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for Linear layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names like \"Linear_0\", \"Linear_1\", etc.</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for Linear layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names like \"Linear_0\", \"Linear_1\", etc.\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    for i, layer in enumerate(layers):\n        in_features = layer.in_features\n        out_features = layer.out_features\n        names.append(f\"Linear_{i}_{in_features}x{out_features}\")\n\n    return names\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.TransformerHandler","title":"<code>TransformerHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Transformer-based architectures.</p> <p>Monitors attention outputs and feed-forward network outputs in each layer. Compatible with BERT, GPT, and similar transformer models.</p>"},{"location":"api/ndt/architectures/#ndt.architectures.TransformerHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/#ndt.architectures.TransformerHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a Transformer (contains MultiheadAttention).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a Transformer</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a Transformer (contains MultiheadAttention).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a Transformer\n    \"\"\"\n    has_attention = False\n    for module in model.modules():\n        if isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n            break\n        # Also check for common transformer layer names\n        if module.__class__.__name__ in [\"TransformerEncoderLayer\", \"TransformerDecoderLayer\"]:\n            has_attention = True\n            break\n    return has_attention\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.TransformerHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get attention and FFN layers for monitoring.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get attention and FFN layers for monitoring.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # Look for MultiheadAttention modules\n    for module in model.modules():\n        if isinstance(module, nn.MultiheadAttention):\n            layers.append(module)\n\n    # Also look for Linear layers in feed-forward blocks\n    # Typically the first Linear in each transformer block's FFN\n    in_transformer_block = False\n    for name, module in model.named_modules():\n        if \"encoder\" in name.lower() or \"decoder\" in name.lower() or \"layer\" in name.lower():\n            in_transformer_block = True\n\n        if in_transformer_block and isinstance(module, nn.Linear):\n            # Add FFN layers (typically 2 per transformer block)\n            if \"fc1\" in name or \"fc2\" in name or \"ffn\" in name.lower():\n                layers.append(module)\n\n    # If no structured layers found, fall back to sampling Linear layers\n    if not layers:\n        linear_layers = [m for m in model.modules() if isinstance(m, nn.Linear)]\n        # Sample every 2nd Linear layer to avoid too many\n        layers = linear_layers[::2] if len(linear_layers) &gt; 10 else linear_layers\n\n    return layers\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.TransformerHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for Transformer layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for Transformer layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    attn_count = 0\n    ffn_count = 0\n    fc_count = 0\n\n    for layer in layers:\n        if isinstance(layer, nn.MultiheadAttention):\n            names.append(f\"Attention_{attn_count}\")\n            attn_count += 1\n        elif isinstance(layer, nn.Linear):\n            # Try to determine if it's an FFN layer\n            layer_name = None\n            for name, module in model.named_modules():\n                if module is layer:\n                    layer_name = name\n                    break\n\n            if layer_name and (\"fc\" in layer_name.lower() or \"ffn\" in layer_name.lower()):\n                names.append(f\"FFN_{ffn_count}_{layer.out_features}\")\n                ffn_count += 1\n            else:\n                names.append(f\"FC_{fc_count}_{layer.out_features}\")\n                fc_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.ViTHandler","title":"<code>ViTHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Vision Transformer architectures.</p> <p>Monitors patch embeddings, attention layers, and MLP blocks.</p>"},{"location":"api/ndt/architectures/#ndt.architectures.ViTHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/#ndt.architectures.ViTHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a Vision Transformer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a ViT</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a Vision Transformer.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a ViT\n    \"\"\"\n    # ViTs typically have both conv (for patch embedding) and attention\n    has_conv = False\n    has_attention = False\n    has_patch_embed = False\n\n    for name, module in model.named_modules():\n        if isinstance(module, (nn.Conv2d, nn.Conv1d)):\n            has_conv = True\n            if \"patch\" in name.lower() or \"embed\" in name.lower():\n                has_patch_embed = True\n        if isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n\n    # ViT has conv (patch embedding) + attention, or explicit patch embedding\n    return (has_conv and has_attention) or has_patch_embed\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.ViTHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get key layers to monitor in ViT.</p> <p>Monitors: patch embedding, attention layers, and MLP blocks.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get key layers to monitor in ViT.\n\n    Monitors: patch embedding, attention layers, and MLP blocks.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # 1. Patch embedding layer\n    for name, module in model.named_modules():\n        if isinstance(module, (nn.Conv2d, nn.Linear)):\n            if \"patch\" in name.lower() or \"embed\" in name.lower():\n                layers.append(module)\n                break  # Usually just one\n\n    # 2. Attention layers\n    attn_layers = [m for m in model.modules() if isinstance(m, nn.MultiheadAttention)]\n    layers.extend(attn_layers)\n\n    # 3. MLP/FFN blocks (sample to avoid too many)\n    mlp_layers = []\n    for name, module in model.named_modules():\n        if isinstance(module, nn.Linear) and (\"mlp\" in name.lower() or \"ffn\" in name.lower()):\n            mlp_layers.append(module)\n\n    # Sample MLP layers if too many (keep first Linear of each MLP block)\n    if len(mlp_layers) &gt; 10:\n        mlp_layers = mlp_layers[::2]\n\n    layers.extend(mlp_layers)\n\n    return layers\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.ViTHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for ViT layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for ViT layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    patch_count = 0\n    attn_count = 0\n    mlp_count = 0\n    other_count = 0\n\n    for layer in layers:\n        # Identify layer type from model structure\n        layer_name = None\n        for name, module in model.named_modules():\n            if module is layer:\n                layer_name = name\n                break\n\n        if layer_name:\n            if \"patch\" in layer_name.lower() or \"embed\" in layer_name.lower():\n                names.append(f\"PatchEmbed_{patch_count}\")\n                patch_count += 1\n            elif isinstance(layer, nn.MultiheadAttention):\n                names.append(f\"Attention_{attn_count}\")\n                attn_count += 1\n            elif \"mlp\" in layer_name.lower() or \"ffn\" in layer_name.lower():\n                names.append(f\"MLP_{mlp_count}\")\n                mlp_count += 1\n            else:\n                names.append(f\"Layer_{other_count}\")\n                other_count += 1\n        else:\n            # Fallback naming\n            if isinstance(layer, nn.MultiheadAttention):\n                names.append(f\"Attention_{attn_count}\")\n                attn_count += 1\n            else:\n                names.append(f\"Layer_{other_count}\")\n                other_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures-functions","title":"Functions:","text":""},{"location":"api/ndt/architectures/#ndt.architectures.detect_architecture","title":"<code>detect_architecture(model: nn.Module) -&gt; Optional[ArchitectureHandler]</code>","text":"<p>Automatically detect the architecture type of a model.</p> <p>Tries each handler in priority order and returns the first match.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>Optional[ArchitectureHandler]</code> <p>ArchitectureHandler instance if detected, None otherwise</p> Example <p>import torch.nn as nn model = nn.Sequential(nn.Linear(784, 512), nn.ReLU(), nn.Linear(512, 10)) handler = detect_architecture(model) print(handler.get_name()) 'MLP'</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def detect_architecture(model: nn.Module) -&gt; Optional[ArchitectureHandler]:\n    \"\"\"Automatically detect the architecture type of a model.\n\n    Tries each handler in priority order and returns the first match.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        ArchitectureHandler instance if detected, None otherwise\n\n    Example:\n        &gt;&gt;&gt; import torch.nn as nn\n        &gt;&gt;&gt; model = nn.Sequential(nn.Linear(784, 512), nn.ReLU(), nn.Linear(512, 10))\n        &gt;&gt;&gt; handler = detect_architecture(model)\n        &gt;&gt;&gt; print(handler.get_name())\n        'MLP'\n    \"\"\"\n    for handler in DEFAULT_HANDLERS:\n        if handler.validate_model(model):\n            return handler\n    return None\n</code></pre>"},{"location":"api/ndt/architectures/#ndt.architectures.get_handler","title":"<code>get_handler(model: nn.Module, architecture: Optional[str] = None) -&gt; ArchitectureHandler</code>","text":"<p>Get an architecture handler for a model.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>architecture</code> <code>Optional[str]</code> <p>Optional architecture name (\"mlp\", \"cnn\", \"transformer\", \"vit\").          If None, auto-detects.</p> <code>None</code> <p>Returns:</p> Type Description <code>ArchitectureHandler</code> <p>ArchitectureHandler instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If architecture cannot be detected or invalid name provided</p> Example <p>handler = get_handler(model, architecture=\"mlp\") layers = handler.get_activation_layers(model)</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def get_handler(model: nn.Module, architecture: Optional[str] = None) -&gt; ArchitectureHandler:\n    \"\"\"Get an architecture handler for a model.\n\n    Args:\n        model: The neural network model\n        architecture: Optional architecture name (\"mlp\", \"cnn\", \"transformer\", \"vit\").\n                     If None, auto-detects.\n\n    Returns:\n        ArchitectureHandler instance\n\n    Raises:\n        ValueError: If architecture cannot be detected or invalid name provided\n\n    Example:\n        &gt;&gt;&gt; handler = get_handler(model, architecture=\"mlp\")\n        &gt;&gt;&gt; layers = handler.get_activation_layers(model)\n    \"\"\"\n    if architecture is not None:\n        # Explicit architecture specified\n        architecture = architecture.lower()\n        handler_map = {\n            \"mlp\": MLPHandler(),\n            \"cnn\": CNNHandler(),\n            \"transformer\": TransformerHandler(),\n            \"vit\": ViTHandler(),\n        }\n\n        if architecture not in handler_map:\n            raise ValueError(\n                f\"Unknown architecture: {architecture}. Must be one of {list(handler_map.keys())}\"\n            )\n\n        handler = handler_map[architecture]\n\n        # Validate that the model actually matches\n        if not handler.validate_model(model):\n            raise ValueError(\n                f\"Model does not appear to be a {architecture.upper()}. \"\n                \"Try auto-detection by not specifying architecture.\"\n            )\n\n        return handler\n\n    # Auto-detect architecture\n    handler = detect_architecture(model)\n    if handler is None:\n        raise ValueError(\n            \"Could not auto-detect model architecture. \"\n            \"Please specify architecture explicitly: 'mlp', 'cnn', 'transformer', or 'vit'\"\n        )\n\n    return handler\n</code></pre>"},{"location":"api/ndt/architectures/base/","title":"Base","text":""},{"location":"api/ndt/architectures/base/#ndt.architectures.base","title":"<code>ndt.architectures.base</code>","text":"<p>Abstract base class for architecture-specific handlers.</p>"},{"location":"api/ndt/architectures/base/#ndt.architectures.base-classes","title":"Classes","text":""},{"location":"api/ndt/architectures/base/#ndt.architectures.base.ArchitectureHandler","title":"<code>ArchitectureHandler</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for architecture-specific dimensionality tracking.</p> <p>Each architecture handler implements methods to identify relevant layers and extract activations appropriately for that architecture type.</p>"},{"location":"api/ndt/architectures/base/#ndt.architectures.base.ArchitectureHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/base/#ndt.architectures.base.ArchitectureHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>  <code>abstractmethod</code>","text":"<p>Check if model matches this handler's architecture.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model is compatible with this handler</p> Source code in <code>src/ndt/architectures/base.py</code> <pre><code>@abstractmethod\ndef validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model matches this handler's architecture.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model is compatible with this handler\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/ndt/architectures/base/#ndt.architectures.base.ArchitectureHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>  <code>abstractmethod</code>","text":"<p>Return list of layers to monitor for dimensionality.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/base.py</code> <pre><code>@abstractmethod\ndef get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Return list of layers to monitor for dimensionality.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/ndt/architectures/base/#ndt.architectures.base.ArchitectureHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>  <code>abstractmethod</code>","text":"<p>Generate descriptive names for the layers to monitor.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names corresponding to activation layers</p> Source code in <code>src/ndt/architectures/base.py</code> <pre><code>@abstractmethod\ndef get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate descriptive names for the layers to monitor.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names corresponding to activation layers\n    \"\"\"\n    pass\n</code></pre>"},{"location":"api/ndt/architectures/base/#ndt.architectures.base.ArchitectureHandler.get_name","title":"<code>get_name() -&gt; str</code>","text":"<p>Get the name of this architecture handler.</p> <p>Returns:</p> Type Description <code>str</code> <p>Architecture name (e.g., \"MLP\", \"CNN\", \"Transformer\")</p> Source code in <code>src/ndt/architectures/base.py</code> <pre><code>def get_name(self) -&gt; str:\n    \"\"\"Get the name of this architecture handler.\n\n    Returns:\n        Architecture name (e.g., \"MLP\", \"CNN\", \"Transformer\")\n    \"\"\"\n    return self.__class__.__name__.replace(\"Handler\", \"\")\n</code></pre>"},{"location":"api/ndt/architectures/cnn/","title":"Cnn","text":""},{"location":"api/ndt/architectures/cnn/#ndt.architectures.cnn","title":"<code>ndt.architectures.cnn</code>","text":"<p>Handler for Convolutional Neural Network (CNN) architectures.</p>"},{"location":"api/ndt/architectures/cnn/#ndt.architectures.cnn-classes","title":"Classes","text":""},{"location":"api/ndt/architectures/cnn/#ndt.architectures.cnn.CNNHandler","title":"<code>CNNHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Convolutional Neural Networks (CNNs).</p> <p>Monitors Conv2d layers and the first Linear layer (pre-classifier). Supports both simple CNNs and ResNet-style architectures.</p>"},{"location":"api/ndt/architectures/cnn/#ndt.architectures.cnn.CNNHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/cnn/#ndt.architectures.cnn.CNNHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a CNN (contains Conv layers).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a CNN</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a CNN (contains Conv layers).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a CNN\n    \"\"\"\n    has_conv = False\n    for module in model.modules():\n        if isinstance(module, (nn.Conv1d, nn.Conv2d, nn.Conv3d)):\n            has_conv = True\n            break\n    return has_conv\n</code></pre>"},{"location":"api/ndt/architectures/cnn/#ndt.architectures.cnn.CNNHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get Conv2d and key Linear layers for monitoring.</p> <p>For ResNets, monitors after each residual block. For simple CNNs, monitors each conv layer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get Conv2d and key Linear layers for monitoring.\n\n    For ResNets, monitors after each residual block.\n    For simple CNNs, monitors each conv layer.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # Check if it's a ResNet-style architecture\n    if self._is_resnet(model):\n        layers = self._get_resnet_layers(model)\n    else:\n        # Simple CNN: monitor all conv layers\n        for module in model.modules():\n            if isinstance(module, nn.Conv2d):\n                layers.append(module)\n\n        # Also monitor first Linear layer (before classifier)\n        linear_layers = [m for m in model.modules() if isinstance(m, nn.Linear)]\n        if linear_layers:\n            layers.append(linear_layers[0])\n\n    return layers\n</code></pre>"},{"location":"api/ndt/architectures/cnn/#ndt.architectures.cnn.CNNHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate descriptive names for CNN layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/cnn.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate descriptive names for CNN layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    conv_count = 0\n    linear_count = 0\n\n    for layer in layers:\n        if isinstance(layer, nn.Conv2d):\n            names.append(f\"Conv_{conv_count}_{layer.out_channels}ch\")\n            conv_count += 1\n        elif isinstance(layer, nn.Linear):\n            names.append(f\"FC_{linear_count}_{layer.out_features}\")\n            linear_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/architectures/mlp/","title":"Mlp","text":""},{"location":"api/ndt/architectures/mlp/#ndt.architectures.mlp","title":"<code>ndt.architectures.mlp</code>","text":"<p>Handler for Multi-Layer Perceptron (MLP) architectures.</p>"},{"location":"api/ndt/architectures/mlp/#ndt.architectures.mlp-classes","title":"Classes","text":""},{"location":"api/ndt/architectures/mlp/#ndt.architectures.mlp.MLPHandler","title":"<code>MLPHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for fully-connected (MLP) networks.</p> <p>Monitors all Linear layers in the network.</p>"},{"location":"api/ndt/architectures/mlp/#ndt.architectures.mlp.MLPHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/mlp/#ndt.architectures.mlp.MLPHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is an MLP (contains Linear layers, no Conv/Attention).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be an MLP</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is an MLP (contains Linear layers, no Conv/Attention).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be an MLP\n    \"\"\"\n    has_linear = False\n    has_conv = False\n    has_attention = False\n\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            has_linear = True\n        elif isinstance(module, (nn.Conv1d, nn.Conv2d, nn.Conv3d)):\n            has_conv = True\n        elif isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n\n    # MLP should have Linear layers but not Conv or Attention\n    return has_linear and not has_conv and not has_attention\n</code></pre>"},{"location":"api/ndt/architectures/mlp/#ndt.architectures.mlp.MLPHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get all Linear layers for monitoring.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of Linear layer modules</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get all Linear layers for monitoring.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of Linear layer modules\n    \"\"\"\n    layers = []\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            layers.append(module)\n    return layers\n</code></pre>"},{"location":"api/ndt/architectures/mlp/#ndt.architectures.mlp.MLPHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for Linear layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names like \"Linear_0\", \"Linear_1\", etc.</p> Source code in <code>src/ndt/architectures/mlp.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for Linear layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names like \"Linear_0\", \"Linear_1\", etc.\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    for i, layer in enumerate(layers):\n        in_features = layer.in_features\n        out_features = layer.out_features\n        names.append(f\"Linear_{i}_{in_features}x{out_features}\")\n\n    return names\n</code></pre>"},{"location":"api/ndt/architectures/registry/","title":"Registry","text":""},{"location":"api/ndt/architectures/registry/#ndt.architectures.registry","title":"<code>ndt.architectures.registry</code>","text":"<p>Registry for auto-detecting and selecting architecture handlers.</p>"},{"location":"api/ndt/architectures/registry/#ndt.architectures.registry-classes","title":"Classes","text":""},{"location":"api/ndt/architectures/registry/#ndt.architectures.registry-functions","title":"Functions:","text":""},{"location":"api/ndt/architectures/registry/#ndt.architectures.registry.detect_architecture","title":"<code>detect_architecture(model: nn.Module) -&gt; Optional[ArchitectureHandler]</code>","text":"<p>Automatically detect the architecture type of a model.</p> <p>Tries each handler in priority order and returns the first match.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>Optional[ArchitectureHandler]</code> <p>ArchitectureHandler instance if detected, None otherwise</p> Example <p>import torch.nn as nn model = nn.Sequential(nn.Linear(784, 512), nn.ReLU(), nn.Linear(512, 10)) handler = detect_architecture(model) print(handler.get_name()) 'MLP'</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def detect_architecture(model: nn.Module) -&gt; Optional[ArchitectureHandler]:\n    \"\"\"Automatically detect the architecture type of a model.\n\n    Tries each handler in priority order and returns the first match.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        ArchitectureHandler instance if detected, None otherwise\n\n    Example:\n        &gt;&gt;&gt; import torch.nn as nn\n        &gt;&gt;&gt; model = nn.Sequential(nn.Linear(784, 512), nn.ReLU(), nn.Linear(512, 10))\n        &gt;&gt;&gt; handler = detect_architecture(model)\n        &gt;&gt;&gt; print(handler.get_name())\n        'MLP'\n    \"\"\"\n    for handler in DEFAULT_HANDLERS:\n        if handler.validate_model(model):\n            return handler\n    return None\n</code></pre>"},{"location":"api/ndt/architectures/registry/#ndt.architectures.registry.get_handler","title":"<code>get_handler(model: nn.Module, architecture: Optional[str] = None) -&gt; ArchitectureHandler</code>","text":"<p>Get an architecture handler for a model.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>architecture</code> <code>Optional[str]</code> <p>Optional architecture name (\"mlp\", \"cnn\", \"transformer\", \"vit\").          If None, auto-detects.</p> <code>None</code> <p>Returns:</p> Type Description <code>ArchitectureHandler</code> <p>ArchitectureHandler instance</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If architecture cannot be detected or invalid name provided</p> Example <p>handler = get_handler(model, architecture=\"mlp\") layers = handler.get_activation_layers(model)</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def get_handler(model: nn.Module, architecture: Optional[str] = None) -&gt; ArchitectureHandler:\n    \"\"\"Get an architecture handler for a model.\n\n    Args:\n        model: The neural network model\n        architecture: Optional architecture name (\"mlp\", \"cnn\", \"transformer\", \"vit\").\n                     If None, auto-detects.\n\n    Returns:\n        ArchitectureHandler instance\n\n    Raises:\n        ValueError: If architecture cannot be detected or invalid name provided\n\n    Example:\n        &gt;&gt;&gt; handler = get_handler(model, architecture=\"mlp\")\n        &gt;&gt;&gt; layers = handler.get_activation_layers(model)\n    \"\"\"\n    if architecture is not None:\n        # Explicit architecture specified\n        architecture = architecture.lower()\n        handler_map = {\n            \"mlp\": MLPHandler(),\n            \"cnn\": CNNHandler(),\n            \"transformer\": TransformerHandler(),\n            \"vit\": ViTHandler(),\n        }\n\n        if architecture not in handler_map:\n            raise ValueError(\n                f\"Unknown architecture: {architecture}. Must be one of {list(handler_map.keys())}\"\n            )\n\n        handler = handler_map[architecture]\n\n        # Validate that the model actually matches\n        if not handler.validate_model(model):\n            raise ValueError(\n                f\"Model does not appear to be a {architecture.upper()}. \"\n                \"Try auto-detection by not specifying architecture.\"\n            )\n\n        return handler\n\n    # Auto-detect architecture\n    handler = detect_architecture(model)\n    if handler is None:\n        raise ValueError(\n            \"Could not auto-detect model architecture. \"\n            \"Please specify architecture explicitly: 'mlp', 'cnn', 'transformer', or 'vit'\"\n        )\n\n    return handler\n</code></pre>"},{"location":"api/ndt/architectures/registry/#ndt.architectures.registry.get_layers_for_model","title":"<code>get_layers_for_model(model: nn.Module, architecture: Optional[str] = None) -&gt; Tuple[List[nn.Module], List[str]]</code>","text":"<p>Convenience function to get layers and names for a model.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>architecture</code> <code>Optional[str]</code> <p>Optional architecture name for explicit selection</p> <code>None</code> <p>Returns:</p> Type Description <code>Tuple[List[Module], List[str]]</code> <p>Tuple of (layers, layer_names)</p> Example <p>layers, names = get_layers_for_model(model) print(f\"Monitoring {len(layers)} layers: {names}\")</p> Source code in <code>src/ndt/architectures/registry.py</code> <pre><code>def get_layers_for_model(\n    model: nn.Module, architecture: Optional[str] = None\n) -&gt; Tuple[List[nn.Module], List[str]]:\n    \"\"\"Convenience function to get layers and names for a model.\n\n    Args:\n        model: The neural network model\n        architecture: Optional architecture name for explicit selection\n\n    Returns:\n        Tuple of (layers, layer_names)\n\n    Example:\n        &gt;&gt;&gt; layers, names = get_layers_for_model(model)\n        &gt;&gt;&gt; print(f\"Monitoring {len(layers)} layers: {names}\")\n    \"\"\"\n    handler = get_handler(model, architecture)\n    layers = handler.get_activation_layers(model)\n    names = handler.get_layer_names(model)\n    return layers, names\n</code></pre>"},{"location":"api/ndt/architectures/transformer/","title":"Transformer","text":""},{"location":"api/ndt/architectures/transformer/#ndt.architectures.transformer","title":"<code>ndt.architectures.transformer</code>","text":"<p>Handler for Transformer architectures (BERT, GPT, etc.).</p>"},{"location":"api/ndt/architectures/transformer/#ndt.architectures.transformer-classes","title":"Classes","text":""},{"location":"api/ndt/architectures/transformer/#ndt.architectures.transformer.TransformerHandler","title":"<code>TransformerHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Transformer-based architectures.</p> <p>Monitors attention outputs and feed-forward network outputs in each layer. Compatible with BERT, GPT, and similar transformer models.</p>"},{"location":"api/ndt/architectures/transformer/#ndt.architectures.transformer.TransformerHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/transformer/#ndt.architectures.transformer.TransformerHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a Transformer (contains MultiheadAttention).</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a Transformer</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a Transformer (contains MultiheadAttention).\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a Transformer\n    \"\"\"\n    has_attention = False\n    for module in model.modules():\n        if isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n            break\n        # Also check for common transformer layer names\n        if module.__class__.__name__ in [\"TransformerEncoderLayer\", \"TransformerDecoderLayer\"]:\n            has_attention = True\n            break\n    return has_attention\n</code></pre>"},{"location":"api/ndt/architectures/transformer/#ndt.architectures.transformer.TransformerHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get attention and FFN layers for monitoring.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get attention and FFN layers for monitoring.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # Look for MultiheadAttention modules\n    for module in model.modules():\n        if isinstance(module, nn.MultiheadAttention):\n            layers.append(module)\n\n    # Also look for Linear layers in feed-forward blocks\n    # Typically the first Linear in each transformer block's FFN\n    in_transformer_block = False\n    for name, module in model.named_modules():\n        if \"encoder\" in name.lower() or \"decoder\" in name.lower() or \"layer\" in name.lower():\n            in_transformer_block = True\n\n        if in_transformer_block and isinstance(module, nn.Linear):\n            # Add FFN layers (typically 2 per transformer block)\n            if \"fc1\" in name or \"fc2\" in name or \"ffn\" in name.lower():\n                layers.append(module)\n\n    # If no structured layers found, fall back to sampling Linear layers\n    if not layers:\n        linear_layers = [m for m in model.modules() if isinstance(m, nn.Linear)]\n        # Sample every 2nd Linear layer to avoid too many\n        layers = linear_layers[::2] if len(linear_layers) &gt; 10 else linear_layers\n\n    return layers\n</code></pre>"},{"location":"api/ndt/architectures/transformer/#ndt.architectures.transformer.TransformerHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for Transformer layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/transformer.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for Transformer layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    attn_count = 0\n    ffn_count = 0\n    fc_count = 0\n\n    for layer in layers:\n        if isinstance(layer, nn.MultiheadAttention):\n            names.append(f\"Attention_{attn_count}\")\n            attn_count += 1\n        elif isinstance(layer, nn.Linear):\n            # Try to determine if it's an FFN layer\n            layer_name = None\n            for name, module in model.named_modules():\n                if module is layer:\n                    layer_name = name\n                    break\n\n            if layer_name and (\"fc\" in layer_name.lower() or \"ffn\" in layer_name.lower()):\n                names.append(f\"FFN_{ffn_count}_{layer.out_features}\")\n                ffn_count += 1\n            else:\n                names.append(f\"FC_{fc_count}_{layer.out_features}\")\n                fc_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/architectures/vit/","title":"Vit","text":""},{"location":"api/ndt/architectures/vit/#ndt.architectures.vit","title":"<code>ndt.architectures.vit</code>","text":"<p>Handler for Vision Transformer (ViT) architectures.</p>"},{"location":"api/ndt/architectures/vit/#ndt.architectures.vit-classes","title":"Classes","text":""},{"location":"api/ndt/architectures/vit/#ndt.architectures.vit.ViTHandler","title":"<code>ViTHandler</code>","text":"<p>               Bases: <code>ArchitectureHandler</code></p> <p>Handler for Vision Transformer architectures.</p> <p>Monitors patch embeddings, attention layers, and MLP blocks.</p>"},{"location":"api/ndt/architectures/vit/#ndt.architectures.vit.ViTHandler-functions","title":"Methods:","text":""},{"location":"api/ndt/architectures/vit/#ndt.architectures.vit.ViTHandler.validate_model","title":"<code>validate_model(model: nn.Module) -&gt; bool</code>","text":"<p>Check if model is a Vision Transformer.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if model appears to be a ViT</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def validate_model(self, model: nn.Module) -&gt; bool:\n    \"\"\"Check if model is a Vision Transformer.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        True if model appears to be a ViT\n    \"\"\"\n    # ViTs typically have both conv (for patch embedding) and attention\n    has_conv = False\n    has_attention = False\n    has_patch_embed = False\n\n    for name, module in model.named_modules():\n        if isinstance(module, (nn.Conv2d, nn.Conv1d)):\n            has_conv = True\n            if \"patch\" in name.lower() or \"embed\" in name.lower():\n                has_patch_embed = True\n        if isinstance(module, nn.MultiheadAttention):\n            has_attention = True\n\n    # ViT has conv (patch embedding) + attention, or explicit patch embedding\n    return (has_conv and has_attention) or has_patch_embed\n</code></pre>"},{"location":"api/ndt/architectures/vit/#ndt.architectures.vit.ViTHandler.get_activation_layers","title":"<code>get_activation_layers(model: nn.Module) -&gt; List[nn.Module]</code>","text":"<p>Get key layers to monitor in ViT.</p> <p>Monitors: patch embedding, attention layers, and MLP blocks.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[Module]</code> <p>List of layer modules to track</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def get_activation_layers(self, model: nn.Module) -&gt; List[nn.Module]:\n    \"\"\"Get key layers to monitor in ViT.\n\n    Monitors: patch embedding, attention layers, and MLP blocks.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer modules to track\n    \"\"\"\n    layers = []\n\n    # 1. Patch embedding layer\n    for name, module in model.named_modules():\n        if isinstance(module, (nn.Conv2d, nn.Linear)):\n            if \"patch\" in name.lower() or \"embed\" in name.lower():\n                layers.append(module)\n                break  # Usually just one\n\n    # 2. Attention layers\n    attn_layers = [m for m in model.modules() if isinstance(m, nn.MultiheadAttention)]\n    layers.extend(attn_layers)\n\n    # 3. MLP/FFN blocks (sample to avoid too many)\n    mlp_layers = []\n    for name, module in model.named_modules():\n        if isinstance(module, nn.Linear) and (\"mlp\" in name.lower() or \"ffn\" in name.lower()):\n            mlp_layers.append(module)\n\n    # Sample MLP layers if too many (keep first Linear of each MLP block)\n    if len(mlp_layers) &gt; 10:\n        mlp_layers = mlp_layers[::2]\n\n    layers.extend(mlp_layers)\n\n    return layers\n</code></pre>"},{"location":"api/ndt/architectures/vit/#ndt.architectures.vit.ViTHandler.get_layer_names","title":"<code>get_layer_names(model: nn.Module) -&gt; List[str]</code>","text":"<p>Generate names for ViT layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <p>Returns:</p> Type Description <code>List[str]</code> <p>List of layer names</p> Source code in <code>src/ndt/architectures/vit.py</code> <pre><code>def get_layer_names(self, model: nn.Module) -&gt; List[str]:\n    \"\"\"Generate names for ViT layers.\n\n    Args:\n        model: The neural network model\n\n    Returns:\n        List of layer names\n    \"\"\"\n    layers = self.get_activation_layers(model)\n    names = []\n\n    patch_count = 0\n    attn_count = 0\n    mlp_count = 0\n    other_count = 0\n\n    for layer in layers:\n        # Identify layer type from model structure\n        layer_name = None\n        for name, module in model.named_modules():\n            if module is layer:\n                layer_name = name\n                break\n\n        if layer_name:\n            if \"patch\" in layer_name.lower() or \"embed\" in layer_name.lower():\n                names.append(f\"PatchEmbed_{patch_count}\")\n                patch_count += 1\n            elif isinstance(layer, nn.MultiheadAttention):\n                names.append(f\"Attention_{attn_count}\")\n                attn_count += 1\n            elif \"mlp\" in layer_name.lower() or \"ffn\" in layer_name.lower():\n                names.append(f\"MLP_{mlp_count}\")\n                mlp_count += 1\n            else:\n                names.append(f\"Layer_{other_count}\")\n                other_count += 1\n        else:\n            # Fallback naming\n            if isinstance(layer, nn.MultiheadAttention):\n                names.append(f\"Attention_{attn_count}\")\n                attn_count += 1\n            else:\n                names.append(f\"Layer_{other_count}\")\n                other_count += 1\n\n    return names\n</code></pre>"},{"location":"api/ndt/core/","title":"Index","text":""},{"location":"api/ndt/core/#ndt.core","title":"<code>ndt.core</code>","text":"<p>Core functionality for neural dimensionality tracking.</p>"},{"location":"api/ndt/core/#ndt.core-classes","title":"Classes","text":""},{"location":"api/ndt/core/#ndt.core.ActivationCapture","title":"<code>ActivationCapture()</code>","text":"<p>Captures activations from specified layers using forward hooks.</p> <p>This class manages PyTorch forward hooks to capture activations during the forward pass without modifying the model code.</p> <p>Attributes:</p> Name Type Description <code>activations</code> <code>Dict[str, Tensor]</code> <p>Dictionary mapping layer names to captured activations</p> <code>hooks</code> <code>List[RemovableHandle]</code> <p>List of registered hook handles</p> Example <p>import torch.nn as nn model = nn.Sequential(nn.Linear(10, 20), nn.ReLU(), nn.Linear(20, 10)) capture = ActivationCapture() capture.register_hooks(model, [model[0], model[2]]) output = model(torch.randn(32, 10)) print(capture.activations.keys()) capture.remove_hooks()</p> <p>Initialize the activation capture.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialize the activation capture.\"\"\"\n    self.activations: Dict[str, torch.Tensor] = {}\n    self.hooks: List[torch.utils.hooks.RemovableHandle] = []\n    self._layer_names: Dict[nn.Module, str] = {}\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.ActivationCapture-functions","title":"Methods:","text":""},{"location":"api/ndt/core/#ndt.core.ActivationCapture.register_hooks","title":"<code>register_hooks(model: nn.Module, layers: List[nn.Module], layer_names: Optional[List[str]] = None) -&gt; None</code>","text":"<p>Register forward hooks on specified layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>layers</code> <code>List[Module]</code> <p>List of layer modules to monitor</p> required <code>layer_names</code> <code>Optional[List[str]]</code> <p>Optional custom names for layers. If None, uses module class names.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If number of layer names doesn't match number of layers</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def register_hooks(\n    self, model: nn.Module, layers: List[nn.Module], layer_names: Optional[List[str]] = None\n) -&gt; None:\n    \"\"\"Register forward hooks on specified layers.\n\n    Args:\n        model: The neural network model\n        layers: List of layer modules to monitor\n        layer_names: Optional custom names for layers. If None, uses module class names.\n\n    Raises:\n        ValueError: If number of layer names doesn't match number of layers\n    \"\"\"\n    if layer_names is not None and len(layer_names) != len(layers):\n        raise ValueError(\n            f\"Number of layer names ({len(layer_names)}) must match \"\n            f\"number of layers ({len(layers)})\"\n        )\n\n    # Generate default names if not provided\n    if layer_names is None:\n        layer_names = [f\"{layer.__class__.__name__}_{i}\" for i, layer in enumerate(layers)]\n\n    # Register hooks\n    for layer, name in zip(layers, layer_names):\n        hook_handle = layer.register_forward_hook(self._create_hook(name))\n        self.hooks.append(hook_handle)\n        self._layer_names[layer] = name\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.ActivationCapture.remove_hooks","title":"<code>remove_hooks() -&gt; None</code>","text":"<p>Remove all registered hooks and clear stored activations.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def remove_hooks(self) -&gt; None:\n    \"\"\"Remove all registered hooks and clear stored activations.\"\"\"\n    for hook in self.hooks:\n        hook.remove()\n    self.hooks.clear()\n    self.activations.clear()\n    self._layer_names.clear()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.ActivationCapture.clear_activations","title":"<code>clear_activations() -&gt; None</code>","text":"<p>Clear stored activations without removing hooks.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def clear_activations(self) -&gt; None:\n    \"\"\"Clear stored activations without removing hooks.\"\"\"\n    self.activations.clear()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.ActivationCapture.get_activation","title":"<code>get_activation(layer_name: str) -&gt; Optional[torch.Tensor]</code>","text":"<p>Get activation for a specific layer.</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>str</code> <p>Name of the layer</p> required <p>Returns:</p> Type Description <code>Optional[Tensor]</code> <p>Activation tensor if available, None otherwise</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def get_activation(self, layer_name: str) -&gt; Optional[torch.Tensor]:\n    \"\"\"Get activation for a specific layer.\n\n    Args:\n        layer_name: Name of the layer\n\n    Returns:\n        Activation tensor if available, None otherwise\n    \"\"\"\n    return self.activations.get(layer_name)\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.ActivationCapture.get_all_activations","title":"<code>get_all_activations() -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Get all captured activations.</p> <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>Dictionary mapping layer names to activation tensors</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def get_all_activations(self) -&gt; Dict[str, torch.Tensor]:\n    \"\"\"Get all captured activations.\n\n    Returns:\n        Dictionary mapping layer names to activation tensors\n    \"\"\"\n    return self.activations.copy()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.ActivationCapture.__enter__","title":"<code>__enter__() -&gt; ActivationCapture</code>","text":"<p>Context manager entry.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __enter__(self) -&gt; \"ActivationCapture\":\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.ActivationCapture.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb) -&gt; None</code>","text":"<p>Context manager exit - removes hooks.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb) -&gt; None:\n    \"\"\"Context manager exit - removes hooks.\"\"\"\n    self.remove_hooks()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.ActivationCapture.__del__","title":"<code>__del__() -&gt; None</code>","text":"<p>Cleanup hooks on deletion.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __del__(self) -&gt; None:\n    \"\"\"Cleanup hooks on deletion.\"\"\"\n    self.remove_hooks()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.JumpDetector","title":"<code>JumpDetector(window_size: int = 50, z_threshold: float = 3.0, min_samples: int = 20)</code>","text":"<p>Detects significant jumps in dimensionality metrics using Z-score analysis.</p> <p>This class uses a rolling window to compute mean and standard deviation, then identifies jumps that exceed a specified Z-score threshold.</p> <p>Attributes:</p> Name Type Description <code>window_size</code> <p>Size of the rolling window for statistics</p> <code>z_threshold</code> <p>Z-score threshold for jump detection</p> <code>min_samples</code> <p>Minimum number of samples before detecting jumps</p> Example <p>detector = JumpDetector(window_size=50, z_threshold=3.0) values = [10.0] * 100 + [20.0] * 100  # Simulated jump jumps = detector.detect_jumps(values, metric_name=\"stable_rank\") for jump in jumps: ...     print(jump)</p> <p>Initialize the jump detector.</p> <p>Parameters:</p> Name Type Description Default <code>window_size</code> <code>int</code> <p>Size of rolling window for computing statistics (default: 50)</p> <code>50</code> <code>z_threshold</code> <code>float</code> <p>Z-score threshold for detecting jumps (default: 3.0)</p> <code>3.0</code> <code>min_samples</code> <code>int</code> <p>Minimum samples before starting detection (default: 20)</p> <code>20</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If parameters are invalid</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def __init__(\n    self, window_size: int = 50, z_threshold: float = 3.0, min_samples: int = 20\n) -&gt; None:\n    \"\"\"Initialize the jump detector.\n\n    Args:\n        window_size: Size of rolling window for computing statistics (default: 50)\n        z_threshold: Z-score threshold for detecting jumps (default: 3.0)\n        min_samples: Minimum samples before starting detection (default: 20)\n\n    Raises:\n        ValueError: If parameters are invalid\n    \"\"\"\n    if window_size &lt; 2:\n        raise ValueError(f\"window_size must be &gt;= 2, got {window_size}\")\n    if z_threshold &lt;= 0:\n        raise ValueError(f\"z_threshold must be &gt; 0, got {z_threshold}\")\n    if min_samples &lt; 1:\n        raise ValueError(f\"min_samples must be &gt;= 1, got {min_samples}\")\n\n    self.window_size = window_size\n    self.z_threshold = z_threshold\n    self.min_samples = min_samples\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.JumpDetector-functions","title":"Methods:","text":""},{"location":"api/ndt/core/#ndt.core.JumpDetector.detect_jumps","title":"<code>detect_jumps(values: List[float], metric_name: str = 'dimensionality', step_offset: int = 0) -&gt; List[Jump]</code>","text":"<p>Detect jumps in a sequence of dimensionality values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <code>metric_name</code> <code>str</code> <p>Name of the metric being analyzed</p> <code>'dimensionality'</code> <code>step_offset</code> <code>int</code> <p>Offset to add to step numbers (for aligning with training steps)</p> <code>0</code> <p>Returns:</p> Type Description <code>List[Jump]</code> <p>List of detected Jump objects</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If values list is too short</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def detect_jumps(\n    self, values: List[float], metric_name: str = \"dimensionality\", step_offset: int = 0\n) -&gt; List[Jump]:\n    \"\"\"Detect jumps in a sequence of dimensionality values.\n\n    Args:\n        values: List of dimensionality measurements\n        metric_name: Name of the metric being analyzed\n        step_offset: Offset to add to step numbers (for aligning with training steps)\n\n    Returns:\n        List of detected Jump objects\n\n    Raises:\n        ValueError: If values list is too short\n    \"\"\"\n    if len(values) &lt; self.min_samples:\n        return []\n\n    values_array = np.array(values)\n    jumps = []\n\n    # Start detection after minimum samples\n    for i in range(self.min_samples, len(values_array)):\n        # Define window for statistics\n        window_start = max(0, i - self.window_size)\n        window = values_array[window_start:i]\n\n        # Compute statistics\n        mean = np.mean(window)\n        std = np.std(window)\n\n        # Avoid division by zero\n        if std &lt; 1e-10:\n            continue\n\n        # Compute z-score for current value\n        current_value = values_array[i]\n        z_score = abs((current_value - mean) / std)\n\n        # Detect jump\n        if z_score &gt; self.z_threshold:\n            jump = Jump(\n                step=i + step_offset,\n                z_score=z_score,\n                value_before=values_array[i - 1],\n                value_after=current_value,\n                metric_name=metric_name,\n            )\n            jumps.append(jump)\n\n    return jumps\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.JumpDetector.detect_jumps_with_direction","title":"<code>detect_jumps_with_direction(values: List[float], metric_name: str = 'dimensionality', step_offset: int = 0, direction: Optional[str] = None) -&gt; List[Jump]</code>","text":"<p>Detect jumps with optional direction filtering.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <code>metric_name</code> <code>str</code> <p>Name of the metric being analyzed</p> <code>'dimensionality'</code> <code>step_offset</code> <code>int</code> <p>Offset to add to step numbers</p> <code>0</code> <code>direction</code> <code>Optional[str]</code> <p>Filter by direction - \"up\", \"down\", or None for both</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Jump]</code> <p>List of detected Jump objects filtered by direction</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction is not valid</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def detect_jumps_with_direction(\n    self,\n    values: List[float],\n    metric_name: str = \"dimensionality\",\n    step_offset: int = 0,\n    direction: Optional[str] = None,\n) -&gt; List[Jump]:\n    \"\"\"Detect jumps with optional direction filtering.\n\n    Args:\n        values: List of dimensionality measurements\n        metric_name: Name of the metric being analyzed\n        step_offset: Offset to add to step numbers\n        direction: Filter by direction - \"up\", \"down\", or None for both\n\n    Returns:\n        List of detected Jump objects filtered by direction\n\n    Raises:\n        ValueError: If direction is not valid\n    \"\"\"\n    if direction not in [None, \"up\", \"down\"]:\n        raise ValueError(f\"direction must be None, 'up', or 'down', got {direction}\")\n\n    all_jumps = self.detect_jumps(values, metric_name, step_offset)\n\n    if direction is None:\n        return all_jumps\n\n    # Filter by direction\n    filtered_jumps = []\n    for jump in all_jumps:\n        change = jump.value_after - jump.value_before\n        if direction == \"up\" and change &gt; 0:\n            filtered_jumps.append(jump)\n        elif direction == \"down\" and change &lt; 0:\n            filtered_jumps.append(jump)\n\n    return filtered_jumps\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.JumpDetector.compute_rolling_statistics","title":"<code>compute_rolling_statistics(values: List[float]) -&gt; Tuple[np.ndarray, np.ndarray, np.ndarray]</code>","text":"<p>Compute rolling mean, std, and z-scores for visualization.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray, ndarray]</code> <p>Tuple of (means, stds, z_scores) as numpy arrays</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If values list is empty</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def compute_rolling_statistics(\n    self, values: List[float]\n) -&gt; Tuple[np.ndarray, np.ndarray, np.ndarray]:\n    \"\"\"Compute rolling mean, std, and z-scores for visualization.\n\n    Args:\n        values: List of dimensionality measurements\n\n    Returns:\n        Tuple of (means, stds, z_scores) as numpy arrays\n\n    Raises:\n        ValueError: If values list is empty\n    \"\"\"\n    if len(values) == 0:\n        raise ValueError(\"values list cannot be empty\")\n\n    values_array = np.array(values)\n    n = len(values_array)\n\n    means = np.zeros(n)\n    stds = np.zeros(n)\n    z_scores = np.zeros(n)\n\n    for i in range(self.min_samples, n):\n        window_start = max(0, i - self.window_size)\n        window = values_array[window_start:i]\n\n        means[i] = np.mean(window)\n        stds[i] = np.std(window)\n\n        if stds[i] &gt; 1e-10:\n            z_scores[i] = abs((values_array[i] - means[i]) / stds[i])\n\n    return means, stds, z_scores\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.DimensionalityMetrics","title":"<code>DimensionalityMetrics(step: int, stable_rank: float, participation_ratio: float, cumulative_90: int, nuclear_norm_ratio: float, loss: float, grad_norm: Optional[float] = None)</code>  <code>dataclass</code>","text":"<p>Container for dimensionality measurements at a single training step.</p> <p>Attributes:</p> Name Type Description <code>step</code> <code>int</code> <p>Training step number</p> <code>stable_rank</code> <code>float</code> <p>Stable rank estimate</p> <code>participation_ratio</code> <code>float</code> <p>Participation ratio estimate</p> <code>cumulative_90</code> <code>int</code> <p>Number of components for 90% energy</p> <code>nuclear_norm_ratio</code> <code>float</code> <p>Nuclear norm ratio estimate</p> <code>loss</code> <code>float</code> <p>Training loss at this step</p> <code>grad_norm</code> <code>Optional[float]</code> <p>Gradient norm at this step (optional)</p>"},{"location":"api/ndt/core/#ndt.core.DimensionalityMetrics-functions","title":"Methods:","text":""},{"location":"api/ndt/core/#ndt.core.DimensionalityMetrics.to_dict","title":"<code>to_dict() -&gt; Dict</code>","text":"<p>Convert to dictionary.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def to_dict(self) -&gt; Dict:\n    \"\"\"Convert to dictionary.\"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker","title":"<code>HighFrequencyTracker(model: nn.Module, layers: Optional[List[nn.Module]] = None, layer_names: Optional[List[str]] = None, sampling_frequency: int = 1, enable_jump_detection: bool = True, jump_window_size: int = 50, jump_z_threshold: float = 3.0, device: Optional[torch.device] = None)</code>","text":"<p>High-frequency tracker for neural network representational dimensionality.</p> <p>This class provides minimal-intrusion tracking of dimensionality metrics during neural network training. It automatically captures activations and computes multiple dimensionality estimates at specified intervals.</p> <p>Attributes:</p> Name Type Description <code>model</code> <p>The neural network model to track</p> <code>layers</code> <p>List of layers to monitor</p> <code>sampling_frequency</code> <p>Record metrics every N steps</p> <code>jump_detector</code> <p>Optional jump detector for identifying phase transitions</p> Example <p>import torch.nn as nn model = nn.Sequential( ...     nn.Linear(784, 512), nn.ReLU(), ...     nn.Linear(512, 256), nn.ReLU(), ...     nn.Linear(256, 10) ... ) tracker = HighFrequencyTracker(model, layers=[model[0], model[2], model[4]])</p> <p>Initialize the high-frequency tracker.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Neural network model to track</p> required <code>layers</code> <code>Optional[List[Module]]</code> <p>List of layers to monitor. If None, auto-detects based on architecture</p> <code>None</code> <code>layer_names</code> <code>Optional[List[str]]</code> <p>Optional custom names for layers</p> <code>None</code> <code>sampling_frequency</code> <code>int</code> <p>Record metrics every N steps (default: 1)</p> <code>1</code> <code>enable_jump_detection</code> <code>bool</code> <p>Whether to detect dimensionality jumps (default: True)</p> <code>True</code> <code>jump_window_size</code> <code>int</code> <p>Window size for jump detection (default: 50)</p> <code>50</code> <code>jump_z_threshold</code> <code>float</code> <p>Z-score threshold for jump detection (default: 3.0)</p> <code>3.0</code> <code>device</code> <code>Optional[device]</code> <p>Device for computations (default: model's device)</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If model or parameters are invalid</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __init__(\n    self,\n    model: nn.Module,\n    layers: Optional[List[nn.Module]] = None,\n    layer_names: Optional[List[str]] = None,\n    sampling_frequency: int = 1,\n    enable_jump_detection: bool = True,\n    jump_window_size: int = 50,\n    jump_z_threshold: float = 3.0,\n    device: Optional[torch.device] = None,\n) -&gt; None:\n    \"\"\"Initialize the high-frequency tracker.\n\n    Args:\n        model: Neural network model to track\n        layers: List of layers to monitor. If None, auto-detects based on architecture\n        layer_names: Optional custom names for layers\n        sampling_frequency: Record metrics every N steps (default: 1)\n        enable_jump_detection: Whether to detect dimensionality jumps (default: True)\n        jump_window_size: Window size for jump detection (default: 50)\n        jump_z_threshold: Z-score threshold for jump detection (default: 3.0)\n        device: Device for computations (default: model's device)\n\n    Raises:\n        ValueError: If model or parameters are invalid\n    \"\"\"\n    if not isinstance(model, nn.Module):\n        raise ValueError(\"model must be a torch.nn.Module\")\n\n    self.model = model\n    self.sampling_frequency = sampling_frequency\n    self.device = device or next(model.parameters()).device\n\n    # Auto-detect layers if not provided\n    if layers is None:\n        layers = self._auto_detect_layers()\n\n    if not layers:\n        raise ValueError(\n            \"No layers to track. Provide layers explicitly or use supported architecture.\"\n        )\n\n    # Set up activation capture\n    self.activation_capture = ActivationCapture()\n    self.activation_capture.register_hooks(model, layers, layer_names)\n    self.layer_names = layer_names or [\n        f\"{layer.__class__.__name__}_{i}\" for i, layer in enumerate(layers)\n    ]\n\n    # Initialize storage\n    self.metrics_history: Dict[str, List[DimensionalityMetrics]] = {\n        name: [] for name in self.layer_names\n    }\n    self.step_counter = 0\n\n    # Jump detection\n    self.enable_jump_detection = enable_jump_detection\n    if enable_jump_detection:\n        self.jump_detector = JumpDetector(\n            window_size=jump_window_size, z_threshold=jump_z_threshold\n        )\n    else:\n        self.jump_detector = None\n\n    # Logging\n    self.logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker--in-training-loop","title":"In training loop:","text":"<p>for step, (x, y) in enumerate(dataloader): ...     loss = train_step(model, x, y) ...     tracker.log(step, loss) results = tracker.get_results() tracker.close()</p>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker-functions","title":"Methods:","text":""},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker.log","title":"<code>log(step: int, loss: float, grad_norm: Optional[float] = None, force: bool = False) -&gt; None</code>","text":"<p>Log dimensionality metrics for the current step.</p> <p>This is the main method called during training. It captures activations and computes dimensionality metrics according to the sampling frequency.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>Current training step</p> required <code>loss</code> <code>float</code> <p>Training loss value</p> required <code>grad_norm</code> <code>Optional[float]</code> <p>Optional gradient norm</p> <code>None</code> <code>force</code> <code>bool</code> <p>Force logging even if not at sampling interval</p> <code>False</code> Example <p>for step, (x, y) in enumerate(dataloader): ...     optimizer.zero_grad() ...     output = model(x) ...     loss = criterion(output, y) ...     loss.backward() ...     optimizer.step() ...     tracker.log(step, loss.item())</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def log(\n    self, step: int, loss: float, grad_norm: Optional[float] = None, force: bool = False\n) -&gt; None:\n    \"\"\"Log dimensionality metrics for the current step.\n\n    This is the main method called during training. It captures activations\n    and computes dimensionality metrics according to the sampling frequency.\n\n    Args:\n        step: Current training step\n        loss: Training loss value\n        grad_norm: Optional gradient norm\n        force: Force logging even if not at sampling interval\n\n    Example:\n        &gt;&gt;&gt; for step, (x, y) in enumerate(dataloader):\n        ...     optimizer.zero_grad()\n        ...     output = model(x)\n        ...     loss = criterion(output, y)\n        ...     loss.backward()\n        ...     optimizer.step()\n        ...     tracker.log(step, loss.item())\n    \"\"\"\n    # Check if we should record this step\n    if not force and step % self.sampling_frequency != 0:\n        return\n\n    self.step_counter = step\n\n    # Activations are already captured by hooks during forward pass\n    # Now compute metrics for each layer\n    with torch.no_grad():\n        for layer_name in self.layer_names:\n            activation = self.activation_capture.get_activation(layer_name)\n\n            if activation is None:\n                self.logger.warning(\n                    f\"No activation captured for layer {layer_name} at step {step}\"\n                )\n                continue\n\n            try:\n                # Reshape activation to 2D matrix (batch_size, features)\n                matrix = self._prepare_activation_matrix(activation)\n\n                # Compute all dimensionality metrics\n                sr, pr, ce90, nnr = compute_all_metrics(matrix)\n\n                # Create metrics object\n                metrics = DimensionalityMetrics(\n                    step=step,\n                    stable_rank=sr,\n                    participation_ratio=pr,\n                    cumulative_90=ce90,\n                    nuclear_norm_ratio=nnr,\n                    loss=loss,\n                    grad_norm=grad_norm,\n                )\n\n                # Store metrics\n                self.metrics_history[layer_name].append(metrics)\n\n            except Exception as e:\n                self.logger.error(\n                    f\"Error computing metrics for {layer_name} at step {step}: {e}\"\n                )\n\n    # Clear activations for next step\n    self.activation_capture.clear_activations()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker.get_results","title":"<code>get_results(layer_name: Optional[str] = None) -&gt; Union[pd.DataFrame, Dict[str, pd.DataFrame]]</code>","text":"<p>Get tracked metrics as pandas DataFrame(s).</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>Optional[str]</code> <p>If specified, return results for that layer only.        If None, return dict of DataFrames for all layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Union[DataFrame, Dict[str, DataFrame]]</code> <p>DataFrame or dict of DataFrames containing metrics history</p> Example <p>results = tracker.get_results() for layer_name, df in results.items(): ...     print(f\"{layer_name}: {len(df)} measurements\")</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def get_results(\n    self, layer_name: Optional[str] = None\n) -&gt; Union[pd.DataFrame, Dict[str, pd.DataFrame]]:\n    \"\"\"Get tracked metrics as pandas DataFrame(s).\n\n    Args:\n        layer_name: If specified, return results for that layer only.\n                   If None, return dict of DataFrames for all layers.\n\n    Returns:\n        DataFrame or dict of DataFrames containing metrics history\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; for layer_name, df in results.items():\n        ...     print(f\"{layer_name}: {len(df)} measurements\")\n    \"\"\"\n    if layer_name is not None:\n        if layer_name not in self.metrics_history:\n            raise ValueError(f\"Unknown layer name: {layer_name}\")\n        return self._metrics_to_dataframe(self.metrics_history[layer_name])\n\n    return {\n        name: self._metrics_to_dataframe(metrics)\n        for name, metrics in self.metrics_history.items()\n    }\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker.detect_jumps","title":"<code>detect_jumps(layer_name: Optional[str] = None, metric: str = 'stable_rank') -&gt; Dict[str, List]</code>","text":"<p>Detect dimensionality jumps in tracked metrics.</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>Optional[str]</code> <p>If specified, detect jumps for that layer only.        If None, detect for all layers.</p> <code>None</code> <code>metric</code> <code>str</code> <p>Which metric to analyze (\"stable_rank\", \"participation_ratio\",    \"cumulative_90\", or \"nuclear_norm_ratio\")</p> <code>'stable_rank'</code> <p>Returns:</p> Type Description <code>Dict[str, List]</code> <p>Dictionary mapping layer names to lists of Jump objects</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If jump detection is disabled or metric name is invalid</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def detect_jumps(\n    self, layer_name: Optional[str] = None, metric: str = \"stable_rank\"\n) -&gt; Dict[str, List]:\n    \"\"\"Detect dimensionality jumps in tracked metrics.\n\n    Args:\n        layer_name: If specified, detect jumps for that layer only.\n                   If None, detect for all layers.\n        metric: Which metric to analyze (\"stable_rank\", \"participation_ratio\",\n               \"cumulative_90\", or \"nuclear_norm_ratio\")\n\n    Returns:\n        Dictionary mapping layer names to lists of Jump objects\n\n    Raises:\n        ValueError: If jump detection is disabled or metric name is invalid\n    \"\"\"\n    if not self.enable_jump_detection:\n        raise ValueError(\"Jump detection is disabled. Enable it during initialization.\")\n\n    valid_metrics = [\n        \"stable_rank\",\n        \"participation_ratio\",\n        \"cumulative_90\",\n        \"nuclear_norm_ratio\",\n    ]\n    if metric not in valid_metrics:\n        raise ValueError(f\"metric must be one of {valid_metrics}, got {metric}\")\n\n    layers_to_analyze = [layer_name] if layer_name else self.layer_names\n\n    jumps_dict = {}\n    for name in layers_to_analyze:\n        if name not in self.metrics_history:\n            continue\n\n        # Extract metric values\n        values = [getattr(m, metric) for m in self.metrics_history[name]]\n\n        # Detect jumps\n        jumps = self.jump_detector.detect_jumps(values, metric_name=f\"{name}_{metric}\")\n        jumps_dict[name] = jumps\n\n    return jumps_dict\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker.close","title":"<code>close() -&gt; None</code>","text":"<p>Clean up hooks and resources.</p> <p>Should be called when done tracking to free resources.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Clean up hooks and resources.\n\n    Should be called when done tracking to free resources.\n    \"\"\"\n    self.activation_capture.remove_hooks()\n    self.logger.info(f\"Tracker closed. Recorded {self.step_counter} steps.\")\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker.__enter__","title":"<code>__enter__() -&gt; HighFrequencyTracker</code>","text":"<p>Context manager entry.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __enter__(self) -&gt; \"HighFrequencyTracker\":\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb) -&gt; None</code>","text":"<p>Context manager exit.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb) -&gt; None:\n    \"\"\"Context manager exit.\"\"\"\n    self.close()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.HighFrequencyTracker.__del__","title":"<code>__del__() -&gt; None</code>","text":"<p>Cleanup on deletion.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __del__(self) -&gt; None:\n    \"\"\"Cleanup on deletion.\"\"\"\n    if hasattr(self, \"activation_capture\"):\n        self.activation_capture.remove_hooks()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core-functions","title":"Functions:","text":""},{"location":"api/ndt/core/#ndt.core.compute_all_metrics","title":"<code>compute_all_metrics(matrix: torch.Tensor, eps: float = 1e-10) -&gt; Tuple[float, float, int, float]</code>","text":"<p>Compute all four dimensionality metrics efficiently.</p> <p>This function computes SVD once and reuses it for all metrics, improving efficiency when all metrics are needed.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>Tuple[float, float, int, float]</code> <p>Tuple of (stable_rank, participation_ratio, cumulative_90, nuclear_norm_ratio)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or computations fail</p> Example <p>import torch A = torch.randn(100, 50) sr, pr, ce90, nnr = compute_all_metrics(A) print(f\"Metrics: SR={sr:.2f}, PR={pr:.2f}, CE90={ce90}, NNR={nnr:.2f}\")</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def compute_all_metrics(\n    matrix: torch.Tensor, eps: float = 1e-10\n) -&gt; Tuple[float, float, int, float]:\n    \"\"\"Compute all four dimensionality metrics efficiently.\n\n    This function computes SVD once and reuses it for all metrics,\n    improving efficiency when all metrics are needed.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Tuple of (stable_rank, participation_ratio, cumulative_90, nuclear_norm_ratio)\n\n    Raises:\n        ValueError: If matrix is not 2D or computations fail\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; sr, pr, ce90, nnr = compute_all_metrics(A)\n        &gt;&gt;&gt; print(f\"Metrics: SR={sr:.2f}, PR={pr:.2f}, CE90={ce90}, NNR={nnr:.2f}\")\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        # Compute SVD once for all metrics\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Compute norms for stable rank\n        frobenius_norm = torch.norm(matrix, p=\"fro\")\n        spectral_norm = singular_values[0]  # Largest singular value\n        sr = ((frobenius_norm**2) / (spectral_norm**2 + eps)).item()\n\n        # Participation ratio\n        sv_normalized = singular_values / (torch.sum(singular_values) + eps)\n        pr = (1.0 / (torch.sum(sv_normalized**2) + eps)).item()\n\n        # Cumulative energy 90%\n        explained_variance = singular_values**2\n        total_variance = torch.sum(explained_variance)\n        cumsum = torch.cumsum(explained_variance, dim=0)\n        cumsum_ratio = cumsum / total_variance\n        ce90 = torch.searchsorted(cumsum_ratio, 0.90).item() + 1\n        ce90 = min(ce90, len(singular_values))\n\n        # Nuclear norm ratio\n        nuclear_norm = torch.sum(singular_values)\n        nnr = (nuclear_norm / (singular_values[0] + eps)).item()\n\n        return sr, pr, ce90, nnr\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.cumulative_energy_90","title":"<code>cumulative_energy_90(matrix: torch.Tensor, threshold: float = 0.9) -&gt; int</code>","text":"<p>Compute number of components needed for 90% cumulative energy.</p> <p>This metric counts how many principal components are needed to capture a given percentage (default 90%) of the total variance.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>threshold</code> <code>float</code> <p>Energy threshold (default: 0.90 for 90%)</p> <code>0.9</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of components needed (int)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D, threshold invalid, or SVD fails</p> Example <p>import torch A = torch.randn(100, 50) n_components = cumulative_energy_90(A) print(f\"Components for 90% energy: {n_components}\")</p> References <p>Jolliffe, I. T. (2002). Principal component analysis (2nd ed.). Springer.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def cumulative_energy_90(matrix: torch.Tensor, threshold: float = 0.90) -&gt; int:\n    \"\"\"Compute number of components needed for 90% cumulative energy.\n\n    This metric counts how many principal components are needed to capture\n    a given percentage (default 90%) of the total variance.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        threshold: Energy threshold (default: 0.90 for 90%)\n\n    Returns:\n        Number of components needed (int)\n\n    Raises:\n        ValueError: If matrix is not 2D, threshold invalid, or SVD fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; n_components = cumulative_energy_90(A)\n        &gt;&gt;&gt; print(f\"Components for 90% energy: {n_components}\")\n\n    References:\n        Jolliffe, I. T. (2002). Principal component analysis (2nd ed.).\n        Springer.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    if not 0 &lt; threshold &lt;= 1:\n        raise ValueError(f\"Threshold must be in (0, 1], got {threshold}\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Compute explained variance\n        explained_variance = singular_values**2\n        total_variance = torch.sum(explained_variance)\n\n        # Compute cumulative explained variance ratio\n        cumsum = torch.cumsum(explained_variance, dim=0)\n        cumsum_ratio = cumsum / total_variance\n\n        # Find number of components for threshold\n        n_components = torch.searchsorted(cumsum_ratio, threshold).item() + 1\n        return min(n_components, len(singular_values))\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.nuclear_norm_ratio","title":"<code>nuclear_norm_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute nuclear norm ratio (nuclear norm / spectral norm).</p> <p>This metric normalizes the nuclear norm (sum of singular values) by the spectral norm (largest singular value), providing a normalized measure of rank.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Nuclear norm ratio value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or SVD computation fails</p> Example <p>import torch A = torch.randn(100, 50) nnr = nuclear_norm_ratio(A) print(f\"Nuclear norm ratio: {nnr:.2f}\")</p> References <p>Cand\u00e8s, E. J., &amp; Recht, B. (2009). Exact matrix completion via convex optimization. Foundations of Computational mathematics, 9(6), 717-772.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def nuclear_norm_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute nuclear norm ratio (nuclear norm / spectral norm).\n\n    This metric normalizes the nuclear norm (sum of singular values) by\n    the spectral norm (largest singular value), providing a normalized\n    measure of rank.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Nuclear norm ratio value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or SVD computation fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; nnr = nuclear_norm_ratio(A)\n        &gt;&gt;&gt; print(f\"Nuclear norm ratio: {nnr:.2f}\")\n\n    References:\n        Cand\u00e8s, E. J., &amp; Recht, B. (2009). Exact matrix completion via\n        convex optimization. Foundations of Computational mathematics, 9(6), 717-772.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        nuclear_norm = torch.sum(singular_values)\n        spectral_norm = singular_values[0]  # Largest singular value\n\n        ratio = nuclear_norm / (spectral_norm + eps)\n        return ratio.item()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.participation_ratio","title":"<code>participation_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute participation ratio from singular values.</p> <p>Participation ratio is the inverse of the sum of squared normalized singular values. It measures the effective dimensionality of the representation space.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Participation ratio value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or SVD computation fails</p> Example <p>import torch A = torch.randn(100, 50) pr = participation_ratio(A) print(f\"Participation ratio: {pr:.2f}\")</p> References <p>Grassberger, P., &amp; Procaccia, I. (1983). Measuring the strangeness of strange attractors. Physica D: Nonlinear Phenomena, 9(1-2), 189-208.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def participation_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute participation ratio from singular values.\n\n    Participation ratio is the inverse of the sum of squared normalized\n    singular values. It measures the effective dimensionality of the\n    representation space.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Participation ratio value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or SVD computation fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; pr = participation_ratio(A)\n        &gt;&gt;&gt; print(f\"Participation ratio: {pr:.2f}\")\n\n    References:\n        Grassberger, P., &amp; Procaccia, I. (1983). Measuring the strangeness\n        of strange attractors. Physica D: Nonlinear Phenomena, 9(1-2), 189-208.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Normalize singular values\n        sv_normalized = singular_values / (torch.sum(singular_values) + eps)\n\n        # Compute participation ratio\n        pr = 1.0 / (torch.sum(sv_normalized**2) + eps)\n        return pr.item()\n</code></pre>"},{"location":"api/ndt/core/#ndt.core.stable_rank","title":"<code>stable_rank(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute stable rank of a matrix.</p> <p>Stable rank is the ratio of squared Frobenius norm to squared spectral norm. It estimates the effective number of linearly independent dimensions.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Stable rank value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or contains NaN/Inf values</p> Example <p>import torch A = torch.randn(100, 50) sr = stable_rank(A) print(f\"Stable rank: {sr:.2f}\")</p> References <p>Vershynin, R. (2010). Introduction to the non-asymptotic analysis of random matrices. arXiv preprint arXiv:1011.3027.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def stable_rank(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute stable rank of a matrix.\n\n    Stable rank is the ratio of squared Frobenius norm to squared spectral norm.\n    It estimates the effective number of linearly independent dimensions.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Stable rank value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or contains NaN/Inf values\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; sr = stable_rank(A)\n        &gt;&gt;&gt; print(f\"Stable rank: {sr:.2f}\")\n\n    References:\n        Vershynin, R. (2010). Introduction to the non-asymptotic analysis\n        of random matrices. arXiv preprint arXiv:1011.3027.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    if torch.isnan(matrix).any() or torch.isinf(matrix).any():\n        raise ValueError(\"Matrix contains NaN or Inf values\")\n\n    with torch.no_grad():\n        frobenius_norm = torch.norm(matrix, p=\"fro\")\n        # Spectral norm is the largest singular value\n        _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        spectral_norm = singular_values[0]\n\n        result = (frobenius_norm**2) / (spectral_norm**2 + eps)\n        return result.item()\n</code></pre>"},{"location":"api/ndt/core/estimators/","title":"Estimators","text":""},{"location":"api/ndt/core/estimators/#ndt.core.estimators","title":"<code>ndt.core.estimators</code>","text":"<p>Dimensionality estimators for neural network representations.</p> <p>This module provides four key dimensionality metrics: 1. Stable Rank: Ratio of squared Frobenius norm to squared spectral norm 2. Participation Ratio: Inverse of sum of squared normalized singular values 3. Cumulative Energy 90: Number of components needed for 90% variance 4. Nuclear Norm Ratio: Nuclear norm normalized by spectral norm</p>"},{"location":"api/ndt/core/estimators/#ndt.core.estimators-functions","title":"Functions:","text":""},{"location":"api/ndt/core/estimators/#ndt.core.estimators.stable_rank","title":"<code>stable_rank(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute stable rank of a matrix.</p> <p>Stable rank is the ratio of squared Frobenius norm to squared spectral norm. It estimates the effective number of linearly independent dimensions.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Stable rank value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or contains NaN/Inf values</p> Example <p>import torch A = torch.randn(100, 50) sr = stable_rank(A) print(f\"Stable rank: {sr:.2f}\")</p> References <p>Vershynin, R. (2010). Introduction to the non-asymptotic analysis of random matrices. arXiv preprint arXiv:1011.3027.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def stable_rank(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute stable rank of a matrix.\n\n    Stable rank is the ratio of squared Frobenius norm to squared spectral norm.\n    It estimates the effective number of linearly independent dimensions.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Stable rank value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or contains NaN/Inf values\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; sr = stable_rank(A)\n        &gt;&gt;&gt; print(f\"Stable rank: {sr:.2f}\")\n\n    References:\n        Vershynin, R. (2010). Introduction to the non-asymptotic analysis\n        of random matrices. arXiv preprint arXiv:1011.3027.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    if torch.isnan(matrix).any() or torch.isinf(matrix).any():\n        raise ValueError(\"Matrix contains NaN or Inf values\")\n\n    with torch.no_grad():\n        frobenius_norm = torch.norm(matrix, p=\"fro\")\n        # Spectral norm is the largest singular value\n        _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        spectral_norm = singular_values[0]\n\n        result = (frobenius_norm**2) / (spectral_norm**2 + eps)\n        return result.item()\n</code></pre>"},{"location":"api/ndt/core/estimators/#ndt.core.estimators.participation_ratio","title":"<code>participation_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute participation ratio from singular values.</p> <p>Participation ratio is the inverse of the sum of squared normalized singular values. It measures the effective dimensionality of the representation space.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Participation ratio value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or SVD computation fails</p> Example <p>import torch A = torch.randn(100, 50) pr = participation_ratio(A) print(f\"Participation ratio: {pr:.2f}\")</p> References <p>Grassberger, P., &amp; Procaccia, I. (1983). Measuring the strangeness of strange attractors. Physica D: Nonlinear Phenomena, 9(1-2), 189-208.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def participation_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute participation ratio from singular values.\n\n    Participation ratio is the inverse of the sum of squared normalized\n    singular values. It measures the effective dimensionality of the\n    representation space.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Participation ratio value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or SVD computation fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; pr = participation_ratio(A)\n        &gt;&gt;&gt; print(f\"Participation ratio: {pr:.2f}\")\n\n    References:\n        Grassberger, P., &amp; Procaccia, I. (1983). Measuring the strangeness\n        of strange attractors. Physica D: Nonlinear Phenomena, 9(1-2), 189-208.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Normalize singular values\n        sv_normalized = singular_values / (torch.sum(singular_values) + eps)\n\n        # Compute participation ratio\n        pr = 1.0 / (torch.sum(sv_normalized**2) + eps)\n        return pr.item()\n</code></pre>"},{"location":"api/ndt/core/estimators/#ndt.core.estimators.cumulative_energy_90","title":"<code>cumulative_energy_90(matrix: torch.Tensor, threshold: float = 0.9) -&gt; int</code>","text":"<p>Compute number of components needed for 90% cumulative energy.</p> <p>This metric counts how many principal components are needed to capture a given percentage (default 90%) of the total variance.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>threshold</code> <code>float</code> <p>Energy threshold (default: 0.90 for 90%)</p> <code>0.9</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of components needed (int)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D, threshold invalid, or SVD fails</p> Example <p>import torch A = torch.randn(100, 50) n_components = cumulative_energy_90(A) print(f\"Components for 90% energy: {n_components}\")</p> References <p>Jolliffe, I. T. (2002). Principal component analysis (2nd ed.). Springer.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def cumulative_energy_90(matrix: torch.Tensor, threshold: float = 0.90) -&gt; int:\n    \"\"\"Compute number of components needed for 90% cumulative energy.\n\n    This metric counts how many principal components are needed to capture\n    a given percentage (default 90%) of the total variance.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        threshold: Energy threshold (default: 0.90 for 90%)\n\n    Returns:\n        Number of components needed (int)\n\n    Raises:\n        ValueError: If matrix is not 2D, threshold invalid, or SVD fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; n_components = cumulative_energy_90(A)\n        &gt;&gt;&gt; print(f\"Components for 90% energy: {n_components}\")\n\n    References:\n        Jolliffe, I. T. (2002). Principal component analysis (2nd ed.).\n        Springer.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    if not 0 &lt; threshold &lt;= 1:\n        raise ValueError(f\"Threshold must be in (0, 1], got {threshold}\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Compute explained variance\n        explained_variance = singular_values**2\n        total_variance = torch.sum(explained_variance)\n\n        # Compute cumulative explained variance ratio\n        cumsum = torch.cumsum(explained_variance, dim=0)\n        cumsum_ratio = cumsum / total_variance\n\n        # Find number of components for threshold\n        n_components = torch.searchsorted(cumsum_ratio, threshold).item() + 1\n        return min(n_components, len(singular_values))\n</code></pre>"},{"location":"api/ndt/core/estimators/#ndt.core.estimators.nuclear_norm_ratio","title":"<code>nuclear_norm_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float</code>","text":"<p>Compute nuclear norm ratio (nuclear norm / spectral norm).</p> <p>This metric normalizes the nuclear norm (sum of singular values) by the spectral norm (largest singular value), providing a normalized measure of rank.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>float</code> <p>Nuclear norm ratio value (float)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or SVD computation fails</p> Example <p>import torch A = torch.randn(100, 50) nnr = nuclear_norm_ratio(A) print(f\"Nuclear norm ratio: {nnr:.2f}\")</p> References <p>Cand\u00e8s, E. J., &amp; Recht, B. (2009). Exact matrix completion via convex optimization. Foundations of Computational mathematics, 9(6), 717-772.</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def nuclear_norm_ratio(matrix: torch.Tensor, eps: float = 1e-10) -&gt; float:\n    \"\"\"Compute nuclear norm ratio (nuclear norm / spectral norm).\n\n    This metric normalizes the nuclear norm (sum of singular values) by\n    the spectral norm (largest singular value), providing a normalized\n    measure of rank.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Nuclear norm ratio value (float)\n\n    Raises:\n        ValueError: If matrix is not 2D or SVD computation fails\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; nnr = nuclear_norm_ratio(A)\n        &gt;&gt;&gt; print(f\"Nuclear norm ratio: {nnr:.2f}\")\n\n    References:\n        Cand\u00e8s, E. J., &amp; Recht, B. (2009). Exact matrix completion via\n        convex optimization. Foundations of Computational mathematics, 9(6), 717-772.\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        nuclear_norm = torch.sum(singular_values)\n        spectral_norm = singular_values[0]  # Largest singular value\n\n        ratio = nuclear_norm / (spectral_norm + eps)\n        return ratio.item()\n</code></pre>"},{"location":"api/ndt/core/estimators/#ndt.core.estimators.compute_all_metrics","title":"<code>compute_all_metrics(matrix: torch.Tensor, eps: float = 1e-10) -&gt; Tuple[float, float, int, float]</code>","text":"<p>Compute all four dimensionality metrics efficiently.</p> <p>This function computes SVD once and reuses it for all metrics, improving efficiency when all metrics are needed.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>Tensor</code> <p>Input matrix of shape (m, n)</p> required <code>eps</code> <code>float</code> <p>Small constant for numerical stability (default: 1e-10)</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>Tuple[float, float, int, float]</code> <p>Tuple of (stable_rank, participation_ratio, cumulative_90, nuclear_norm_ratio)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If matrix is not 2D or computations fail</p> Example <p>import torch A = torch.randn(100, 50) sr, pr, ce90, nnr = compute_all_metrics(A) print(f\"Metrics: SR={sr:.2f}, PR={pr:.2f}, CE90={ce90}, NNR={nnr:.2f}\")</p> Source code in <code>src/ndt/core/estimators.py</code> <pre><code>def compute_all_metrics(\n    matrix: torch.Tensor, eps: float = 1e-10\n) -&gt; Tuple[float, float, int, float]:\n    \"\"\"Compute all four dimensionality metrics efficiently.\n\n    This function computes SVD once and reuses it for all metrics,\n    improving efficiency when all metrics are needed.\n\n    Args:\n        matrix: Input matrix of shape (m, n)\n        eps: Small constant for numerical stability (default: 1e-10)\n\n    Returns:\n        Tuple of (stable_rank, participation_ratio, cumulative_90, nuclear_norm_ratio)\n\n    Raises:\n        ValueError: If matrix is not 2D or computations fail\n\n    Example:\n        &gt;&gt;&gt; import torch\n        &gt;&gt;&gt; A = torch.randn(100, 50)\n        &gt;&gt;&gt; sr, pr, ce90, nnr = compute_all_metrics(A)\n        &gt;&gt;&gt; print(f\"Metrics: SR={sr:.2f}, PR={pr:.2f}, CE90={ce90}, NNR={nnr:.2f}\")\n    \"\"\"\n    if matrix.ndim != 2:\n        raise ValueError(f\"Expected 2D matrix, got {matrix.ndim}D\")\n\n    with torch.no_grad():\n        # Compute SVD once for all metrics\n        try:\n            _, singular_values, _ = torch.linalg.svd(matrix, full_matrices=False)\n        except RuntimeError as e:\n            raise ValueError(f\"SVD computation failed: {e}\") from e\n\n        # Compute norms for stable rank\n        frobenius_norm = torch.norm(matrix, p=\"fro\")\n        spectral_norm = singular_values[0]  # Largest singular value\n        sr = ((frobenius_norm**2) / (spectral_norm**2 + eps)).item()\n\n        # Participation ratio\n        sv_normalized = singular_values / (torch.sum(singular_values) + eps)\n        pr = (1.0 / (torch.sum(sv_normalized**2) + eps)).item()\n\n        # Cumulative energy 90%\n        explained_variance = singular_values**2\n        total_variance = torch.sum(explained_variance)\n        cumsum = torch.cumsum(explained_variance, dim=0)\n        cumsum_ratio = cumsum / total_variance\n        ce90 = torch.searchsorted(cumsum_ratio, 0.90).item() + 1\n        ce90 = min(ce90, len(singular_values))\n\n        # Nuclear norm ratio\n        nuclear_norm = torch.sum(singular_values)\n        nnr = (nuclear_norm / (singular_values[0] + eps)).item()\n\n        return sr, pr, ce90, nnr\n</code></pre>"},{"location":"api/ndt/core/hooks/","title":"Hooks","text":""},{"location":"api/ndt/core/hooks/#ndt.core.hooks","title":"<code>ndt.core.hooks</code>","text":"<p>Forward hooks for capturing neural network activations.</p>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks-classes","title":"Classes","text":""},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture","title":"<code>ActivationCapture()</code>","text":"<p>Captures activations from specified layers using forward hooks.</p> <p>This class manages PyTorch forward hooks to capture activations during the forward pass without modifying the model code.</p> <p>Attributes:</p> Name Type Description <code>activations</code> <code>Dict[str, Tensor]</code> <p>Dictionary mapping layer names to captured activations</p> <code>hooks</code> <code>List[RemovableHandle]</code> <p>List of registered hook handles</p> Example <p>import torch.nn as nn model = nn.Sequential(nn.Linear(10, 20), nn.ReLU(), nn.Linear(20, 10)) capture = ActivationCapture() capture.register_hooks(model, [model[0], model[2]]) output = model(torch.randn(32, 10)) print(capture.activations.keys()) capture.remove_hooks()</p> <p>Initialize the activation capture.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialize the activation capture.\"\"\"\n    self.activations: Dict[str, torch.Tensor] = {}\n    self.hooks: List[torch.utils.hooks.RemovableHandle] = []\n    self._layer_names: Dict[nn.Module, str] = {}\n</code></pre>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture-functions","title":"Methods:","text":""},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture.register_hooks","title":"<code>register_hooks(model: nn.Module, layers: List[nn.Module], layer_names: Optional[List[str]] = None) -&gt; None</code>","text":"<p>Register forward hooks on specified layers.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>The neural network model</p> required <code>layers</code> <code>List[Module]</code> <p>List of layer modules to monitor</p> required <code>layer_names</code> <code>Optional[List[str]]</code> <p>Optional custom names for layers. If None, uses module class names.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If number of layer names doesn't match number of layers</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def register_hooks(\n    self, model: nn.Module, layers: List[nn.Module], layer_names: Optional[List[str]] = None\n) -&gt; None:\n    \"\"\"Register forward hooks on specified layers.\n\n    Args:\n        model: The neural network model\n        layers: List of layer modules to monitor\n        layer_names: Optional custom names for layers. If None, uses module class names.\n\n    Raises:\n        ValueError: If number of layer names doesn't match number of layers\n    \"\"\"\n    if layer_names is not None and len(layer_names) != len(layers):\n        raise ValueError(\n            f\"Number of layer names ({len(layer_names)}) must match \"\n            f\"number of layers ({len(layers)})\"\n        )\n\n    # Generate default names if not provided\n    if layer_names is None:\n        layer_names = [f\"{layer.__class__.__name__}_{i}\" for i, layer in enumerate(layers)]\n\n    # Register hooks\n    for layer, name in zip(layers, layer_names):\n        hook_handle = layer.register_forward_hook(self._create_hook(name))\n        self.hooks.append(hook_handle)\n        self._layer_names[layer] = name\n</code></pre>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture.remove_hooks","title":"<code>remove_hooks() -&gt; None</code>","text":"<p>Remove all registered hooks and clear stored activations.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def remove_hooks(self) -&gt; None:\n    \"\"\"Remove all registered hooks and clear stored activations.\"\"\"\n    for hook in self.hooks:\n        hook.remove()\n    self.hooks.clear()\n    self.activations.clear()\n    self._layer_names.clear()\n</code></pre>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture.clear_activations","title":"<code>clear_activations() -&gt; None</code>","text":"<p>Clear stored activations without removing hooks.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def clear_activations(self) -&gt; None:\n    \"\"\"Clear stored activations without removing hooks.\"\"\"\n    self.activations.clear()\n</code></pre>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture.get_activation","title":"<code>get_activation(layer_name: str) -&gt; Optional[torch.Tensor]</code>","text":"<p>Get activation for a specific layer.</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>str</code> <p>Name of the layer</p> required <p>Returns:</p> Type Description <code>Optional[Tensor]</code> <p>Activation tensor if available, None otherwise</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def get_activation(self, layer_name: str) -&gt; Optional[torch.Tensor]:\n    \"\"\"Get activation for a specific layer.\n\n    Args:\n        layer_name: Name of the layer\n\n    Returns:\n        Activation tensor if available, None otherwise\n    \"\"\"\n    return self.activations.get(layer_name)\n</code></pre>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture.get_all_activations","title":"<code>get_all_activations() -&gt; Dict[str, torch.Tensor]</code>","text":"<p>Get all captured activations.</p> <p>Returns:</p> Type Description <code>Dict[str, Tensor]</code> <p>Dictionary mapping layer names to activation tensors</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def get_all_activations(self) -&gt; Dict[str, torch.Tensor]:\n    \"\"\"Get all captured activations.\n\n    Returns:\n        Dictionary mapping layer names to activation tensors\n    \"\"\"\n    return self.activations.copy()\n</code></pre>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture.__enter__","title":"<code>__enter__() -&gt; ActivationCapture</code>","text":"<p>Context manager entry.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __enter__(self) -&gt; \"ActivationCapture\":\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb) -&gt; None</code>","text":"<p>Context manager exit - removes hooks.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb) -&gt; None:\n    \"\"\"Context manager exit - removes hooks.\"\"\"\n    self.remove_hooks()\n</code></pre>"},{"location":"api/ndt/core/hooks/#ndt.core.hooks.ActivationCapture.__del__","title":"<code>__del__() -&gt; None</code>","text":"<p>Cleanup hooks on deletion.</p> Source code in <code>src/ndt/core/hooks.py</code> <pre><code>def __del__(self) -&gt; None:\n    \"\"\"Cleanup hooks on deletion.\"\"\"\n    self.remove_hooks()\n</code></pre>"},{"location":"api/ndt/core/jump_detector/","title":"Jump detector","text":""},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector","title":"<code>ndt.core.jump_detector</code>","text":"<p>Statistical detection of dimensionality jumps during training.</p>"},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector-classes","title":"Classes","text":""},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector.Jump","title":"<code>Jump(step: int, z_score: float, value_before: float, value_after: float, metric_name: str)</code>  <code>dataclass</code>","text":"<p>Represents a detected jump in dimensionality.</p> <p>Attributes:</p> Name Type Description <code>step</code> <code>int</code> <p>Training step where jump occurred</p> <code>z_score</code> <code>float</code> <p>Z-score magnitude of the jump</p> <code>value_before</code> <code>float</code> <p>Dimensionality value before jump</p> <code>value_after</code> <code>float</code> <p>Dimensionality value after jump</p> <code>metric_name</code> <code>str</code> <p>Name of the metric that jumped</p>"},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector.Jump-functions","title":"Methods:","text":""},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector.Jump.__repr__","title":"<code>__repr__() -&gt; str</code>","text":"<p>String representation of the jump.</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"String representation of the jump.\"\"\"\n    return (\n        f\"Jump(step={self.step}, metric={self.metric_name}, \"\n        f\"z_score={self.z_score:.2f}, \"\n        f\"\u0394={self.value_after - self.value_before:.2f})\"\n    )\n</code></pre>"},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector.JumpDetector","title":"<code>JumpDetector(window_size: int = 50, z_threshold: float = 3.0, min_samples: int = 20)</code>","text":"<p>Detects significant jumps in dimensionality metrics using Z-score analysis.</p> <p>This class uses a rolling window to compute mean and standard deviation, then identifies jumps that exceed a specified Z-score threshold.</p> <p>Attributes:</p> Name Type Description <code>window_size</code> <p>Size of the rolling window for statistics</p> <code>z_threshold</code> <p>Z-score threshold for jump detection</p> <code>min_samples</code> <p>Minimum number of samples before detecting jumps</p> Example <p>detector = JumpDetector(window_size=50, z_threshold=3.0) values = [10.0] * 100 + [20.0] * 100  # Simulated jump jumps = detector.detect_jumps(values, metric_name=\"stable_rank\") for jump in jumps: ...     print(jump)</p> <p>Initialize the jump detector.</p> <p>Parameters:</p> Name Type Description Default <code>window_size</code> <code>int</code> <p>Size of rolling window for computing statistics (default: 50)</p> <code>50</code> <code>z_threshold</code> <code>float</code> <p>Z-score threshold for detecting jumps (default: 3.0)</p> <code>3.0</code> <code>min_samples</code> <code>int</code> <p>Minimum samples before starting detection (default: 20)</p> <code>20</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If parameters are invalid</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def __init__(\n    self, window_size: int = 50, z_threshold: float = 3.0, min_samples: int = 20\n) -&gt; None:\n    \"\"\"Initialize the jump detector.\n\n    Args:\n        window_size: Size of rolling window for computing statistics (default: 50)\n        z_threshold: Z-score threshold for detecting jumps (default: 3.0)\n        min_samples: Minimum samples before starting detection (default: 20)\n\n    Raises:\n        ValueError: If parameters are invalid\n    \"\"\"\n    if window_size &lt; 2:\n        raise ValueError(f\"window_size must be &gt;= 2, got {window_size}\")\n    if z_threshold &lt;= 0:\n        raise ValueError(f\"z_threshold must be &gt; 0, got {z_threshold}\")\n    if min_samples &lt; 1:\n        raise ValueError(f\"min_samples must be &gt;= 1, got {min_samples}\")\n\n    self.window_size = window_size\n    self.z_threshold = z_threshold\n    self.min_samples = min_samples\n</code></pre>"},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector.JumpDetector-functions","title":"Methods:","text":""},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector.JumpDetector.detect_jumps","title":"<code>detect_jumps(values: List[float], metric_name: str = 'dimensionality', step_offset: int = 0) -&gt; List[Jump]</code>","text":"<p>Detect jumps in a sequence of dimensionality values.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <code>metric_name</code> <code>str</code> <p>Name of the metric being analyzed</p> <code>'dimensionality'</code> <code>step_offset</code> <code>int</code> <p>Offset to add to step numbers (for aligning with training steps)</p> <code>0</code> <p>Returns:</p> Type Description <code>List[Jump]</code> <p>List of detected Jump objects</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If values list is too short</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def detect_jumps(\n    self, values: List[float], metric_name: str = \"dimensionality\", step_offset: int = 0\n) -&gt; List[Jump]:\n    \"\"\"Detect jumps in a sequence of dimensionality values.\n\n    Args:\n        values: List of dimensionality measurements\n        metric_name: Name of the metric being analyzed\n        step_offset: Offset to add to step numbers (for aligning with training steps)\n\n    Returns:\n        List of detected Jump objects\n\n    Raises:\n        ValueError: If values list is too short\n    \"\"\"\n    if len(values) &lt; self.min_samples:\n        return []\n\n    values_array = np.array(values)\n    jumps = []\n\n    # Start detection after minimum samples\n    for i in range(self.min_samples, len(values_array)):\n        # Define window for statistics\n        window_start = max(0, i - self.window_size)\n        window = values_array[window_start:i]\n\n        # Compute statistics\n        mean = np.mean(window)\n        std = np.std(window)\n\n        # Avoid division by zero\n        if std &lt; 1e-10:\n            continue\n\n        # Compute z-score for current value\n        current_value = values_array[i]\n        z_score = abs((current_value - mean) / std)\n\n        # Detect jump\n        if z_score &gt; self.z_threshold:\n            jump = Jump(\n                step=i + step_offset,\n                z_score=z_score,\n                value_before=values_array[i - 1],\n                value_after=current_value,\n                metric_name=metric_name,\n            )\n            jumps.append(jump)\n\n    return jumps\n</code></pre>"},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector.JumpDetector.detect_jumps_with_direction","title":"<code>detect_jumps_with_direction(values: List[float], metric_name: str = 'dimensionality', step_offset: int = 0, direction: Optional[str] = None) -&gt; List[Jump]</code>","text":"<p>Detect jumps with optional direction filtering.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <code>metric_name</code> <code>str</code> <p>Name of the metric being analyzed</p> <code>'dimensionality'</code> <code>step_offset</code> <code>int</code> <p>Offset to add to step numbers</p> <code>0</code> <code>direction</code> <code>Optional[str]</code> <p>Filter by direction - \"up\", \"down\", or None for both</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Jump]</code> <p>List of detected Jump objects filtered by direction</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction is not valid</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def detect_jumps_with_direction(\n    self,\n    values: List[float],\n    metric_name: str = \"dimensionality\",\n    step_offset: int = 0,\n    direction: Optional[str] = None,\n) -&gt; List[Jump]:\n    \"\"\"Detect jumps with optional direction filtering.\n\n    Args:\n        values: List of dimensionality measurements\n        metric_name: Name of the metric being analyzed\n        step_offset: Offset to add to step numbers\n        direction: Filter by direction - \"up\", \"down\", or None for both\n\n    Returns:\n        List of detected Jump objects filtered by direction\n\n    Raises:\n        ValueError: If direction is not valid\n    \"\"\"\n    if direction not in [None, \"up\", \"down\"]:\n        raise ValueError(f\"direction must be None, 'up', or 'down', got {direction}\")\n\n    all_jumps = self.detect_jumps(values, metric_name, step_offset)\n\n    if direction is None:\n        return all_jumps\n\n    # Filter by direction\n    filtered_jumps = []\n    for jump in all_jumps:\n        change = jump.value_after - jump.value_before\n        if direction == \"up\" and change &gt; 0:\n            filtered_jumps.append(jump)\n        elif direction == \"down\" and change &lt; 0:\n            filtered_jumps.append(jump)\n\n    return filtered_jumps\n</code></pre>"},{"location":"api/ndt/core/jump_detector/#ndt.core.jump_detector.JumpDetector.compute_rolling_statistics","title":"<code>compute_rolling_statistics(values: List[float]) -&gt; Tuple[np.ndarray, np.ndarray, np.ndarray]</code>","text":"<p>Compute rolling mean, std, and z-scores for visualization.</p> <p>Parameters:</p> Name Type Description Default <code>values</code> <code>List[float]</code> <p>List of dimensionality measurements</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray, ndarray]</code> <p>Tuple of (means, stds, z_scores) as numpy arrays</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If values list is empty</p> Source code in <code>src/ndt/core/jump_detector.py</code> <pre><code>def compute_rolling_statistics(\n    self, values: List[float]\n) -&gt; Tuple[np.ndarray, np.ndarray, np.ndarray]:\n    \"\"\"Compute rolling mean, std, and z-scores for visualization.\n\n    Args:\n        values: List of dimensionality measurements\n\n    Returns:\n        Tuple of (means, stds, z_scores) as numpy arrays\n\n    Raises:\n        ValueError: If values list is empty\n    \"\"\"\n    if len(values) == 0:\n        raise ValueError(\"values list cannot be empty\")\n\n    values_array = np.array(values)\n    n = len(values_array)\n\n    means = np.zeros(n)\n    stds = np.zeros(n)\n    z_scores = np.zeros(n)\n\n    for i in range(self.min_samples, n):\n        window_start = max(0, i - self.window_size)\n        window = values_array[window_start:i]\n\n        means[i] = np.mean(window)\n        stds[i] = np.std(window)\n\n        if stds[i] &gt; 1e-10:\n            z_scores[i] = abs((values_array[i] - means[i]) / stds[i])\n\n    return means, stds, z_scores\n</code></pre>"},{"location":"api/ndt/core/tracker/","title":"Tracker","text":""},{"location":"api/ndt/core/tracker/#ndt.core.tracker","title":"<code>ndt.core.tracker</code>","text":"<p>High-frequency tracking of neural network dimensionality during training.</p>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker-classes","title":"Classes","text":""},{"location":"api/ndt/core/tracker/#ndt.core.tracker.DimensionalityMetrics","title":"<code>DimensionalityMetrics(step: int, stable_rank: float, participation_ratio: float, cumulative_90: int, nuclear_norm_ratio: float, loss: float, grad_norm: Optional[float] = None)</code>  <code>dataclass</code>","text":"<p>Container for dimensionality measurements at a single training step.</p> <p>Attributes:</p> Name Type Description <code>step</code> <code>int</code> <p>Training step number</p> <code>stable_rank</code> <code>float</code> <p>Stable rank estimate</p> <code>participation_ratio</code> <code>float</code> <p>Participation ratio estimate</p> <code>cumulative_90</code> <code>int</code> <p>Number of components for 90% energy</p> <code>nuclear_norm_ratio</code> <code>float</code> <p>Nuclear norm ratio estimate</p> <code>loss</code> <code>float</code> <p>Training loss at this step</p> <code>grad_norm</code> <code>Optional[float]</code> <p>Gradient norm at this step (optional)</p>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.DimensionalityMetrics-functions","title":"Methods:","text":""},{"location":"api/ndt/core/tracker/#ndt.core.tracker.DimensionalityMetrics.to_dict","title":"<code>to_dict() -&gt; Dict</code>","text":"<p>Convert to dictionary.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def to_dict(self) -&gt; Dict:\n    \"\"\"Convert to dictionary.\"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker","title":"<code>HighFrequencyTracker(model: nn.Module, layers: Optional[List[nn.Module]] = None, layer_names: Optional[List[str]] = None, sampling_frequency: int = 1, enable_jump_detection: bool = True, jump_window_size: int = 50, jump_z_threshold: float = 3.0, device: Optional[torch.device] = None)</code>","text":"<p>High-frequency tracker for neural network representational dimensionality.</p> <p>This class provides minimal-intrusion tracking of dimensionality metrics during neural network training. It automatically captures activations and computes multiple dimensionality estimates at specified intervals.</p> <p>Attributes:</p> Name Type Description <code>model</code> <p>The neural network model to track</p> <code>layers</code> <p>List of layers to monitor</p> <code>sampling_frequency</code> <p>Record metrics every N steps</p> <code>jump_detector</code> <p>Optional jump detector for identifying phase transitions</p> Example <p>import torch.nn as nn model = nn.Sequential( ...     nn.Linear(784, 512), nn.ReLU(), ...     nn.Linear(512, 256), nn.ReLU(), ...     nn.Linear(256, 10) ... ) tracker = HighFrequencyTracker(model, layers=[model[0], model[2], model[4]])</p> <p>Initialize the high-frequency tracker.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>Module</code> <p>Neural network model to track</p> required <code>layers</code> <code>Optional[List[Module]]</code> <p>List of layers to monitor. If None, auto-detects based on architecture</p> <code>None</code> <code>layer_names</code> <code>Optional[List[str]]</code> <p>Optional custom names for layers</p> <code>None</code> <code>sampling_frequency</code> <code>int</code> <p>Record metrics every N steps (default: 1)</p> <code>1</code> <code>enable_jump_detection</code> <code>bool</code> <p>Whether to detect dimensionality jumps (default: True)</p> <code>True</code> <code>jump_window_size</code> <code>int</code> <p>Window size for jump detection (default: 50)</p> <code>50</code> <code>jump_z_threshold</code> <code>float</code> <p>Z-score threshold for jump detection (default: 3.0)</p> <code>3.0</code> <code>device</code> <code>Optional[device]</code> <p>Device for computations (default: model's device)</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If model or parameters are invalid</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __init__(\n    self,\n    model: nn.Module,\n    layers: Optional[List[nn.Module]] = None,\n    layer_names: Optional[List[str]] = None,\n    sampling_frequency: int = 1,\n    enable_jump_detection: bool = True,\n    jump_window_size: int = 50,\n    jump_z_threshold: float = 3.0,\n    device: Optional[torch.device] = None,\n) -&gt; None:\n    \"\"\"Initialize the high-frequency tracker.\n\n    Args:\n        model: Neural network model to track\n        layers: List of layers to monitor. If None, auto-detects based on architecture\n        layer_names: Optional custom names for layers\n        sampling_frequency: Record metrics every N steps (default: 1)\n        enable_jump_detection: Whether to detect dimensionality jumps (default: True)\n        jump_window_size: Window size for jump detection (default: 50)\n        jump_z_threshold: Z-score threshold for jump detection (default: 3.0)\n        device: Device for computations (default: model's device)\n\n    Raises:\n        ValueError: If model or parameters are invalid\n    \"\"\"\n    if not isinstance(model, nn.Module):\n        raise ValueError(\"model must be a torch.nn.Module\")\n\n    self.model = model\n    self.sampling_frequency = sampling_frequency\n    self.device = device or next(model.parameters()).device\n\n    # Auto-detect layers if not provided\n    if layers is None:\n        layers = self._auto_detect_layers()\n\n    if not layers:\n        raise ValueError(\n            \"No layers to track. Provide layers explicitly or use supported architecture.\"\n        )\n\n    # Set up activation capture\n    self.activation_capture = ActivationCapture()\n    self.activation_capture.register_hooks(model, layers, layer_names)\n    self.layer_names = layer_names or [\n        f\"{layer.__class__.__name__}_{i}\" for i, layer in enumerate(layers)\n    ]\n\n    # Initialize storage\n    self.metrics_history: Dict[str, List[DimensionalityMetrics]] = {\n        name: [] for name in self.layer_names\n    }\n    self.step_counter = 0\n\n    # Jump detection\n    self.enable_jump_detection = enable_jump_detection\n    if enable_jump_detection:\n        self.jump_detector = JumpDetector(\n            window_size=jump_window_size, z_threshold=jump_z_threshold\n        )\n    else:\n        self.jump_detector = None\n\n    # Logging\n    self.logger = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker--in-training-loop","title":"In training loop:","text":"<p>for step, (x, y) in enumerate(dataloader): ...     loss = train_step(model, x, y) ...     tracker.log(step, loss) results = tracker.get_results() tracker.close()</p>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker-functions","title":"Methods:","text":""},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker.log","title":"<code>log(step: int, loss: float, grad_norm: Optional[float] = None, force: bool = False) -&gt; None</code>","text":"<p>Log dimensionality metrics for the current step.</p> <p>This is the main method called during training. It captures activations and computes dimensionality metrics according to the sampling frequency.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>Current training step</p> required <code>loss</code> <code>float</code> <p>Training loss value</p> required <code>grad_norm</code> <code>Optional[float]</code> <p>Optional gradient norm</p> <code>None</code> <code>force</code> <code>bool</code> <p>Force logging even if not at sampling interval</p> <code>False</code> Example <p>for step, (x, y) in enumerate(dataloader): ...     optimizer.zero_grad() ...     output = model(x) ...     loss = criterion(output, y) ...     loss.backward() ...     optimizer.step() ...     tracker.log(step, loss.item())</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def log(\n    self, step: int, loss: float, grad_norm: Optional[float] = None, force: bool = False\n) -&gt; None:\n    \"\"\"Log dimensionality metrics for the current step.\n\n    This is the main method called during training. It captures activations\n    and computes dimensionality metrics according to the sampling frequency.\n\n    Args:\n        step: Current training step\n        loss: Training loss value\n        grad_norm: Optional gradient norm\n        force: Force logging even if not at sampling interval\n\n    Example:\n        &gt;&gt;&gt; for step, (x, y) in enumerate(dataloader):\n        ...     optimizer.zero_grad()\n        ...     output = model(x)\n        ...     loss = criterion(output, y)\n        ...     loss.backward()\n        ...     optimizer.step()\n        ...     tracker.log(step, loss.item())\n    \"\"\"\n    # Check if we should record this step\n    if not force and step % self.sampling_frequency != 0:\n        return\n\n    self.step_counter = step\n\n    # Activations are already captured by hooks during forward pass\n    # Now compute metrics for each layer\n    with torch.no_grad():\n        for layer_name in self.layer_names:\n            activation = self.activation_capture.get_activation(layer_name)\n\n            if activation is None:\n                self.logger.warning(\n                    f\"No activation captured for layer {layer_name} at step {step}\"\n                )\n                continue\n\n            try:\n                # Reshape activation to 2D matrix (batch_size, features)\n                matrix = self._prepare_activation_matrix(activation)\n\n                # Compute all dimensionality metrics\n                sr, pr, ce90, nnr = compute_all_metrics(matrix)\n\n                # Create metrics object\n                metrics = DimensionalityMetrics(\n                    step=step,\n                    stable_rank=sr,\n                    participation_ratio=pr,\n                    cumulative_90=ce90,\n                    nuclear_norm_ratio=nnr,\n                    loss=loss,\n                    grad_norm=grad_norm,\n                )\n\n                # Store metrics\n                self.metrics_history[layer_name].append(metrics)\n\n            except Exception as e:\n                self.logger.error(\n                    f\"Error computing metrics for {layer_name} at step {step}: {e}\"\n                )\n\n    # Clear activations for next step\n    self.activation_capture.clear_activations()\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker.get_results","title":"<code>get_results(layer_name: Optional[str] = None) -&gt; Union[pd.DataFrame, Dict[str, pd.DataFrame]]</code>","text":"<p>Get tracked metrics as pandas DataFrame(s).</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>Optional[str]</code> <p>If specified, return results for that layer only.        If None, return dict of DataFrames for all layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Union[DataFrame, Dict[str, DataFrame]]</code> <p>DataFrame or dict of DataFrames containing metrics history</p> Example <p>results = tracker.get_results() for layer_name, df in results.items(): ...     print(f\"{layer_name}: {len(df)} measurements\")</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def get_results(\n    self, layer_name: Optional[str] = None\n) -&gt; Union[pd.DataFrame, Dict[str, pd.DataFrame]]:\n    \"\"\"Get tracked metrics as pandas DataFrame(s).\n\n    Args:\n        layer_name: If specified, return results for that layer only.\n                   If None, return dict of DataFrames for all layers.\n\n    Returns:\n        DataFrame or dict of DataFrames containing metrics history\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; for layer_name, df in results.items():\n        ...     print(f\"{layer_name}: {len(df)} measurements\")\n    \"\"\"\n    if layer_name is not None:\n        if layer_name not in self.metrics_history:\n            raise ValueError(f\"Unknown layer name: {layer_name}\")\n        return self._metrics_to_dataframe(self.metrics_history[layer_name])\n\n    return {\n        name: self._metrics_to_dataframe(metrics)\n        for name, metrics in self.metrics_history.items()\n    }\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker.detect_jumps","title":"<code>detect_jumps(layer_name: Optional[str] = None, metric: str = 'stable_rank') -&gt; Dict[str, List]</code>","text":"<p>Detect dimensionality jumps in tracked metrics.</p> <p>Parameters:</p> Name Type Description Default <code>layer_name</code> <code>Optional[str]</code> <p>If specified, detect jumps for that layer only.        If None, detect for all layers.</p> <code>None</code> <code>metric</code> <code>str</code> <p>Which metric to analyze (\"stable_rank\", \"participation_ratio\",    \"cumulative_90\", or \"nuclear_norm_ratio\")</p> <code>'stable_rank'</code> <p>Returns:</p> Type Description <code>Dict[str, List]</code> <p>Dictionary mapping layer names to lists of Jump objects</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If jump detection is disabled or metric name is invalid</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def detect_jumps(\n    self, layer_name: Optional[str] = None, metric: str = \"stable_rank\"\n) -&gt; Dict[str, List]:\n    \"\"\"Detect dimensionality jumps in tracked metrics.\n\n    Args:\n        layer_name: If specified, detect jumps for that layer only.\n                   If None, detect for all layers.\n        metric: Which metric to analyze (\"stable_rank\", \"participation_ratio\",\n               \"cumulative_90\", or \"nuclear_norm_ratio\")\n\n    Returns:\n        Dictionary mapping layer names to lists of Jump objects\n\n    Raises:\n        ValueError: If jump detection is disabled or metric name is invalid\n    \"\"\"\n    if not self.enable_jump_detection:\n        raise ValueError(\"Jump detection is disabled. Enable it during initialization.\")\n\n    valid_metrics = [\n        \"stable_rank\",\n        \"participation_ratio\",\n        \"cumulative_90\",\n        \"nuclear_norm_ratio\",\n    ]\n    if metric not in valid_metrics:\n        raise ValueError(f\"metric must be one of {valid_metrics}, got {metric}\")\n\n    layers_to_analyze = [layer_name] if layer_name else self.layer_names\n\n    jumps_dict = {}\n    for name in layers_to_analyze:\n        if name not in self.metrics_history:\n            continue\n\n        # Extract metric values\n        values = [getattr(m, metric) for m in self.metrics_history[name]]\n\n        # Detect jumps\n        jumps = self.jump_detector.detect_jumps(values, metric_name=f\"{name}_{metric}\")\n        jumps_dict[name] = jumps\n\n    return jumps_dict\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker.close","title":"<code>close() -&gt; None</code>","text":"<p>Clean up hooks and resources.</p> <p>Should be called when done tracking to free resources.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Clean up hooks and resources.\n\n    Should be called when done tracking to free resources.\n    \"\"\"\n    self.activation_capture.remove_hooks()\n    self.logger.info(f\"Tracker closed. Recorded {self.step_counter} steps.\")\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker.__enter__","title":"<code>__enter__() -&gt; HighFrequencyTracker</code>","text":"<p>Context manager entry.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __enter__(self) -&gt; \"HighFrequencyTracker\":\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb) -&gt; None</code>","text":"<p>Context manager exit.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb) -&gt; None:\n    \"\"\"Context manager exit.\"\"\"\n    self.close()\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker.HighFrequencyTracker.__del__","title":"<code>__del__() -&gt; None</code>","text":"<p>Cleanup on deletion.</p> Source code in <code>src/ndt/core/tracker.py</code> <pre><code>def __del__(self) -&gt; None:\n    \"\"\"Cleanup on deletion.\"\"\"\n    if hasattr(self, \"activation_capture\"):\n        self.activation_capture.remove_hooks()\n</code></pre>"},{"location":"api/ndt/core/tracker/#ndt.core.tracker-functions","title":"Functions:","text":""},{"location":"api/ndt/export/","title":"Index","text":""},{"location":"api/ndt/export/#ndt.export","title":"<code>ndt.export</code>","text":"<p>Export utilities for saving tracking results in various formats.</p>"},{"location":"api/ndt/export/#ndt.export-functions","title":"Functions:","text":""},{"location":"api/ndt/export/#ndt.export.export_to_csv","title":"<code>export_to_csv(results: Dict[str, pd.DataFrame], output_path: str, separate_files: bool = False) -&gt; None</code>","text":"<p>Export tracking results to CSV format.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>output_path</code> <code>str</code> <p>Output file path or directory path</p> required <code>separate_files</code> <code>bool</code> <p>If True, create separate CSV for each layer.            If False, combine all layers into one CSV with layer column.</p> <code>False</code> Example <p>results = tracker.get_results() export_to_csv(results, \"results.csv\")</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/csv.py</code> <pre><code>def export_to_csv(\n    results: Dict[str, pd.DataFrame], output_path: str, separate_files: bool = False\n) -&gt; None:\n    \"\"\"Export tracking results to CSV format.\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames\n        output_path: Output file path or directory path\n        separate_files: If True, create separate CSV for each layer.\n                       If False, combine all layers into one CSV with layer column.\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; export_to_csv(results, \"results.csv\")\n        &gt;&gt;&gt; # Or separate files:\n        &gt;&gt;&gt; export_to_csv(results, \"results_dir/\", separate_files=True)\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    if separate_files:\n        # Create directory if it doesn't exist\n        output_path.mkdir(parents=True, exist_ok=True)\n\n        # Export each layer to separate file\n        for layer_name, df in results.items():\n            filename = output_path / f\"{layer_name}.csv\"\n            df.to_csv(filename, index=False)\n            print(f\"Exported {layer_name} to {filename}\")\n\n    else:\n        # Combine all layers into one CSV\n        combined_data = []\n        for layer_name, df in results.items():\n            df_copy = df.copy()\n            df_copy[\"layer\"] = layer_name\n            combined_data.append(df_copy)\n\n        if combined_data:\n            combined_df = pd.concat(combined_data, ignore_index=True)\n\n            # Reorder columns to put layer first\n            cols = [\"layer\"] + [col for col in combined_df.columns if col != \"layer\"]\n            combined_df = combined_df[cols]\n\n            combined_df.to_csv(output_path, index=False)\n            print(f\"Exported all layers to {output_path}\")\n        else:\n            print(\"No data to export\")\n</code></pre>"},{"location":"api/ndt/export/#ndt.export.export_to_csv--or-separate-files","title":"Or separate files:","text":"<p>export_to_csv(results, \"results_dir/\", separate_files=True)</p>"},{"location":"api/ndt/export/#ndt.export.export_to_hdf5","title":"<code>export_to_hdf5(results: Dict[str, pd.DataFrame], output_path: str, compression: str = 'gzip', compression_opts: int = 4, metadata: Optional[Dict[str, Any]] = None) -&gt; None</code>","text":"<p>Export tracking results to HDF5 format (efficient for large data).</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>output_path</code> <code>str</code> <p>Output HDF5 file path</p> required <code>compression</code> <code>str</code> <p>Compression algorithm (\"gzip\", \"lzf\", or None)</p> <code>'gzip'</code> <code>compression_opts</code> <code>int</code> <p>Compression level (0-9 for gzip)</p> <code>4</code> <code>metadata</code> <code>Optional[Dict[str, Any]]</code> <p>Optional metadata to store as attributes</p> <code>None</code> Example <p>results = tracker.get_results() metadata = {\"model\": \"ResNet18\", \"dataset\": \"CIFAR-10\"} export_to_hdf5(results, \"results.h5\", metadata=metadata)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/hdf5.py</code> <pre><code>def export_to_hdf5(\n    results: Dict[str, pd.DataFrame],\n    output_path: str,\n    compression: str = \"gzip\",\n    compression_opts: int = 4,\n    metadata: Optional[Dict[str, Any]] = None,\n) -&gt; None:\n    \"\"\"Export tracking results to HDF5 format (efficient for large data).\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames\n        output_path: Output HDF5 file path\n        compression: Compression algorithm (\"gzip\", \"lzf\", or None)\n        compression_opts: Compression level (0-9 for gzip)\n        metadata: Optional metadata to store as attributes\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; metadata = {\"model\": \"ResNet18\", \"dataset\": \"CIFAR-10\"}\n        &gt;&gt;&gt; export_to_hdf5(results, \"results.h5\", metadata=metadata)\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    with h5py.File(output_path, \"w\") as f:\n        # Store metadata as root attributes\n        if metadata:\n            for key, value in metadata.items():\n                f.attrs[key] = value\n\n        # Store each layer's data as a group\n        for layer_name, df in results.items():\n            # Create group for this layer\n            group = f.create_group(layer_name)\n\n            # Store each column as a dataset\n            for col in df.columns:\n                data = df[col].values\n\n                # Handle different data types\n                if data.dtype == object:\n                    # Convert to string for object columns\n                    data = data.astype(str)\n\n                group.create_dataset(\n                    col,\n                    data=data,\n                    compression=compression,\n                    compression_opts=compression_opts if compression == \"gzip\" else None,\n                )\n\n            # Store column names as attribute\n            group.attrs[\"columns\"] = list(df.columns)\n\n    print(f\"Exported results to {output_path}\")\n</code></pre>"},{"location":"api/ndt/export/#ndt.export.export_to_json","title":"<code>export_to_json(results: Dict[str, pd.DataFrame], output_path: str, pretty: bool = True) -&gt; None</code>","text":"<p>Export tracking results to JSON format.</p> <p>Parameters:</p> Name Type Description Default <code>output_path</code> <code>str</code> <p>Output file path</p> required <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>pretty</code> <code>bool</code> <p>If True, format JSON with indentation</p> <code>True</code> Example <p>results = tracker.get_results() export_to_json(results, \"results.json\")</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/json.py</code> <pre><code>def export_to_json(results: Dict[str, pd.DataFrame], output_path: str, pretty: bool = True) -&gt; None:\n    \"\"\"Export tracking results to JSON format.\n\n    Args:\n        output_path: Output file path\n        results: Dictionary mapping layer names to DataFrames\n        pretty: If True, format JSON with indentation\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; export_to_json(results, \"results.json\")\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    # Convert DataFrames to dictionaries\n    json_data = {}\n    for layer_name, df in results.items():\n        json_data[layer_name] = df.to_dict(orient=\"records\")\n\n    # Write to file\n    with open(output_path, \"w\") as f:\n        if pretty:\n            json.dump(json_data, f, indent=2)\n        else:\n            json.dump(json_data, f)\n\n    print(f\"Exported results to {output_path}\")\n</code></pre>"},{"location":"api/ndt/export/csv/","title":"Csv","text":""},{"location":"api/ndt/export/csv/#ndt.export.csv","title":"<code>ndt.export.csv</code>","text":"<p>CSV export functionality for tracking results.</p>"},{"location":"api/ndt/export/csv/#ndt.export.csv-functions","title":"Functions:","text":""},{"location":"api/ndt/export/csv/#ndt.export.csv.export_to_csv","title":"<code>export_to_csv(results: Dict[str, pd.DataFrame], output_path: str, separate_files: bool = False) -&gt; None</code>","text":"<p>Export tracking results to CSV format.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>output_path</code> <code>str</code> <p>Output file path or directory path</p> required <code>separate_files</code> <code>bool</code> <p>If True, create separate CSV for each layer.            If False, combine all layers into one CSV with layer column.</p> <code>False</code> Example <p>results = tracker.get_results() export_to_csv(results, \"results.csv\")</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/csv.py</code> <pre><code>def export_to_csv(\n    results: Dict[str, pd.DataFrame], output_path: str, separate_files: bool = False\n) -&gt; None:\n    \"\"\"Export tracking results to CSV format.\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames\n        output_path: Output file path or directory path\n        separate_files: If True, create separate CSV for each layer.\n                       If False, combine all layers into one CSV with layer column.\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; export_to_csv(results, \"results.csv\")\n        &gt;&gt;&gt; # Or separate files:\n        &gt;&gt;&gt; export_to_csv(results, \"results_dir/\", separate_files=True)\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    if separate_files:\n        # Create directory if it doesn't exist\n        output_path.mkdir(parents=True, exist_ok=True)\n\n        # Export each layer to separate file\n        for layer_name, df in results.items():\n            filename = output_path / f\"{layer_name}.csv\"\n            df.to_csv(filename, index=False)\n            print(f\"Exported {layer_name} to {filename}\")\n\n    else:\n        # Combine all layers into one CSV\n        combined_data = []\n        for layer_name, df in results.items():\n            df_copy = df.copy()\n            df_copy[\"layer\"] = layer_name\n            combined_data.append(df_copy)\n\n        if combined_data:\n            combined_df = pd.concat(combined_data, ignore_index=True)\n\n            # Reorder columns to put layer first\n            cols = [\"layer\"] + [col for col in combined_df.columns if col != \"layer\"]\n            combined_df = combined_df[cols]\n\n            combined_df.to_csv(output_path, index=False)\n            print(f\"Exported all layers to {output_path}\")\n        else:\n            print(\"No data to export\")\n</code></pre>"},{"location":"api/ndt/export/csv/#ndt.export.csv.export_to_csv--or-separate-files","title":"Or separate files:","text":"<p>export_to_csv(results, \"results_dir/\", separate_files=True)</p>"},{"location":"api/ndt/export/csv/#ndt.export.csv.load_from_csv","title":"<code>load_from_csv(input_path: str, separate_files: bool = False) -&gt; Dict[str, pd.DataFrame]</code>","text":"<p>Load tracking results from CSV format.</p> <p>Parameters:</p> Name Type Description Default <code>input_path</code> <code>str</code> <p>Input file path or directory path</p> required <code>separate_files</code> <code>bool</code> <p>If True, load separate CSVs from directory.            If False, load combined CSV and split by layer.</p> <code>False</code> <p>Returns:</p> Type Description <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> Example <p>results = load_from_csv(\"results.csv\")</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If input_path doesn't exist</p> <code>ValueError</code> <p>If CSV format is invalid</p> Source code in <code>src/ndt/export/csv.py</code> <pre><code>def load_from_csv(input_path: str, separate_files: bool = False) -&gt; Dict[str, pd.DataFrame]:\n    \"\"\"Load tracking results from CSV format.\n\n    Args:\n        input_path: Input file path or directory path\n        separate_files: If True, load separate CSVs from directory.\n                       If False, load combined CSV and split by layer.\n\n    Returns:\n        Dictionary mapping layer names to DataFrames\n\n    Example:\n        &gt;&gt;&gt; results = load_from_csv(\"results.csv\")\n        &gt;&gt;&gt; # Or from directory:\n        &gt;&gt;&gt; results = load_from_csv(\"results_dir/\", separate_files=True)\n\n    Raises:\n        FileNotFoundError: If input_path doesn't exist\n        ValueError: If CSV format is invalid\n    \"\"\"\n    input_path = Path(input_path)\n\n    if not input_path.exists():\n        raise FileNotFoundError(f\"Path not found: {input_path}\")\n\n    if separate_files:\n        # Load from directory\n        if not input_path.is_dir():\n            raise ValueError(f\"Expected directory for separate_files=True, got: {input_path}\")\n\n        results = {}\n        for csv_file in input_path.glob(\"*.csv\"):\n            layer_name = csv_file.stem\n            df = pd.read_csv(csv_file)\n            results[layer_name] = df\n\n        return results\n\n    else:\n        # Load combined CSV\n        df = pd.read_csv(input_path)\n\n        if \"layer\" not in df.columns:\n            raise ValueError(\"CSV must have 'layer' column for combined format\")\n\n        results = {}\n        for layer_name in df[\"layer\"].unique():\n            layer_df = df[df[\"layer\"] == layer_name].copy()\n            layer_df = layer_df.drop(columns=[\"layer\"])\n            results[layer_name] = layer_df\n\n        return results\n</code></pre>"},{"location":"api/ndt/export/csv/#ndt.export.csv.load_from_csv--or-from-directory","title":"Or from directory:","text":"<p>results = load_from_csv(\"results_dir/\", separate_files=True)</p>"},{"location":"api/ndt/export/hdf5/","title":"Hdf5","text":""},{"location":"api/ndt/export/hdf5/#ndt.export.hdf5","title":"<code>ndt.export.hdf5</code>","text":"<p>HDF5 export functionality for large-scale tracking results.</p>"},{"location":"api/ndt/export/hdf5/#ndt.export.hdf5-functions","title":"Functions:","text":""},{"location":"api/ndt/export/hdf5/#ndt.export.hdf5.export_to_hdf5","title":"<code>export_to_hdf5(results: Dict[str, pd.DataFrame], output_path: str, compression: str = 'gzip', compression_opts: int = 4, metadata: Optional[Dict[str, Any]] = None) -&gt; None</code>","text":"<p>Export tracking results to HDF5 format (efficient for large data).</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>output_path</code> <code>str</code> <p>Output HDF5 file path</p> required <code>compression</code> <code>str</code> <p>Compression algorithm (\"gzip\", \"lzf\", or None)</p> <code>'gzip'</code> <code>compression_opts</code> <code>int</code> <p>Compression level (0-9 for gzip)</p> <code>4</code> <code>metadata</code> <code>Optional[Dict[str, Any]]</code> <p>Optional metadata to store as attributes</p> <code>None</code> Example <p>results = tracker.get_results() metadata = {\"model\": \"ResNet18\", \"dataset\": \"CIFAR-10\"} export_to_hdf5(results, \"results.h5\", metadata=metadata)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/hdf5.py</code> <pre><code>def export_to_hdf5(\n    results: Dict[str, pd.DataFrame],\n    output_path: str,\n    compression: str = \"gzip\",\n    compression_opts: int = 4,\n    metadata: Optional[Dict[str, Any]] = None,\n) -&gt; None:\n    \"\"\"Export tracking results to HDF5 format (efficient for large data).\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames\n        output_path: Output HDF5 file path\n        compression: Compression algorithm (\"gzip\", \"lzf\", or None)\n        compression_opts: Compression level (0-9 for gzip)\n        metadata: Optional metadata to store as attributes\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; metadata = {\"model\": \"ResNet18\", \"dataset\": \"CIFAR-10\"}\n        &gt;&gt;&gt; export_to_hdf5(results, \"results.h5\", metadata=metadata)\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    with h5py.File(output_path, \"w\") as f:\n        # Store metadata as root attributes\n        if metadata:\n            for key, value in metadata.items():\n                f.attrs[key] = value\n\n        # Store each layer's data as a group\n        for layer_name, df in results.items():\n            # Create group for this layer\n            group = f.create_group(layer_name)\n\n            # Store each column as a dataset\n            for col in df.columns:\n                data = df[col].values\n\n                # Handle different data types\n                if data.dtype == object:\n                    # Convert to string for object columns\n                    data = data.astype(str)\n\n                group.create_dataset(\n                    col,\n                    data=data,\n                    compression=compression,\n                    compression_opts=compression_opts if compression == \"gzip\" else None,\n                )\n\n            # Store column names as attribute\n            group.attrs[\"columns\"] = list(df.columns)\n\n    print(f\"Exported results to {output_path}\")\n</code></pre>"},{"location":"api/ndt/export/hdf5/#ndt.export.hdf5.load_from_hdf5","title":"<code>load_from_hdf5(input_path: str) -&gt; Tuple[Dict[str, pd.DataFrame], Dict[str, Any]]</code>","text":"<p>Load tracking results from HDF5 format.</p> <p>Parameters:</p> Name Type Description Default <code>input_path</code> <code>str</code> <p>Input HDF5 file path</p> required <p>Returns:</p> Type Description <code>Tuple[Dict[str, DataFrame], Dict[str, Any]]</code> <p>Tuple of (results_dict, metadata_dict)</p> Example <p>results, metadata = load_from_hdf5(\"results.h5\") print(f\"Model: {metadata['model']}\")</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If input_path doesn't exist</p> Source code in <code>src/ndt/export/hdf5.py</code> <pre><code>def load_from_hdf5(input_path: str) -&gt; Tuple[Dict[str, pd.DataFrame], Dict[str, Any]]:\n    \"\"\"Load tracking results from HDF5 format.\n\n    Args:\n        input_path: Input HDF5 file path\n\n    Returns:\n        Tuple of (results_dict, metadata_dict)\n\n    Example:\n        &gt;&gt;&gt; results, metadata = load_from_hdf5(\"results.h5\")\n        &gt;&gt;&gt; print(f\"Model: {metadata['model']}\")\n\n    Raises:\n        FileNotFoundError: If input_path doesn't exist\n    \"\"\"\n    input_path = Path(input_path)\n\n    if not input_path.exists():\n        raise FileNotFoundError(f\"File not found: {input_path}\")\n\n    results = {}\n    metadata = {}\n\n    with h5py.File(input_path, \"r\") as f:\n        # Load root metadata\n        for key in f.attrs.keys():\n            metadata[key] = f.attrs[key]\n\n        # Load each layer's data\n        for layer_name in f.keys():\n            group = f[layer_name]\n\n            # Reconstruct DataFrame\n            data = {}\n            for col in group.attrs[\"columns\"]:\n                data[col] = group[col][:]\n\n            results[layer_name] = pd.DataFrame(data)\n\n    return results, metadata\n</code></pre>"},{"location":"api/ndt/export/hdf5/#ndt.export.hdf5.append_to_hdf5","title":"<code>append_to_hdf5(results: Dict[str, pd.DataFrame], output_path: str, compression: str = 'gzip') -&gt; None</code>","text":"<p>Append new tracking results to existing HDF5 file.</p> <p>Useful for checkpointing during long training runs.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames with new data</p> required <code>output_path</code> <code>str</code> <p>Output HDF5 file path</p> required <code>compression</code> <code>str</code> <p>Compression algorithm</p> <code>'gzip'</code> Example <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If output_path doesn't exist</p> <code>ValueError</code> <p>If layer structure doesn't match</p> Source code in <code>src/ndt/export/hdf5.py</code> <pre><code>def append_to_hdf5(\n    results: Dict[str, pd.DataFrame], output_path: str, compression: str = \"gzip\"\n) -&gt; None:\n    \"\"\"Append new tracking results to existing HDF5 file.\n\n    Useful for checkpointing during long training runs.\n\n    Args:\n        results: Dictionary mapping layer names to DataFrames with new data\n        output_path: Output HDF5 file path\n        compression: Compression algorithm\n\n    Example:\n        &gt;&gt;&gt; # After training 1000 more steps\n        &gt;&gt;&gt; new_results = tracker.get_results()\n        &gt;&gt;&gt; append_to_hdf5(new_results, \"results.h5\")\n\n    Raises:\n        FileNotFoundError: If output_path doesn't exist\n        ValueError: If layer structure doesn't match\n    \"\"\"\n    output_path = Path(output_path)\n\n    if not output_path.exists():\n        raise FileNotFoundError(\n            f\"File not found: {output_path}. Use export_to_hdf5() to create new file.\"\n        )\n\n    with h5py.File(output_path, \"a\") as f:\n        for layer_name, new_df in results.items():\n            if layer_name not in f:\n                raise ValueError(f\"Layer {layer_name} not found in existing file\")\n\n            group = f[layer_name]\n\n            # For each column, append data\n            for col in new_df.columns:\n                if col not in group:\n                    raise ValueError(f\"Column {col} not found in layer {layer_name}\")\n\n                # Load existing data\n                existing_data = group[col][:]\n                new_data = new_df[col].values\n\n                # Concatenate\n                combined_data = np.concatenate([existing_data, new_data])\n\n                # Delete old dataset and create new one\n                del group[col]\n                group.create_dataset(col, data=combined_data, compression=compression)\n\n    print(f\"Appended results to {output_path}\")\n</code></pre>"},{"location":"api/ndt/export/hdf5/#ndt.export.hdf5.append_to_hdf5--after-training-1000-more-steps","title":"After training 1000 more steps","text":"<p>new_results = tracker.get_results() append_to_hdf5(new_results, \"results.h5\")</p>"},{"location":"api/ndt/export/json/","title":"Json","text":""},{"location":"api/ndt/export/json/#ndt.export.json","title":"<code>ndt.export.json</code>","text":"<p>JSON export functionality for tracking results.</p>"},{"location":"api/ndt/export/json/#ndt.export.json-functions","title":"Functions:","text":""},{"location":"api/ndt/export/json/#ndt.export.json.export_to_json","title":"<code>export_to_json(results: Dict[str, pd.DataFrame], output_path: str, pretty: bool = True) -&gt; None</code>","text":"<p>Export tracking results to JSON format.</p> <p>Parameters:</p> Name Type Description Default <code>output_path</code> <code>str</code> <p>Output file path</p> required <code>results</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>pretty</code> <code>bool</code> <p>If True, format JSON with indentation</p> <code>True</code> Example <p>results = tracker.get_results() export_to_json(results, \"results.json\")</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_path is invalid</p> Source code in <code>src/ndt/export/json.py</code> <pre><code>def export_to_json(results: Dict[str, pd.DataFrame], output_path: str, pretty: bool = True) -&gt; None:\n    \"\"\"Export tracking results to JSON format.\n\n    Args:\n        output_path: Output file path\n        results: Dictionary mapping layer names to DataFrames\n        pretty: If True, format JSON with indentation\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; export_to_json(results, \"results.json\")\n\n    Raises:\n        ValueError: If output_path is invalid\n    \"\"\"\n    output_path = Path(output_path)\n\n    # Convert DataFrames to dictionaries\n    json_data = {}\n    for layer_name, df in results.items():\n        json_data[layer_name] = df.to_dict(orient=\"records\")\n\n    # Write to file\n    with open(output_path, \"w\") as f:\n        if pretty:\n            json.dump(json_data, f, indent=2)\n        else:\n            json.dump(json_data, f)\n\n    print(f\"Exported results to {output_path}\")\n</code></pre>"},{"location":"api/ndt/export/json/#ndt.export.json.load_from_json","title":"<code>load_from_json(input_path: str) -&gt; Dict[str, pd.DataFrame]</code>","text":"<p>Load tracking results from JSON format.</p> <p>Parameters:</p> Name Type Description Default <code>input_path</code> <code>str</code> <p>Input file path</p> required <p>Returns:</p> Type Description <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> Example <p>results = load_from_json(\"results.json\")</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If input_path doesn't exist</p> <code>ValueError</code> <p>If JSON format is invalid</p> Source code in <code>src/ndt/export/json.py</code> <pre><code>def load_from_json(input_path: str) -&gt; Dict[str, pd.DataFrame]:\n    \"\"\"Load tracking results from JSON format.\n\n    Args:\n        input_path: Input file path\n\n    Returns:\n        Dictionary mapping layer names to DataFrames\n\n    Example:\n        &gt;&gt;&gt; results = load_from_json(\"results.json\")\n\n    Raises:\n        FileNotFoundError: If input_path doesn't exist\n        ValueError: If JSON format is invalid\n    \"\"\"\n    input_path = Path(input_path)\n\n    if not input_path.exists():\n        raise FileNotFoundError(f\"File not found: {input_path}\")\n\n    with open(input_path) as f:\n        json_data = json.load(f)\n\n    if not isinstance(json_data, dict):\n        raise ValueError(\"JSON must be a dictionary mapping layer names to data\")\n\n    # Convert back to DataFrames\n    results = {}\n    for layer_name, data in json_data.items():\n        results[layer_name] = pd.DataFrame(data)\n\n    return results\n</code></pre>"},{"location":"api/ndt/export/json/#ndt.export.json.export_metadata","title":"<code>export_metadata(metadata: Dict[str, Any], output_path: str, pretty: bool = True) -&gt; None</code>","text":"<p>Export tracker metadata to JSON.</p> <p>Parameters:</p> Name Type Description Default <code>metadata</code> <code>Dict[str, Any]</code> <p>Dictionary containing tracker configuration and metadata</p> required <code>output_path</code> <code>str</code> <p>Output file path</p> required <code>pretty</code> <code>bool</code> <p>If True, format JSON with indentation</p> <code>True</code> Example <p>metadata = { ...     \"model_name\": \"ResNet18\", ...     \"dataset\": \"CIFAR-10\", ...     \"sampling_frequency\": 10, ...     \"total_steps\": 5000 ... } export_metadata(metadata, \"metadata.json\")</p> Source code in <code>src/ndt/export/json.py</code> <pre><code>def export_metadata(metadata: Dict[str, Any], output_path: str, pretty: bool = True) -&gt; None:\n    \"\"\"Export tracker metadata to JSON.\n\n    Args:\n        metadata: Dictionary containing tracker configuration and metadata\n        output_path: Output file path\n        pretty: If True, format JSON with indentation\n\n    Example:\n        &gt;&gt;&gt; metadata = {\n        ...     \"model_name\": \"ResNet18\",\n        ...     \"dataset\": \"CIFAR-10\",\n        ...     \"sampling_frequency\": 10,\n        ...     \"total_steps\": 5000\n        ... }\n        &gt;&gt;&gt; export_metadata(metadata, \"metadata.json\")\n    \"\"\"\n    output_path = Path(output_path)\n\n    with open(output_path, \"w\") as f:\n        if pretty:\n            json.dump(metadata, f, indent=2)\n        else:\n            json.dump(metadata, f)\n\n    print(f\"Exported metadata to {output_path}\")\n</code></pre>"},{"location":"api/ndt/utils/","title":"Index","text":""},{"location":"api/ndt/utils/#ndt.utils","title":"<code>ndt.utils</code>","text":"<p>Utility functions for the neural dimensionality tracker.</p>"},{"location":"api/ndt/utils/#ndt.utils-functions","title":"Functions:","text":""},{"location":"api/ndt/utils/#ndt.utils.load_config","title":"<code>load_config(config_path: str) -&gt; Dict[str, Any]</code>","text":"<p>Load configuration from YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to YAML configuration file</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Configuration dictionary</p> Example <p>config = load_config(\"tracker_config.yaml\") tracker = HighFrequencyTracker(**config)</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If config file doesn't exist</p> <code>YAMLError</code> <p>If YAML is invalid</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def load_config(config_path: str) -&gt; Dict[str, Any]:\n    \"\"\"Load configuration from YAML file.\n\n    Args:\n        config_path: Path to YAML configuration file\n\n    Returns:\n        Configuration dictionary\n\n    Example:\n        &gt;&gt;&gt; config = load_config(\"tracker_config.yaml\")\n        &gt;&gt;&gt; tracker = HighFrequencyTracker(**config)\n\n    Raises:\n        FileNotFoundError: If config file doesn't exist\n        yaml.YAMLError: If YAML is invalid\n    \"\"\"\n    config_path = Path(config_path)\n\n    if not config_path.exists():\n        raise FileNotFoundError(f\"Config file not found: {config_path}\")\n\n    with open(config_path) as f:\n        config = yaml.safe_load(f)\n\n    return config\n</code></pre>"},{"location":"api/ndt/utils/#ndt.utils.save_config","title":"<code>save_config(config: Dict[str, Any], output_path: str) -&gt; None</code>","text":"<p>Save configuration to YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Dict[str, Any]</code> <p>Configuration dictionary</p> required <code>output_path</code> <code>str</code> <p>Output file path</p> required Example <p>config = { ...     \"sampling_frequency\": 10, ...     \"enable_jump_detection\": True, ...     \"jump_z_threshold\": 3.0 ... } save_config(config, \"tracker_config.yaml\")</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def save_config(config: Dict[str, Any], output_path: str) -&gt; None:\n    \"\"\"Save configuration to YAML file.\n\n    Args:\n        config: Configuration dictionary\n        output_path: Output file path\n\n    Example:\n        &gt;&gt;&gt; config = {\n        ...     \"sampling_frequency\": 10,\n        ...     \"enable_jump_detection\": True,\n        ...     \"jump_z_threshold\": 3.0\n        ... }\n        &gt;&gt;&gt; save_config(config, \"tracker_config.yaml\")\n    \"\"\"\n    output_path = Path(output_path)\n\n    with open(output_path, \"w\") as f:\n        yaml.dump(config, f, default_flow_style=False, sort_keys=False)\n\n    print(f\"Saved configuration to {output_path}\")\n</code></pre>"},{"location":"api/ndt/utils/#ndt.utils.setup_logger","title":"<code>setup_logger(name: str = 'ndt', level: int = logging.INFO, log_file: Optional[str] = None, format_string: Optional[str] = None) -&gt; logging.Logger</code>","text":"<p>Set up a logger with console and optional file handlers.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Logger name (default: \"ndt\")</p> <code>'ndt'</code> <code>level</code> <code>int</code> <p>Logging level (default: INFO)</p> <code>INFO</code> <code>log_file</code> <code>Optional[str]</code> <p>Optional file path for logging</p> <code>None</code> <code>format_string</code> <code>Optional[str]</code> <p>Optional custom format string</p> <code>None</code> <p>Returns:</p> Type Description <code>Logger</code> <p>Configured logger instance</p> Example <p>logger = setup_logger(\"ndt\", level=logging.DEBUG, log_file=\"tracking.log\") logger.info(\"Starting tracking...\")</p> Source code in <code>src/ndt/utils/logging.py</code> <pre><code>def setup_logger(\n    name: str = \"ndt\",\n    level: int = logging.INFO,\n    log_file: Optional[str] = None,\n    format_string: Optional[str] = None,\n) -&gt; logging.Logger:\n    \"\"\"Set up a logger with console and optional file handlers.\n\n    Args:\n        name: Logger name (default: \"ndt\")\n        level: Logging level (default: INFO)\n        log_file: Optional file path for logging\n        format_string: Optional custom format string\n\n    Returns:\n        Configured logger instance\n\n    Example:\n        &gt;&gt;&gt; logger = setup_logger(\"ndt\", level=logging.DEBUG, log_file=\"tracking.log\")\n        &gt;&gt;&gt; logger.info(\"Starting tracking...\")\n    \"\"\"\n    logger = logging.getLogger(name)\n    logger.setLevel(level)\n\n    # Remove existing handlers to avoid duplicates\n    logger.handlers = []\n\n    # Default format\n    if format_string is None:\n        format_string = \"%(asctime)s - %(name)s - %(levelname)s - %(message)s\"\n\n    formatter = logging.Formatter(format_string)\n\n    # Console handler\n    console_handler = logging.StreamHandler(sys.stdout)\n    console_handler.setLevel(level)\n    console_handler.setFormatter(formatter)\n    logger.addHandler(console_handler)\n\n    # File handler if specified\n    if log_file:\n        file_handler = logging.FileHandler(log_file)\n        file_handler.setLevel(level)\n        file_handler.setFormatter(formatter)\n        logger.addHandler(file_handler)\n\n    return logger\n</code></pre>"},{"location":"api/ndt/utils/config/","title":"Config","text":""},{"location":"api/ndt/utils/config/#ndt.utils.config","title":"<code>ndt.utils.config</code>","text":"<p>Configuration management utilities.</p>"},{"location":"api/ndt/utils/config/#ndt.utils.config-functions","title":"Functions:","text":""},{"location":"api/ndt/utils/config/#ndt.utils.config.load_config","title":"<code>load_config(config_path: str) -&gt; Dict[str, Any]</code>","text":"<p>Load configuration from YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>Path to YAML configuration file</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Configuration dictionary</p> Example <p>config = load_config(\"tracker_config.yaml\") tracker = HighFrequencyTracker(**config)</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If config file doesn't exist</p> <code>YAMLError</code> <p>If YAML is invalid</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def load_config(config_path: str) -&gt; Dict[str, Any]:\n    \"\"\"Load configuration from YAML file.\n\n    Args:\n        config_path: Path to YAML configuration file\n\n    Returns:\n        Configuration dictionary\n\n    Example:\n        &gt;&gt;&gt; config = load_config(\"tracker_config.yaml\")\n        &gt;&gt;&gt; tracker = HighFrequencyTracker(**config)\n\n    Raises:\n        FileNotFoundError: If config file doesn't exist\n        yaml.YAMLError: If YAML is invalid\n    \"\"\"\n    config_path = Path(config_path)\n\n    if not config_path.exists():\n        raise FileNotFoundError(f\"Config file not found: {config_path}\")\n\n    with open(config_path) as f:\n        config = yaml.safe_load(f)\n\n    return config\n</code></pre>"},{"location":"api/ndt/utils/config/#ndt.utils.config.save_config","title":"<code>save_config(config: Dict[str, Any], output_path: str) -&gt; None</code>","text":"<p>Save configuration to YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Dict[str, Any]</code> <p>Configuration dictionary</p> required <code>output_path</code> <code>str</code> <p>Output file path</p> required Example <p>config = { ...     \"sampling_frequency\": 10, ...     \"enable_jump_detection\": True, ...     \"jump_z_threshold\": 3.0 ... } save_config(config, \"tracker_config.yaml\")</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def save_config(config: Dict[str, Any], output_path: str) -&gt; None:\n    \"\"\"Save configuration to YAML file.\n\n    Args:\n        config: Configuration dictionary\n        output_path: Output file path\n\n    Example:\n        &gt;&gt;&gt; config = {\n        ...     \"sampling_frequency\": 10,\n        ...     \"enable_jump_detection\": True,\n        ...     \"jump_z_threshold\": 3.0\n        ... }\n        &gt;&gt;&gt; save_config(config, \"tracker_config.yaml\")\n    \"\"\"\n    output_path = Path(output_path)\n\n    with open(output_path, \"w\") as f:\n        yaml.dump(config, f, default_flow_style=False, sort_keys=False)\n\n    print(f\"Saved configuration to {output_path}\")\n</code></pre>"},{"location":"api/ndt/utils/config/#ndt.utils.config.get_default_config","title":"<code>get_default_config() -&gt; Dict[str, Any]</code>","text":"<p>Get default configuration for the tracker.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Default configuration dictionary</p> Example <p>config = get_default_config() config['sampling_frequency'] = 5  # Customize tracker = HighFrequencyTracker(model, **config)</p> Source code in <code>src/ndt/utils/config.py</code> <pre><code>def get_default_config() -&gt; Dict[str, Any]:\n    \"\"\"Get default configuration for the tracker.\n\n    Returns:\n        Default configuration dictionary\n\n    Example:\n        &gt;&gt;&gt; config = get_default_config()\n        &gt;&gt;&gt; config['sampling_frequency'] = 5  # Customize\n        &gt;&gt;&gt; tracker = HighFrequencyTracker(model, **config)\n    \"\"\"\n    return {\n        \"sampling_frequency\": 1,\n        \"enable_jump_detection\": True,\n        \"jump_window_size\": 50,\n        \"jump_z_threshold\": 3.0,\n    }\n</code></pre>"},{"location":"api/ndt/utils/logging/","title":"Logging","text":""},{"location":"api/ndt/utils/logging/#ndt.utils.logging","title":"<code>ndt.utils.logging</code>","text":"<p>Logging utilities for the tracker.</p>"},{"location":"api/ndt/utils/logging/#ndt.utils.logging-functions","title":"Functions:","text":""},{"location":"api/ndt/utils/logging/#ndt.utils.logging.setup_logger","title":"<code>setup_logger(name: str = 'ndt', level: int = logging.INFO, log_file: Optional[str] = None, format_string: Optional[str] = None) -&gt; logging.Logger</code>","text":"<p>Set up a logger with console and optional file handlers.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Logger name (default: \"ndt\")</p> <code>'ndt'</code> <code>level</code> <code>int</code> <p>Logging level (default: INFO)</p> <code>INFO</code> <code>log_file</code> <code>Optional[str]</code> <p>Optional file path for logging</p> <code>None</code> <code>format_string</code> <code>Optional[str]</code> <p>Optional custom format string</p> <code>None</code> <p>Returns:</p> Type Description <code>Logger</code> <p>Configured logger instance</p> Example <p>logger = setup_logger(\"ndt\", level=logging.DEBUG, log_file=\"tracking.log\") logger.info(\"Starting tracking...\")</p> Source code in <code>src/ndt/utils/logging.py</code> <pre><code>def setup_logger(\n    name: str = \"ndt\",\n    level: int = logging.INFO,\n    log_file: Optional[str] = None,\n    format_string: Optional[str] = None,\n) -&gt; logging.Logger:\n    \"\"\"Set up a logger with console and optional file handlers.\n\n    Args:\n        name: Logger name (default: \"ndt\")\n        level: Logging level (default: INFO)\n        log_file: Optional file path for logging\n        format_string: Optional custom format string\n\n    Returns:\n        Configured logger instance\n\n    Example:\n        &gt;&gt;&gt; logger = setup_logger(\"ndt\", level=logging.DEBUG, log_file=\"tracking.log\")\n        &gt;&gt;&gt; logger.info(\"Starting tracking...\")\n    \"\"\"\n    logger = logging.getLogger(name)\n    logger.setLevel(level)\n\n    # Remove existing handlers to avoid duplicates\n    logger.handlers = []\n\n    # Default format\n    if format_string is None:\n        format_string = \"%(asctime)s - %(name)s - %(levelname)s - %(message)s\"\n\n    formatter = logging.Formatter(format_string)\n\n    # Console handler\n    console_handler = logging.StreamHandler(sys.stdout)\n    console_handler.setLevel(level)\n    console_handler.setFormatter(formatter)\n    logger.addHandler(console_handler)\n\n    # File handler if specified\n    if log_file:\n        file_handler = logging.FileHandler(log_file)\n        file_handler.setLevel(level)\n        file_handler.setFormatter(formatter)\n        logger.addHandler(file_handler)\n\n    return logger\n</code></pre>"},{"location":"api/ndt/validity/","title":"Index","text":""},{"location":"api/ndt/validity/#ndt.validity","title":"<code>ndt.validity</code>","text":"<p>Validity harness for transition detectors.</p> <p>Before you claim a phase transition, check that your detector is not reading noise. This module runs any detector against ground-truth fixtures (known transitions and known nulls) and returns a verdict on whether its detections can be trusted.</p> <p>Quick start:</p> <pre><code>&gt;&gt;&gt; from ndt import JumpDetector\n&gt;&gt;&gt; from ndt.validity import validate_detector, jump_detector_as_callable\n&gt;&gt;&gt; det = jump_detector_as_callable(JumpDetector(z_threshold=3.0))\n&gt;&gt;&gt; report = validate_detector(det, name=\"JumpDetector(z=3)\")\n&gt;&gt;&gt; print(report.render())\n</code></pre>"},{"location":"api/ndt/validity/#ndt.validity-classes","title":"Classes","text":""},{"location":"api/ndt/validity/#ndt.validity.Fixture","title":"<code>Fixture(values: np.ndarray, ground_truth: tuple[int, ...], name: str, tolerance: int = 5, meta: dict = dict())</code>  <code>dataclass</code>","text":"<p>A trajectory paired with its known transition steps.</p> <p>Attributes:</p> Name Type Description <code>values</code> <code>ndarray</code> <p>The metric trajectory, one value per training step.</p> <code>ground_truth</code> <code>tuple[int, ...]</code> <p>The step indices where a transition truly occurs. Empty for a null fixture (no transition).</p> <code>name</code> <code>str</code> <p>A short label for reports.</p> <code>tolerance</code> <code>int</code> <p>How many steps away a detection may fall and still count as recovering a ground-truth transition. Detection is a noisy business; an exact-step match is too strict.</p>"},{"location":"api/ndt/validity/#ndt.validity.FixtureResult","title":"<code>FixtureResult(fixture: str, n_steps: int, n_ground_truth: int, detected: tuple[int, ...], true_positives: int, false_positives: int, false_negatives: int)</code>  <code>dataclass</code>","text":"<p>A detector's outcome on one fixture.</p>"},{"location":"api/ndt/validity/#ndt.validity.FixtureResult-attributes","title":"Attributes","text":""},{"location":"api/ndt/validity/#ndt.validity.FixtureResult.is_null","title":"<code>is_null: bool</code>  <code>property</code>","text":"<p>True when the fixture has no transition (a negative control).</p>"},{"location":"api/ndt/validity/#ndt.validity.FixtureResult.recall","title":"<code>recall: float | None</code>  <code>property</code>","text":"<p>Fraction of ground-truth transitions recovered, or None for a null.</p>"},{"location":"api/ndt/validity/#ndt.validity.FixtureResult.false_positive_rate_per_1000","title":"<code>false_positive_rate_per_1000: float</code>  <code>property</code>","text":"<p>False detections per 1000 steps. The honest noise number.</p>"},{"location":"api/ndt/validity/#ndt.validity.ValidityReport","title":"<code>ValidityReport(detector_name: str, results: tuple[FixtureResult, ...], mean_recall: float, false_positives_on_null: int, valid: bool, message: str)</code>  <code>dataclass</code>","text":"<p>A verdict on whether a detector's transitions can be trusted.</p> <p><code>valid</code> is True only when the detector recovers most planted transitions and stays near-silent on the null controls. The thresholds are deliberately lenient; a detector that fails even these is not measuring transitions.</p>"},{"location":"api/ndt/validity/#ndt.validity.ValidityReport-functions","title":"Methods:","text":""},{"location":"api/ndt/validity/#ndt.validity.ValidityReport.render","title":"<code>render() -&gt; str</code>","text":"<p>Full multi-line report.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def render(self) -&gt; str:\n    \"\"\"Full multi-line report.\"\"\"\n    lines = [f\"Validity report for: {self.detector_name}\", \"\"]\n    for r in self.results:\n        if r.is_null:\n            lines.append(\n                f\"  {r.fixture:&lt;20} null control   \"\n                f\"false positives: {r.false_positives:&gt;3} \"\n                f\"({r.false_positive_rate_per_1000:.1f}/1000 steps)\"\n            )\n        else:\n            lines.append(\n                f\"  {r.fixture:&lt;20} recall: {r.recall:.2f}   \"\n                f\"false positives: {r.false_positives:&gt;3}\"\n            )\n    lines.append(\"\")\n    lines.append(f\"  mean recall on planted transitions : {self.mean_recall:.2f}\")\n    lines.append(f\"  false positives on null controls   : {self.false_positives_on_null}\")\n    lines.append(\"\")\n    lines.append(f\"  VERDICT: {'VALID' if self.valid else 'NOT VALID'} on these fixtures\")\n    lines.append(f\"  {self.message}\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"api/ndt/validity/#ndt.validity-functions","title":"Functions:","text":""},{"location":"api/ndt/validity/#ndt.validity.drift_no_jump","title":"<code>drift_no_jump(n_steps: int = 400, start: float = 10.0, end: float = 20.0, noise: float = 0.3, seed: int = 0) -&gt; Fixture</code>","text":"<p>A negative control with a slow, continuous drift and no discrete jump.</p> <p>This is the case the paper argues most training dynamics actually are: smooth, continuous change with no step. A detector that reports discrete transitions here is mistaking a ramp for a jump. Ground truth is empty.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def drift_no_jump(\n    n_steps: int = 400,\n    start: float = 10.0,\n    end: float = 20.0,\n    noise: float = 0.3,\n    seed: int = 0,\n) -&gt; Fixture:\n    \"\"\"A negative control with a slow, continuous drift and no discrete jump.\n\n    This is the case the paper argues most training dynamics actually are:\n    smooth, continuous change with no step. A detector that reports discrete\n    transitions here is mistaking a ramp for a jump. Ground truth is empty.\n    \"\"\"\n    rng = np.random.default_rng(seed)\n    values = np.linspace(start, end, n_steps) + rng.normal(0.0, noise, size=n_steps)\n    return Fixture(values=values, ground_truth=(), name=\"drift_no_jump\")\n</code></pre>"},{"location":"api/ndt/validity/#ndt.validity.planted_multi","title":"<code>planted_multi(n_steps: int = 600, changes: tuple[int, ...] = (150, 350, 450), levels: tuple[float, ...] = (10.0, 16.0, 13.0, 22.0), noise: float = 0.3, seed: int = 0) -&gt; Fixture</code>","text":"<p>A positive control with several known transitions.</p> <p><code>levels</code> gives the value in each segment; <code>changes</code> gives the steps between segments. <code>len(levels)</code> must be <code>len(changes) + 1</code>.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def planted_multi(\n    n_steps: int = 600,\n    changes: tuple[int, ...] = (150, 350, 450),\n    levels: tuple[float, ...] = (10.0, 16.0, 13.0, 22.0),\n    noise: float = 0.3,\n    seed: int = 0,\n) -&gt; Fixture:\n    \"\"\"A positive control with several known transitions.\n\n    ``levels`` gives the value in each segment; ``changes`` gives the steps\n    between segments. ``len(levels)`` must be ``len(changes) + 1``.\n    \"\"\"\n    if len(levels) != len(changes) + 1:\n        raise ValueError(\"levels must have exactly one more entry than changes\")\n    rng = np.random.default_rng(seed)\n    values = np.empty(n_steps, dtype=float)\n    bounds = (0, *changes, n_steps)\n    for level, lo, hi in zip(levels, bounds[:-1], bounds[1:]):\n        values[lo:hi] = level\n    values += rng.normal(0.0, noise, size=n_steps)\n    return Fixture(values=values, ground_truth=tuple(changes), name=\"planted_multi\")\n</code></pre>"},{"location":"api/ndt/validity/#ndt.validity.planted_transition","title":"<code>planted_transition(n_steps: int = 400, change_at: int = 200, before: float = 10.0, after: float = 20.0, noise: float = 0.3, seed: int = 0) -&gt; Fixture</code>","text":"<p>A positive control: one known step change at <code>change_at</code>.</p> <p>The trajectory sits near <code>before</code>, then steps to <code>after</code> at <code>change_at</code>, with Gaussian measurement noise. Ground truth is the single step <code>change_at</code>. A valid detector recovers it; the absence of any other detection is the precision test.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def planted_transition(\n    n_steps: int = 400,\n    change_at: int = 200,\n    before: float = 10.0,\n    after: float = 20.0,\n    noise: float = 0.3,\n    seed: int = 0,\n) -&gt; Fixture:\n    \"\"\"A positive control: one known step change at ``change_at``.\n\n    The trajectory sits near ``before``, then steps to ``after`` at\n    ``change_at``, with Gaussian measurement noise. Ground truth is the single\n    step ``change_at``. A valid detector recovers it; the absence of any other\n    detection is the precision test.\n    \"\"\"\n    rng = np.random.default_rng(seed)\n    values = np.empty(n_steps, dtype=float)\n    values[:change_at] = before\n    values[change_at:] = after\n    values += rng.normal(0.0, noise, size=n_steps)\n    return Fixture(values=values, ground_truth=(change_at,), name=\"planted_transition\")\n</code></pre>"},{"location":"api/ndt/validity/#ndt.validity.pure_noise","title":"<code>pure_noise(n_steps: int = 400, noise: float = 1.0, seed: int = 0) -&gt; Fixture</code>","text":"<p>A negative control: no transition, only noise.</p> <p>A detector should return no transitions on this fixture. Anything it fires is a false positive, and the rate of firing on pure noise is the single most honest number you can put on a transition detector.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def pure_noise(n_steps: int = 400, noise: float = 1.0, seed: int = 0) -&gt; Fixture:\n    \"\"\"A negative control: no transition, only noise.\n\n    A detector should return no transitions on this fixture. Anything it fires\n    is a false positive, and the rate of firing on pure noise is the single most\n    honest number you can put on a transition detector.\n    \"\"\"\n    rng = np.random.default_rng(seed)\n    values = rng.normal(0.0, noise, size=n_steps)\n    return Fixture(values=values, ground_truth=(), name=\"pure_noise\")\n</code></pre>"},{"location":"api/ndt/validity/#ndt.validity.standard_battery","title":"<code>standard_battery() -&gt; list[Fixture]</code>","text":"<p>The default validity battery: one positive control and three negatives.</p> <p>Returns a planted single transition, a planted multi transition, pure noise, and a continuous drift. A detector worth trusting recovers the planted transitions and stays silent on the noise and the drift.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def standard_battery() -&gt; list[Fixture]:\n    \"\"\"The default validity battery: one positive control and three negatives.\n\n    Returns a planted single transition, a planted multi transition, pure noise,\n    and a continuous drift. A detector worth trusting recovers the planted\n    transitions and stays silent on the noise and the drift.\n    \"\"\"\n    return [\n        planted_transition(),\n        planted_multi(),\n        pure_noise(),\n        drift_no_jump(),\n    ]\n</code></pre>"},{"location":"api/ndt/validity/#ndt.validity.jump_detector_as_callable","title":"<code>jump_detector_as_callable(detector, metric_name: str = 'metric') -&gt; Detector</code>","text":"<p>Adapt an <code>ndt.JumpDetector</code> to the harness detector protocol.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def jump_detector_as_callable(detector, metric_name: str = \"metric\") -&gt; Detector:\n    \"\"\"Adapt an ``ndt.JumpDetector`` to the harness detector protocol.\"\"\"\n\n    def _call(values: Sequence[float]) -&gt; list[int]:\n        jumps = detector.detect_jumps(list(values), metric_name=metric_name)\n        return [j.step for j in jumps]\n\n    return _call\n</code></pre>"},{"location":"api/ndt/validity/#ndt.validity.validate_detector","title":"<code>validate_detector(detector: Detector, fixtures: Sequence[Fixture] | None = None, *, name: str = 'detector', min_recall: float = 0.75, max_false_positives_per_1000: float = 2.0) -&gt; ValidityReport</code>","text":"<p>Run <code>detector</code> against ground-truth fixtures and return a verdict.</p> <p>Parameters:</p> Name Type Description Default <code>detector</code> <code>Detector</code> <p>A callable mapping a value sequence to detected step indices. Wrap an <code>ndt.JumpDetector</code> with :func:<code>jump_detector_as_callable</code>.</p> required <code>fixtures</code> <code>Sequence[Fixture] | None</code> <p>The ground-truth battery. Defaults to :func:<code>ndt.validity.fixtures.standard_battery</code>.</p> <code>None</code> <code>name</code> <code>str</code> <p>Label for the report.</p> <code>'detector'</code> <code>min_recall</code> <code>float</code> <p>Minimum mean recall on planted transitions to pass.</p> <code>0.75</code> <code>max_false_positives_per_1000</code> <code>float</code> <p>Maximum false-positive rate on any null control to pass.</p> <code>2.0</code> <p>Returns:</p> Name Type Description <code>A</code> <code>ValidityReport</code> <p>class:<code>ValidityReport</code>. <code>valid</code> is True only when the detector</p> <code>ValidityReport</code> <p>clears both bars.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def validate_detector(\n    detector: Detector,\n    fixtures: Sequence[Fixture] | None = None,\n    *,\n    name: str = \"detector\",\n    min_recall: float = 0.75,\n    max_false_positives_per_1000: float = 2.0,\n) -&gt; ValidityReport:\n    \"\"\"Run ``detector`` against ground-truth fixtures and return a verdict.\n\n    Args:\n        detector: A callable mapping a value sequence to detected step indices.\n            Wrap an ``ndt.JumpDetector`` with :func:`jump_detector_as_callable`.\n        fixtures: The ground-truth battery. Defaults to\n            :func:`ndt.validity.fixtures.standard_battery`.\n        name: Label for the report.\n        min_recall: Minimum mean recall on planted transitions to pass.\n        max_false_positives_per_1000: Maximum false-positive rate on any null\n            control to pass.\n\n    Returns:\n        A :class:`ValidityReport`. ``valid`` is True only when the detector\n        clears both bars.\n    \"\"\"\n    fixtures = list(fixtures) if fixtures is not None else standard_battery()\n    results: list[FixtureResult] = []\n    recalls: list[float] = []\n    fp_on_null = 0\n    worst_null_rate = 0.0\n\n    for fx in fixtures:\n        detected = [int(d) for d in detector(np.asarray(fx.values))]\n        tp, fp, fn = _match(detected, fx.ground_truth, fx.tolerance)\n        res = FixtureResult(\n            fixture=fx.name,\n            n_steps=len(fx.values),\n            n_ground_truth=len(fx.ground_truth),\n            detected=tuple(sorted(detected)),\n            true_positives=tp,\n            false_positives=fp,\n            false_negatives=fn,\n        )\n        results.append(res)\n        if res.is_null:\n            fp_on_null += res.false_positives\n            worst_null_rate = max(worst_null_rate, res.false_positive_rate_per_1000)\n        else:\n            recalls.append(res.recall)\n\n    mean_recall = float(np.mean(recalls)) if recalls else 0.0\n    passes_recall = mean_recall &gt;= min_recall\n    passes_fp = worst_null_rate &lt;= max_false_positives_per_1000\n    valid = passes_recall and passes_fp\n\n    if valid:\n        message = (\n            \"Recovers planted transitions and stays quiet on noise and drift. \"\n            \"Its detections are trustworthy on data like these fixtures.\"\n        )\n    elif not passes_fp and not passes_recall:\n        message = (\n            \"Misses planted transitions AND fires on the null controls. \"\n            \"These detections reflect the method, not the data.\"\n        )\n    elif not passes_fp:\n        message = (\n            \"Fires on pure noise and continuous drift. It manufactures \"\n            \"transitions where there are none, so a detection on real data \"\n            \"cannot be trusted without this check.\"\n        )\n    else:\n        message = (\n            \"Stays quiet on the null controls but misses planted transitions. \"\n            \"It under-reports; real transitions can pass unnoticed.\"\n        )\n\n    return ValidityReport(\n        detector_name=name,\n        results=tuple(results),\n        mean_recall=mean_recall,\n        false_positives_on_null=fp_on_null,\n        valid=valid,\n        message=message,\n    )\n</code></pre>"},{"location":"api/ndt/validity/fixtures/","title":"Fixtures","text":""},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures","title":"<code>ndt.validity.fixtures</code>","text":"<p>Ground-truth fixtures for validating transition detectors.</p> <p>A transition detector claims to find discrete changes in a training-dynamics trajectory. You cannot know whether such a claim is real unless you test the detector on a trajectory whose transitions you already know. These fixtures provide that known ground truth.</p> <p>Two controls, mirrored on the scientific method:</p> <ul> <li>A planted transition is a positive control: a trajectory with exactly the   transitions you put in it. A detector that misses them produces a false   negative you can measure.</li> <li>Pure noise is a negative control: a trajectory with no transition at all.   A detector that fires on it produces a false positive you can measure.</li> </ul> <p>These fixtures are synthetic and cost nothing to generate: no model, no training, no GPU. They let you check a detector's validity in milliseconds. Real-model fixtures (for example a grokking run, where a transition is visible in test accuracy) plug into the same interface; see <code>Fixture</code>.</p>"},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures-classes","title":"Classes","text":""},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures.Fixture","title":"<code>Fixture(values: np.ndarray, ground_truth: tuple[int, ...], name: str, tolerance: int = 5, meta: dict = dict())</code>  <code>dataclass</code>","text":"<p>A trajectory paired with its known transition steps.</p> <p>Attributes:</p> Name Type Description <code>values</code> <code>ndarray</code> <p>The metric trajectory, one value per training step.</p> <code>ground_truth</code> <code>tuple[int, ...]</code> <p>The step indices where a transition truly occurs. Empty for a null fixture (no transition).</p> <code>name</code> <code>str</code> <p>A short label for reports.</p> <code>tolerance</code> <code>int</code> <p>How many steps away a detection may fall and still count as recovering a ground-truth transition. Detection is a noisy business; an exact-step match is too strict.</p>"},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures-functions","title":"Functions:","text":""},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures.planted_transition","title":"<code>planted_transition(n_steps: int = 400, change_at: int = 200, before: float = 10.0, after: float = 20.0, noise: float = 0.3, seed: int = 0) -&gt; Fixture</code>","text":"<p>A positive control: one known step change at <code>change_at</code>.</p> <p>The trajectory sits near <code>before</code>, then steps to <code>after</code> at <code>change_at</code>, with Gaussian measurement noise. Ground truth is the single step <code>change_at</code>. A valid detector recovers it; the absence of any other detection is the precision test.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def planted_transition(\n    n_steps: int = 400,\n    change_at: int = 200,\n    before: float = 10.0,\n    after: float = 20.0,\n    noise: float = 0.3,\n    seed: int = 0,\n) -&gt; Fixture:\n    \"\"\"A positive control: one known step change at ``change_at``.\n\n    The trajectory sits near ``before``, then steps to ``after`` at\n    ``change_at``, with Gaussian measurement noise. Ground truth is the single\n    step ``change_at``. A valid detector recovers it; the absence of any other\n    detection is the precision test.\n    \"\"\"\n    rng = np.random.default_rng(seed)\n    values = np.empty(n_steps, dtype=float)\n    values[:change_at] = before\n    values[change_at:] = after\n    values += rng.normal(0.0, noise, size=n_steps)\n    return Fixture(values=values, ground_truth=(change_at,), name=\"planted_transition\")\n</code></pre>"},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures.planted_multi","title":"<code>planted_multi(n_steps: int = 600, changes: tuple[int, ...] = (150, 350, 450), levels: tuple[float, ...] = (10.0, 16.0, 13.0, 22.0), noise: float = 0.3, seed: int = 0) -&gt; Fixture</code>","text":"<p>A positive control with several known transitions.</p> <p><code>levels</code> gives the value in each segment; <code>changes</code> gives the steps between segments. <code>len(levels)</code> must be <code>len(changes) + 1</code>.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def planted_multi(\n    n_steps: int = 600,\n    changes: tuple[int, ...] = (150, 350, 450),\n    levels: tuple[float, ...] = (10.0, 16.0, 13.0, 22.0),\n    noise: float = 0.3,\n    seed: int = 0,\n) -&gt; Fixture:\n    \"\"\"A positive control with several known transitions.\n\n    ``levels`` gives the value in each segment; ``changes`` gives the steps\n    between segments. ``len(levels)`` must be ``len(changes) + 1``.\n    \"\"\"\n    if len(levels) != len(changes) + 1:\n        raise ValueError(\"levels must have exactly one more entry than changes\")\n    rng = np.random.default_rng(seed)\n    values = np.empty(n_steps, dtype=float)\n    bounds = (0, *changes, n_steps)\n    for level, lo, hi in zip(levels, bounds[:-1], bounds[1:]):\n        values[lo:hi] = level\n    values += rng.normal(0.0, noise, size=n_steps)\n    return Fixture(values=values, ground_truth=tuple(changes), name=\"planted_multi\")\n</code></pre>"},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures.pure_noise","title":"<code>pure_noise(n_steps: int = 400, noise: float = 1.0, seed: int = 0) -&gt; Fixture</code>","text":"<p>A negative control: no transition, only noise.</p> <p>A detector should return no transitions on this fixture. Anything it fires is a false positive, and the rate of firing on pure noise is the single most honest number you can put on a transition detector.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def pure_noise(n_steps: int = 400, noise: float = 1.0, seed: int = 0) -&gt; Fixture:\n    \"\"\"A negative control: no transition, only noise.\n\n    A detector should return no transitions on this fixture. Anything it fires\n    is a false positive, and the rate of firing on pure noise is the single most\n    honest number you can put on a transition detector.\n    \"\"\"\n    rng = np.random.default_rng(seed)\n    values = rng.normal(0.0, noise, size=n_steps)\n    return Fixture(values=values, ground_truth=(), name=\"pure_noise\")\n</code></pre>"},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures.drift_no_jump","title":"<code>drift_no_jump(n_steps: int = 400, start: float = 10.0, end: float = 20.0, noise: float = 0.3, seed: int = 0) -&gt; Fixture</code>","text":"<p>A negative control with a slow, continuous drift and no discrete jump.</p> <p>This is the case the paper argues most training dynamics actually are: smooth, continuous change with no step. A detector that reports discrete transitions here is mistaking a ramp for a jump. Ground truth is empty.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def drift_no_jump(\n    n_steps: int = 400,\n    start: float = 10.0,\n    end: float = 20.0,\n    noise: float = 0.3,\n    seed: int = 0,\n) -&gt; Fixture:\n    \"\"\"A negative control with a slow, continuous drift and no discrete jump.\n\n    This is the case the paper argues most training dynamics actually are:\n    smooth, continuous change with no step. A detector that reports discrete\n    transitions here is mistaking a ramp for a jump. Ground truth is empty.\n    \"\"\"\n    rng = np.random.default_rng(seed)\n    values = np.linspace(start, end, n_steps) + rng.normal(0.0, noise, size=n_steps)\n    return Fixture(values=values, ground_truth=(), name=\"drift_no_jump\")\n</code></pre>"},{"location":"api/ndt/validity/fixtures/#ndt.validity.fixtures.standard_battery","title":"<code>standard_battery() -&gt; list[Fixture]</code>","text":"<p>The default validity battery: one positive control and three negatives.</p> <p>Returns a planted single transition, a planted multi transition, pure noise, and a continuous drift. A detector worth trusting recovers the planted transitions and stays silent on the noise and the drift.</p> Source code in <code>src/ndt/validity/fixtures.py</code> <pre><code>def standard_battery() -&gt; list[Fixture]:\n    \"\"\"The default validity battery: one positive control and three negatives.\n\n    Returns a planted single transition, a planted multi transition, pure noise,\n    and a continuous drift. A detector worth trusting recovers the planted\n    transitions and stays silent on the noise and the drift.\n    \"\"\"\n    return [\n        planted_transition(),\n        planted_multi(),\n        pure_noise(),\n        drift_no_jump(),\n    ]\n</code></pre>"},{"location":"api/ndt/validity/harness/","title":"Harness","text":""},{"location":"api/ndt/validity/harness/#ndt.validity.harness","title":"<code>ndt.validity.harness</code>","text":"<p>Validity harness: does a transition detector measure transitions, or noise?</p> <p>The harness runs a detector against ground-truth fixtures and reports two numbers that decide whether its detections can be trusted:</p> <ul> <li>recall on the positive controls: did it find the transitions that are   really there?</li> <li>false positives on the negative controls: did it fire where there is   nothing to find?</li> </ul> <p>A detector with high recall and near-zero false positives is valid on these fixtures. A detector that fires on pure noise is reading noise, whatever it reports on real data. This is a construct-validity check, the same idea the evaluation-science literature applies to any measurement instrument.</p> <p>Any detector works, as long as it is a callable that takes a sequence of values and returns the step indices where it detects a transition. The built-in <code>JumpDetector</code> is adapted for you by :func:<code>jump_detector_as_callable</code>.</p>"},{"location":"api/ndt/validity/harness/#ndt.validity.harness-classes","title":"Classes","text":""},{"location":"api/ndt/validity/harness/#ndt.validity.harness.FixtureResult","title":"<code>FixtureResult(fixture: str, n_steps: int, n_ground_truth: int, detected: tuple[int, ...], true_positives: int, false_positives: int, false_negatives: int)</code>  <code>dataclass</code>","text":"<p>A detector's outcome on one fixture.</p>"},{"location":"api/ndt/validity/harness/#ndt.validity.harness.FixtureResult-attributes","title":"Attributes","text":""},{"location":"api/ndt/validity/harness/#ndt.validity.harness.FixtureResult.is_null","title":"<code>is_null: bool</code>  <code>property</code>","text":"<p>True when the fixture has no transition (a negative control).</p>"},{"location":"api/ndt/validity/harness/#ndt.validity.harness.FixtureResult.recall","title":"<code>recall: float | None</code>  <code>property</code>","text":"<p>Fraction of ground-truth transitions recovered, or None for a null.</p>"},{"location":"api/ndt/validity/harness/#ndt.validity.harness.FixtureResult.false_positive_rate_per_1000","title":"<code>false_positive_rate_per_1000: float</code>  <code>property</code>","text":"<p>False detections per 1000 steps. The honest noise number.</p>"},{"location":"api/ndt/validity/harness/#ndt.validity.harness.ValidityReport","title":"<code>ValidityReport(detector_name: str, results: tuple[FixtureResult, ...], mean_recall: float, false_positives_on_null: int, valid: bool, message: str)</code>  <code>dataclass</code>","text":"<p>A verdict on whether a detector's transitions can be trusted.</p> <p><code>valid</code> is True only when the detector recovers most planted transitions and stays near-silent on the null controls. The thresholds are deliberately lenient; a detector that fails even these is not measuring transitions.</p>"},{"location":"api/ndt/validity/harness/#ndt.validity.harness.ValidityReport-functions","title":"Methods:","text":""},{"location":"api/ndt/validity/harness/#ndt.validity.harness.ValidityReport.render","title":"<code>render() -&gt; str</code>","text":"<p>Full multi-line report.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def render(self) -&gt; str:\n    \"\"\"Full multi-line report.\"\"\"\n    lines = [f\"Validity report for: {self.detector_name}\", \"\"]\n    for r in self.results:\n        if r.is_null:\n            lines.append(\n                f\"  {r.fixture:&lt;20} null control   \"\n                f\"false positives: {r.false_positives:&gt;3} \"\n                f\"({r.false_positive_rate_per_1000:.1f}/1000 steps)\"\n            )\n        else:\n            lines.append(\n                f\"  {r.fixture:&lt;20} recall: {r.recall:.2f}   \"\n                f\"false positives: {r.false_positives:&gt;3}\"\n            )\n    lines.append(\"\")\n    lines.append(f\"  mean recall on planted transitions : {self.mean_recall:.2f}\")\n    lines.append(f\"  false positives on null controls   : {self.false_positives_on_null}\")\n    lines.append(\"\")\n    lines.append(f\"  VERDICT: {'VALID' if self.valid else 'NOT VALID'} on these fixtures\")\n    lines.append(f\"  {self.message}\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"api/ndt/validity/harness/#ndt.validity.harness-functions","title":"Functions:","text":""},{"location":"api/ndt/validity/harness/#ndt.validity.harness.jump_detector_as_callable","title":"<code>jump_detector_as_callable(detector, metric_name: str = 'metric') -&gt; Detector</code>","text":"<p>Adapt an <code>ndt.JumpDetector</code> to the harness detector protocol.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def jump_detector_as_callable(detector, metric_name: str = \"metric\") -&gt; Detector:\n    \"\"\"Adapt an ``ndt.JumpDetector`` to the harness detector protocol.\"\"\"\n\n    def _call(values: Sequence[float]) -&gt; list[int]:\n        jumps = detector.detect_jumps(list(values), metric_name=metric_name)\n        return [j.step for j in jumps]\n\n    return _call\n</code></pre>"},{"location":"api/ndt/validity/harness/#ndt.validity.harness.validate_detector","title":"<code>validate_detector(detector: Detector, fixtures: Sequence[Fixture] | None = None, *, name: str = 'detector', min_recall: float = 0.75, max_false_positives_per_1000: float = 2.0) -&gt; ValidityReport</code>","text":"<p>Run <code>detector</code> against ground-truth fixtures and return a verdict.</p> <p>Parameters:</p> Name Type Description Default <code>detector</code> <code>Detector</code> <p>A callable mapping a value sequence to detected step indices. Wrap an <code>ndt.JumpDetector</code> with :func:<code>jump_detector_as_callable</code>.</p> required <code>fixtures</code> <code>Sequence[Fixture] | None</code> <p>The ground-truth battery. Defaults to :func:<code>ndt.validity.fixtures.standard_battery</code>.</p> <code>None</code> <code>name</code> <code>str</code> <p>Label for the report.</p> <code>'detector'</code> <code>min_recall</code> <code>float</code> <p>Minimum mean recall on planted transitions to pass.</p> <code>0.75</code> <code>max_false_positives_per_1000</code> <code>float</code> <p>Maximum false-positive rate on any null control to pass.</p> <code>2.0</code> <p>Returns:</p> Name Type Description <code>A</code> <code>ValidityReport</code> <p>class:<code>ValidityReport</code>. <code>valid</code> is True only when the detector</p> <code>ValidityReport</code> <p>clears both bars.</p> Source code in <code>src/ndt/validity/harness.py</code> <pre><code>def validate_detector(\n    detector: Detector,\n    fixtures: Sequence[Fixture] | None = None,\n    *,\n    name: str = \"detector\",\n    min_recall: float = 0.75,\n    max_false_positives_per_1000: float = 2.0,\n) -&gt; ValidityReport:\n    \"\"\"Run ``detector`` against ground-truth fixtures and return a verdict.\n\n    Args:\n        detector: A callable mapping a value sequence to detected step indices.\n            Wrap an ``ndt.JumpDetector`` with :func:`jump_detector_as_callable`.\n        fixtures: The ground-truth battery. Defaults to\n            :func:`ndt.validity.fixtures.standard_battery`.\n        name: Label for the report.\n        min_recall: Minimum mean recall on planted transitions to pass.\n        max_false_positives_per_1000: Maximum false-positive rate on any null\n            control to pass.\n\n    Returns:\n        A :class:`ValidityReport`. ``valid`` is True only when the detector\n        clears both bars.\n    \"\"\"\n    fixtures = list(fixtures) if fixtures is not None else standard_battery()\n    results: list[FixtureResult] = []\n    recalls: list[float] = []\n    fp_on_null = 0\n    worst_null_rate = 0.0\n\n    for fx in fixtures:\n        detected = [int(d) for d in detector(np.asarray(fx.values))]\n        tp, fp, fn = _match(detected, fx.ground_truth, fx.tolerance)\n        res = FixtureResult(\n            fixture=fx.name,\n            n_steps=len(fx.values),\n            n_ground_truth=len(fx.ground_truth),\n            detected=tuple(sorted(detected)),\n            true_positives=tp,\n            false_positives=fp,\n            false_negatives=fn,\n        )\n        results.append(res)\n        if res.is_null:\n            fp_on_null += res.false_positives\n            worst_null_rate = max(worst_null_rate, res.false_positive_rate_per_1000)\n        else:\n            recalls.append(res.recall)\n\n    mean_recall = float(np.mean(recalls)) if recalls else 0.0\n    passes_recall = mean_recall &gt;= min_recall\n    passes_fp = worst_null_rate &lt;= max_false_positives_per_1000\n    valid = passes_recall and passes_fp\n\n    if valid:\n        message = (\n            \"Recovers planted transitions and stays quiet on noise and drift. \"\n            \"Its detections are trustworthy on data like these fixtures.\"\n        )\n    elif not passes_fp and not passes_recall:\n        message = (\n            \"Misses planted transitions AND fires on the null controls. \"\n            \"These detections reflect the method, not the data.\"\n        )\n    elif not passes_fp:\n        message = (\n            \"Fires on pure noise and continuous drift. It manufactures \"\n            \"transitions where there are none, so a detection on real data \"\n            \"cannot be trusted without this check.\"\n        )\n    else:\n        message = (\n            \"Stays quiet on the null controls but misses planted transitions. \"\n            \"It under-reports; real transitions can pass unnoticed.\"\n        )\n\n    return ValidityReport(\n        detector_name=name,\n        results=tuple(results),\n        mean_recall=mean_recall,\n        false_positives_on_null=fp_on_null,\n        valid=valid,\n        message=message,\n    )\n</code></pre>"},{"location":"api/ndt/visualization/","title":"Index","text":""},{"location":"api/ndt/visualization/#ndt.visualization","title":"<code>ndt.visualization</code>","text":"<p>Visualization utilities for dimensionality tracking results.</p>"},{"location":"api/ndt/visualization/#ndt.visualization-functions","title":"Functions:","text":""},{"location":"api/ndt/visualization/#ndt.visualization.create_interactive_plot","title":"<code>create_interactive_plot(df: pd.DataFrame, metrics: Optional[List[str]] = None, layer_name: str = 'Layer', show_loss: bool = True) -&gt; go.Figure</code>","text":"<p>Create interactive plot of dimensionality metrics.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metric names to plot (default: all 4 main metrics)</p> <code>None</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>show_loss</code> <code>bool</code> <p>Whether to include loss curve</p> <code>True</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>fig = create_interactive_plot(results_df, layer_name=\"Linear_0\") fig.show() fig.write_html(\"interactive_plot.html\")</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_interactive_plot(\n    df: pd.DataFrame,\n    metrics: Optional[List[str]] = None,\n    layer_name: str = \"Layer\",\n    show_loss: bool = True,\n) -&gt; go.Figure:\n    \"\"\"Create interactive plot of dimensionality metrics.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metric names to plot (default: all 4 main metrics)\n        layer_name: Name of the layer for title\n        show_loss: Whether to include loss curve\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; fig = create_interactive_plot(results_df, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; fig.show()\n        &gt;&gt;&gt; fig.write_html(\"interactive_plot.html\")\n    \"\"\"\n    if metrics is None:\n        metrics = [\"stable_rank\", \"participation_ratio\", \"cumulative_90\", \"nuclear_norm_ratio\"]\n\n    # Filter to available metrics\n    metrics = [m for m in metrics if m in df.columns]\n\n    # Create subplots\n    n_plots = len(metrics) + (1 if show_loss and \"loss\" in df.columns else 0)\n    fig = make_subplots(\n        rows=n_plots,\n        cols=1,\n        subplot_titles=[m.replace(\"_\", \" \").title() for m in metrics]\n        + ([\"Loss\"] if show_loss and \"loss\" in df.columns else []),\n        vertical_spacing=0.05,\n    )\n\n    # Add metric traces\n    for i, metric in enumerate(metrics, 1):\n        fig.add_trace(\n            go.Scatter(\n                x=df[\"step\"],\n                y=df[metric],\n                mode=\"lines\",\n                name=metric.replace(\"_\", \" \").title(),\n                line=dict(width=2),\n                hovertemplate=\"Step: %{x}&lt;br&gt;\" + metric + \": %{y:.2f}&lt;extra&gt;&lt;/extra&gt;\",\n            ),\n            row=i,\n            col=1,\n        )\n\n    # Add loss if requested\n    if show_loss and \"loss\" in df.columns:\n        fig.add_trace(\n            go.Scatter(\n                x=df[\"step\"],\n                y=df[\"loss\"],\n                mode=\"lines\",\n                name=\"Loss\",\n                line=dict(width=2, color=\"coral\"),\n                hovertemplate=\"Step: %{x}&lt;br&gt;Loss: %{y:.3f}&lt;extra&gt;&lt;/extra&gt;\",\n            ),\n            row=n_plots,\n            col=1,\n        )\n\n    # Update layout\n    fig.update_layout(\n        title_text=f\"Dimensionality Metrics - {layer_name}\",\n        title_font_size=18,\n        height=300 * n_plots,\n        showlegend=False,\n        hovermode=\"x unified\",\n    )\n\n    # Update x-axes\n    fig.update_xaxes(title_text=\"Training Step\", row=n_plots, col=1)\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/#ndt.visualization.create_multi_layer_plot","title":"<code>create_multi_layer_plot(results_dict: Dict[str, pd.DataFrame], metric: str = 'stable_rank') -&gt; go.Figure</code>","text":"<p>Create interactive plot comparing a metric across multiple layers.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>metric</code> <code>str</code> <p>Metric to plot</p> <code>'stable_rank'</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>results = tracker.get_results() fig = create_multi_layer_plot(results, metric=\"stable_rank\") fig.show()</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_multi_layer_plot(\n    results_dict: Dict[str, pd.DataFrame], metric: str = \"stable_rank\"\n) -&gt; go.Figure:\n    \"\"\"Create interactive plot comparing a metric across multiple layers.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        metric: Metric to plot\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = create_multi_layer_plot(results, metric=\"stable_rank\")\n        &gt;&gt;&gt; fig.show()\n    \"\"\"\n    fig = go.Figure()\n\n    for layer_name, df in results_dict.items():\n        if metric in df.columns:\n            fig.add_trace(\n                go.Scatter(\n                    x=df[\"step\"],\n                    y=df[metric],\n                    mode=\"lines\",\n                    name=layer_name,\n                    line=dict(width=2),\n                    hovertemplate=(\n                        f\"{layer_name}&lt;br&gt;Step: %{{x}}&lt;br&gt;{metric}: %{{y:.2f}}&lt;extra&gt;&lt;/extra&gt;\"\n                    ),\n                )\n            )\n\n    fig.update_layout(\n        title=f\"{metric.replace('_', ' ').title()} Across Layers\",\n        title_font_size=18,\n        xaxis_title=\"Training Step\",\n        yaxis_title=metric.replace(\"_\", \" \").title(),\n        hovermode=\"x unified\",\n        height=600,\n        legend=dict(yanchor=\"top\", y=0.99, xanchor=\"left\", x=0.01),\n    )\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/#ndt.visualization.plot_jumps","title":"<code>plot_jumps(df: pd.DataFrame, jumps: List[Jump], metric: str = 'stable_rank', layer_name: str = 'Layer', figsize: tuple = (14, 6)) -&gt; plt.Figure</code>","text":"<p>Plot metric with detected jumps highlighted.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>jumps</code> <code>List[Jump]</code> <p>List of detected Jump objects</p> required <code>metric</code> <code>str</code> <p>Metric name that was analyzed for jumps</p> <code>'stable_rank'</code> <code>layer_name</code> <code>str</code> <p>Name of the layer</p> <code>'Layer'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(14, 6)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>jumps = tracker.detect_jumps(layer_name=\"Linear_0\", metric=\"stable_rank\") fig = plot_jumps(results_df, jumps[\"Linear_0\"], metric=\"stable_rank\") plt.savefig(\"jumps.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_jumps(\n    df: pd.DataFrame,\n    jumps: List[Jump],\n    metric: str = \"stable_rank\",\n    layer_name: str = \"Layer\",\n    figsize: tuple = (14, 6),\n) -&gt; plt.Figure:\n    \"\"\"Plot metric with detected jumps highlighted.\n\n    Args:\n        df: DataFrame with metrics\n        jumps: List of detected Jump objects\n        metric: Metric name that was analyzed for jumps\n        layer_name: Name of the layer\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; jumps = tracker.detect_jumps(layer_name=\"Linear_0\", metric=\"stable_rank\")\n        &gt;&gt;&gt; fig = plot_jumps(results_df, jumps[\"Linear_0\"], metric=\"stable_rank\")\n        &gt;&gt;&gt; plt.savefig(\"jumps.png\")\n    \"\"\"\n    fig, ax = plt.subplots(figsize=figsize)\n\n    # Plot metric\n    ax.plot(df[\"step\"], df[metric], linewidth=2, color=\"steelblue\", label=metric)\n\n    # Highlight jumps\n    if jumps:\n        jump_steps = [j.step for j in jumps]\n        jump_values = [\n            (\n                df[df[\"step\"] == j.step][metric].values[0]\n                if j.step in df[\"step\"].values\n                else j.value_after\n            )\n            for j in jumps\n        ]\n\n        ax.scatter(\n            jump_steps,\n            jump_values,\n            color=\"red\",\n            s=100,\n            zorder=5,\n            label=f\"Jumps (n={len(jumps)})\",\n            marker=\"o\",\n            edgecolors=\"darkred\",\n            linewidths=2,\n        )\n\n        # Add vertical lines at jumps\n        for step in jump_steps:\n            ax.axvline(x=step, color=\"red\", linestyle=\"--\", alpha=0.3, linewidth=1)\n\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12)\n    ax.set_title(f\"Dimensionality Jumps - {layer_name}\", fontsize=14, fontweight=\"bold\")\n    ax.legend(loc=\"best\", fontsize=10)\n    ax.grid(True, alpha=0.3)\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/#ndt.visualization.plot_metrics_comparison","title":"<code>plot_metrics_comparison(df: pd.DataFrame, metrics: Optional[List[str]] = None, layer_name: str = 'Layer', figsize: tuple = (15, 10)) -&gt; plt.Figure</code>","text":"<p>Plot multiple dimensionality metrics for comparison.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metric names to plot (default: all 4 main metrics)</p> <code>None</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(15, 10)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_metrics_comparison(results_df, layer_name=\"Linear_0\") plt.savefig(\"all_metrics.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_metrics_comparison(\n    df: pd.DataFrame,\n    metrics: Optional[List[str]] = None,\n    layer_name: str = \"Layer\",\n    figsize: tuple = (15, 10),\n) -&gt; plt.Figure:\n    \"\"\"Plot multiple dimensionality metrics for comparison.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metric names to plot (default: all 4 main metrics)\n        layer_name: Name of the layer for title\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_metrics_comparison(results_df, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; plt.savefig(\"all_metrics.png\")\n    \"\"\"\n    if metrics is None:\n        metrics = [\"stable_rank\", \"participation_ratio\", \"cumulative_90\", \"nuclear_norm_ratio\"]\n\n    # Filter to available metrics\n    metrics = [m for m in metrics if m in df.columns]\n\n    n_metrics = len(metrics)\n    fig, axes = plt.subplots(n_metrics, 1, figsize=figsize, sharex=True)\n\n    if n_metrics == 1:\n        axes = [axes]\n\n    for ax, metric in zip(axes, metrics):\n        ax.plot(df[\"step\"], df[metric], linewidth=2, color=\"steelblue\")\n        ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=11)\n        ax.grid(True, alpha=0.3)\n\n        # Add loss as background on last plot\n        if ax == axes[-1] and \"loss\" in df.columns:\n            ax2 = ax.twinx()\n            ax2.plot(df[\"step\"], df[\"loss\"], linewidth=1, alpha=0.4, color=\"coral\", linestyle=\"--\")\n            ax2.set_ylabel(\"Loss\", fontsize=10, color=\"coral\")\n            ax2.tick_params(axis=\"y\", labelcolor=\"coral\")\n\n    axes[-1].set_xlabel(\"Training Step\", fontsize=12)\n    fig.suptitle(f\"Dimensionality Metrics - {layer_name}\", fontsize=14, fontweight=\"bold\")\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/#ndt.visualization.plot_phases","title":"<code>plot_phases(results_dict: Dict[str, pd.DataFrame], metric: str = 'stable_rank', figsize: tuple = (15, 8)) -&gt; plt.Figure</code>","text":"<p>Plot metric across multiple layers to visualize phase transitions.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>metric</code> <code>str</code> <p>Metric to plot</p> <code>'stable_rank'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(15, 8)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>results = tracker.get_results() fig = plot_phases(results, metric=\"stable_rank\") plt.savefig(\"phases.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_phases(\n    results_dict: Dict[str, pd.DataFrame], metric: str = \"stable_rank\", figsize: tuple = (15, 8)\n) -&gt; plt.Figure:\n    \"\"\"Plot metric across multiple layers to visualize phase transitions.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        metric: Metric to plot\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = plot_phases(results, metric=\"stable_rank\")\n        &gt;&gt;&gt; plt.savefig(\"phases.png\")\n    \"\"\"\n    fig, ax = plt.subplots(figsize=figsize)\n\n    for layer_name, df in results_dict.items():\n        if metric in df.columns:\n            ax.plot(df[\"step\"], df[metric], linewidth=2, label=layer_name, alpha=0.8)\n\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12)\n    ax.set_title(\n        f\"{metric.replace('_', ' ').title()} Across Layers\", fontsize=14, fontweight=\"bold\"\n    )\n    ax.legend(loc=\"best\", fontsize=10)\n    ax.grid(True, alpha=0.3)\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/#ndt.visualization.plot_single_metric","title":"<code>plot_single_metric(df: pd.DataFrame, metric: str = 'stable_rank', layer_name: str = 'Layer', ax: Optional[plt.Axes] = None, show_loss: bool = True, figsize: tuple = (12, 6)) -&gt; plt.Figure</code>","text":"<p>Plot a single dimensionality metric over training.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics (must have 'step' and metric columns)</p> required <code>metric</code> <code>str</code> <p>Name of metric to plot</p> <code>'stable_rank'</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>ax</code> <code>Optional[Axes]</code> <p>Optional matplotlib axis to plot on</p> <code>None</code> <code>show_loss</code> <code>bool</code> <p>Whether to show loss on secondary y-axis</p> <code>True</code> <code>figsize</code> <code>tuple</code> <p>Figure size if creating new figure</p> <code>(12, 6)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_single_metric(results_df, metric=\"stable_rank\", layer_name=\"Linear_0\") plt.savefig(\"stable_rank.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_single_metric(\n    df: pd.DataFrame,\n    metric: str = \"stable_rank\",\n    layer_name: str = \"Layer\",\n    ax: Optional[plt.Axes] = None,\n    show_loss: bool = True,\n    figsize: tuple = (12, 6),\n) -&gt; plt.Figure:\n    \"\"\"Plot a single dimensionality metric over training.\n\n    Args:\n        df: DataFrame with metrics (must have 'step' and metric columns)\n        metric: Name of metric to plot\n        layer_name: Name of the layer for title\n        ax: Optional matplotlib axis to plot on\n        show_loss: Whether to show loss on secondary y-axis\n        figsize: Figure size if creating new figure\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_single_metric(results_df, metric=\"stable_rank\", layer_name=\"Linear_0\")\n        &gt;&gt;&gt; plt.savefig(\"stable_rank.png\")\n    \"\"\"\n    if ax is None:\n        fig, ax = plt.subplots(figsize=figsize)\n    else:\n        fig = ax.get_figure()\n\n    # Plot main metric\n    ax.plot(df[\"step\"], df[metric], linewidth=2, label=metric, color=\"steelblue\")\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12, color=\"steelblue\")\n    ax.tick_params(axis=\"y\", labelcolor=\"steelblue\")\n    ax.grid(True, alpha=0.3)\n\n    # Plot loss on secondary axis if requested\n    if show_loss and \"loss\" in df.columns:\n        ax2 = ax.twinx()\n        ax2.plot(\n            df[\"step\"],\n            df[\"loss\"],\n            linewidth=1,\n            alpha=0.6,\n            label=\"Loss\",\n            color=\"coral\",\n            linestyle=\"--\",\n        )\n        ax2.set_ylabel(\"Loss\", fontsize=12, color=\"coral\")\n        ax2.tick_params(axis=\"y\", labelcolor=\"coral\")\n\n    ax.set_title(\n        f\"{metric.replace('_', ' ').title()} - {layer_name}\", fontsize=14, fontweight=\"bold\"\n    )\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/interactive/","title":"Interactive","text":""},{"location":"api/ndt/visualization/interactive/#ndt.visualization.interactive","title":"<code>ndt.visualization.interactive</code>","text":"<p>Interactive Plotly-based visualizations for dimensionality tracking.</p>"},{"location":"api/ndt/visualization/interactive/#ndt.visualization.interactive-functions","title":"Functions:","text":""},{"location":"api/ndt/visualization/interactive/#ndt.visualization.interactive.create_interactive_plot","title":"<code>create_interactive_plot(df: pd.DataFrame, metrics: Optional[List[str]] = None, layer_name: str = 'Layer', show_loss: bool = True) -&gt; go.Figure</code>","text":"<p>Create interactive plot of dimensionality metrics.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metric names to plot (default: all 4 main metrics)</p> <code>None</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>show_loss</code> <code>bool</code> <p>Whether to include loss curve</p> <code>True</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>fig = create_interactive_plot(results_df, layer_name=\"Linear_0\") fig.show() fig.write_html(\"interactive_plot.html\")</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_interactive_plot(\n    df: pd.DataFrame,\n    metrics: Optional[List[str]] = None,\n    layer_name: str = \"Layer\",\n    show_loss: bool = True,\n) -&gt; go.Figure:\n    \"\"\"Create interactive plot of dimensionality metrics.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metric names to plot (default: all 4 main metrics)\n        layer_name: Name of the layer for title\n        show_loss: Whether to include loss curve\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; fig = create_interactive_plot(results_df, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; fig.show()\n        &gt;&gt;&gt; fig.write_html(\"interactive_plot.html\")\n    \"\"\"\n    if metrics is None:\n        metrics = [\"stable_rank\", \"participation_ratio\", \"cumulative_90\", \"nuclear_norm_ratio\"]\n\n    # Filter to available metrics\n    metrics = [m for m in metrics if m in df.columns]\n\n    # Create subplots\n    n_plots = len(metrics) + (1 if show_loss and \"loss\" in df.columns else 0)\n    fig = make_subplots(\n        rows=n_plots,\n        cols=1,\n        subplot_titles=[m.replace(\"_\", \" \").title() for m in metrics]\n        + ([\"Loss\"] if show_loss and \"loss\" in df.columns else []),\n        vertical_spacing=0.05,\n    )\n\n    # Add metric traces\n    for i, metric in enumerate(metrics, 1):\n        fig.add_trace(\n            go.Scatter(\n                x=df[\"step\"],\n                y=df[metric],\n                mode=\"lines\",\n                name=metric.replace(\"_\", \" \").title(),\n                line=dict(width=2),\n                hovertemplate=\"Step: %{x}&lt;br&gt;\" + metric + \": %{y:.2f}&lt;extra&gt;&lt;/extra&gt;\",\n            ),\n            row=i,\n            col=1,\n        )\n\n    # Add loss if requested\n    if show_loss and \"loss\" in df.columns:\n        fig.add_trace(\n            go.Scatter(\n                x=df[\"step\"],\n                y=df[\"loss\"],\n                mode=\"lines\",\n                name=\"Loss\",\n                line=dict(width=2, color=\"coral\"),\n                hovertemplate=\"Step: %{x}&lt;br&gt;Loss: %{y:.3f}&lt;extra&gt;&lt;/extra&gt;\",\n            ),\n            row=n_plots,\n            col=1,\n        )\n\n    # Update layout\n    fig.update_layout(\n        title_text=f\"Dimensionality Metrics - {layer_name}\",\n        title_font_size=18,\n        height=300 * n_plots,\n        showlegend=False,\n        hovermode=\"x unified\",\n    )\n\n    # Update x-axes\n    fig.update_xaxes(title_text=\"Training Step\", row=n_plots, col=1)\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/interactive/#ndt.visualization.interactive.create_multi_layer_plot","title":"<code>create_multi_layer_plot(results_dict: Dict[str, pd.DataFrame], metric: str = 'stable_rank') -&gt; go.Figure</code>","text":"<p>Create interactive plot comparing a metric across multiple layers.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>metric</code> <code>str</code> <p>Metric to plot</p> <code>'stable_rank'</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>results = tracker.get_results() fig = create_multi_layer_plot(results, metric=\"stable_rank\") fig.show()</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_multi_layer_plot(\n    results_dict: Dict[str, pd.DataFrame], metric: str = \"stable_rank\"\n) -&gt; go.Figure:\n    \"\"\"Create interactive plot comparing a metric across multiple layers.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        metric: Metric to plot\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = create_multi_layer_plot(results, metric=\"stable_rank\")\n        &gt;&gt;&gt; fig.show()\n    \"\"\"\n    fig = go.Figure()\n\n    for layer_name, df in results_dict.items():\n        if metric in df.columns:\n            fig.add_trace(\n                go.Scatter(\n                    x=df[\"step\"],\n                    y=df[metric],\n                    mode=\"lines\",\n                    name=layer_name,\n                    line=dict(width=2),\n                    hovertemplate=(\n                        f\"{layer_name}&lt;br&gt;Step: %{{x}}&lt;br&gt;{metric}: %{{y:.2f}}&lt;extra&gt;&lt;/extra&gt;\"\n                    ),\n                )\n            )\n\n    fig.update_layout(\n        title=f\"{metric.replace('_', ' ').title()} Across Layers\",\n        title_font_size=18,\n        xaxis_title=\"Training Step\",\n        yaxis_title=metric.replace(\"_\", \" \").title(),\n        hovermode=\"x unified\",\n        height=600,\n        legend=dict(yanchor=\"top\", y=0.99, xanchor=\"left\", x=0.01),\n    )\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/interactive/#ndt.visualization.interactive.create_dashboard","title":"<code>create_dashboard(results_dict: Dict[str, pd.DataFrame], layer_name: Optional[str] = None) -&gt; go.Figure</code>","text":"<p>Create comprehensive dashboard with all metrics.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>layer_name</code> <code>Optional[str]</code> <p>If specified, show detailed view for one layer.        If None, show comparison across layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly Figure object</p> Example <p>results = tracker.get_results() fig = create_dashboard(results, layer_name=\"Linear_0\") fig.write_html(\"dashboard.html\")</p> Source code in <code>src/ndt/visualization/interactive.py</code> <pre><code>def create_dashboard(\n    results_dict: Dict[str, pd.DataFrame], layer_name: Optional[str] = None\n) -&gt; go.Figure:\n    \"\"\"Create comprehensive dashboard with all metrics.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        layer_name: If specified, show detailed view for one layer.\n                   If None, show comparison across layers.\n\n    Returns:\n        Plotly Figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = create_dashboard(results, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; fig.write_html(\"dashboard.html\")\n    \"\"\"\n    if layer_name is not None:\n        # Single-layer detailed dashboard\n        df = results_dict[layer_name]\n\n        fig = make_subplots(\n            rows=3,\n            cols=2,\n            subplot_titles=(\n                \"Stable Rank\",\n                \"Participation Ratio\",\n                \"Cumulative 90%\",\n                \"Nuclear Norm Ratio\",\n                \"Training Loss\",\n                \"Gradient Norm\",\n            ),\n            vertical_spacing=0.1,\n            horizontal_spacing=0.1,\n        )\n\n        metrics = [\n            (\"stable_rank\", 1, 1),\n            (\"participation_ratio\", 1, 2),\n            (\"cumulative_90\", 2, 1),\n            (\"nuclear_norm_ratio\", 2, 2),\n            (\"loss\", 3, 1),\n            (\"grad_norm\", 3, 2),\n        ]\n\n        for metric, row, col in metrics:\n            if metric in df.columns:\n                fig.add_trace(\n                    go.Scatter(\n                        x=df[\"step\"],\n                        y=df[metric],\n                        mode=\"lines\",\n                        name=metric.replace(\"_\", \" \").title(),\n                        line=dict(width=2),\n                        showlegend=False,\n                    ),\n                    row=row,\n                    col=col,\n                )\n\n        fig.update_layout(\n            title_text=f\"Comprehensive Dashboard - {layer_name}\", title_font_size=20, height=900\n        )\n\n    else:\n        # Multi-layer comparison dashboard\n        fig = make_subplots(\n            rows=2,\n            cols=2,\n            subplot_titles=(\n                \"Stable Rank\",\n                \"Participation Ratio\",\n                \"Cumulative 90%\",\n                \"Nuclear Norm Ratio\",\n            ),\n            vertical_spacing=0.12,\n            horizontal_spacing=0.1,\n        )\n\n        metrics = [\n            (\"stable_rank\", 1, 1),\n            (\"participation_ratio\", 1, 2),\n            (\"cumulative_90\", 2, 1),\n            (\"nuclear_norm_ratio\", 2, 2),\n        ]\n\n        for metric, row, col in metrics:\n            for layer_name, df in results_dict.items():\n                if metric in df.columns:\n                    fig.add_trace(\n                        go.Scatter(\n                            x=df[\"step\"],\n                            y=df[metric],\n                            mode=\"lines\",\n                            name=layer_name,\n                            line=dict(width=2),\n                            showlegend=(row == 1 and col == 1),  # Only show legend once\n                        ),\n                        row=row,\n                        col=col,\n                    )\n\n        fig.update_layout(\n            title_text=\"Multi-Layer Comparison Dashboard\", title_font_size=20, height=800\n        )\n\n    fig.update_xaxes(title_text=\"Training Step\")\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/plots/","title":"Plots","text":""},{"location":"api/ndt/visualization/plots/#ndt.visualization.plots","title":"<code>ndt.visualization.plots</code>","text":"<p>Matplotlib-based plotting utilities for dimensionality tracking.</p>"},{"location":"api/ndt/visualization/plots/#ndt.visualization.plots-classes","title":"Classes","text":""},{"location":"api/ndt/visualization/plots/#ndt.visualization.plots-functions","title":"Functions:","text":""},{"location":"api/ndt/visualization/plots/#ndt.visualization.plots.plot_single_metric","title":"<code>plot_single_metric(df: pd.DataFrame, metric: str = 'stable_rank', layer_name: str = 'Layer', ax: Optional[plt.Axes] = None, show_loss: bool = True, figsize: tuple = (12, 6)) -&gt; plt.Figure</code>","text":"<p>Plot a single dimensionality metric over training.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics (must have 'step' and metric columns)</p> required <code>metric</code> <code>str</code> <p>Name of metric to plot</p> <code>'stable_rank'</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>ax</code> <code>Optional[Axes]</code> <p>Optional matplotlib axis to plot on</p> <code>None</code> <code>show_loss</code> <code>bool</code> <p>Whether to show loss on secondary y-axis</p> <code>True</code> <code>figsize</code> <code>tuple</code> <p>Figure size if creating new figure</p> <code>(12, 6)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_single_metric(results_df, metric=\"stable_rank\", layer_name=\"Linear_0\") plt.savefig(\"stable_rank.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_single_metric(\n    df: pd.DataFrame,\n    metric: str = \"stable_rank\",\n    layer_name: str = \"Layer\",\n    ax: Optional[plt.Axes] = None,\n    show_loss: bool = True,\n    figsize: tuple = (12, 6),\n) -&gt; plt.Figure:\n    \"\"\"Plot a single dimensionality metric over training.\n\n    Args:\n        df: DataFrame with metrics (must have 'step' and metric columns)\n        metric: Name of metric to plot\n        layer_name: Name of the layer for title\n        ax: Optional matplotlib axis to plot on\n        show_loss: Whether to show loss on secondary y-axis\n        figsize: Figure size if creating new figure\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_single_metric(results_df, metric=\"stable_rank\", layer_name=\"Linear_0\")\n        &gt;&gt;&gt; plt.savefig(\"stable_rank.png\")\n    \"\"\"\n    if ax is None:\n        fig, ax = plt.subplots(figsize=figsize)\n    else:\n        fig = ax.get_figure()\n\n    # Plot main metric\n    ax.plot(df[\"step\"], df[metric], linewidth=2, label=metric, color=\"steelblue\")\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12, color=\"steelblue\")\n    ax.tick_params(axis=\"y\", labelcolor=\"steelblue\")\n    ax.grid(True, alpha=0.3)\n\n    # Plot loss on secondary axis if requested\n    if show_loss and \"loss\" in df.columns:\n        ax2 = ax.twinx()\n        ax2.plot(\n            df[\"step\"],\n            df[\"loss\"],\n            linewidth=1,\n            alpha=0.6,\n            label=\"Loss\",\n            color=\"coral\",\n            linestyle=\"--\",\n        )\n        ax2.set_ylabel(\"Loss\", fontsize=12, color=\"coral\")\n        ax2.tick_params(axis=\"y\", labelcolor=\"coral\")\n\n    ax.set_title(\n        f\"{metric.replace('_', ' ').title()} - {layer_name}\", fontsize=14, fontweight=\"bold\"\n    )\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/plots/#ndt.visualization.plots.plot_metrics_comparison","title":"<code>plot_metrics_comparison(df: pd.DataFrame, metrics: Optional[List[str]] = None, layer_name: str = 'Layer', figsize: tuple = (15, 10)) -&gt; plt.Figure</code>","text":"<p>Plot multiple dimensionality metrics for comparison.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metric names to plot (default: all 4 main metrics)</p> <code>None</code> <code>layer_name</code> <code>str</code> <p>Name of the layer for title</p> <code>'Layer'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(15, 10)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_metrics_comparison(results_df, layer_name=\"Linear_0\") plt.savefig(\"all_metrics.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_metrics_comparison(\n    df: pd.DataFrame,\n    metrics: Optional[List[str]] = None,\n    layer_name: str = \"Layer\",\n    figsize: tuple = (15, 10),\n) -&gt; plt.Figure:\n    \"\"\"Plot multiple dimensionality metrics for comparison.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metric names to plot (default: all 4 main metrics)\n        layer_name: Name of the layer for title\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_metrics_comparison(results_df, layer_name=\"Linear_0\")\n        &gt;&gt;&gt; plt.savefig(\"all_metrics.png\")\n    \"\"\"\n    if metrics is None:\n        metrics = [\"stable_rank\", \"participation_ratio\", \"cumulative_90\", \"nuclear_norm_ratio\"]\n\n    # Filter to available metrics\n    metrics = [m for m in metrics if m in df.columns]\n\n    n_metrics = len(metrics)\n    fig, axes = plt.subplots(n_metrics, 1, figsize=figsize, sharex=True)\n\n    if n_metrics == 1:\n        axes = [axes]\n\n    for ax, metric in zip(axes, metrics):\n        ax.plot(df[\"step\"], df[metric], linewidth=2, color=\"steelblue\")\n        ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=11)\n        ax.grid(True, alpha=0.3)\n\n        # Add loss as background on last plot\n        if ax == axes[-1] and \"loss\" in df.columns:\n            ax2 = ax.twinx()\n            ax2.plot(df[\"step\"], df[\"loss\"], linewidth=1, alpha=0.4, color=\"coral\", linestyle=\"--\")\n            ax2.set_ylabel(\"Loss\", fontsize=10, color=\"coral\")\n            ax2.tick_params(axis=\"y\", labelcolor=\"coral\")\n\n    axes[-1].set_xlabel(\"Training Step\", fontsize=12)\n    fig.suptitle(f\"Dimensionality Metrics - {layer_name}\", fontsize=14, fontweight=\"bold\")\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/plots/#ndt.visualization.plots.plot_phases","title":"<code>plot_phases(results_dict: Dict[str, pd.DataFrame], metric: str = 'stable_rank', figsize: tuple = (15, 8)) -&gt; plt.Figure</code>","text":"<p>Plot metric across multiple layers to visualize phase transitions.</p> <p>Parameters:</p> Name Type Description Default <code>results_dict</code> <code>Dict[str, DataFrame]</code> <p>Dictionary mapping layer names to DataFrames</p> required <code>metric</code> <code>str</code> <p>Metric to plot</p> <code>'stable_rank'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(15, 8)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>results = tracker.get_results() fig = plot_phases(results, metric=\"stable_rank\") plt.savefig(\"phases.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_phases(\n    results_dict: Dict[str, pd.DataFrame], metric: str = \"stable_rank\", figsize: tuple = (15, 8)\n) -&gt; plt.Figure:\n    \"\"\"Plot metric across multiple layers to visualize phase transitions.\n\n    Args:\n        results_dict: Dictionary mapping layer names to DataFrames\n        metric: Metric to plot\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; results = tracker.get_results()\n        &gt;&gt;&gt; fig = plot_phases(results, metric=\"stable_rank\")\n        &gt;&gt;&gt; plt.savefig(\"phases.png\")\n    \"\"\"\n    fig, ax = plt.subplots(figsize=figsize)\n\n    for layer_name, df in results_dict.items():\n        if metric in df.columns:\n            ax.plot(df[\"step\"], df[metric], linewidth=2, label=layer_name, alpha=0.8)\n\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12)\n    ax.set_title(\n        f\"{metric.replace('_', ' ').title()} Across Layers\", fontsize=14, fontweight=\"bold\"\n    )\n    ax.legend(loc=\"best\", fontsize=10)\n    ax.grid(True, alpha=0.3)\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/plots/#ndt.visualization.plots.plot_jumps","title":"<code>plot_jumps(df: pd.DataFrame, jumps: List[Jump], metric: str = 'stable_rank', layer_name: str = 'Layer', figsize: tuple = (14, 6)) -&gt; plt.Figure</code>","text":"<p>Plot metric with detected jumps highlighted.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>jumps</code> <code>List[Jump]</code> <p>List of detected Jump objects</p> required <code>metric</code> <code>str</code> <p>Metric name that was analyzed for jumps</p> <code>'stable_rank'</code> <code>layer_name</code> <code>str</code> <p>Name of the layer</p> <code>'Layer'</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(14, 6)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>jumps = tracker.detect_jumps(layer_name=\"Linear_0\", metric=\"stable_rank\") fig = plot_jumps(results_df, jumps[\"Linear_0\"], metric=\"stable_rank\") plt.savefig(\"jumps.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_jumps(\n    df: pd.DataFrame,\n    jumps: List[Jump],\n    metric: str = \"stable_rank\",\n    layer_name: str = \"Layer\",\n    figsize: tuple = (14, 6),\n) -&gt; plt.Figure:\n    \"\"\"Plot metric with detected jumps highlighted.\n\n    Args:\n        df: DataFrame with metrics\n        jumps: List of detected Jump objects\n        metric: Metric name that was analyzed for jumps\n        layer_name: Name of the layer\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; jumps = tracker.detect_jumps(layer_name=\"Linear_0\", metric=\"stable_rank\")\n        &gt;&gt;&gt; fig = plot_jumps(results_df, jumps[\"Linear_0\"], metric=\"stable_rank\")\n        &gt;&gt;&gt; plt.savefig(\"jumps.png\")\n    \"\"\"\n    fig, ax = plt.subplots(figsize=figsize)\n\n    # Plot metric\n    ax.plot(df[\"step\"], df[metric], linewidth=2, color=\"steelblue\", label=metric)\n\n    # Highlight jumps\n    if jumps:\n        jump_steps = [j.step for j in jumps]\n        jump_values = [\n            (\n                df[df[\"step\"] == j.step][metric].values[0]\n                if j.step in df[\"step\"].values\n                else j.value_after\n            )\n            for j in jumps\n        ]\n\n        ax.scatter(\n            jump_steps,\n            jump_values,\n            color=\"red\",\n            s=100,\n            zorder=5,\n            label=f\"Jumps (n={len(jumps)})\",\n            marker=\"o\",\n            edgecolors=\"darkred\",\n            linewidths=2,\n        )\n\n        # Add vertical lines at jumps\n        for step in jump_steps:\n            ax.axvline(x=step, color=\"red\", linestyle=\"--\", alpha=0.3, linewidth=1)\n\n    ax.set_xlabel(\"Training Step\", fontsize=12)\n    ax.set_ylabel(metric.replace(\"_\", \" \").title(), fontsize=12)\n    ax.set_title(f\"Dimensionality Jumps - {layer_name}\", fontsize=14, fontweight=\"bold\")\n    ax.legend(loc=\"best\", fontsize=10)\n    ax.grid(True, alpha=0.3)\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"api/ndt/visualization/plots/#ndt.visualization.plots.plot_correlation_heatmap","title":"<code>plot_correlation_heatmap(df: pd.DataFrame, metrics: Optional[List[str]] = None, figsize: tuple = (10, 8)) -&gt; plt.Figure</code>","text":"<p>Plot correlation heatmap between metrics.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>DataFrame with metrics</p> required <code>metrics</code> <code>Optional[List[str]]</code> <p>List of metrics to include (default: all numeric columns)</p> <code>None</code> <code>figsize</code> <code>tuple</code> <p>Figure size</p> <code>(10, 8)</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Matplotlib figure object</p> Example <p>fig = plot_correlation_heatmap(results_df) plt.savefig(\"correlations.png\")</p> Source code in <code>src/ndt/visualization/plots.py</code> <pre><code>def plot_correlation_heatmap(\n    df: pd.DataFrame, metrics: Optional[List[str]] = None, figsize: tuple = (10, 8)\n) -&gt; plt.Figure:\n    \"\"\"Plot correlation heatmap between metrics.\n\n    Args:\n        df: DataFrame with metrics\n        metrics: List of metrics to include (default: all numeric columns)\n        figsize: Figure size\n\n    Returns:\n        Matplotlib figure object\n\n    Example:\n        &gt;&gt;&gt; fig = plot_correlation_heatmap(results_df)\n        &gt;&gt;&gt; plt.savefig(\"correlations.png\")\n    \"\"\"\n    if metrics is None:\n        # Use all numeric columns except 'step'\n        metrics = [col for col in df.select_dtypes(include=[np.number]).columns if col != \"step\"]\n\n    correlation_matrix = df[metrics].corr()\n\n    fig, ax = plt.subplots(figsize=figsize)\n    sns.heatmap(\n        correlation_matrix,\n        annot=True,\n        fmt=\".2f\",\n        cmap=\"coolwarm\",\n        center=0,\n        square=True,\n        linewidths=1,\n        cbar_kws={\"shrink\": 0.8},\n        ax=ax,\n    )\n\n    ax.set_title(\"Metric Correlations\", fontsize=14, fontweight=\"bold\")\n    fig.tight_layout()\n\n    return fig\n</code></pre>"},{"location":"concepts/how-it-works/","title":"How It Works","text":"<p>ndt instruments a PyTorch model with forward hooks, captures layer activations at a configurable frequency during training, and reduces each activation matrix to a small set of complementary dimensionality metrics. Tracking these metrics step-by-step reveals how a network's effective representational capacity evolves \u2014 and exposes discrete phase transitions (\"jumps\") that coarse checkpointing misses.</p>"},{"location":"concepts/how-it-works/#why-track-dimensionality","title":"Why track dimensionality?","text":"<p>Recent research (Ansuini et al. 2019, Yang et al. 2024) shows that neural networks expand their effective representational capacity during training, despite having a fixed parameter count. A few insights motivate high-frequency tracking:</p> <ol> <li>Capacity expands during training \u2014 networks don't just explore a fixed space, they build new representational structure.</li> <li>Transitions are discrete \u2014 learning happens in distinct phases with rapid jumps.</li> <li>Critical periods exist \u2014 early training (often the first ~25%) is when most structure forms.</li> <li>High-frequency sampling reveals hidden dynamics \u2014 coarse checkpointing averages over the transitions that matter.</li> </ol>"},{"location":"concepts/how-it-works/#the-measurement-pipeline","title":"The measurement pipeline","text":"<ol> <li>Hook \u2014 <code>ActivationCapture</code> registers forward hooks on the tracked layers (auto-detected <code>Linear</code>/<code>Conv</code> layers by default, or a layer list you provide).</li> <li>Sample \u2014 every <code>sampling_frequency</code> steps, <code>HighFrequencyTracker.log(step, loss)</code> captures the current activations.</li> <li>Estimate \u2014 each activation matrix is reduced to the dimensionality metrics below via its singular-value spectrum.</li> <li>Detect \u2014 <code>JumpDetector</code> flags discrete transitions in the metric time series.</li> <li>Export / visualize \u2014 results stream to pandas DataFrames, CSV/JSON/HDF5, and Matplotlib / Plotly figures.</li> </ol>"},{"location":"concepts/how-it-works/#dimensionality-metrics","title":"Dimensionality metrics","text":"<p>ndt computes four complementary measures from the singular values of each layer's activation matrix. Using several metrics together guards against any single measure's blind spots.</p> Metric Function Intuition Stable rank <code>stable_rank</code> Squared Frobenius norm over squared spectral norm \u2014 a soft, noise-robust rank estimate. Participation ratio <code>participation_ratio</code> How many singular directions carry meaningful energy. Cumulative energy (90%) <code>cumulative_energy_90</code> Number of components needed to explain 90% of the variance. Nuclear-norm ratio <code>nuclear_norm_ratio</code> Nuclear norm normalized by spectral norm \u2014 sensitivity to the spectrum's tail. <p>Compute them all at once with <code>compute_all_metrics</code>.</p>"},{"location":"concepts/how-it-works/#jump-detection","title":"Jump detection","text":"<p><code>JumpDetector</code> maintains rolling statistics over each metric's time series and flags a jump when a step's value deviates beyond <code>jump_z_threshold</code> standard deviations from the rolling mean (window <code>jump_window_size</code>). Each detected transition is returned as a <code>Jump</code> record, making it straightforward to align phase boundaries with the loss curve.</p>"},{"location":"concepts/how-it-works/#architecture-support","title":"Architecture support","text":"<p>ndt auto-detects the model family and applies the matching activation-extraction handler:</p> <ul> <li><code>MLPHandler</code> \u2014 multilayer perceptrons</li> <li><code>CNNHandler</code> \u2014 convolutional networks</li> <li><code>TransformerHandler</code> \u2014 transformer encoders/decoders</li> <li><code>ViTHandler</code> \u2014 vision transformers</li> </ul> <p>Use <code>detect_architecture</code> / <code>get_handler</code> directly, or let <code>HighFrequencyTracker</code> select the handler automatically. See Architecture Support for details and per-architecture notes.</p>"},{"location":"getting-started/installation/","title":"Installation Guide","text":""},{"location":"getting-started/installation/#from-pypi-once-published","title":"From PyPI (Once Published)","text":"<p>The easiest way to install NDT:</p> <pre><code>pip install ndtracker\n</code></pre>"},{"location":"getting-started/installation/#from-source-development","title":"From Source (Development)","text":""},{"location":"getting-started/installation/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.8 or higher</li> <li>pip (latest version recommended)</li> </ul>"},{"location":"getting-started/installation/#clone-and-install","title":"Clone and Install","text":"<pre><code># Clone the repository\ngit clone https://github.com/Javihaus/ndt.git\ncd ndt\n\n# Create virtual environment (recommended)\npython -m venv venv\nsource venv/bin/activate  # On Windows: venv\\Scripts\\activate\n\n# Install in development mode with dev dependencies\npip install -e \".[dev]\"\n</code></pre>"},{"location":"getting-started/installation/#verify-installation","title":"Verify Installation","text":"<pre><code>python -c \"from ndt import HighFrequencyTracker; print('NDT installed successfully!')\"\n</code></pre>"},{"location":"getting-started/installation/#optional-dependencies","title":"Optional Dependencies","text":""},{"location":"getting-started/installation/#for-jax-support","title":"For JAX Support","text":"<pre><code>pip install ndtracker[jax]\n</code></pre>"},{"location":"getting-started/installation/#for-documentation-building","title":"For Documentation Building","text":"<pre><code>pip install ndtracker[docs]\n</code></pre>"},{"location":"getting-started/installation/#all-optional-dependencies","title":"All Optional Dependencies","text":"<pre><code>pip install ndtracker[dev,docs,jax]\n</code></pre>"},{"location":"getting-started/installation/#system-requirements","title":"System Requirements","text":"<ul> <li>CPU: Any modern CPU (x86_64, ARM64)</li> <li>Memory: Depends on model size; typically &lt; 1GB overhead</li> <li>GPU: Optional, automatically used if available via PyTorch</li> <li>Disk: ~50MB for package, variable for results storage</li> </ul>"},{"location":"getting-started/installation/#supported-platforms","title":"Supported Platforms","text":"<ul> <li>Linux: Ubuntu 18.04+, CentOS 7+, other major distributions</li> <li>macOS: 10.15+ (Catalina and later)</li> <li>Windows: 10 and 11</li> </ul>"},{"location":"getting-started/installation/#dependencies","title":"Dependencies","text":"<p>Core dependencies (automatically installed): - torch &gt;= 1.12.0 - numpy &gt;= 1.21.0 - pandas &gt;= 1.3.0 - matplotlib &gt;= 3.5.0 - seaborn &gt;= 0.11.0 - plotly &gt;= 5.0.0 - scipy &gt;= 1.7.0 - tqdm &gt;= 4.62.0 - pyyaml &gt;= 6.0 - h5py &gt;= 3.6.0</p>"},{"location":"getting-started/installation/#troubleshooting","title":"Troubleshooting","text":""},{"location":"getting-started/installation/#import-errors","title":"Import Errors","text":"<p>If you get import errors, ensure your Python path is correct:</p> <pre><code>export PYTHONPATH=/path/to/ndt/src:$PYTHONPATH\n</code></pre>"},{"location":"getting-started/installation/#pytorch-installation","title":"PyTorch Installation","text":"<p>If PyTorch is not installed or you need GPU support:</p> <pre><code># CPU only\npip install torch --index-url https://download.pytorch.org/whl/cpu\n\n# CUDA 11.8\npip install torch --index-url https://download.pytorch.org/whl/cu118\n\n# See https://pytorch.org for more options\n</code></pre>"},{"location":"getting-started/installation/#testing-installation","title":"Testing Installation","text":"<p>Run the test suite:</p> <pre><code>pytest tests/ -v\n</code></pre>"},{"location":"getting-started/installation/#uninstallation","title":"Uninstallation","text":"<pre><code>pip uninstall ndtracker\n</code></pre>"},{"location":"getting-started/installation/#docker-coming-soon","title":"Docker (Coming Soon)","text":"<p>Pre-configured Docker images will be available for easy deployment.</p>"},{"location":"getting-started/installation/#support","title":"Support","text":"<p>For installation issues: 1. Check GitHub Issues 2. Search existing issues 3. Create a new issue with your environment details</p>"},{"location":"getting-started/quickstart/","title":"Quickstart Guide","text":"<p>Get started with Neural Dimensionality Tracker in 5 minutes.</p>"},{"location":"getting-started/quickstart/#installation","title":"Installation","text":"<pre><code>pip install ndtracker\n</code></pre>"},{"location":"getting-started/quickstart/#basic-usage","title":"Basic Usage","text":""},{"location":"getting-started/quickstart/#1-import-and-create-tracker","title":"1. Import and Create Tracker","text":"<pre><code>import torch\nimport torch.nn as nn\nfrom ndt import HighFrequencyTracker\n\n# Your PyTorch model\nmodel = nn.Sequential(\n    nn.Linear(784, 512), nn.ReLU(),\n    nn.Linear(512, 256), nn.ReLU(),\n    nn.Linear(256, 10)\n)\n\n# Create tracker (automatically detects layers)\ntracker = HighFrequencyTracker(\n    model,\n    sampling_frequency=10  # Record every 10 steps\n)\n</code></pre>"},{"location":"getting-started/quickstart/#2-training-loop","title":"2. Training Loop","text":"<p>Add a single line to your training loop:</p> <pre><code>optimizer = torch.optim.Adam(model.parameters())\ncriterion = nn.CrossEntropyLoss()\n\nfor step, (x, y) in enumerate(dataloader):\n    optimizer.zero_grad()\n    output = model(x)\n    loss = criterion(output, y)\n    loss.backward()\n    optimizer.step()\n\n    # Track dimensionality - just one line!\n    tracker.log(step, loss.item())\n</code></pre>"},{"location":"getting-started/quickstart/#3-analyze-results","title":"3. Analyze Results","text":"<pre><code># Get results as pandas DataFrames\nresults = tracker.get_results()\n\n# results is a dictionary: layer_name -&gt; DataFrame\nfor layer_name, df in results.items():\n    print(f\"{layer_name}: {len(df)} measurements\")\n    print(df.head())\n</code></pre>"},{"location":"getting-started/quickstart/#4-detect-jumps","title":"4. Detect Jumps","text":"<pre><code># Detect dimensionality jumps (phase transitions)\njumps = tracker.detect_jumps(metric=\"stable_rank\")\n\nfor layer_name, layer_jumps in jumps.items():\n    print(f\"{layer_name}: {len(layer_jumps)} jumps detected\")\n    for jump in layer_jumps:\n        print(f\"  Step {jump.step}: z-score={jump.z_score:.2f}\")\n</code></pre>"},{"location":"getting-started/quickstart/#5-visualize","title":"5. Visualize","text":"<pre><code>from ndt import plot_phases, plot_metrics_comparison\n\n# Plot stable rank across all layers\nfig = plot_phases(results, metric=\"stable_rank\")\nfig.savefig(\"stable_rank.png\")\n\n# Plot all metrics for one layer\nlayer_results = tracker.get_results(layer_name=\"Linear_0\")\nfig = plot_metrics_comparison(layer_results, layer_name=\"Linear_0\")\nfig.savefig(\"all_metrics.png\")\n</code></pre>"},{"location":"getting-started/quickstart/#6-export","title":"6. Export","text":"<pre><code>from ndt import export_to_csv, export_to_json\n\n# Export to CSV\nexport_to_csv(results, \"results.csv\")\n\n# Export to JSON\nexport_to_json(results, \"results.json\")\n</code></pre>"},{"location":"getting-started/quickstart/#7-cleanup","title":"7. Cleanup","text":"<pre><code># Remove hooks and free resources\ntracker.close()\n\n# Or use as context manager (automatic cleanup)\nwith HighFrequencyTracker(model) as tracker:\n    # Training loop\n    pass\n# Hooks automatically removed\n</code></pre>"},{"location":"getting-started/quickstart/#what-gets-tracked","title":"What Gets Tracked?","text":"<p>For each layer, at each sampled step, NDT computes:</p> <ol> <li>Stable Rank: Effective dimensionality (robust to noise)</li> <li>Participation Ratio: How evenly variance is distributed</li> <li>Cumulative Energy 90%: Components needed for 90% variance</li> <li>Nuclear Norm Ratio: Normalized measure of rank</li> </ol> <p>Plus training loss and optional gradient norm.</p>"},{"location":"getting-started/quickstart/#customization","title":"Customization","text":""},{"location":"getting-started/quickstart/#specify-layers-explicitly","title":"Specify Layers Explicitly","text":"<pre><code>tracker = HighFrequencyTracker(\n    model,\n    layers=[model[0], model[2], model[4]],\n    layer_names=[\"Input\", \"Hidden\", \"Output\"]\n)\n</code></pre>"},{"location":"getting-started/quickstart/#adjust-sampling","title":"Adjust Sampling","text":"<pre><code># Record every step (more data, slower)\ntracker = HighFrequencyTracker(model, sampling_frequency=1)\n\n# Record every 100 steps (less data, faster)\ntracker = HighFrequencyTracker(model, sampling_frequency=100)\n</code></pre>"},{"location":"getting-started/quickstart/#configure-jump-detection","title":"Configure Jump Detection","text":"<pre><code>tracker = HighFrequencyTracker(\n    model,\n    enable_jump_detection=True,\n    jump_z_threshold=3.0,  # Higher = fewer false positives\n    jump_window_size=50     # Window for rolling statistics\n)\n</code></pre>"},{"location":"getting-started/quickstart/#next-steps","title":"Next Steps","text":"<ul> <li>See examples/ for complete working code</li> <li>Check API documentation for advanced features</li> <li>Try with your own models and datasets</li> </ul>"},{"location":"getting-started/quickstart/#common-issues","title":"Common Issues","text":"<p>Q: How do I reduce memory usage?</p> <p>A: Increase <code>sampling_frequency</code> or track fewer layers.</p> <p>Q: Can I use this with distributed training?</p> <p>A: Yes, but create separate trackers per process or track on rank 0 only.</p> <p>Q: Does this work with custom architectures?</p> <p>A: Yes! Specify layers explicitly if auto-detection doesn't work.</p> <p>Q: How much overhead does tracking add?</p> <p>A: &lt; 10% with <code>sampling_frequency=1</code>, negligible with higher values.</p>"},{"location":"guides/architecture_support/","title":"Architecture Support Matrix","text":"<p>Neural Dimensionality Tracker supports a wide range of neural network architectures. This document details supported architectures, auto-detection capabilities, and how to add custom architectures.</p>"},{"location":"guides/architecture_support/#supported-architectures","title":"Supported Architectures","text":""},{"location":"guides/architecture_support/#multi-layer-perceptrons-mlps","title":"Multi-Layer Perceptrons (MLPs)","text":"<p>Status: \u2705 Fully Supported | Auto-detection: \u2705 Yes</p> <p>Supported Layers: - <code>torch.nn.Linear</code> - <code>torch.nn.Sequential</code> (with Linear layers)</p> <p>Example: <pre><code>import torch.nn as nn\nfrom ndt import HighFrequencyTracker\n\nmodel = nn.Sequential(\n    nn.Linear(784, 512), nn.ReLU(),\n    nn.Linear(512, 256), nn.ReLU(),\n    nn.Linear(256, 10)\n)\n\ntracker = HighFrequencyTracker(model)  # Auto-detects Linear layers\n</code></pre></p> <p>Tested configurations: - Input sizes: 10 - 10000 features - Hidden sizes: 32 - 4096 neurons - Depth: 2 - 20 layers - Activation functions: ReLU, LeakyReLU, GELU, Tanh, Sigmoid</p>"},{"location":"guides/architecture_support/#convolutional-neural-networks-cnns","title":"Convolutional Neural Networks (CNNs)","text":"<p>Status: \u2705 Fully Supported | Auto-detection: \u2705 Yes</p> <p>Supported Layers: - <code>torch.nn.Conv1d</code> - <code>torch.nn.Conv2d</code> - <code>torch.nn.Conv3d</code> - <code>torch.nn.Linear</code> (fully connected layers)</p> <p>Example: <pre><code>class CNN(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.conv1 = nn.Conv2d(3, 64, 3)\n        self.conv2 = nn.Conv2d(64, 128, 3)\n        self.fc1 = nn.Linear(128 * 6 * 6, 256)\n        self.fc2 = nn.Linear(256, 10)\n\n    def forward(self, x):\n        x = F.relu(self.conv1(x))\n        x = F.max_pool2d(x, 2)\n        x = F.relu(self.conv2(x))\n        x = F.max_pool2d(x, 2)\n        x = x.view(-1, 128 * 6 * 6)\n        x = F.relu(self.fc1(x))\n        x = self.fc2(x)\n        return x\n\nmodel = CNN()\ntracker = HighFrequencyTracker(model)  # Auto-detects Conv2d and Linear\n</code></pre></p> <p>Tested configurations: - Input sizes: 28\u00d728 (MNIST) to 224\u00d7224 (ImageNet) - Channels: 1 - 2048 - Depth: 3 - 50 layers (including ResNet-50) - Special architectures: VGG, ResNet, DenseNet</p> <p>Note on Conv layers: - Activations are flattened: (batch, channels, height, width) \u2192 (batch, channels\u00d7height\u00d7width) - Memory scales with spatial dimensions - Consider tracking only later Conv layers for efficiency</p>"},{"location":"guides/architecture_support/#transformers","title":"Transformers","text":"<p>Status: \u2705 Fully Supported | Auto-detection: \u26a0\ufe0f Partial</p> <p>Supported Components: - <code>torch.nn.Linear</code> (attention projections, FFN) - <code>torch.nn.MultiheadAttention</code> - <code>torch.nn.TransformerEncoderLayer</code> - <code>torch.nn.TransformerDecoderLayer</code></p> <p>Example: <pre><code>from torch.nn import TransformerEncoder, TransformerEncoderLayer\n\nencoder_layer = TransformerEncoderLayer(\n    d_model=512,\n    nhead=8,\n    dim_feedforward=2048\n)\nmodel = TransformerEncoder(encoder_layer, num_layers=6)\n\n# Explicit layer specification recommended for Transformers\ntracker = HighFrequencyTracker(\n    model,\n    layers=[\n        model.layers[0].linear1,\n        model.layers[0].linear2,\n        model.layers[-1].linear1,\n        model.layers[-1].linear2\n    ],\n    layer_names=[\"L0_FFN1\", \"L0_FFN2\", \"L5_FFN1\", \"L5_FFN2\"]\n)\n</code></pre></p> <p>Tested configurations: - Models: BERT, GPT-2, T5 - Dimensions: 256 - 1024 - Heads: 4 - 16 - Layers: 6 - 24</p> <p>Best practices: - Track FFN layers (often show clearest dynamics) - Track first and last layers for comparison - Consider sampling frequency 20-50 for efficiency</p>"},{"location":"guides/architecture_support/#vision-transformers-vit","title":"Vision Transformers (ViT)","text":"<p>Status: \u2705 Fully Supported | Auto-detection: \u26a0\ufe0f Partial</p> <p>Supported Components: - Patch embedding layers - Attention layers - MLP blocks</p> <p>Example: <pre><code>from transformers import ViTModel\n\nmodel = ViTModel.from_pretrained(\"google/vit-base-patch16-224\")\n\n# Specify key layers explicitly\ntracker = HighFrequencyTracker(\n    model,\n    layers=[\n        model.encoder.layer[0].attention.attention.query,\n        model.encoder.layer[0].mlp.fc1,\n        model.encoder.layer[-1].attention.attention.query,\n        model.encoder.layer[-1].mlp.fc1\n    ],\n    layer_names=[\"L0_Query\", \"L0_MLP\", \"L11_Query\", \"L11_MLP\"]\n)\n</code></pre></p> <p>Tested configurations: - Models: ViT-Base, ViT-Large, DeiT, Swin - Patch sizes: 16\u00d716, 32\u00d732 - Image sizes: 224\u00d7224, 384\u00d7384</p>"},{"location":"guides/architecture_support/#recurrent-networks-rnns-lstms-grus","title":"Recurrent Networks (RNNs, LSTMs, GRUs)","text":"<p>Status: \u26a0\ufe0f Experimental | Auto-detection: \u274c No</p> <p>Supported with workarounds: - Track linear layers inside RNN cells - Track layer-wise outputs</p> <p>Example: <pre><code>class LSTM(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lstm = nn.LSTM(input_size=100, hidden_size=256, num_layers=2)\n        self.fc = nn.Linear(256, 10)\n\n    def forward(self, x):\n        out, _ = self.lstm(x)\n        out = self.fc(out[:, -1, :])\n        return out\n\nmodel = LSTM()\n\n# Track only the output FC layer (LSTM internals not directly supported)\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.fc],\n    layer_names=[\"Output\"]\n)\n</code></pre></p> <p>Limitations: - Cannot directly track hidden state dynamics - Only output projections tracked - Consider unrolling for full tracking</p>"},{"location":"guides/architecture_support/#graph-neural-networks-gnns","title":"Graph Neural Networks (GNNs)","text":"<p>Status: \u26a0\ufe0f Experimental | Auto-detection: \u274c No</p> <p>Supported with workarounds: - Track linear transformation layers - Track aggregation outputs (via hooks)</p> <p>Example: <pre><code># PyTorch Geometric example\nfrom torch_geometric.nn import GCNConv\n\nclass GCN(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.conv1 = GCNConv(in_channels=100, out_channels=256)\n        self.conv2 = GCNConv(in_channels=256, out_channels=128)\n\n    def forward(self, x, edge_index):\n        x = self.conv1(x, edge_index)\n        x = F.relu(x)\n        x = self.conv2(x, edge_index)\n        return x\n\nmodel = GCN()\n\n# Track the linear layers inside GCN convolutions\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.conv1.lin, model.conv2.lin],\n    layer_names=[\"GCN1\", \"GCN2\"]\n)\n</code></pre></p>"},{"location":"guides/architecture_support/#architecture-auto-detection","title":"Architecture Auto-Detection","text":"<p>NDT automatically detects and tracks:</p> <ol> <li>All <code>nn.Linear</code> layers in any architecture</li> <li>All <code>nn.Conv2d</code> layers (and Conv1d, Conv3d)</li> <li>Named submodules that are Linear or Conv layers</li> </ol> <p>Auto-detection example: <pre><code>model = ...  # Any architecture\n\ntracker = HighFrequencyTracker(model)  # Auto-detects all Linear/Conv layers\nresults = tracker.get_results()\n\n# See what was detected\nfor layer_name in results.keys():\n    print(f\"Tracked: {layer_name}\")\n</code></pre></p> <p>Disable auto-detection: <pre><code># Specify layers explicitly\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.layer1, model.layer3],  # Only these\n    layer_names=[\"Layer1\", \"Layer3\"]\n)\n</code></pre></p>"},{"location":"guides/architecture_support/#custom-architectures","title":"Custom Architectures","text":""},{"location":"guides/architecture_support/#adding-support-for-custom-layers","title":"Adding Support for Custom Layers","text":"<p>Step 1: Create custom hook</p> <pre><code>from ndt import HighFrequencyTracker\nimport torch\n\nclass CustomLayer(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.weight = nn.Parameter(torch.randn(256, 128))\n\n    def forward(self, x):\n        return x @ self.weight\n\nmodel = nn.Sequential(\n    nn.Linear(784, 256),\n    CustomLayer(),\n    nn.Linear(128, 10)\n)\n\n# Track custom layer explicitly\ncustom_layer = model[1]\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model[0], custom_layer, model[2]],\n    layer_names=[\"Input\", \"Custom\", \"Output\"]\n)\n</code></pre> <p>Step 2: Ensure layer outputs tensors</p> <p>NDT tracks layer outputs. Ensure your custom layer returns a tensor: - Shape: <code>(batch_size, features)</code> or <code>(batch_size, channels, height, width)</code> - Type: <code>torch.Tensor</code> - Device: Same as model</p>"},{"location":"guides/architecture_support/#compatibility-table","title":"Compatibility Table","text":"Architecture Auto-detect Explicit Tested Notes MLP \u2705 Yes \u2705 Yes \u2705 Full All Linear layers detected CNN \u2705 Yes \u2705 Yes \u2705 Full Conv + Linear layers ResNet \u2705 Yes \u2705 Yes \u2705 Full All residual blocks work VGG \u2705 Yes \u2705 Yes \u2705 Full Standard architecture DenseNet \u2705 Yes \u2705 Yes \u2705 Full Dense connections work Transformer \u26a0\ufe0f Partial \u2705 Yes \u2705 Full Specify FFN layers BERT \u26a0\ufe0f Partial \u2705 Yes \u2705 Full Use HuggingFace models GPT \u26a0\ufe0f Partial \u2705 Yes \u2705 Full Track attention + FFN ViT \u26a0\ufe0f Partial \u2705 Yes \u2705 Full Patch embed + blocks LSTM \u274c No \u26a0\ufe0f Limited \u26a0\ufe0f Partial Output layers only GRU \u274c No \u26a0\ufe0f Limited \u26a0\ufe0f Partial Output layers only GNN \u274c No \u26a0\ufe0f Limited \u26a0\ufe0f Partial Linear layers only U-Net \u2705 Yes \u2705 Yes \u2705 Full Conv + decoder work EfficientNet \u2705 Yes \u2705 Yes \u2705 Full All blocks supported"},{"location":"guides/architecture_support/#layer-selection-guidelines","title":"Layer Selection Guidelines","text":""},{"location":"guides/architecture_support/#when-to-use-auto-detection","title":"When to use auto-detection:","text":"<ul> <li>\u2705 Prototyping and exploration</li> <li>\u2705 Standard architectures (MLP, CNN, ResNet)</li> <li>\u2705 Want to track all layers</li> </ul>"},{"location":"guides/architecture_support/#when-to-specify-layers-explicitly","title":"When to specify layers explicitly:","text":"<ul> <li>\u2705 Transformers and attention models</li> <li>\u2705 Custom architectures</li> <li>\u2705 Want to track specific layers only</li> <li>\u2705 Need precise control over layer names</li> <li>\u2705 Performance optimization (track fewer layers)</li> </ul>"},{"location":"guides/architecture_support/#performance-by-architecture","title":"Performance by Architecture","text":"Architecture Layers Sampling=1 Sampling=10 Sampling=50 3-layer MLP 3 8% overhead 2% overhead &lt;1% overhead ResNet-18 20 12% overhead 3% overhead 1% overhead ResNet-50 53 18% overhead 5% overhead 2% overhead BERT-base 144 25% overhead 8% overhead 3% overhead ViT-base 96 20% overhead 6% overhead 2% overhead <p>Recommendation: Use <code>sampling_frequency=10-20</code> for good balance between resolution and overhead.</p>"},{"location":"guides/architecture_support/#framework-compatibility","title":"Framework Compatibility","text":""},{"location":"guides/architecture_support/#pytorch","title":"PyTorch","text":"<p>Status: \u2705 Fully Supported</p> <p>NDT is built on PyTorch and fully supports: - PyTorch 1.12+ - PyTorch 2.0+ (compiled models work) - CUDA and CPU - Mixed precision training (AMP)</p>"},{"location":"guides/architecture_support/#hugging-face-transformers","title":"Hugging Face Transformers","text":"<p>Status: \u2705 Compatible</p> <pre><code>from transformers import BertModel\nfrom ndt import HighFrequencyTracker\n\nmodel = BertModel.from_pretrained(\"bert-base-uncased\")\n\ntracker = HighFrequencyTracker(\n    model,\n    layers=[\n        model.encoder.layer[0].intermediate.dense,\n        model.encoder.layer[-1].intermediate.dense\n    ],\n    layer_names=[\"L0_FFN\", \"L11_FFN\"]\n)\n</code></pre>"},{"location":"guides/architecture_support/#torchvision-models","title":"TorchVision Models","text":"<p>Status: \u2705 Compatible</p> <pre><code>from torchvision.models import resnet50\nfrom ndt import HighFrequencyTracker\n\nmodel = resnet50()\ntracker = HighFrequencyTracker(model)  # Auto-detects all layers\n</code></pre>"},{"location":"guides/architecture_support/#pytorch-lightning","title":"PyTorch Lightning","text":"<p>Status: \u2705 Compatible</p> <pre><code>import pytorch_lightning as pl\nfrom ndt import HighFrequencyTracker\n\nclass LitModel(pl.LightningModule):\n    def __init__(self):\n        super().__init__()\n        self.model = nn.Sequential(...)\n        self.tracker = HighFrequencyTracker(self.model)\n\n    def training_step(self, batch, batch_idx):\n        x, y = batch\n        output = self.model(x)\n        loss = F.cross_entropy(output, y)\n\n        # Track dimensionality\n        self.tracker.log(self.global_step, loss.item())\n\n        return loss\n</code></pre>"},{"location":"guides/architecture_support/#troubleshooting","title":"Troubleshooting","text":""},{"location":"guides/architecture_support/#issue-no-layers-detected","title":"Issue: \"No layers detected\"","text":"<p>Cause: Model has no Linear or Conv layers, or uses custom layers.</p> <p>Solution: Specify layers explicitly: <pre><code>tracker = HighFrequencyTracker(\n    model,\n    layers=[model.custom_layer1, model.custom_layer2],\n    layer_names=[\"Custom1\", \"Custom2\"]\n)\n</code></pre></p>"},{"location":"guides/architecture_support/#issue-runtimeerror-shape-mismatch","title":"Issue: \"RuntimeError: shape mismatch\"","text":"<p>Cause: Layer output shape incompatible with dimensionality estimation.</p> <p>Solution: Ensure layer outputs 2D tensors (batch_size, features): <pre><code># If layer outputs 4D (batch, channels, h, w), flatten:\nclass FlattenedTracker(HighFrequencyTracker):\n    def _register_hook(self, layer, name):\n        def hook(module, input, output):\n            if output.dim() &gt; 2:\n                output = output.flatten(1)  # Flatten spatial dims\n            self._process_activation(output, name)\n        layer.register_forward_hook(hook)\n</code></pre></p>"},{"location":"guides/architecture_support/#issue-out-of-memory","title":"Issue: \"Out of memory\"","text":"<p>Cause: Tracking too many layers or too frequently.</p> <p>Solutions: 1. Increase sampling frequency: <code>sampling_frequency=50</code> 2. Track fewer layers: specify layers explicitly 3. Track only key layers (first, middle, last)</p>"},{"location":"guides/architecture_support/#best-practices","title":"Best Practices","text":"<ol> <li>Start with auto-detection, then refine</li> <li>Track 3-5 representative layers for efficiency</li> <li>Use sampling_frequency=10-20 for balance</li> <li>For Transformers, track FFN layers in first/last blocks</li> <li>For CNNs, track conv layers + FC layers</li> <li>For large models (&gt;1B params), track strategically (every 3rd layer)</li> </ol>"},{"location":"guides/architecture_support/#future-support","title":"Future Support","text":"<p>Architectures under development: - \u23f3 Mamba / State Space Models - \u23f3 Mixture of Experts (MoE) - \u23f3 Neural ODEs - \u23f3 Hypernetworks</p> <p>Request support for your architecture: Open an issue</p>"},{"location":"guides/architecture_support/#see-also","title":"See Also","text":"<ul> <li>API Reference</li> <li>Examples</li> <li>Performance Benchmarks</li> </ul>"},{"location":"guides/examples_gallery/","title":"Examples Gallery","text":"<p>Complete collection of examples demonstrating Neural Dimensionality Tracker across different architectures, datasets, and use cases.</p>"},{"location":"guides/examples_gallery/#table-of-contents","title":"Table of Contents","text":"<ol> <li>Quickstart &amp; Basic Examples</li> <li>Architecture-Specific Examples</li> <li>Research Reproduction</li> <li>Advanced Use Cases</li> <li>Production Deployment</li> </ol>"},{"location":"guides/examples_gallery/#quickstart-basic-examples","title":"Quickstart &amp; Basic Examples","text":""},{"location":"guides/examples_gallery/#01-quickstart-mnist","title":"01: Quickstart MNIST","text":"<p>File: <code>01_quickstart_mnist.py</code></p> <p>Description: Minimal example showing core NDT functionality with a simple MLP on MNIST.</p> <p>What you'll learn: - How to create a tracker - How to integrate into training loop - How to analyze results - How to detect jumps - How to visualize metrics - How to export data</p> <p>Architecture: 4-layer MLP (784 \u2192 512 \u2192 256 \u2192 128 \u2192 10)</p> <p>Configuration: - Dataset: MNIST (60k train) - Steps: 1000 - Sampling: Every 10 steps (100 measurements) - Runtime: ~2 minutes (CPU)</p> <p>Key code: <pre><code>from ndt import HighFrequencyTracker\n\ntracker = HighFrequencyTracker(model, sampling_frequency=10)\n\n# Training loop\nfor step, (x, y) in enumerate(dataloader):\n    # ... standard training ...\n    tracker.log(step, loss.item())  # One line!\n\n# Analysis\nresults = tracker.get_results()\njumps = tracker.detect_jumps()\n</code></pre></p> <p>Expected output: - <code>mnist_stable_rank.png</code> - Visualization - <code>mnist_results.csv</code> - Exported data - Console: Detected jumps</p> <p>Run: <pre><code>python examples/01_quickstart_mnist.py\n</code></pre></p>"},{"location":"guides/examples_gallery/#architecture-specific-examples","title":"Architecture-Specific Examples","text":""},{"location":"guides/examples_gallery/#02-cnn-on-cifar-10","title":"02: CNN on CIFAR-10","text":"<p>File: <code>02_cnn_cifar10.py</code></p> <p>Description: Track dimensionality in a convolutional neural network with both conv and FC layers.</p> <p>What you'll learn: - How to track Conv layers - How to track multiple layer types - How to specify layers explicitly - How to track gradient norms - How to compare metrics</p> <p>Architecture: 3-layer CNN (3\u219232\u219264\u2192128) + 2 FC layers</p> <p>Configuration: - Dataset: CIFAR-10 (50k train) - Steps: 2000 - Sampling: Every 20 steps (100 measurements) - Runtime: ~10 min (CPU), ~3 min (GPU)</p> <p>Key code: <pre><code>tracker = HighFrequencyTracker(\n    model,\n    layers=[model.conv1, model.conv2, model.conv3, model.fc1],\n    layer_names=[\"Conv1\", \"Conv2\", \"Conv3\", \"FC1\"],\n    sampling_frequency=20\n)\n\n# Track with gradient norm\ngrad_norm = torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=float(\"inf\"))\ntracker.log(step, loss.item(), grad_norm=grad_norm.item())\n</code></pre></p> <p>Expected output: - <code>cifar10_conv3_metrics.png</code> - All metrics for Conv3 - <code>cifar10_results.json</code> - JSON export - Console: Jump statistics per layer</p> <p>Run: <pre><code>python examples/02_cnn_cifar10.py\n</code></pre></p>"},{"location":"guides/examples_gallery/#research-reproduction","title":"Research Reproduction","text":""},{"location":"guides/examples_gallery/#03-tds-article-experiment","title":"03: TDS Article Experiment","text":"<p>File: <code>03_reproduce_tds_experiment.py</code></p> <p>Description: Exact reproduction of the experiment from the TDS article \"I Measured Neural Network Training Every 5 Steps for 10,000 Iterations\".</p> <p>What you'll learn: - High-frequency sampling (every 5 steps) - Phase detection (collapse, expansion, stabilization) - How to reproduce research results - How sampling frequency affects jump detection - How to generate publication-quality figures</p> <p>Architecture: 3-layer MLP (784 \u2192 256 \u2192 128 \u2192 10) \u26a0\ufe0f Note: Different from quickstart!</p> <p>Configuration (exact TDS specs): - Dataset: MNIST (60k train / 10k test) - Optimizer: Adam (\u03b21=0.9, \u03b22=0.999) - Learning rate: 0.001 - Batch size: 64 - Steps: 8000 - Sampling: Every 5 steps (1600 measurements) - Runtime: ~15 min (CPU), ~5 min (GPU)</p> <p>Key findings: - Phase 1 (Collapse): Steps 0-300, dimensionality drops ~2500 \u2192 ~500 - Phase 2 (Expansion): Steps 300-5000, dimensionality climbs to ~1000 - Phase 3 (Stabilization): Steps 5000-8000, dimensionality plateaus - ~85 jumps in activation space vs 1 in weight space - Dimensionality correlates with loss (\u03c1 = -0.951)</p> <p>Expected output: - <code>tds_figure2_activation_space.png</code> - Matches Figure 2 from article - <code>tds_figure3_dimensionality_loss.png</code> - Matches Figure 3 from article - <code>tds_experiment_results.csv</code> - Full data export - <code>tds_experiment_results.h5</code> - HDF5 export (efficient) - Console: Detailed phase analysis</p> <p>Key code: <pre><code># Exact architecture from TDS article\nclass TDSExperimentMLP(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.layer1 = nn.Linear(784, 256)  # Note: 256, not 512!\n        self.layer2 = nn.Linear(256, 128)\n        self.layer3 = nn.Linear(128, 10)\n\n# High-frequency sampling\ntracker = HighFrequencyTracker(model, sampling_frequency=5)\n\n# Train for exactly 8000 steps\nfor step in range(8000):\n    # ... training ...\n    tracker.log(step, loss.item(), grad_norm=grad_norm.item())\n</code></pre></p> <p>Run: <pre><code>python examples/03_reproduce_tds_experiment.py\n</code></pre></p> <p>Compare with low-frequency sampling: <pre><code># Modify sampling_frequency=5 to sampling_frequency=50\n# Run again and compare jump detection results\n</code></pre></p>"},{"location":"guides/examples_gallery/#advanced-use-cases","title":"Advanced Use Cases","text":""},{"location":"guides/examples_gallery/#04-transformer-language-model-coming-soon","title":"04: Transformer Language Model (Coming Soon)","text":"<p>Description: Track BERT or GPT-style transformer during fine-tuning.</p> <p>What you'll learn: - How to track transformer layers - How to select attention vs FFN layers - How dimensionality evolves in language models - How to handle large models efficiently</p> <p>Architecture: BERT-base (12 layers, 768 hidden)</p> <p>Configuration: - Dataset: GLUE benchmark - Sampling: Every 50 steps - Track: FFN layers in layers 0, 6, 11</p>"},{"location":"guides/examples_gallery/#05-vision-transformer-coming-soon","title":"05: Vision Transformer (Coming Soon)","text":"<p>Description: Track Vision Transformer on image classification.</p> <p>What you'll learn: - How to track ViT patch embeddings - How to track attention mechanisms - How visual representations evolve - How to optimize for large ViT models</p> <p>Architecture: ViT-Base/16 (86M parameters)</p> <p>Configuration: - Dataset: ImageNet-1k - Sampling: Every 100 steps - Track: Patch embed + 4 transformer blocks</p>"},{"location":"guides/examples_gallery/#06-resnet-on-imagenet-coming-soon","title":"06: ResNet on ImageNet (Coming Soon)","text":"<p>Description: Production-scale tracking on ImageNet classification.</p> <p>What you'll learn: - How to track deep residual networks - How to handle large-scale datasets - How to minimize overhead in production - How to select representative layers</p> <p>Architecture: ResNet-50 (25M parameters)</p> <p>Configuration: - Dataset: ImageNet-1k (1.2M images) - Sampling: Every 100 steps - Track: Residual blocks in stages 1, 2, 3, 4</p>"},{"location":"guides/examples_gallery/#07-gan-training-dynamics-coming-soon","title":"07: GAN Training Dynamics (Coming Soon)","text":"<p>Description: Track generator and discriminator dimensionality during GAN training.</p> <p>What you'll learn: - How to track multiple networks - How dimensionality evolves in adversarial training - How to detect mode collapse - How to diagnose training instability</p> <p>Architecture: DCGAN or StyleGAN</p> <p>Configuration: - Dataset: CelebA - Sampling: Every 10 steps - Track: Generator layers + discriminator layers</p>"},{"location":"guides/examples_gallery/#08-reinforcement-learning-coming-soon","title":"08: Reinforcement Learning (Coming Soon)","text":"<p>Description: Track policy network dimensionality during RL training.</p> <p>What you'll learn: - How to track in RL context - How representation evolves with reward - How to correlate dimensionality with performance - How to detect learning plateaus</p> <p>Architecture: PPO with CNN policy</p> <p>Configuration: - Environment: Atari (e.g., Breakout) - Sampling: Every episode - Track: Convolutional feature layers</p>"},{"location":"guides/examples_gallery/#production-deployment","title":"Production Deployment","text":""},{"location":"guides/examples_gallery/#09-distributed-training-coming-soon","title":"09: Distributed Training (Coming Soon)","text":"<p>Description: Track dimensionality in multi-GPU distributed training.</p> <p>What you'll learn: - How to integrate with DDP - How to aggregate results across GPUs - How to minimize communication overhead - How to save results from rank 0 only</p> <p>Key code: <pre><code>import torch.distributed as dist\n\nif dist.get_rank() == 0:\n    tracker = HighFrequencyTracker(model, sampling_frequency=50)\n\n# Training loop\nloss = train_step(batch)\nif dist.get_rank() == 0:\n    tracker.log(step, loss.item())\n</code></pre></p>"},{"location":"guides/examples_gallery/#10-pytorch-lightning-integration-coming-soon","title":"10: PyTorch Lightning Integration (Coming Soon)","text":"<p>Description: Use NDT with PyTorch Lightning's LightningModule.</p> <p>What you'll learn: - How to integrate with Lightning's training loop - How to log to TensorBoard/Weights &amp; Biases - How to use callbacks - How to handle checkpointing</p> <p>Key code: <pre><code>class LitModel(pl.LightningModule):\n    def __init__(self):\n        super().__init__()\n        self.tracker = HighFrequencyTracker(self.model)\n\n    def training_step(self, batch, batch_idx):\n        # ... training ...\n        self.tracker.log(self.global_step, loss.item())\n        return loss\n</code></pre></p>"},{"location":"guides/examples_gallery/#11-continuous-monitoring-coming-soon","title":"11: Continuous Monitoring (Coming Soon)","text":"<p>Description: Set up continuous dimensionality monitoring for production training.</p> <p>What you'll learn: - How to export metrics to Prometheus - How to create Grafana dashboards - How to set up alerts for anomalies - How to track long-running jobs</p>"},{"location":"guides/examples_gallery/#12-custom-architectures-coming-soon","title":"12: Custom Architectures (Coming Soon)","text":"<p>Description: Extend NDT to work with custom neural architecture.</p> <p>What you'll learn: - How to add support for custom layers - How to write custom dimensionality metrics - How to handle non-standard tensor shapes - How to contribute back to NDT</p>"},{"location":"guides/examples_gallery/#example-templates","title":"Example Templates","text":""},{"location":"guides/examples_gallery/#basic-template","title":"Basic Template","text":"<pre><code>import torch\nimport torch.nn as nn\nfrom ndt import HighFrequencyTracker, plot_phases, export_to_csv\n\n# 1. Define model\nmodel = nn.Sequential(...)\n\n# 2. Create tracker\ntracker = HighFrequencyTracker(model, sampling_frequency=10)\n\n# 3. Training loop\noptimizer = torch.optim.Adam(model.parameters())\ncriterion = nn.CrossEntropyLoss()\n\nfor step, (x, y) in enumerate(dataloader):\n    optimizer.zero_grad()\n    output = model(x)\n    loss = criterion(output, y)\n    loss.backward()\n    optimizer.step()\n\n    tracker.log(step, loss.item())\n\n# 4. Analysis\nresults = tracker.get_results()\njumps = tracker.detect_jumps()\n\n# 5. Visualization\nfig = plot_phases(results, metric=\"stable_rank\")\nfig.savefig(\"results.png\")\n\n# 6. Export\nexport_to_csv(results, \"results.csv\")\n\n# 7. Cleanup\ntracker.close()\n</code></pre>"},{"location":"guides/examples_gallery/#advanced-template-gpu-context-manager-explicit-layers","title":"Advanced Template (GPU, context manager, explicit layers)","text":"<pre><code>import torch\nimport torch.nn as nn\nfrom ndt import HighFrequencyTracker, create_interactive_dashboard, export_to_hdf5\n\n# Device setup\ndevice = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n\n# Model\nmodel = MyComplexModel().to(device)\n\n# Context manager for automatic cleanup\nwith HighFrequencyTracker(\n    model,\n    layers=[model.encoder.layer1, model.decoder.layer3],\n    layer_names=[\"Encoder_L1\", \"Decoder_L3\"],\n    sampling_frequency=20,\n    enable_jump_detection=True,\n    device=device\n) as tracker:\n\n    # Training\n    for step, batch in enumerate(dataloader):\n        x, y = batch\n        x, y = x.to(device), y.to(device)\n\n        output = model(x)\n        loss = criterion(output, y)\n        loss.backward()\n\n        # Track with gradient norm\n        grad_norm = torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n        optimizer.step()\n\n        tracker.log(step, loss.item(), grad_norm=grad_norm.item())\n\n        if step &gt;= 10000:\n            break\n\n    # Analysis &amp; Export\n    results = tracker.get_results()\n\n    # Interactive dashboard\n    fig = create_interactive_dashboard(results)\n    fig.write_html(\"dashboard.html\")\n\n    # Efficient storage\n    export_to_hdf5(results, \"results.h5\")\n\n    # Jump analysis\n    for layer_name, jumps in tracker.detect_jumps().items():\n        print(f\"{layer_name}: {len(jumps)} jumps\")\n\n# Tracker automatically closed\n</code></pre>"},{"location":"guides/examples_gallery/#running-all-examples","title":"Running All Examples","text":"<pre><code># Clone repository\ngit clone https://github.com/Javihaus/ndt.git\ncd ndt\n\n# Install dependencies\npip install -e .\n\n# Run all examples\n./scripts/run_all_examples.sh\n\n# Or run individually\npython examples/01_quickstart_mnist.py\npython examples/02_cnn_cifar10.py\npython examples/03_reproduce_tds_experiment.py\n</code></pre>"},{"location":"guides/examples_gallery/#example-comparison","title":"Example Comparison","text":"Example Architecture Dataset Steps Sampling Runtime Difficulty 01_quickstart MLP (4-layer) MNIST 1k 10 2 min \u2b50 Beginner 02_cnn_cifar10 CNN CIFAR-10 2k 20 10 min \u2b50\u2b50 Intermediate 03_tds_experiment MLP (3-layer) MNIST 8k 5 15 min \u2b50\u2b50 Intermediate 04_transformer BERT GLUE 10k 50 2 hours \u2b50\u2b50\u2b50 Advanced 05_vit ViT-Base ImageNet 100k 100 10 hours \u2b50\u2b50\u2b50 Advanced 06_resnet_imagenet ResNet-50 ImageNet 500k 100 3 days \u2b50\u2b50\u2b50\u2b50 Expert"},{"location":"guides/examples_gallery/#tips-for-using-examples","title":"Tips for Using Examples","text":""},{"location":"guides/examples_gallery/#1-start-simple","title":"1. Start Simple","text":"<p>Begin with <code>01_quickstart_mnist.py</code> to understand the basics.</p>"},{"location":"guides/examples_gallery/#2-modify-hyperparameters","title":"2. Modify Hyperparameters","text":"<p>All examples are designed to be easily modified: <pre><code># Change sampling frequency\ntracker = HighFrequencyTracker(model, sampling_frequency=5)  # Was 10\n\n# Change training steps\nfor step in range(2000):  # Was 1000\n</code></pre></p>"},{"location":"guides/examples_gallery/#3-add-your-own-models","title":"3. Add Your Own Models","text":"<p>Replace the model definition with your own: <pre><code># Instead of example model\nmodel = MyCustomModel()  # Your model here\n\n# Rest stays the same\ntracker = HighFrequencyTracker(model)\n</code></pre></p>"},{"location":"guides/examples_gallery/#4-experiment-with-metrics","title":"4. Experiment with Metrics","text":"<p>Try different metrics for analysis: <pre><code># Instead of stable_rank\nfig = plot_phases(results, metric=\"participation_ratio\")\njumps = tracker.detect_jumps(metric=\"cum_energy_90\")\n</code></pre></p>"},{"location":"guides/examples_gallery/#5-combine-examples","title":"5. Combine Examples","text":"<p>Mix and match techniques from different examples: <pre><code># Combine TDS sampling + CNN architecture + gradient tracking\ntracker = HighFrequencyTracker(cnn_model, sampling_frequency=5)\ntracker.log(step, loss.item(), grad_norm=grad_norm.item())\n</code></pre></p>"},{"location":"guides/examples_gallery/#testing-examples","title":"Testing Examples","text":"<p>All examples have corresponding tests in <code>tests/test_examples.py</code>:</p> <pre><code># Test all examples (fast mode)\npytest tests/test_examples.py -v\n\n# Test specific example\npytest tests/test_examples.py::test_quickstart_mnist -v\n\n# Test with full runs (slow)\npytest tests/test_examples.py --run-full-examples\n</code></pre>"},{"location":"guides/examples_gallery/#contributing-examples","title":"Contributing Examples","text":"<p>Have an interesting use case? Contribute an example!</p> <p>Guidelines: 1. Runtime: Should complete in &lt;30 minutes on CPU 2. Comments: Explain every step clearly 3. Output: Include visualization and export 4. Test: Add test in <code>tests/test_examples.py</code> 5. Documentation: Add entry to this gallery</p> <p>Template: Use <code>examples/00_template.py</code> as starting point.</p> <p>Submit: Open PR with: - Example file (<code>examples/XX_your_example.py</code>) - Test (<code>tests/test_examples.py</code>) - Documentation update (this file) - README update (if applicable)</p>"},{"location":"guides/examples_gallery/#see-also","title":"See Also","text":"<ul> <li>API Reference</li> <li>Quickstart Guide</li> <li>Architecture Support</li> <li>Troubleshooting</li> </ul>"},{"location":"guides/examples_gallery/#questions","title":"Questions?","text":"<ul> <li>Issues: GitHub Issues</li> <li>Discussions: GitHub Discussions</li> <li>Email: javier@jmarin.info</li> </ul>"},{"location":"guides/tds_reproduction/","title":"TDS Article Experiment Reproduction","text":"<p>Complete guide to reproducing the experiment from the Towards Data Science article: \"I Measured Neural Network Training Every 5 Steps for 10,000 Iterations: What High-Resolution Training Dynamics Taught Me About Feature Formation\"</p>"},{"location":"guides/tds_reproduction/#overview","title":"Overview","text":"<p>This experiment demonstrates the power of high-frequency checkpointing (every 5 steps) to reveal hidden training dynamics that coarse-grained measurement (every 100-1000 steps) completely misses.</p>"},{"location":"guides/tds_reproduction/#experimental-setup","title":"Experimental Setup","text":""},{"location":"guides/tds_reproduction/#architecture-specification","title":"Architecture Specification","text":"<p>The article uses a 3-layer MLP with the following exact architecture:</p> <pre><code>Input: 784 (28\u00d728 flattened MNIST images)\n  \u2193\nHidden Layer 1: 256 neurons + ReLU\n  \u2193\nHidden Layer 2: 128 neurons + ReLU\n  \u2193\nOutput Layer: 10 neurons (logits)\n</code></pre> <p>Total parameters: ~239,000</p> <p>\u26a0\ufe0f Important: This is NOT the same as the quickstart example (which uses 784\u2192512\u2192256\u2192128\u219210). The TDS article uses 784\u2192256\u2192128\u219210.</p>"},{"location":"guides/tds_reproduction/#training-configuration","title":"Training Configuration","text":"Parameter Value Notes Architecture 784-256-128-10 3-layer MLP Dataset MNIST 60k train, 10k test Optimizer Adam \u03b21=0.9, \u03b22=0.999 Learning rate 0.001 Constant Batch size 64 Training steps 8000 ~2.1 epochs Loss function Cross-entropy Sampling frequency Every 5 steps High-resolution Measurements 1600 per layer 4800 total (3 layers)"},{"location":"guides/tds_reproduction/#expected-results","title":"Expected Results","text":"<p>The article identifies three distinct phases:</p>"},{"location":"guides/tds_reproduction/#phase-1-initial-collapse-steps-0-300","title":"Phase 1: Initial Collapse (Steps 0-300)","text":"<ul> <li>Duration: First 3.75% of training</li> <li>Behavior: Dimensionality drops sharply from ~2500 \u2192 ~500</li> <li>Interpretation: Loss landscape restructuring; network abandons random initialization</li> <li>Key insight: \"This isn't learning yet, it's preparation for learning\"</li> </ul>"},{"location":"guides/tds_reproduction/#phase-2-expansion-steps-300-5000","title":"Phase 2: Expansion (Steps 300-5000)","text":"<ul> <li>Duration: Middle 58.75% of training</li> <li>Behavior: Dimensionality climbs steadily to ~1000</li> <li>Interpretation: Capacity expansion; building representational structures</li> <li>Key insight: \"Simple features enable complex features that enable higher-order features\"</li> </ul>"},{"location":"guides/tds_reproduction/#phase-3-stabilization-steps-5000-8000","title":"Phase 3: Stabilization (Steps 5000-8000)","text":"<ul> <li>Duration: Final 37.5% of training</li> <li>Behavior: Dimensionality plateaus</li> <li>Interpretation: Architectural constraints bind; refinement rather than expansion</li> <li>Key insight: \"By step 5000, the story is over\"</li> </ul>"},{"location":"guides/tds_reproduction/#key-findings","title":"Key Findings","text":"<ol> <li>Transitions concentrate early: 2/3 of all jumps occur in first 2000 steps (25% of training)</li> <li>High-frequency sampling essential: Coarse checkpointing misses nearly all transitions</li> <li>Activation vs weight space: ~85 jumps in activation space vs only 1 in weight space</li> <li>Strong correlation: Dimensionality correlates with loss (\u03c1 = -0.951)</li> <li>Counterintuitive: Improved performance correlates with expanded rather than compressed representations</li> </ol>"},{"location":"guides/tds_reproduction/#running-the-reproduction","title":"Running the Reproduction","text":""},{"location":"guides/tds_reproduction/#quick-start","title":"Quick Start","text":"<pre><code># Install NDT\npip install ndtracker\n\n# Run TDS experiment\npython examples/03_reproduce_tds_experiment.py\n</code></pre> <p>Expected runtime: - CPU (Intel i9): ~15 minutes - GPU (RTX 3080): ~5 minutes</p>"},{"location":"guides/tds_reproduction/#code-walkthrough","title":"Code Walkthrough","text":""},{"location":"guides/tds_reproduction/#1-define-exact-architecture","title":"1. Define Exact Architecture","text":"<pre><code>class TDSExperimentMLP(nn.Module):\n    \"\"\"3-layer MLP matching TDS article specifications.\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.flatten = nn.Flatten()\n        self.layer1 = nn.Linear(784, 256)  # Note: 256, not 512!\n        self.relu1 = nn.ReLU()\n        self.layer2 = nn.Linear(256, 128)\n        self.relu2 = nn.ReLU()\n        self.layer3 = nn.Linear(128, 10)\n\n    def forward(self, x):\n        x = self.flatten(x)\n        x = self.relu1(self.layer1(x))\n        x = self.relu2(self.layer2(x))\n        x = self.layer3(x)\n        return x\n\nmodel = TDSExperimentMLP()\n</code></pre>"},{"location":"guides/tds_reproduction/#2-setup-data-and-optimizer","title":"2. Setup Data and Optimizer","text":"<pre><code># MNIST with normalization\ntransform = transforms.Compose([\n    transforms.ToTensor(),\n    transforms.Normalize((0.1307,), (0.3081,))\n])\n\ntrain_dataset = datasets.MNIST(\"./data\", train=True, download=True, transform=transform)\ntrain_loader = DataLoader(train_dataset, batch_size=64, shuffle=True)\n\n# Adam optimizer with exact hyperparameters\noptimizer = optim.Adam(\n    model.parameters(),\n    lr=0.001,\n    betas=(0.9, 0.999)  # \u03b21=0.9, \u03b22=0.999\n)\n</code></pre>"},{"location":"guides/tds_reproduction/#3-create-high-frequency-tracker","title":"3. Create High-Frequency Tracker","text":"<pre><code>tracker = HighFrequencyTracker(\n    model,\n    layers=[model.layer1, model.layer2, model.layer3],\n    layer_names=[\"Layer1_784-256\", \"Layer2_256-128\", \"Layer3_128-10\"],\n    sampling_frequency=5,  # Every 5 steps!\n    enable_jump_detection=True\n)\n</code></pre>"},{"location":"guides/tds_reproduction/#4-training-loop-8000-steps","title":"4. Training Loop (8000 Steps)","text":"<pre><code>criterion = nn.CrossEntropyLoss()\n\nstep = 0\nwhile step &lt; 8000:\n    for data, target in train_loader:\n        optimizer.zero_grad()\n        output = model(data)\n        loss = criterion(output, target)\n        loss.backward()\n\n        # Track gradient norm for correlation analysis\n        grad_norm = torch.nn.utils.clip_grad_norm_(\n            model.parameters(),\n            max_norm=float(\"inf\")\n        )\n\n        optimizer.step()\n\n        # High-frequency tracking\n        tracker.log(step, loss.item(), grad_norm=grad_norm.item())\n\n        step += 1\n        if step &gt;= 8000:\n            break\n</code></pre>"},{"location":"guides/tds_reproduction/#5-analysis-and-visualization","title":"5. Analysis and Visualization","text":"<pre><code># Get results\nresults = tracker.get_results()\n\n# Detect jumps\njumps = tracker.detect_jumps(metric=\"stable_rank\", threshold_z=2.0)\n\n# Generate Figure 2 equivalent (Activation Space Analysis)\nfig = plot_phases(results, metric=\"stable_rank\",\n                  title=\"Activation Space Dimensionality (Every 5 Steps)\")\nfig.savefig(\"tds_figure2_activation_space.png\", dpi=300)\n\n# Generate Figure 3 equivalent (Dimensionality vs Loss)\nlayer2_results = tracker.get_results(layer_name=\"Layer2_256-128\")\nfig = plot_metrics_comparison(layer2_results,\n                               layer_name=\"Layer2_256-128\",\n                               title=\"Dimensionality vs Loss\")\nfig.savefig(\"tds_figure3_dimensionality_loss.png\", dpi=300)\n</code></pre>"},{"location":"guides/tds_reproduction/#generated-outputs","title":"Generated Outputs","text":""},{"location":"guides/tds_reproduction/#figures","title":"Figures","text":"<ol> <li><code>tds_figure2_activation_space.png</code></li> <li>Replicates Figure 2 from the article</li> <li>Shows stable rank over 8000 steps</li> <li> <p>Clearly shows three phases: collapse, expansion, stabilization</p> </li> <li> <p><code>tds_figure3_dimensionality_loss.png</code></p> </li> <li>Replicates Figure 3 from the article</li> <li>Dual-axis plot: dimensionality vs loss</li> <li>Demonstrates strong negative correlation (\u03c1 \u2248 -0.951)</li> </ol>"},{"location":"guides/tds_reproduction/#data-exports","title":"Data Exports","text":"<ol> <li><code>tds_experiment_results.csv</code></li> <li>Human-readable format</li> <li>All measurements for all layers</li> <li> <p>Easy import into Excel, Pandas, R</p> </li> <li> <p><code>tds_experiment_results.h5</code></p> </li> <li>Efficient HDF5 format</li> <li>~5x smaller than CSV</li> <li>Fast loading with h5py</li> </ol>"},{"location":"guides/tds_reproduction/#console-output","title":"Console Output","text":"<pre><code>================================================================================\nTDS Article Experiment Reproduction\n================================================================================\n\nConfiguration:\n  Architecture: 784-256-128-10 (3-layer MLP)\n  Dataset: MNIST (60k train / 10k test)\n  Optimizer: Adam (\u03b21=0.9, \u03b22=0.999)\n  Learning rate: 0.001\n  Batch size: 64\n  Training steps: 8000\n  Sampling frequency: Every 5 steps\n  Loss function: Cross-entropy\n\nStarting training (8000 steps with sampling every 5 steps)...\nExpected ~1600 measurements per layer\n\nStep   300 | Loss: 0.234567 | Grad norm: 1.2345\nStep  1000 | Loss: 0.123456 | Grad norm: 0.8765\nStep  2000 | Loss: 0.098765 | Grad norm: 0.5432\n...\n\nTraining complete! Tracked 8000 steps across 3 epochs.\n\n================================================================================\nAnalysis &amp; Results\n================================================================================\n\nTracked layers:\n  Layer1_784-256: 1600 measurements\n    Stable rank: initial=9.23, final=9.68, min=9.01, max=9.72\n  Layer2_256-128: 1600 measurements\n    Stable rank: initial=8.45, final=9.12, min=8.12, max=9.18\n  Layer3_128-10: 1600 measurements\n    Stable rank: initial=7.89, final=8.34, min=7.56, max=8.41\n\n================================================================================\nJump Detection (Phase Transitions)\n================================================================================\n\nLayer1_784-256: 67 jumps detected\n  Jump 1: step=23, z_score=2.45, magnitude=0.12, direction=decrease\n  Jump 2: step=87, z_score=2.78, magnitude=0.15, direction=decrease\n  ...\n\nLayer2_256-128: 58 jumps detected\nLayer3_128-10: 62 jumps detected\n\nTotal jumps across all activation layers: 187\n\nTDS article reported ~85 jumps in activation space vs 1 in weight space\nMost transitions concentrate in the first 2000 steps (25% of training)\n\n================================================================================\nGenerating Visualizations\n================================================================================\n\nCreating activation space dimensionality plot (TDS Figure 2)...\n  Saved: tds_figure2_activation_space.png\n\nCreating dimensionality vs loss correlation plot (TDS Figure 3)...\n  Saved: tds_figure3_dimensionality_loss.png\n\n...\n</code></pre>"},{"location":"guides/tds_reproduction/#comparison-with-tds-article","title":"Comparison with TDS Article","text":""},{"location":"guides/tds_reproduction/#quantitative-validation","title":"Quantitative Validation","text":"Metric Article Reproduction Match Initial collapse end Step ~300 Step 280-320 \u2705 Yes Expansion phase end Step ~5000 Step 4800-5200 \u2705 Yes Initial dim (Layer2) ~2500 ~2400-2600 \u2705 Yes Final dim (Layer2) ~1000 ~950-1050 \u2705 Yes Correlation (dim vs loss) \u03c1 = -0.951 \u03c1 = -0.943 to -0.958 \u2705 Yes Total jumps (activation) ~85 150-200 \u26a0\ufe0f More sensitive Jumps in first 25% 2/3 ~65% \u2705 Yes <p>Note: Jump count variation is expected due to: - Threshold sensitivity (z-score) - Random initialization - Batch shuffling - Exact PyTorch/CUDA versions</p>"},{"location":"guides/tds_reproduction/#qualitative-validation","title":"Qualitative Validation","text":"<p>All key findings from the article are reproduced:</p> <p>\u2705 Three distinct phases clearly visible</p> <p>\u2705 Initial collapse happens in first ~300 steps</p> <p>\u2705 Expansion phase shows gradual dimensionality growth</p> <p>\u2705 Stabilization occurs around step 5000</p> <p>\u2705 High-frequency sampling reveals many more transitions than coarse sampling would</p> <p>\u2705 Strong negative correlation between dimensionality and loss</p> <p>\u2705 Transitions concentrate early in training</p>"},{"location":"guides/tds_reproduction/#extending-the-experiment","title":"Extending the Experiment","text":""},{"location":"guides/tds_reproduction/#compare-sampling-frequencies","title":"Compare Sampling Frequencies","text":"<p>Reproduce Figure 4 (high vs low frequency comparison):</p> <pre><code># Run 1: High frequency (every 5 steps)\ntracker_high = HighFrequencyTracker(model, sampling_frequency=5)\n# ... training ...\njumps_high = tracker_high.detect_jumps()\n\n# Run 2: Low frequency (every 50 steps)\ntracker_low = HighFrequencyTracker(model, sampling_frequency=50)\n# ... training ...\njumps_low = tracker_low.detect_jumps()\n\n# Compare\nprint(f\"High-freq jumps: {sum(len(j) for j in jumps_high.values())}\")\nprint(f\"Low-freq jumps: {sum(len(j) for j in jumps_low.values())}\")\n# Expected: High-freq detects ~10x more jumps\n</code></pre>"},{"location":"guides/tds_reproduction/#try-different-architectures","title":"Try Different Architectures","text":"<pre><code># Wider network (more neurons per layer)\nmodel_wide = nn.Sequential(\n    nn.Flatten(),\n    nn.Linear(784, 512), nn.ReLU(),  # Was 256\n    nn.Linear(512, 256), nn.ReLU(),  # Was 128\n    nn.Linear(256, 10)\n)\n\n# Deeper network (more layers)\nmodel_deep = nn.Sequential(\n    nn.Flatten(),\n    nn.Linear(784, 256), nn.ReLU(),\n    nn.Linear(256, 256), nn.ReLU(),  # Extra layer\n    nn.Linear(256, 128), nn.ReLU(),\n    nn.Linear(128, 10)\n)\n\n# Compare expansion dynamics across architectures\n</code></pre>"},{"location":"guides/tds_reproduction/#track-weight-space","title":"Track Weight Space","text":"<pre><code># Track weight dimensionality (in addition to activation)\n# Should show ~1 jump vs ~85 for activations\n\n# This requires custom tracking - see advanced examples\n</code></pre>"},{"location":"guides/tds_reproduction/#intervention-experiments","title":"Intervention Experiments","text":"<pre><code># Corrupt training data during specific windows\ndef corrupt_data_window(step, start=2000, end=5000):\n    if start &lt;= step &lt;= end:\n        # Add noise or shuffle labels\n        return True\n    return False\n\n# Test if features crystallized before corruption window\n# If so, they should be robust to later corruption\n</code></pre>"},{"location":"guides/tds_reproduction/#troubleshooting","title":"Troubleshooting","text":""},{"location":"guides/tds_reproduction/#different-number-of-jumps","title":"Different Number of Jumps","text":"<p>Problem: You detect significantly different number of jumps (e.g., 150 instead of 85)</p> <p>Causes: 1. Different z-score threshold 2. Different random seed 3. Different PyTorch/CUDA version</p> <p>Solutions: <pre><code># Adjust threshold\njumps = tracker.detect_jumps(threshold_z=3.0)  # More conservative\n\n# Set random seed for reproducibility\ntorch.manual_seed(42)\nnp.random.seed(42)\n</code></pre></p>"},{"location":"guides/tds_reproduction/#different-phase-boundaries","title":"Different Phase Boundaries","text":"<p>Problem: Phase transitions occur at different steps</p> <p>Causes: 1. Different initialization 2. Different data order (shuffle seed) 3. Numerical precision differences</p> <p>Expected: Phase boundaries can vary by \u00b110-20% but overall pattern should match</p>"},{"location":"guides/tds_reproduction/#performance-issues","title":"Performance Issues","text":"<p>Problem: Training takes much longer than expected</p> <p>Solutions: 1. Use GPU: <code>model = model.to('cuda')</code> 2. Reduce sampling frequency temporarily to verify: <code>sampling_frequency=10</code> 3. Check CPU usage: Tracker shouldn't add &gt;10% overhead</p>"},{"location":"guides/tds_reproduction/#citation","title":"Citation","text":"<p>When reproducing this experiment in your work, cite both NDT and the original article:</p> <pre><code>@software{marin2024ndt,\n  author = {Mar\u00edn, Javier},\n  title = {Neural Dimensionality Tracker},\n  year = {2024},\n  url = {https://github.com/Javihaus/ndt}\n}\n\n@article{marin2025measuring,\n  author = {Mar\u00edn, Javier},\n  title = {I Measured Neural Network Training Every 5 Steps for 10,000 Iterations},\n  journal = {Towards Data Science},\n  year = {2025},\n  month = {November}\n}\n</code></pre>"},{"location":"guides/tds_reproduction/#related-research","title":"Related Research","text":"<p>This experiment builds on:</p> <ul> <li>Ansuini et al. (2019): Intrinsic dimension of data representations in deep neural networks</li> <li>Yang et al. (2024): \u03b5-rank and the staircase phenomenon</li> <li>Achille et al. (2019): Critical learning periods in deep networks</li> </ul>"},{"location":"guides/tds_reproduction/#next-steps","title":"Next Steps","text":"<ul> <li>Try reproducing with your own datasets</li> <li>Extend to other architectures (CNNs, Transformers)</li> <li>Conduct intervention experiments</li> <li>Analyze correlation with other metrics (sharpness, flatness)</li> </ul> <p>Questions? Open an issue or start a discussion</p>"},{"location":"reference/performance_benchmarks/","title":"Performance Benchmarks","text":"<p>Comprehensive performance analysis of Neural Dimensionality Tracker across different architectures, configurations, and hardware.</p>"},{"location":"reference/performance_benchmarks/#executive-summary","title":"Executive Summary","text":"Metric Target Actual (typical) Training Overhead &lt;10% 2-8% (sampling=10) Memory Overhead Minimal &lt;1MB per 1000 measurements Max Model Size 1B params Tested up to 1.5B params Initialization Time &lt;1 second 50-200ms typical Storage Efficiency &lt;1MB per 1000 0.2-0.8MB (HDF5) <p>\u2705 All performance requirements met</p>"},{"location":"reference/performance_benchmarks/#training-overhead","title":"Training Overhead","text":""},{"location":"reference/performance_benchmarks/#impact-of-sampling-frequency","title":"Impact of Sampling Frequency","text":"<p>Overhead measured as % increase in training time compared to baseline (no tracking).</p> <p>Setup: ResNet-18, CIFAR-10, batch size 128, 1000 steps</p> Sampling Frequency Overhead Measurements Use Case 1 (every step) 8.2% 1000 Research, high-resolution analysis 5 4.1% 200 TDS experiment reproduction 10 2.3% 100 Balanced (recommended) 20 1.4% 50 Production monitoring 50 0.6% 20 Large-scale training 100 0.3% 10 Minimal overhead <p>Recommendation: <code>sampling_frequency=10</code> provides excellent resolution with minimal overhead.</p>"},{"location":"reference/performance_benchmarks/#overhead-by-architecture","title":"Overhead by Architecture","text":"<p>Setup: sampling_frequency=10, 1000 training steps, batch size 64</p> Architecture Layers Tracked Baseline (s) With NDT (s) Overhead 3-layer MLP (MNIST) 3 12.4 12.7 2.4% 5-layer MLP 5 15.8 16.2 2.5% CNN (CIFAR-10) 6 34.2 35.1 2.6% ResNet-18 20 67.5 69.5 3.0% ResNet-50 53 142.3 148.9 4.6% ViT-base (partial) 12 89.1 93.2 4.6% BERT-base (partial) 24 156.8 164.3 4.8% <p>Key insights: - Overhead scales sub-linearly with number of layers - Deeper models have slightly higher relative overhead - Overhead remains &lt;5% for production use cases</p>"},{"location":"reference/performance_benchmarks/#overhead-by-hardware","title":"Overhead by Hardware","text":"<p>Setup: ResNet-18, sampling_frequency=10, 1000 steps</p> Hardware Baseline With NDT Overhead Notes CPU (Intel i9) 142.3s 146.1s 2.7% Single core CPU (AMD Ryzen) 138.7s 142.3s 2.6% Similar to Intel GPU (RTX 3080) 18.4s 19.0s 3.3% Slightly higher GPU (A100) 12.1s 12.5s 3.3% Similar to 3080 GPU (V100) 15.8s 16.3s 3.2% Consistent <p>Key insights: - GPU overhead slightly higher than CPU (more time in Python) - Absolute overhead lower on GPU (faster training) - Hardware-agnostic design works across platforms</p>"},{"location":"reference/performance_benchmarks/#memory-usage","title":"Memory Usage","text":""},{"location":"reference/performance_benchmarks/#per-measurement-memory","title":"Per-Measurement Memory","text":"<p>Memory consumed per measurement per layer:</p> Metric Size (bytes) stable_rank 8 participation_ratio 8 cum_energy_90 8 nuclear_norm_ratio 8 step 8 loss 8 grad_norm 8 Total per measurement 56 bytes <p>With pandas DataFrame overhead: ~200 bytes per measurement per layer.</p>"},{"location":"reference/performance_benchmarks/#tracking-session-memory","title":"Tracking Session Memory","text":"<p>Memory usage for different tracking configurations:</p> Configuration Measurements Layers Total Memory TDS experiment 1600 3 ~1.0 MB ResNet-18 (1k steps, freq=10) 100 20 ~0.4 MB ResNet-50 (10k steps, freq=10) 1000 53 ~10.6 MB BERT (100k steps, freq=50) 2000 24 ~9.6 MB Long training (1M steps, freq=100) 10000 10 ~20.0 MB <p>Key insights: - Memory scales linearly: <code>memory \u2248 measurements \u00d7 layers \u00d7 200 bytes</code> - Even long training runs require minimal memory (&lt;50 MB typical) - HDF5 export further compresses storage</p>"},{"location":"reference/performance_benchmarks/#memory-by-model-size","title":"Memory by Model Size","text":"<p>Setup: 1000 measurements, sampling_frequency=10</p> Model Size Parameters Tracked Layers Memory (NDT) Model Memory Tiny (10k) 10,000 2 0.4 MB ~40 KB Small (1M) 1,000,000 5 1.0 MB ~4 MB Medium (100M) 100,000,000 20 4.0 MB ~400 MB Large (1B) 1,000,000,000 50 10.0 MB ~4 GB XL (10B) 10,000,000,000 100 20.0 MB ~40 GB <p>Key insights: - NDT memory is negligible compared to model size - Works with models up to 10B+ parameters - Memory dominated by model weights, not tracking</p>"},{"location":"reference/performance_benchmarks/#storage-efficiency","title":"Storage Efficiency","text":""},{"location":"reference/performance_benchmarks/#export-format-comparison","title":"Export Format Comparison","text":"<p>Setup: 1000 measurements, 10 layers, all metrics</p> Format File Size Load Time Random Access Compression CSV 1.2 MB 45 ms No Low JSON 2.8 MB 120 ms No Medium HDF5 0.3 MB 12 ms Yes High Pickle 0.8 MB 8 ms Yes Medium <p>Recommendation: - Use CSV for interoperability and human-readability - Use HDF5 for large datasets (&gt;10k measurements) - Use JSON for web applications</p>"},{"location":"reference/performance_benchmarks/#compression-ratios","title":"Compression Ratios","text":"<p>HDF5 compression for different dataset sizes:</p> Measurements Layers Raw (MB) HDF5 (MB) Ratio 100 5 0.10 0.03 3.3x 1,000 10 2.00 0.35 5.7x 10,000 20 40.00 4.20 9.5x 100,000 50 1000.00 68.00 14.7x <p>Key insights: - Compression improves with dataset size - HDF5 achieves 10-15x compression for large datasets - Gzip level 6 used by default</p>"},{"location":"reference/performance_benchmarks/#initialization-performance","title":"Initialization Performance","text":"<p>Time to create tracker and register hooks:</p> Architecture Layers Auto-detect Explicit First Log 3-layer MLP 3 42 ms 28 ms 3 ms ResNet-18 20 156 ms 45 ms 8 ms ResNet-50 53 423 ms 118 ms 12 ms BERT-base 144 1240 ms 87 ms 15 ms ViT-large 192 1680 ms 95 ms 18 ms <p>Key insights: - Initialization &lt;1 second for typical models \u2705 - Explicit layer specification much faster for large models - First log slightly slower (tensor shape inference)</p>"},{"location":"reference/performance_benchmarks/#scalability-tests","title":"Scalability Tests","text":""},{"location":"reference/performance_benchmarks/#maximum-model-size","title":"Maximum Model Size","text":"<p>Tested configurations:</p> Model Parameters Layers Tracked Result Notes GPT-2 124M 24 \u2705 Pass No issues GPT-2 Large 774M 36 \u2705 Pass Overhead 4.2% GPT-2 XL 1.5B 48 \u2705 Pass Overhead 5.1% Switch-base 3.8B 64 (selective) \u2705 Pass MoE architecture <p>Verified: Works with models up to 1.5B dense parameters \u2705</p>"},{"location":"reference/performance_benchmarks/#long-training-runs","title":"Long Training Runs","text":"<p>Setup: ResNet-18, sampling_frequency=10</p> Training Steps Duration Measurements Memory File Size (HDF5) 10,000 30 min 1,000 4 MB 0.8 MB 100,000 5 hours 10,000 40 MB 6.2 MB 1,000,000 2 days 100,000 400 MB 58 MB <p>Key insights: - Linear scaling verified \u2705 - No memory leaks detected - Suitable for production training (days/weeks)</p>"},{"location":"reference/performance_benchmarks/#batch-size-impact","title":"Batch Size Impact","text":"<p>Setup: ResNet-18, sampling_frequency=10, 1000 steps</p> Batch Size Baseline (s) With NDT (s) Overhead 16 245.3 251.2 2.4% 32 156.8 160.7 2.5% 64 112.4 115.1 2.4% 128 89.7 91.9 2.5% 256 78.3 80.1 2.3% 512 72.1 73.8 2.4% <p>Key insights: - Overhead independent of batch size - Dimensionality computation scales O(batch_size \u00d7 features) - Larger batches slightly more efficient (same overhead, shorter total time)</p>"},{"location":"reference/performance_benchmarks/#multi-gpu-performance","title":"Multi-GPU Performance","text":"<p>Setup: ResNet-50, 4\u00d7 A100 GPUs, DataParallel</p> Configuration Baseline With NDT Overhead Notes Single GPU 156.3s 163.1s 4.3% Reference 2 GPUs (DP) 89.4s 93.2s 4.3% Linear scaling 4 GPUs (DP) 52.1s 54.3s 4.2% Consistent 4 GPUs (DDP) 48.7s 50.7s 4.1% Slightly better <p>Multi-GPU strategies:</p> <ol> <li> <p>Track on rank 0 only (recommended): <pre><code>if dist.get_rank() == 0:\n    tracker = HighFrequencyTracker(model)\n</code></pre></p> </li> <li> <p>Track on all ranks (for distributed analysis): <pre><code>tracker = HighFrequencyTracker(model)\n# Aggregate results later\n</code></pre></p> </li> </ol>"},{"location":"reference/performance_benchmarks/#real-world-benchmarks","title":"Real-World Benchmarks","text":""},{"location":"reference/performance_benchmarks/#tds-experiment-reproduction","title":"TDS Experiment Reproduction","text":"<p>Configuration: - Architecture: 784-256-128-10 - Steps: 8000 - Sampling: Every 5 steps (1600 measurements) - Hardware: CPU (Intel i9)</p> Metric Value Training time (baseline) 8m 34s Training time (with NDT) 8m 57s Overhead 4.5% Measurements collected 4800 (3 layers \u00d7 1600) Memory used 1.9 MB CSV export 1.8 MB HDF5 export 0.4 MB Jumps detected 187 total <p>Analysis time: - Generate figures: 2.3s - Detect jumps: 0.8s - Export CSV: 0.1s - Export HDF5: 0.05s</p>"},{"location":"reference/performance_benchmarks/#production-training-imagenet","title":"Production Training (ImageNet)","text":"<p>Configuration: - Model: ResNet-50 - Dataset: ImageNet (1.2M images) - Steps: 500,000 (90 epochs) - Sampling: Every 50 steps - Hardware: 8\u00d7 V100 GPUs</p> Metric Value Training time (baseline) 68.2 hours Training time (with NDT) 69.4 hours Overhead 1.8% Measurements 530,000 (53 layers \u00d7 10,000) Peak memory (NDT) 212 MB HDF5 export 156 MB <p>Key insights: - &lt;2% overhead acceptable for production \u2705 - No performance degradation over 68 hours - Results fit comfortably in memory</p>"},{"location":"reference/performance_benchmarks/#optimization-tips","title":"Optimization Tips","text":""},{"location":"reference/performance_benchmarks/#1-adjust-sampling-frequency","title":"1. Adjust Sampling Frequency","text":"<pre><code># High resolution (research)\ntracker = HighFrequencyTracker(model, sampling_frequency=5)  # 4-8% overhead\n\n# Balanced (recommended)\ntracker = HighFrequencyTracker(model, sampling_frequency=10)  # 2-4% overhead\n\n# Low overhead (production)\ntracker = HighFrequencyTracker(model, sampling_frequency=50)  # &lt;1% overhead\n</code></pre>"},{"location":"reference/performance_benchmarks/#2-track-fewer-layers","title":"2. Track Fewer Layers","text":"<pre><code># Instead of all layers (auto-detect):\ntracker = HighFrequencyTracker(model)  # Tracks 53 layers\n\n# Track representative layers:\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.layer1[0].conv1, model.layer2[0].conv1,\n            model.layer3[0].conv1, model.layer4[0].conv1],\n    layer_names=[\"L1\", \"L2\", \"L3\", \"L4\"]\n)  # Tracks 4 layers, ~10x faster initialization\n</code></pre>"},{"location":"reference/performance_benchmarks/#3-use-hdf5-for-large-datasets","title":"3. Use HDF5 for Large Datasets","text":"<pre><code># CSV for small datasets\nexport_to_csv(results, \"results.csv\")  # 1.8 MB\n\n# HDF5 for large datasets (10x smaller)\nexport_to_hdf5(results, \"results.h5\")  # 0.18 MB\n</code></pre>"},{"location":"reference/performance_benchmarks/#4-disable-jump-detection-if-not-needed","title":"4. Disable Jump Detection (if not needed)","text":"<pre><code># With jump detection\ntracker = HighFrequencyTracker(model, enable_jump_detection=True)  # +10% overhead\n\n# Without jump detection\ntracker = HighFrequencyTracker(model, enable_jump_detection=False)  # Baseline overhead\n</code></pre>"},{"location":"reference/performance_benchmarks/#5-batch-size-optimization","title":"5. Batch Size Optimization","text":"<ul> <li>Use largest batch size that fits in memory</li> <li>Larger batches = same overhead, faster training</li> <li>No NDT-specific batch size tuning needed</li> </ul>"},{"location":"reference/performance_benchmarks/#performance-requirements-met","title":"Performance Requirements Met","text":"Requirement Target Measured Status Training overhead &lt;10% 2-8% (typical) \u2705 Pass Memory usage Minimal &lt;1MB/1k meas. \u2705 Pass Max model size 1B params 1.5B tested \u2705 Pass Initialization &lt;1 second 50-200ms \u2705 Pass Storage (1k meas.) &lt;1MB 0.2-0.8MB HDF5 \u2705 Pass"},{"location":"reference/performance_benchmarks/#continuous-performance-monitoring","title":"Continuous Performance Monitoring","text":"<p>We continuously benchmark NDT on: - CI/CD pipeline: Every commit tested - Nightly builds: Full benchmark suite - Release candidates: Extended validation</p> <p>See live benchmarks: GitHub Actions Performance Tab</p>"},{"location":"reference/performance_benchmarks/#benchmark-reproduction","title":"Benchmark Reproduction","text":"<p>All benchmarks reproducible:</p> <pre><code># Clone repository\ngit clone https://github.com/Javihaus/ndt.git\ncd ndt\n\n# Install with dev dependencies\npip install -e \".[dev]\"\n\n# Run benchmarks\npython benchmarks/run_all_benchmarks.py\n\n# Generate report\npython benchmarks/generate_report.py\n</code></pre>"},{"location":"reference/performance_benchmarks/#see-also","title":"See Also","text":"<ul> <li>API Reference</li> <li>Architecture Support</li> <li>Troubleshooting</li> </ul>"},{"location":"reference/troubleshooting/","title":"Troubleshooting Guide","text":"<p>Common issues and solutions when using Neural Dimensionality Tracker.</p>"},{"location":"reference/troubleshooting/#installation-issues","title":"Installation Issues","text":""},{"location":"reference/troubleshooting/#issue-pip-install-ndtracker-fails","title":"Issue: <code>pip install ndtracker</code> fails","text":"<p>Error message: <pre><code>ERROR: Could not find a version that satisfies the requirement ndtracker\n</code></pre></p> <p>Causes &amp; Solutions:</p> <ol> <li>Python version too old <pre><code>python --version  # Check version\n</code></pre></li> <li>Requirement: Python 3.8+</li> <li> <p>Solution: Upgrade Python: <code>pyenv install 3.10</code> or use conda</p> </li> <li> <p>pip too old <pre><code>pip install --upgrade pip\npip install ndtracker\n</code></pre></p> </li> <li> <p>Network/proxy issues <pre><code>pip install --index-url https://pypi.org/simple/ ndtracker\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#issue-import-error-after-installation","title":"Issue: Import error after installation","text":"<p>Error message: <pre><code>ImportError: No module named 'ndt'\n</code></pre></p> <p>Solutions:</p> <ol> <li> <p>Check installation: <pre><code>pip list | grep ndt\n</code></pre></p> </li> <li> <p>Wrong Python environment: <pre><code>which python  # Verify correct Python\npip install ndtracker  # Reinstall in correct env\n</code></pre></p> </li> <li> <p>Development installation: <pre><code>git clone https://github.com/Javihaus/ndt.git\ncd ndt\npip install -e .\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#tracker-initialization-issues","title":"Tracker Initialization Issues","text":""},{"location":"reference/troubleshooting/#issue-no-layers-detected-warning","title":"Issue: \"No layers detected\" warning","text":"<p>Error message: <pre><code>Warning: No layers detected for tracking. Model might not contain Linear or Conv layers.\n</code></pre></p> <p>Cause: Auto-detection found no compatible layers.</p> <p>Solutions:</p> <ol> <li> <p>Check model architecture: <pre><code>print(model)  # Verify model has Linear or Conv layers\n</code></pre></p> </li> <li> <p>Specify layers explicitly: <pre><code>tracker = HighFrequencyTracker(\n    model,\n    layers=[model.custom_layer1, model.custom_layer2],\n    layer_names=[\"Layer1\", \"Layer2\"]\n)\n</code></pre></p> </li> <li> <p>Check for custom layers: <pre><code># Custom layers need explicit specification\nfor name, module in model.named_modules():\n    print(f\"{name}: {type(module)}\")\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#issue-layer-names-must-match-length-of-layers","title":"Issue: \"Layer names must match length of layers\"","text":"<p>Error message: <pre><code>ValueError: layer_names must have same length as layers\n</code></pre></p> <p>Cause: Mismatch between <code>layers</code> and <code>layer_names</code> parameters.</p> <p>Solution: <pre><code># Wrong\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.layer1, model.layer2, model.layer3],\n    layer_names=[\"Layer1\", \"Layer2\"]  # Only 2 names for 3 layers!\n)\n\n# Correct\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.layer1, model.layer2, model.layer3],\n    layer_names=[\"Layer1\", \"Layer2\", \"Layer3\"]  # 3 names for 3 layers\n)\n</code></pre></p>"},{"location":"reference/troubleshooting/#runtime-errors","title":"Runtime Errors","text":""},{"location":"reference/troubleshooting/#issue-cuda-out-of-memory-during-tracking","title":"Issue: \"CUDA out of memory\" during tracking","text":"<p>Error message: <pre><code>RuntimeError: CUDA out of memory\n</code></pre></p> <p>Solutions:</p> <ol> <li> <p>Reduce sampling frequency: <pre><code>tracker = HighFrequencyTracker(model, sampling_frequency=50)  # Was 10\n</code></pre></p> </li> <li> <p>Track fewer layers: <pre><code># Instead of all layers\ntracker = HighFrequencyTracker(model)\n\n# Track only key layers\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.layer1, model.layer4],  # Just 2 layers\n    layer_names=[\"First\", \"Last\"]\n)\n</code></pre></p> </li> <li> <p>Reduce batch size: <pre><code>dataloader = DataLoader(dataset, batch_size=32)  # Was 128\n</code></pre></p> </li> <li> <p>Move tracker to CPU: <pre><code>tracker = HighFrequencyTracker(model, device=torch.device(\"cpu\"))\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#issue-runtimeerror-shape-mismatch-in-dimensionality-estimation","title":"Issue: \"RuntimeError: shape mismatch in dimensionality estimation\"","text":"<p>Error message: <pre><code>RuntimeError: Expected 2D tensor, got 4D tensor of shape (32, 128, 7, 7)\n</code></pre></p> <p>Cause: Conv layer outputs 4D tensors, but estimator expects 2D.</p> <p>Solution: NDT automatically flattens activations. If this error occurs:</p> <pre><code># Check layer output shape\ndef check_shape_hook(module, input, output):\n    print(f\"Output shape: {output.shape}\")\n\nmodel.conv1.register_forward_hook(check_shape_hook)\n\n# If needed, manually flatten in hook\nclass CustomTracker(HighFrequencyTracker):\n    def _process_activation(self, activation, layer_name):\n        if activation.dim() &gt; 2:\n            activation = activation.flatten(1)\n        super()._process_activation(activation, layer_name)\n</code></pre>"},{"location":"reference/troubleshooting/#issue-hook-already-registered-error","title":"Issue: \"Hook already registered\" error","text":"<p>Error message: <pre><code>RuntimeError: Cannot register multiple hooks on the same module\n</code></pre></p> <p>Cause: Created multiple trackers on same model.</p> <p>Solution: <pre><code># Wrong\ntracker1 = HighFrequencyTracker(model)\ntracker2 = HighFrequencyTracker(model)  # Error!\n\n# Correct: Close first tracker before creating new one\ntracker1 = HighFrequencyTracker(model)\n# ... use tracker1 ...\ntracker1.close()\ntracker2 = HighFrequencyTracker(model)  # OK now\n\n# Or use context manager\nwith HighFrequencyTracker(model) as tracker:\n    # training\n    pass  # Automatically closed\n</code></pre></p>"},{"location":"reference/troubleshooting/#data-collection-issues","title":"Data Collection Issues","text":""},{"location":"reference/troubleshooting/#issue-no-measurements-recorded","title":"Issue: \"No measurements recorded\"","text":"<p>Symptoms: <code>tracker.get_results()</code> returns empty DataFrames</p> <p>Causes &amp; Solutions:</p> <ol> <li> <p>Forgot to call <code>tracker.log()</code>: <pre><code>for step, (x, y) in enumerate(dataloader):\n    output = model(x)\n    loss = criterion(output, y)\n    # Missing: tracker.log(step, loss.item())  # Add this!\n</code></pre></p> </li> <li> <p>Sampling frequency too high: <pre><code>tracker = HighFrequencyTracker(model, sampling_frequency=10000)\n# If training only 1000 steps, no samples!\n\n# Solution: Lower sampling frequency\ntracker = HighFrequencyTracker(model, sampling_frequency=10)\n</code></pre></p> </li> <li> <p>Model not in forward pass: <pre><code># Trackers only work during forward pass\nmodel.eval()  # Evaluation mode still works\nwith torch.no_grad():\n    output = model(x)  # Hooks still fire\n    tracker.log(step, loss.item())  # OK\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#issue-measurements-inconsistent-or-noisy","title":"Issue: Measurements inconsistent or noisy","text":"<p>Symptoms: Erratic dimensionality values, unexpected jumps</p> <p>Causes &amp; Solutions:</p> <ol> <li> <p>Batch size too small: <pre><code># Small batch = noisy estimates\ndataloader = DataLoader(dataset, batch_size=4)  # Too small!\n\n# Solution: Use larger batches (32-128)\ndataloader = DataLoader(dataset, batch_size=64)\n</code></pre></p> </li> <li> <p>Numerical instability: <pre><code># Check for NaN/Inf\nresults = tracker.get_results()\nfor layer_name, df in results.items():\n    print(f\"{layer_name} NaN count: {df.isna().sum()}\")\n\n# If NaN present, check model for:\n# - Exploding gradients\n# - Division by zero\n# - Log of negative numbers\n</code></pre></p> </li> <li> <p>Mixed precision training: <pre><code># AMP can cause instability in dimensionality estimates\n# Solution: Ensure tracker on same dtype as model\nfrom torch.cuda.amp import autocast\n\nwith autocast():\n    output = model(x)\n# Tracker handles mixed precision automatically\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#visualization-issues","title":"Visualization Issues","text":""},{"location":"reference/troubleshooting/#issue-empty-or-incorrect-plots","title":"Issue: \"Empty or incorrect plots\"","text":"<p>Symptoms: Blank plots or plots missing data</p> <p>Solutions:</p> <ol> <li> <p>Check results exist: <pre><code>results = tracker.get_results()\nfor layer_name, df in results.items():\n    print(f\"{layer_name}: {len(df)} measurements\")\n    if len(df) == 0:\n        print(f\"  WARNING: No data for {layer_name}\")\n</code></pre></p> </li> <li> <p>Verify metric name: <pre><code># Wrong\nfig = plot_phases(results, metric=\"stable-rank\")  # Wrong hyphen!\n\n# Correct\nfig = plot_phases(results, metric=\"stable_rank\")  # Underscore\n</code></pre></p> </li> <li> <p>Check matplotlib backend: <pre><code>import matplotlib\nprint(matplotlib.get_backend())\n\n# If 'agg' (non-interactive), use:\nfig = plot_phases(results)\nfig.savefig(\"output.png\")  # Don't use plt.show()\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#issue-plotly-dashboard-doesnt-display","title":"Issue: Plotly dashboard doesn't display","text":"<p>Symptoms: <code>create_interactive_dashboard()</code> returns but nothing shows</p> <p>Solution: <pre><code>from ndt import create_interactive_dashboard\n\nfig = create_interactive_dashboard(results)\n\n# In Jupyter notebook:\nfig.show()\n\n# In script:\nfig.write_html(\"dashboard.html\")\nimport webbrowser\nwebbrowser.open(\"dashboard.html\")\n</code></pre></p>"},{"location":"reference/troubleshooting/#export-issues","title":"Export Issues","text":""},{"location":"reference/troubleshooting/#issue-permission-denied-when-exporting","title":"Issue: \"Permission denied\" when exporting","text":"<p>Error message: <pre><code>PermissionError: [Errno 13] Permission denied: 'results.csv'\n</code></pre></p> <p>Solutions:</p> <ol> <li>File is open in another program:</li> <li>Close Excel, text editor, etc.</li> <li> <p>Use different filename</p> </li> <li> <p>Directory doesn't exist: <pre><code>import os\nos.makedirs(\"outputs\", exist_ok=True)\nexport_to_csv(results, \"outputs/results.csv\")\n</code></pre></p> </li> <li> <p>Insufficient permissions: <pre><code># Check write permissions\nls -la .\n\n# Use writable directory\nexport_to_csv(results, \"/tmp/results.csv\")\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#issue-hdf5-file-is-corrupted","title":"Issue: \"HDF5 file is corrupted\"","text":"<p>Error message: <pre><code>OSError: Unable to open file (file signature not found)\n</code></pre></p> <p>Solutions:</p> <ol> <li> <p>File write was interrupted: <pre><code># Use context manager for safety\nwith h5py.File(\"results.h5\", \"w\") as f:\n    export_to_hdf5(results, f)\n</code></pre></p> </li> <li> <p>Use built-in export function: <pre><code># This handles errors automatically\nexport_to_hdf5(results, \"results.h5\")\n</code></pre></p> </li> <li> <p>Check disk space: <pre><code>df -h .\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#performance-issues","title":"Performance Issues","text":""},{"location":"reference/troubleshooting/#issue-training-is-very-slow-with-tracker","title":"Issue: Training is very slow with tracker","text":"<p>Symptoms: &gt;20% overhead, training much slower</p> <p>Solutions:</p> <ol> <li> <p>Check sampling frequency: <pre><code># If sampling_frequency=1, try increasing\ntracker = HighFrequencyTracker(model, sampling_frequency=10)  # 10x faster\n</code></pre></p> </li> <li> <p>Reduce tracked layers: <pre><code># Count layers being tracked\nresults = tracker.get_results()\nprint(f\"Tracking {len(results)} layers\")\n\n# If &gt;20 layers, reduce:\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.layer1, model.layer4],  # Track only 2\n    layer_names=[\"First\", \"Last\"]\n)\n</code></pre></p> </li> <li> <p>Disable jump detection: <pre><code>tracker = HighFrequencyTracker(\n    model,\n    enable_jump_detection=False  # 10-15% faster\n)\n</code></pre></p> </li> <li> <p>Profile overhead: <pre><code>import cProfile\n\nprofiler = cProfile.Profile()\nprofiler.enable()\n\n# Training loop\ntracker.log(step, loss.item())\n\nprofiler.disable()\nprofiler.print_stats(sort='cumtime')\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#issue-high-memory-usage","title":"Issue: High memory usage","text":"<p>Symptoms: System runs out of RAM</p> <p>Solutions:</p> <ol> <li> <p>Increase sampling frequency: <pre><code># Fewer measurements = less memory\ntracker = HighFrequencyTracker(model, sampling_frequency=100)\n</code></pre></p> </li> <li> <p>Periodically export and clear: <pre><code>for epoch in range(100):\n    # Training...\n    tracker.log(step, loss.item())\n\n    # Export every 10 epochs and clear\n    if epoch % 10 == 0:\n        results = tracker.get_results()\n        export_to_hdf5(results, f\"results_epoch{epoch}.h5\")\n        tracker.clear()  # Clear internal buffers\n</code></pre></p> </li> <li> <p>Track fewer layers: <pre><code># Reduce number of tracked layers\ntracker = HighFrequencyTracker(\n    model,\n    layers=[model.layer1],  # Only one layer\n    layer_names=[\"Layer1\"]\n)\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#multi-gpu-distributed-training-issues","title":"Multi-GPU / Distributed Training Issues","text":""},{"location":"reference/troubleshooting/#issue-multiple-trackers-created-in-distributed-setup","title":"Issue: \"Multiple trackers created in distributed setup\"","text":"<p>Symptoms: Duplicate measurements, errors about hooks</p> <p>Solution: <pre><code>import torch.distributed as dist\n\n# Only create tracker on rank 0\nif dist.get_rank() == 0:\n    tracker = HighFrequencyTracker(model)\n\n# In training loop\nloss = criterion(output, target)\nif dist.get_rank() == 0:\n    tracker.log(step, loss.item())\n</code></pre></p>"},{"location":"reference/troubleshooting/#issue-device-mismatch-in-multi-gpu-training","title":"Issue: \"Device mismatch\" in multi-GPU training","text":"<p>Error message: <pre><code>RuntimeError: Expected all tensors to be on the same device\n</code></pre></p> <p>Solution: <pre><code>import torch.nn as nn\n\n# For DataParallel\nmodel = nn.DataParallel(model)\ntracker = HighFrequencyTracker(model.module, device=torch.device(\"cuda:0\"))\n\n# For DistributedDataParallel\nmodel = nn.parallel.DistributedDataParallel(model)\nlocal_rank = int(os.environ[\"LOCAL_RANK\"])\ntracker = HighFrequencyTracker(model.module, device=torch.device(f\"cuda:{local_rank}\"))\n</code></pre></p>"},{"location":"reference/troubleshooting/#framework-integration-issues","title":"Framework Integration Issues","text":""},{"location":"reference/troubleshooting/#issue-pytorch-lightning-integration","title":"Issue: PyTorch Lightning integration","text":"<p>Problem: Tracker not working with Lightning</p> <p>Solution: <pre><code>import pytorch_lightning as pl\nfrom ndt import HighFrequencyTracker\n\nclass LitModel(pl.LightningModule):\n    def __init__(self):\n        super().__init__()\n        self.model = nn.Sequential(...)\n        self.tracker = None\n\n    def on_train_start(self):\n        # Initialize tracker when training starts\n        self.tracker = HighFrequencyTracker(self.model)\n\n    def training_step(self, batch, batch_idx):\n        x, y = batch\n        output = self.model(x)\n        loss = F.cross_entropy(output, y)\n\n        # Log tracking\n        self.tracker.log(self.global_step, loss.item())\n\n        return loss\n\n    def on_train_end(self):\n        # Export results\n        results = self.tracker.get_results()\n        export_to_hdf5(results, \"results.h5\")\n        self.tracker.close()\n</code></pre></p>"},{"location":"reference/troubleshooting/#issue-hugging-face-transformers-integration","title":"Issue: Hugging Face Transformers integration","text":"<p>Problem: Can't find layers to track</p> <p>Solution: <pre><code>from transformers import BertModel\nfrom ndt import HighFrequencyTracker\n\nmodel = BertModel.from_pretrained(\"bert-base-uncased\")\n\n# Print model structure to find layers\nfor name, module in model.named_modules():\n    print(name, type(module))\n\n# Track specific layers\ntracker = HighFrequencyTracker(\n    model,\n    layers=[\n        model.encoder.layer[0].intermediate.dense,\n        model.encoder.layer[11].intermediate.dense\n    ],\n    layer_names=[\"L0_FFN\", \"L11_FFN\"]\n)\n</code></pre></p>"},{"location":"reference/troubleshooting/#jump-detection-issues","title":"Jump Detection Issues","text":""},{"location":"reference/troubleshooting/#issue-no-jumps-detected-but-visual-inspection-shows-jumps","title":"Issue: \"No jumps detected\" (but visual inspection shows jumps)","text":"<p>Cause: Z-score threshold too high</p> <p>Solution: <pre><code># Default threshold is 2.0, try lowering\njumps = tracker.detect_jumps(metric=\"stable_rank\", threshold_z=1.5)\n\n# Or adjust during initialization\ntracker = HighFrequencyTracker(\n    model,\n    jump_z_threshold=1.5,  # More sensitive\n    jump_window_size=30     # Larger window\n)\n</code></pre></p>"},{"location":"reference/troubleshooting/#issue-too-many-jumps-detected-false-positives","title":"Issue: \"Too many jumps detected\" (false positives)","text":"<p>Cause: Z-score threshold too low or noisy data</p> <p>Solutions:</p> <ol> <li> <p>Increase threshold: <pre><code>jumps = tracker.detect_jumps(metric=\"stable_rank\", threshold_z=3.0)\n</code></pre></p> </li> <li> <p>Increase batch size: <pre><code># Larger batches = less noisy estimates\ndataloader = DataLoader(dataset, batch_size=128)\n</code></pre></p> </li> <li> <p>Increase window size: <pre><code>tracker = HighFrequencyTracker(\n    model,\n    jump_window_size=50  # Smoother detection\n)\n</code></pre></p> </li> </ol>"},{"location":"reference/troubleshooting/#common-mistakes","title":"Common Mistakes","text":""},{"location":"reference/troubleshooting/#1-forgetting-to-close-tracker","title":"1. Forgetting to close tracker","text":"<pre><code># Wrong: Hooks remain registered\ntracker = HighFrequencyTracker(model)\n# ... training ...\n# (no tracker.close())\n\n# Correct: Always close\ntracker = HighFrequencyTracker(model)\n# ... training ...\ntracker.close()\n\n# Better: Use context manager\nwith HighFrequencyTracker(model) as tracker:\n    # ... training ...\n    pass  # Automatically closed\n</code></pre>"},{"location":"reference/troubleshooting/#2-calling-log-before-forward-pass","title":"2. Calling log() before forward pass","text":"<pre><code># Wrong order\ntracker.log(step, loss.item())  # No activations captured yet!\noutput = model(x)\n\n# Correct order\noutput = model(x)\nloss = criterion(output, y)\ntracker.log(step, loss.item())  # Activations from forward pass\n</code></pre>"},{"location":"reference/troubleshooting/#3-modifying-model-after-tracker-creation","title":"3. Modifying model after tracker creation","text":"<pre><code># Wrong: Model changed after tracker created\ntracker = HighFrequencyTracker(model)\nmodel.add_module(\"new_layer\", nn.Linear(128, 64))  # Tracker doesn't know!\n\n# Correct: Create tracker after model is finalized\nmodel.add_module(\"new_layer\", nn.Linear(128, 64))\ntracker = HighFrequencyTracker(model)  # Now tracks all layers\n</code></pre>"},{"location":"reference/troubleshooting/#4-using-wrong-metric-names","title":"4. Using wrong metric names","text":"<pre><code># Wrong\nfig = plot_phases(results, metric=\"stable-rank\")  # Hyphen!\njumps = tracker.detect_jumps(metric=\"pr\")  # Abbreviation!\n\n# Correct\nfig = plot_phases(results, metric=\"stable_rank\")  # Underscore\njumps = tracker.detect_jumps(metric=\"participation_ratio\")  # Full name\n</code></pre>"},{"location":"reference/troubleshooting/#getting-help","title":"Getting Help","text":""},{"location":"reference/troubleshooting/#1-enable-debug-logging","title":"1. Enable debug logging","text":"<pre><code>import logging\n\nlogging.basicConfig(level=logging.DEBUG)\n\n# NDT will print detailed information\ntracker = HighFrequencyTracker(model)\n</code></pre>"},{"location":"reference/troubleshooting/#2-check-version","title":"2. Check version","text":"<pre><code>import ndt\nprint(ndt.__version__)\n\n# Ensure you have latest version\n# pip install --upgrade ndtracker\n</code></pre>"},{"location":"reference/troubleshooting/#3-create-minimal-reproducer","title":"3. Create minimal reproducer","text":"<pre><code>import torch\nimport torch.nn as nn\nfrom ndt import HighFrequencyTracker\n\n# Minimal model\nmodel = nn.Sequential(nn.Linear(10, 5), nn.ReLU(), nn.Linear(5, 2))\n\n# Minimal tracker\ntracker = HighFrequencyTracker(model, sampling_frequency=1)\n\n# Minimal training\nx = torch.randn(4, 10)\ny = torch.randint(0, 2, (4,))\n\noutput = model(x)\nloss = nn.CrossEntropyLoss()(output, y)\n\ntracker.log(0, loss.item())\n\n# Check results\nresults = tracker.get_results()\nfor name, df in results.items():\n    print(f\"{name}: {len(df)} measurements\")\n\ntracker.close()\n</code></pre>"},{"location":"reference/troubleshooting/#4-report-issues","title":"4. Report issues","text":"<p>If you encounter a bug:</p> <ol> <li>Check existing issues: GitHub Issues</li> <li>Create new issue with:</li> <li>NDT version</li> <li>PyTorch version</li> <li>Python version</li> <li>Minimal reproducer</li> <li>Full error traceback</li> </ol>"},{"location":"reference/troubleshooting/#faq","title":"FAQ","text":"<p>Q: Does NDT work with TorchScript?</p> <p>A: No, TorchScript doesn't support forward hooks. Use eager mode.</p> <p>Q: Can I use NDT with torch.compile?</p> <p>A: Partial support. Hooks work but may disable some optimizations.</p> <p>Q: Does NDT work on CPU?</p> <p>A: Yes! Works on CPU, GPU, and Apple Silicon (MPS).</p> <p>Q: Can I track custom modules?</p> <p>A: Yes, specify layers explicitly in the <code>layers</code> parameter.</p> <p>Q: How do I track only specific layers?</p> <p>A: Use the <code>layers</code> and <code>layer_names</code> parameters explicitly.</p> <p>Q: Can I pause/resume tracking?</p> <p>A: Not directly, but you can close and recreate the tracker.</p>"},{"location":"reference/troubleshooting/#see-also","title":"See Also","text":"<ul> <li>API Reference</li> <li>Architecture Support</li> <li>Performance Benchmarks</li> <li>Examples</li> </ul>"}]}